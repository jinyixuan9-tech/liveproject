(() => {
  const PLUGIN_ID = "vela-live";
  const APP_ID = "vela-live-home";
  const VERSION = "0.2.18";
  const STRANGER_AVATAR = "https://imgbed.heliar.top/i/sbLXJ9cX7mtcq4Ua_%E9%9F%A9%E5%A5%B3%E9%83%BD%E5%9C%A8%E7%94%A8%E7%9A%84%E6%B3%A8%E9%94%80%E7%B3%BB%E5%A4%B4%E5%83%8F%EF%BC%88%E5%8F%AF%E5%AD%98%EF%BC%89_8_%E6%81%8B%E6%97%B6%E9%9B%A8_%E6%9D%A5%E8%87%AA%E5%B0%8F%E7%BA%A2%E4%B9%A6%E7%BD%91%E9%A1%B5%E7%89%88.webp";
  const RECOMMENDATION_TOPICS = ["随便看看","日常","恋爱","音乐","游戏","工作","吃播","户外","多人联播","NSFW"];
  const LANGUAGE_PREFERENCE_OPTIONS = [
    { key:"chinese_main", label:"中文为主" },
    { key:"chinese_more", label:"中文较多" },
    { key:"balanced", label:"全球均衡" },
    { key:"overseas_more", label:"海外较多" },
    { key:"overseas_main", label:"海外为主" }
  ];
  const VELA_ICON = "data:image/png;base64,";
  const REFRESH_ICON = "data:image/png;base64,";
  const PAPER_PLANE_ICON = "data:image/png;base64,";
  const planeIconHTML = (cls = "") => `<img src="${PAPER_PLANE_ICON}" alt="" class="${cls ? `${cls} ` : ""}v-plane-icon">`;
  const refreshIconHTML = () => `<img src="${REFRESH_ICON}" alt="" class="v-refresh-icon">`;

  if (!window.RochePlugin || typeof window.RochePlugin.register !== "function") {
    console.warn("[Vela] RochePlugin.register is unavailable.");
    return;
  }

  const escapeHTML = (value = "") => String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");

  const normalizeHandle = (value = "") => {
    const raw = String(value || "").trim();
    if (!raw) return "";
    return raw.startsWith("@") ? raw : `@${raw}`;
  };

  const initials = (value = "") => {
    const clean = String(value || "").trim();
    if (!clean) return "V";
    return clean.slice(0, 2).toUpperCase();
  };

  const isImageURL = (value = "") => /^(https?:|data:image\/|blob:)/i.test(String(value || "").trim());

  const avatarHTML = (value = "", fallback = "V") => {
    const raw = String(value || "").trim();
    if (isImageURL(raw)) {
      return `<img src="${escapeHTML(raw)}" alt="" loading="lazy">`;
    }
    return escapeHTML(raw || initials(fallback));
  };

  const displayProfileName = (profile = {}) =>
    String(profile?.handle || profile?.name || profile?.displayName || "未命名").trim();

  const safeArray = (value) => Array.isArray(value) ? value : [];

  const VELA_DEFAULT_PRESET = `Vela 是一个以频道、直播、贴文、社群与私信为核心的虚拟创作者平台。生成内容时优先遵循当前账号设定、角色人设、账号公开程度、直播内容方向、粉丝画像，以及已经发生过的平台事件。

绝对禁止扮演 user。不得替 user 发言、替 user 发送弹幕、替 user 私信、替 user 做选择或决定 user 的心理与行为。只有 user 明确输入或操作过的内容，才视为 user 的行为。

后台知道的真实身份关系不等于角色或平台网友知道。角色与网友只能依据他们实际掌握的公开信息和线索进行判断；匿名或不露脸账号可以被怀疑、猜测或认错，但不能无依据直接识破。平台对外默认使用当前账号公开的网名、频道名、displayName 或 handle；只有“账号风格”明确说明本名已经对外公开或粉丝知道时，外部用户才可以自然使用本名，未说明则默认不知道。平台是全球化社区，评论、直播聊天室、社群与私信的语言应参考角色地区、人设、账号风格和粉丝画像自然分布，不要默认所有陌生人都说中文。

直播应保持连续推进，不要每次生成都像重新开播。user 发弹幕时主播可以看到并回应，也可以自然略过；user 只选择继续观看时，只推进主播行为、话题和观众互动，不强制让主播与 user 对话。

观众弹幕应有差异，参考频道粉丝画像，不要所有网友都像同一个人。频道、角色和平台账号应有自己的活动，不要永远围绕 user 运转。商业邀约、私信与推荐内容要符合频道规模和内容方向，避免高频、重复或无缘由出现。`;

  function createDefaultState() {
    return {
      activePage: "home",
      homeFilter: "all",
      dmTab: "dm",
      subscriptions: {},
      subscriptionsByIdentity: {},
      follows: {},
      followDefaultsInitialized: false,
      followDefaultsV0213: false,
      appointmentReminderMuteDate: "",
      subscriptionProfiles: {},
      postInteractionIdentity: {},
      communityInteractionIdentity: {},
      dmInteractionIdentity: {},
      liveInteractionIdentity: {},
      homeFeedByChannel: {},
      homeActivities: [],
      liveChatsById: {},
      liveChatQueues: {},
      recommendationSamplesSeeded: false,
      communityChats: {},
      communityChatQueues: {},
      communitySettings: {},
      dmThreads: {},
      pendingOwnLiveInput: null,
      drawerOpen: false,
      publishOpen: false,
      viewerIdentityId: "",
      rocheLink: {
        ownerPersonaId: "",
        linkedCharacterIds: []
      },
      identities: [],
      channels: [],
      recommendedLives: [],
      schedule: {
        visible: false,
        source: "",
        messageId: "",
        dateText: "",
        title: "",
        detail: "",
        type: ""
      },
      businessDeals: {},
      wallet: {
        cashBalanceCNY: 0,
        coinBalance: 0,
        linkedAccount: null,
        settlementCurrency: "",
        fxRates: { CNY: 1, USD: 0.149, JPY: 21.9, KRW: 207, HKD: 1.16, EUR: 0.128 },
        fxUpdatedAt: 0,
        subscriptionAccruals: [],
        subscriptionPurchases: {},
        transactions: []
      },
      roleEarnings: {},
      messages: [],
      communityPosts: [],
      liveReplays: [],
      postReplies: {},
      postOverrides: {},
      liveSession: null,
      pendingLiveUserMessage: null,
      liveProgress: {},
      liveStats: {},
      roleCommerce: {},
      generationPreset: {
        mode: "default",
        customText: "",
        nsfwText: "成熟向内容可以更大胆、更暧昧、更有成人主播或成人社交平台氛围，可包含性感但不露骨的造型、夜生活、付费订阅与成熟向玩笑；不要生成露骨色情细节、明确性行为或色情裸露描写。"
      },
      appearance: {
        topbarHeight: 62,
        bottombarHeight: 78
      },
      platformSettings: {
        recommendationCount: 3,
        allowRoleAliasesInRecommendations: false,
        allowBusinessDM: true,
        activityLevel: 50,
        strangerInteraction: "normal",
        languagePreference: "overseas_more"
      },
      liveChat: []
    };
  }

  function formatMoney(value) {
    const abs = Math.abs(Number(value || 0));
    return `${value < 0 ? "-" : "+"} ¥${abs.toFixed(2)}`;
  }

  function formatViewers(value) {
    const n = Number(value || 0);
    if (n >= 10000) return `${(n / 10000).toFixed(n >= 100000 ? 0 : 1)}万`;
    if (n >= 1000) return `${(n / 1000).toFixed(1)}K`;
    return String(n);
  }

  function styles() {
    return `
<style data-vela-style>
.vela-roche{--v-bg:#f2f2f7;--v-card:#fff;--v-text:#111111;--v-muted:#8e8e93;--v-line:rgba(60,60,67,.16);--v-soft:#f2f2f7;--v-blue:#007aff;--v-blue-soft:#eaf3ff;--v-red:#ff3b30;--v-live:var(--v-red);--v-top-h:62px;--v-bottom-h:78px;position:relative;width:100%;height:100%;min-height:0;background:var(--v-bg);color:var(--v-text);overflow:hidden;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","PingFang SC","Microsoft YaHei",sans-serif;-webkit-tap-highlight-color:transparent;isolation:isolate}
.vela-roche *{box-sizing:border-box}.vela-roche button,.vela-roche input,.vela-roche textarea{font:inherit}.vela-roche button{cursor:pointer}.vela-roche .v-top{position:absolute;z-index:20;left:0;right:0;top:0;height:var(--v-top-h);padding:0 14px;display:flex;align-items:center;justify-content:space-between;background:rgba(246,246,248,.95);backdrop-filter:blur(16px);border-bottom:1px solid rgba(0,0,0,.035)}
.vela-roche .v-brand{display:flex;align-items:center;gap:10px;font-size:20px;font-weight:900}.vela-roche .v-logo{width:30px;height:30px;border-radius:10px;background:#fff;display:grid;place-items:center;overflow:hidden;border:1px solid var(--v-line)}.vela-roche .v-logo img{width:100%;height:100%;object-fit:cover;display:block}.vela-roche .v-plainbtn{width:42px;height:42px;border:0;border-radius:14px;background:#fff;color:#111;font-size:19px;display:grid;place-items:center}.vela-roche .v-top-actions{display:flex;gap:7px}
.vela-roche .v-main{position:absolute;inset:var(--v-top-h) 0 calc(var(--v-bottom-h) + env(safe-area-inset-bottom,0px));overflow:auto;overscroll-behavior:contain;padding:12px 14px 24px;-webkit-overflow-scrolling:touch}.vela-roche .v-page{display:none}.vela-roche .v-page.is-active{display:block}.vela-roche .v-title{display:flex;justify-content:space-between;align-items:flex-end;margin:5px 2px 13px;font-size:22px;font-weight:900}.vela-roche .v-title small{font-size:11px;color:var(--v-muted);font-weight:700}
.vela-roche .v-channels{display:flex;gap:12px;overflow-x:auto;padding:2px 0 11px;scrollbar-width:none}.vela-roche .v-channels::-webkit-scrollbar{display:none}.vela-roche .v-channel{width:64px;flex:0 0 auto;text-align:center;border:0;background:none;color:inherit;padding:0}.vela-roche .v-avatar-wrap{position:relative;width:58px;height:58px;border-radius:50%;padding:2px;background:linear-gradient(135deg,#111,#aaa);margin:auto}.vela-roche .v-avatar{width:100%;height:100%;display:grid;place-items:center;border-radius:50%;background:linear-gradient(135deg,#dadbe1,#fff);font-weight:900;font-size:18px;overflow:hidden}.vela-roche .v-live-dot{position:absolute;right:-4px;bottom:-3px;background:var(--v-live);color:#fff;border:3px solid var(--v-bg);border-radius:8px;font-size:8px;font-weight:900;padding:2px 5px}.vela-roche .v-channel-name{font-size:11px;font-weight:750;margin-top:6px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.vela-roche .v-schedule{background:linear-gradient(135deg,#0f5fc4,#163d7a);color:#fff;border-radius:22px;padding:13px 14px;margin:4px 0 13px}.vela-roche .v-schedule-top,.vela-roche .v-schedule-main{display:flex;align-items:center;justify-content:space-between;gap:10px}.vela-roche .v-kicker{font-size:10px;opacity:.62;font-weight:800}.vela-roche .v-schedule-time{font-size:17px;font-weight:900;margin-top:3px}.vela-roche .v-pill{font-size:10px;background:#fff;color:#111;border-radius:9px;padding:5px 8px;font-weight:900}.vela-roche .v-schedule-main{margin-top:10px}.vela-roche .v-schedule-icon{width:38px;height:38px;border-radius:13px;background:#2a2a30;display:grid;place-items:center;font-weight:900}.vela-roche .v-schedule-copy{flex:1;min-width:0}.vela-roche .v-schedule-copy b{display:block;font-size:13px}.vela-roche .v-schedule-copy span{display:block;font-size:11px;opacity:.68;margin-top:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.vela-roche .v-small-dark{border:0;background:#2a2a30;color:#fff;border-radius:12px;padding:9px 11px;font-size:11px;font-weight:850;min-height:38px}
.vela-roche .v-home-card{overflow:hidden}.vela-roche .v-home-card-hit{width:100%;border:0;background:none;color:inherit;text-align:left;padding:0}.vela-roche .v-home-post-cover{height:178px;border-radius:19px;margin-top:12px;background:radial-gradient(circle at 72% 24%,rgba(255,255,255,.9),transparent 24%),linear-gradient(135deg,#d8d8df,#f4f4f7);display:grid;place-items:center;font-size:38px}.vela-roche .v-home-post-meta{display:flex;gap:14px;margin-top:10px;color:var(--v-muted);font-size:11px}.vela-roche .v-home-open{font-size:10px;color:var(--v-muted);font-weight:800}.vela-roche .v-task-toggle{width:100%;border:0;background:var(--v-soft);color:var(--v-text);border-radius:15px;padding:12px 13px;display:flex;align-items:center;justify-content:space-between;font-weight:900;margin-top:12px}.vela-roche .v-task-detail{margin-top:8px;border:1px solid var(--v-line);border-radius:16px;padding:0 13px;background:var(--v-card)}.vela-roche .v-brand-card{background:var(--v-card);border-radius:22px;padding:15px;margin-bottom:12px}.vela-roche .v-brand-style{font-size:11px;color:var(--v-muted);margin-top:4px}.vela-roche .v-product-box{margin-top:13px;background:var(--v-soft);border-radius:16px;padding:12px}.vela-roche .v-product-box b{font-size:13px}.vela-roche .v-product-box p{font-size:12px;line-height:1.55;color:var(--v-muted);margin:6px 0 0}.vela-roche .v-scheduled-card{width:100%;border:0;background:#111;color:#fff;border-radius:20px;padding:15px;text-align:left}.vela-roche .v-scheduled-card .v-sub{color:rgba(255,255,255,.68)}.vela-roche .v-profile-actions{display:flex;gap:8px;margin-top:12px}.vela-roche .v-profile-actions button{border:0;border-radius:12px;padding:9px 11px;font-size:11px;font-weight:900}.vela-roche .v-post-detail-cover{height:220px;border-radius:22px;background:radial-gradient(circle at 68% 18%,rgba(255,255,255,.86),transparent 28%),linear-gradient(135deg,#d5d6de,#f5f5f8);display:grid;place-items:center;font-size:52px;margin-top:12px}
.vela-roche .v-filters{display:flex;gap:8px;margin:9px 0 14px}.vela-roche .v-chip{border:0;border-radius:14px;background:#e9e9ee;padding:9px 14px;font-size:12px;font-weight:850;color:#111}.vela-roche .v-chip.is-active{background:var(--v-blue);color:#fff}.vela-roche .v-card{background:#fff;border-radius:24px;padding:14px;margin-bottom:14px}.vela-roche .v-author{display:flex;align-items:center;gap:10px}.vela-roche .v-mini{width:41px;height:41px;border-radius:14px;background:#ececf1;display:grid;place-items:center;font-weight:900}.vela-roche .v-meta{min-width:0;flex:1}.vela-roche .v-name{font-size:14px;font-weight:900}.vela-roche .v-sub{font-size:11px;color:var(--v-muted);margin-top:2px}.vela-roche .v-stage{display:flex;align-items:flex-end;width:100%;aspect-ratio:16/9;margin-top:12px;padding:14px;border:0;border-radius:20px;background:radial-gradient(circle at 72% 16%,rgba(255,255,255,.34),transparent 29%),linear-gradient(135deg,#1b1b22,#7a7d88);color:#fff;text-align:left;overflow:hidden;position:relative}.vela-roche .v-stage:after{content:"";position:absolute;inset:0;background:linear-gradient(transparent 38%,rgba(0,0,0,.48))}.vela-roche .v-stage-copy{position:relative;z-index:1}.vela-roche .v-badge{display:inline-block;background:var(--v-live);padding:5px 8px;border-radius:8px;font-size:9px;font-weight:900;margin-bottom:7px}.vela-roche .v-live-title{font-size:18px;font-weight:900;line-height:1.25}.vela-roche .v-viewers{font-size:11px;opacity:.82;margin-top:4px}.vela-roche .v-engage{display:flex;gap:14px;margin-top:10px;color:#555;font-size:12px}.vela-roche .v-postimg{height:170px;border-radius:19px;margin-top:12px;background:linear-gradient(135deg,#d7d8df,#fafafd);display:grid;place-items:center;font-size:42px}.vela-roche .v-posttext{font-size:14px;line-height:1.55;margin-top:10px}
.vela-roche .v-rec-card{padding:0;overflow:hidden}.vela-roche .v-rec-cover{display:flex;align-items:flex-end;aspect-ratio:16/9;padding:14px;background:radial-gradient(circle at 75% 15%,rgba(255,255,255,.4),transparent 26%),linear-gradient(135deg,#2a2b31,#8c8f99);color:#fff;position:relative}.vela-roche .v-rec-cover:after{content:"";position:absolute;inset:0;background:linear-gradient(transparent 42%,rgba(0,0,0,.55))}.vela-roche .v-rec-cover>*{position:relative;z-index:1}.vela-roche .v-rec-body{padding:12px 14px 14px;display:flex;gap:10px;align-items:center}.vela-roche .v-follow{border:0;background:var(--v-blue);color:#fff;border-radius:12px;padding:9px 12px;font-size:11px;font-weight:900;min-height:38px}.vela-roche .v-follow.is-on{background:#e9e9ee;color:#111}
.vela-roche .v-msg{display:flex;align-items:center;gap:11px;padding:12px 2px;border-bottom:1px solid var(--v-line);border-top:0;border-left:0;border-right:0;background:none;width:100%;text-align:left;color:inherit}.vela-roche .v-msg:last-child{border-bottom:0}.vela-roche .v-preview{font-size:12px;color:var(--v-muted);margin-top:4px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.vela-roche .v-unread{min-width:20px;height:20px;border-radius:10px;background:#111;color:#fff;font-size:10px;display:grid;place-items:center}.vela-roche .v-biz{font-size:9px;padding:3px 6px;border-radius:7px;background:#fff0d4;color:#925e00;margin-left:5px}.vela-roche .v-wallet{background:linear-gradient(135deg,#0d4f9e,#173864);color:#fff;border-radius:28px;padding:22px;margin:7px 0 16px}.vela-roche .v-balance-label{font-size:12px;opacity:.65}.vela-roche .v-balance{font-size:35px;font-weight:900;margin:7px 0 18px}.vela-roche .v-wallet-actions{display:flex;gap:8px}.vela-roche .v-wallet-actions button{flex:1;border:0;background:#fff;color:#111;border-radius:14px;padding:12px;font-weight:900}.vela-roche .v-tx{display:flex;justify-content:space-between;gap:12px;padding:13px 2px;border-bottom:1px solid var(--v-line);font-size:13px}.vela-roche .v-tx:last-child{border-bottom:0}.vela-roche .v-tx small{display:block;color:var(--v-muted);margin-top:3px}
.vela-roche .v-nav{position:absolute;z-index:30;left:0;right:0;bottom:0;height:calc(var(--v-bottom-h) + env(safe-area-inset-bottom,0px));padding-bottom:env(safe-area-inset-bottom,0px);display:grid;grid-template-columns:repeat(5,1fr);background:rgba(255,255,255,.96);backdrop-filter:blur(18px);border-top:1px solid var(--v-line)}.vela-roche .v-nav button{border:0;background:none;color:#8b8b93;font-size:10px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;min-width:0;min-height:52px}.vela-roche .v-nav button b{font-size:20px;font-weight:500}.vela-roche .v-nav button.is-active{color:var(--v-blue);font-weight:900}.vela-roche .v-nav .v-publish b{width:42px;height:42px;border-radius:15px;background:#111;color:#fff;display:grid;place-items:center;font-size:25px;margin-top:-5px;box-shadow:0 7px 18px rgba(0,0,0,.17)}
.vela-roche .v-mask{position:absolute;z-index:50;inset:0;background:rgba(0,0,0,.28);opacity:0;pointer-events:none;transition:opacity .18s}.vela-roche .v-mask.is-open{opacity:1;pointer-events:auto}.vela-roche .v-drawer{position:absolute;z-index:60;top:0;bottom:0;left:0;width:min(84%,350px);padding:18px 16px calc(16px + env(safe-area-inset-bottom,0px));background:#fff;transform:translateX(-102%);transition:transform .22s ease;overflow:auto;display:flex;flex-direction:column}.vela-roche .v-drawer.is-open{transform:none}.vela-roche .v-drawer-main{flex:1;min-height:0}.vela-roche .v-userhead{width:100%;border:0;background:#f6f6f8;border-radius:22px;padding:13px;display:flex;align-items:center;gap:12px;margin:6px 0 18px;text-align:left;color:inherit}.vela-roche .v-userav{width:58px;height:58px;border-radius:20px;background:linear-gradient(135deg,#111,#777);color:#fff;display:grid;place-items:center;font-weight:900;flex:0 0 auto}.vela-roche .v-avatar img,.vela-roche .v-mini img,.vela-roche .v-userav img{width:100%;height:100%;object-fit:cover;border-radius:inherit;display:block}.vela-roche .v-usercopy{min-width:0;flex:1}.vela-roche .v-userhead b{display:block;font-size:18px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.vela-roche .v-userhead span{display:block;color:var(--v-muted);font-size:12px;margin-top:3px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.vela-roche .v-identity-chip{display:inline-flex!important;width:auto!important;margin-top:7px!important;padding:4px 7px;border-radius:8px;background:#111;color:#fff!important;font-size:9px!important;font-weight:900}.vela-roche .v-drawer-section{margin-top:14px}.vela-roche .v-drawer-label{padding:0 4px 7px;color:#a0a0a8;font-size:10px;font-weight:900;letter-spacing:.06em}.vela-roche .v-drawer-item{width:100%;padding:13px 4px;border:0;border-bottom:1px solid var(--v-line);background:none;display:flex;align-items:center;justify-content:space-between;gap:10px;text-align:left;color:inherit;font-size:14px;font-weight:800;min-height:48px}.vela-roche .v-drawer-item .v-itemcopy{display:flex;align-items:center;gap:10px;min-width:0}.vela-roche .v-drawer-item .v-itemicon{width:30px;height:30px;border-radius:10px;background:#f2f2f5;display:grid;place-items:center;font-size:14px;flex:0 0 auto}.vela-roche .v-drawer-item .v-itembadge{font-size:10px;color:var(--v-muted);font-weight:800}.vela-roche .v-drawer-foot{padding-top:18px;margin-top:auto}.vela-roche .v-version{font-size:10px;color:#b0b0b7;text-align:center;margin:0 0 9px}.vela-roche .v-exit{width:100%;min-height:48px;border:0;border-radius:15px;background:#f3f3f6;color:#b3261e;font-weight:900;font-size:13px}
.vela-roche .v-sheetmask{position:absolute;z-index:80;inset:0;background:rgba(0,0,0,.28);display:none;align-items:flex-end}.vela-roche .v-sheetmask.is-open{display:flex}.vela-roche .v-sheet{width:100%;max-height:82%;overflow:auto;background:#fff;border-radius:28px 28px 0 0;padding:13px 16px calc(24px + env(safe-area-inset-bottom,0px));overscroll-behavior:contain}.vela-roche .v-handle{width:42px;height:5px;border-radius:3px;background:#d2d2d7;margin:0 auto 15px}.vela-roche .v-create-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.vela-roche .v-create{border:0;border-radius:18px;background:#f1f1f4;padding:17px;text-align:left;font-weight:900;min-height:92px}.vela-roche .v-create b{display:block;font-size:24px;margin-bottom:12px}.vela-roche .v-hint{font-size:11px;line-height:1.5;color:var(--v-muted)}
.vela-roche .v-subscreen{position:absolute;z-index:70;inset:0;background:var(--v-bg);transform:translateX(102%);transition:transform .22s ease;overflow:auto;overscroll-behavior:contain;-webkit-overflow-scrolling:touch}.vela-roche .v-subscreen.is-open{transform:none}.vela-roche .v-subhead{position:sticky;z-index:2;top:0;height:var(--v-top-h);padding:0 14px;display:flex;align-items:center;gap:10px;background:rgba(246,246,248,.96);backdrop-filter:blur(16px);border-bottom:1px solid var(--v-line)}.vela-roche .v-subhead>button:first-child{width:42px;height:42px;border:0;border-radius:14px;background:#fff;font-size:21px;flex:0 0 auto}.vela-roche .v-subhead .v-head-action{width:auto!important;height:34px!important;min-height:34px!important;padding:0 10px!important;border:0;border-radius:11px;background:#fff!important;color:#111!important;font-size:11px!important;font-weight:850!important;white-space:nowrap}.vela-roche .v-subbody{padding:14px 14px 28px}.vela-roche .v-tabs{display:flex;gap:8px;overflow:auto;margin-bottom:13px}.vela-roche .v-tabs button{border:0;border-radius:13px;background:#e9e9ee;padding:9px 12px;font-size:12px;font-weight:850}.vela-roche .v-tabs button.is-active{background:#111;color:#fff}.vela-roche .v-empty{padding:65px 14px;text-align:center;color:var(--v-muted);font-size:13px;line-height:1.6}.vela-roche .v-row{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:13px 0;border-bottom:1px solid var(--v-line)}.vela-roche .v-row:last-child{border-bottom:0}.vela-roche .v-row b{font-size:13px}.vela-roche .v-row small{display:block;color:var(--v-muted);font-size:11px;margin-top:3px}.vela-roche .v-action{border:0;border-radius:12px;background:var(--v-blue);color:#fff;padding:9px 12px;font-size:11px;font-weight:900;min-height:38px}.vela-roche .v-action.light{background:#efeff3;color:#111}.vela-roche .v-linkbox{border:1px solid var(--v-line);border-radius:18px;padding:12px;margin:10px 0;background:#fafafd}.vela-roche .v-linkstatus{display:flex;align-items:center;gap:7px;font-size:11px;color:var(--v-muted);margin-top:4px}.vela-roche .v-dot{width:7px;height:7px;border-radius:50%;background:#b9b9c1;flex:0 0 auto}.vela-roche .v-dot.on{background:#38a169}.vela-roche .v-rolecard{border-top:1px solid var(--v-line);padding-top:12px;margin-top:12px}.vela-roche .v-rolecard:first-child{border-top:0;padding-top:0;margin-top:0}
.vela-roche .v-profile-shell{padding-bottom:18px}.vela-roche .v-profile-banner{height:156px;background:linear-gradient(135deg,#d5d6dc,#f5f5f8);overflow:hidden;position:relative}.vela-roche .v-profile-banner img{width:100%;height:100%;object-fit:cover;display:block}.vela-roche .v-profile-info{position:relative;padding:48px 16px 14px;background:var(--v-card)}.vela-roche .v-profile-avatar{width:84px;height:84px;border-radius:50%;border:4px solid var(--v-card);background:linear-gradient(135deg,#222,#888);color:#fff;display:grid;place-items:center;font-weight:900;font-size:18px;overflow:hidden;position:absolute;left:16px;top:-42px;margin:0}.vela-roche .v-profile-avatar img{width:100%;height:100%;object-fit:cover;display:block}.vela-roche .v-profile-main{display:flex;align-items:flex-end;justify-content:space-between;gap:12px}.vela-roche .v-profile-maincopy{min-width:0;flex:1;padding-top:8px}.vela-roche .v-profile-maincopy h2{font-size:22px;line-height:1.2;margin:0 0 4px}.vela-roche .v-profile-handle{font-size:12px;color:var(--v-muted)}.vela-roche .v-profile-stats{display:flex;gap:14px;margin-top:10px;font-size:12px;color:var(--v-muted)}.vela-roche .v-profile-stats b{color:var(--v-text);font-size:13px}.vela-roche .v-profile-bio{font-size:13px;line-height:1.55;margin-top:11px;white-space:pre-wrap}.vela-roche .v-profile-tabs{display:grid;grid-template-columns:repeat(3,1fr);background:var(--v-card);border-top:1px solid var(--v-line);border-bottom:1px solid var(--v-line)}.vela-roche .v-profile-tabs button{border:0;background:var(--v-card);padding:13px 6px;font-size:12px;font-weight:850;color:var(--v-muted);position:relative}.vela-roche .v-profile-tabs button.is-active{color:#111}.vela-roche .v-profile-tabs button.is-active:after{content:"";position:absolute;left:24%;right:24%;bottom:0;height:2px;background:#111;border-radius:2px}.vela-roche .v-profile-tabbody{padding:14px}.vela-roche .v-editform{display:grid;gap:12px}.vela-roche .v-field label{display:block;font-size:11px;font-weight:850;margin-bottom:6px}.vela-roche .v-field input,.vela-roche .v-field textarea,.vela-roche .v-field select{width:100%;border:1px solid var(--v-line);background:var(--v-card);border-radius:13px;padding:11px 12px;font-size:16px;color:var(--v-text);outline:none}.vela-roche .v-field textarea{min-height:88px;resize:vertical}.vela-roche .v-field select{min-height:44px}.vela-roche .v-fieldpair{display:grid;grid-template-columns:1fr 1fr;gap:10px}.vela-roche .v-identity-card{border:1px solid var(--v-line);background:#fff;border-radius:18px;padding:12px;margin-top:10px}.vela-roche .v-identity-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.vela-roche .v-roche-compact{background:#fafafd;border:1px solid var(--v-line);border-radius:18px;padding:12px}.vela-roche .v-roche-persona{display:flex;align-items:center;gap:9px;padding:9px 0;border-top:1px solid var(--v-line)}.vela-roche .v-roche-persona:first-of-type{border-top:0}.vela-roche .v-role-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.vela-roche .v-profile-empty{padding:42px 12px;text-align:center;color:var(--v-muted);font-size:12px;line-height:1.6}.vela-roche .v-settings-note{font-size:12px;line-height:1.65;color:var(--v-muted)}.vela-roche .v-live-screen{background:#0d0d10;color:#fff}.vela-roche .v-live-screen .v-subhead{background:rgba(13,13,16,.95);border-bottom-color:#26262c}.vela-roche .v-live-screen .v-subhead button{background:#24242a;color:#fff}.vela-roche .v-live-body{padding:10px 12px 28px}.vela-roche .v-live-stage{aspect-ratio:16/9;border-radius:20px;background:radial-gradient(circle at 70% 15%,rgba(255,255,255,.25),transparent 27%),linear-gradient(135deg,#1d1e24,#747783);position:relative;overflow:hidden;padding:14px;display:flex;align-items:flex-end}.vela-roche .v-live-stage:after{content:"";position:absolute;inset:0;background:linear-gradient(transparent 43%,rgba(0,0,0,.56))}.vela-roche .v-live-stage-copy{position:relative;z-index:1}.vela-roche .v-live-stage h2{font-size:19px;line-height:1.3;margin:7px 0 4px}.vela-roche .v-live-stage p{margin:0;font-size:11px;opacity:.8}.vela-roche .v-chat-panel{margin-top:10px;background:#fff;color:#111;border-radius:22px;padding:13px}.vela-roche .v-chat-title{font-weight:900;margin-bottom:7px}.vela-roche .v-chatline{display:flex;gap:8px;padding:7px 0;font-size:12px;line-height:1.45}.vela-roche .v-chatline b{flex:0 0 auto}.vela-roche .v-composer{display:grid;grid-template-columns:1fr 44px 44px;gap:8px;margin-top:10px}.vela-roche .v-composer input{width:100%;min-width:0;border:0;border-radius:15px;background:#efeff2;padding:11px 12px;font-size:16px}.vela-roche .v-composer button{border:0;border-radius:15px;background:#111;color:#fff;min-height:44px}.vela-roche .v-continue{width:100%;margin-top:8px;border:0;border-radius:15px;background:#24242a;color:#fff;padding:12px;font-weight:900;min-height:44px}.vela-roche .v-toast{position:absolute;z-index:120;left:50%;bottom:calc(92px + env(safe-area-inset-bottom,0px));transform:translate(-50%,12px);background:#111;color:#fff;border-radius:13px;padding:10px 13px;font-size:11px;opacity:0;pointer-events:none;transition:.18s;white-space:nowrap;max-width:86%;overflow:hidden;text-overflow:ellipsis}.vela-roche .v-toast.is-show{opacity:1;transform:translate(-50%,0)}
.vela-roche .v-settings-group{background:var(--v-card);border-radius:20px;padding:14px;margin-bottom:12px}.vela-roche .v-settings-group h3{font-size:14px;margin:0 0 10px}.vela-roche .v-setting-row{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:12px 0;border-top:1px solid var(--v-line)}.vela-roche .v-setting-row:first-of-type{border-top:0}.vela-roche .v-setting-row .v-meta{min-width:0}.vela-roche .v-switch{appearance:none;width:46px;height:28px;border-radius:16px;background:#d8d8de;position:relative;transition:.18s;flex:0 0 auto}.vela-roche .v-switch:after{content:"";position:absolute;width:22px;height:22px;left:3px;top:3px;border-radius:50%;background:#fff;transition:.18s}.vela-roche .v-switch:checked{background:#111}.vela-roche .v-switch:checked:after{transform:translateX(18px)}.vela-roche .v-range{width:100%;accent-color:var(--v-blue)}.vela-roche .v-preset-box{white-space:pre-wrap;font-size:12px;line-height:1.65;color:var(--v-muted);background:var(--v-soft);border-radius:16px;padding:12px}.vela-roche .v-settings-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}
.vela-roche .v-social-avatar{width:42px;height:42px;border-radius:50%;background:var(--v-soft);display:grid;place-items:center;font-weight:900;overflow:hidden;flex:0 0 auto}.vela-roche .v-social-avatar img{width:100%;height:100%;object-fit:cover;display:block}.vela-roche .v-social-head{display:flex;align-items:center;gap:10px}.vela-roche .v-social-copy{min-width:0;flex:1}.vela-roche .v-social-name{font-size:14px;font-weight:900}.vela-roche .v-social-handle{font-size:11px;color:var(--v-muted);margin-top:2px}.vela-roche .v-social-text{font-size:14px;line-height:1.55;margin-top:10px;white-space:pre-wrap}.vela-roche .v-social-media{width:100%;height:190px;border:0;border-radius:18px;margin-top:10px;background:radial-gradient(circle at 70% 20%,rgba(255,255,255,.9),transparent 28%),linear-gradient(135deg,#d9dae1,#f5f5f8);display:grid;place-items:center;font-size:38px;color:var(--v-text);overflow:hidden}.vela-roche .v-social-media img{width:100%;height:100%;object-fit:cover;display:block}.vela-roche .v-social-actions{display:flex;gap:20px;align-items:center;margin-top:10px}.vela-roche .v-social-actions button{border:0;background:none;color:var(--v-muted);font-size:12px;padding:3px 0}.vela-roche .v-translate-btn{border:0;background:none;color:var(--v-muted);font-size:10px;font-weight:800;padding:3px 0;margin-top:4px}.vela-roche .v-translation{font-size:12px;line-height:1.55;color:var(--v-muted);margin-top:5px;padding-left:9px;border-left:2px solid var(--v-line);white-space:pre-wrap}.vela-roche .v-comment-list{border-top:1px solid var(--v-line);margin-top:14px;padding-top:5px}.vela-roche .v-comment{display:flex;gap:9px;padding:10px 0;border-bottom:1px solid var(--v-line)}.vela-roche .v-comment:last-child{border-bottom:0}.vela-roche .v-comment .v-social-avatar{width:34px;height:34px;font-size:11px}.vela-roche .v-comment-body{min-width:0;flex:1}.vela-roche .v-comment-name{font-size:11px;font-weight:900}.vela-roche .v-comment-text{font-size:12px;line-height:1.5;margin-top:3px}.vela-roche .v-community-compose{width:100%;border:1px dashed var(--v-line);background:var(--v-card);color:var(--v-text);border-radius:17px;padding:13px;text-align:left;font-size:12px;font-weight:850;margin-bottom:12px}
.vela-roche .v-live-screen{background:var(--v-bg)!important;color:var(--v-text)!important}.vela-roche .v-live-screen .v-subhead{background:rgba(255,255,255,.96)!important;border-bottom-color:var(--v-line)!important;color:#111!important}.vela-roche .v-live-screen .v-subhead>button:first-child{background:#f0f0f3!important;color:#111!important}.vela-roche .v-live-screen .v-small-dark{background:#f0f0f3!important;color:#111!important}.vela-roche .v-live-body{padding:10px 12px 28px}.vela-roche .v-live-stage{border-radius:18px;background:radial-gradient(circle at 70% 15%,rgba(255,255,255,.18),transparent 27%),linear-gradient(135deg,#1b1c20,#777a83);padding:12px;align-items:flex-start}.vela-roche .v-live-stage:after{background:linear-gradient(rgba(0,0,0,.08),rgba(0,0,0,.28))}.vela-roche .v-live-stage-copy{width:100%;display:flex;justify-content:space-between;align-items:flex-start}.vela-roche .v-live-info{background:var(--v-card);border-radius:18px;padding:13px;margin-top:9px}.vela-roche .v-live-info h2{font-size:17px;line-height:1.35;margin:0}.vela-roche .v-live-info p{font-size:11px;color:var(--v-muted);margin:5px 0 0}.vela-roche .v-chat-panel{margin-top:9px;background:var(--v-card);color:var(--v-text);border:1px solid var(--v-line);border-radius:18px;padding:13px}.vela-roche .v-chatline{display:flex;gap:8px;padding:8px 0}.vela-roche .v-chatline .v-social-avatar{width:31px;height:31px;font-size:10px}.vela-roche .v-chatline-body{min-width:0;flex:1}.vela-roche .v-chatline-head{font-size:10px;font-weight:900}.vela-roche .v-chatline-text{font-size:12px;line-height:1.45;margin-top:2px}.vela-roche .v-composer input{background:var(--v-soft);color:var(--v-text)}.vela-roche .v-composer button{background:#fff;color:#111;box-shadow:inset 0 0 0 1px var(--v-line)}.vela-roche .v-continue{background:#111;color:#fff}

.vela-roche.is-dark .v-sheet{background:#101116!important}
.vela-roche.is-dark .v-ios-group,.vela-roche.is-dark .v-ios-media-card,.vela-roche.is-dark .v-replay-card,.vela-roche.is-dark .v-replay-summary,.vela-roche.is-dark .v-replay-event,.vela-roche.is-dark .v-replay-chat{background:var(--v-card)}
.vela-roche.is-dark .v-ios-media-preview{background:linear-gradient(145deg,#20242b,#2c313b)}
.vela-roche.is-dark .v-publish-sheet-actions{background:linear-gradient(transparent,#101116 28%)}

.vela-roche.is-dark .v-live-screen .v-subhead{background:rgba(25,26,32,.96)!important;color:#fff!important}.vela-roche.is-dark .v-live-screen .v-subhead>button:first-child,.vela-roche.is-dark .v-live-screen .v-small-dark{background:#292b33!important;color:#fff!important}.vela-roche.is-dark .v-social-media{background:linear-gradient(135deg,#25272f,#343741)}

/* v0.1.15 · iOS-like surface polish */
.vela-roche .v-sheet{background:#f2f2f7!important;border-radius:28px 28px 0 0!important;padding-left:14px!important;padding-right:14px!important}
.vela-roche .v-sheet .v-handle{background:#c7c7cc!important;width:36px!important;height:5px!important}
.vela-roche .v-publish-sheet-title{min-height:42px;margin-bottom:10px!important}
.vela-roche .v-publish-sheet-title h3{font-size:17px!important;font-weight:800!important;letter-spacing:-.01em}
.vela-roche .v-publish-sheet-title button{width:34px;height:34px;border-radius:17px!important;background:rgba(118,118,128,.12)!important;color:var(--v-text)!important;display:grid;place-items:center}
.vela-roche .v-ios-publish{display:grid;gap:12px}
.vela-roche .v-ios-group{background:var(--v-card);border-radius:20px;overflow:hidden;border:0;box-shadow:0 .5px 0 rgba(60,60,67,.08)}
.vela-roche .v-ios-group .v-field{padding:11px 13px;border-top:1px solid var(--v-line)}
.vela-roche .v-ios-group .v-field:first-child{border-top:0}
.vela-roche .v-ios-group .v-field label{font-size:11px;color:var(--v-muted);margin-bottom:6px}
.vela-roche .v-ios-group .v-field input,.vela-roche .v-ios-group .v-field textarea,.vela-roche .v-ios-group .v-field select{border:0!important;background:transparent!important;border-radius:0!important;padding:2px 0!important;min-height:30px!important;font-size:16px!important}
.vela-roche .v-ios-group .v-field textarea{min-height:72px!important}
.vela-roche .v-ios-media-card{background:var(--v-card);border-radius:20px;padding:12px}
.vela-roche .v-ios-media-preview{height:168px;border-radius:16px;background:linear-gradient(145deg,#edf4ff,#f8f9fc);display:grid;place-items:center;overflow:hidden;position:relative;color:var(--v-blue)}
.vela-roche .v-ios-media-preview img{width:100%;height:100%;object-fit:cover;display:block}
.vela-roche .v-ios-media-placeholder{display:grid;place-items:center;gap:6px;color:var(--v-muted);font-size:11px}
.vela-roche .v-ios-media-placeholder b{width:38px;height:38px;border-radius:19px;background:var(--v-blue);color:#fff;display:grid;place-items:center;font-size:24px;font-weight:500}
.vela-roche .v-ios-media-actions{display:grid;grid-template-columns:1fr auto;gap:8px;margin-top:10px}
.vela-roche .v-ios-media-picker{position:relative;min-height:42px;border-radius:13px;background:var(--v-blue-soft);color:var(--v-blue);display:flex;align-items:center;justify-content:center;gap:7px;font-size:12px;font-weight:800;overflow:hidden}
.vela-roche .v-ios-media-picker input[type=file]{position:absolute!important;inset:0!important;width:100%!important;height:100%!important;opacity:0!important;cursor:pointer!important;pointer-events:auto!important}
.vela-roche .v-ios-media-remove{min-width:72px;border:0;border-radius:13px;background:rgba(118,118,128,.12);color:var(--v-muted);font-size:11px;font-weight:800;padding:0 12px}
.vela-roche .v-ios-url{margin-top:9px;background:rgba(118,118,128,.08);border-radius:13px;padding:9px 11px}
.vela-roche .v-ios-url label{display:block;font-size:10px;color:var(--v-muted);margin-bottom:5px}
.vela-roche .v-ios-url input{width:100%;border:0;background:transparent;color:var(--v-text);font-size:15px;outline:none}
.vela-roche .v-publish-sheet-actions{position:sticky;bottom:0;padding:8px 0 calc(3px + env(safe-area-inset-bottom,0px));background:linear-gradient(transparent,#f2f2f7 28%)}
.vela-roche .v-publish-sheet-actions .v-action{border-radius:14px!important;min-height:46px!important}
.vela-roche .v-primary-red{background:var(--v-blue)!important;color:#fff!important}
.vela-roche .v-live-info{padding:8px 11px!important;border-radius:15px!important;margin-top:7px!important}
.vela-roche .v-live-info-head{margin-bottom:4px!important;gap:5px!important}
.vela-roche .v-live-info-head .v-badge{padding:4px 6px!important;border-radius:7px!important;font-size:8px!important}
.vela-roche .v-live-info-head .v-cohost-pill{padding:4px 7px!important;border-radius:8px!important;font-size:9px!important}
.vela-roche .v-live-info h2{font-size:15px!important;line-height:1.28!important;margin:0!important}
.vela-roche .v-live-info p{font-size:10px!important;line-height:1.35!important;margin:3px 0 0!important;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}
.vela-roche .v-live-info .v-translate-btn{margin-top:2px!important}
.vela-roche .v-remote-grid{position:absolute;z-index:2;inset:0;display:grid!important;gap:1px;background:rgba(255,255,255,.13);pointer-events:none}
.vela-roche .v-remote-grid[data-count="2"]{grid-template-columns:1fr 1fr}
.vela-roche .v-remote-grid[data-count="3"]{grid-template-columns:1fr 1fr;grid-template-rows:1fr 1fr}
.vela-roche .v-remote-grid[data-count="3"] .v-remote-pane:first-child{grid-row:1 / span 2}
.vela-roche .v-remote-grid[data-count="4"]{grid-template-columns:1fr 1fr;grid-template-rows:1fr 1fr}
.vela-roche .v-remote-pane{position:relative!important;min-width:0;background:linear-gradient(150deg,#252a33,#4c5668)!important;padding:58px 9px 10px!important;display:flex!important;align-items:flex-end!important}
.vela-roche .v-remote-pane:nth-child(2n){background:linear-gradient(150deg,#313743,#5d687b)!important}
.vela-roche .v-remote-pane-label{position:absolute!important;left:9px!important;top:58px!important;z-index:4;max-width:calc(100% - 18px);background:rgba(15,15,18,.58);backdrop-filter:blur(8px);border-radius:15px;padding:4px 7px 4px 4px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.vela-roche .v-remote-pane-event{position:relative;z-index:4;width:100%;max-height:42%;overflow:auto;border-radius:13px;background:rgba(15,15,18,.72);backdrop-filter:blur(10px);padding:8px 9px;color:#fff}
.vela-roche .v-remote-event-label{font-size:8px;font-weight:900;color:rgba(255,255,255,.66);margin-bottom:3px}
.vela-roche .v-remote-event-text{font-size:10.5px;line-height:1.42}
.vela-roche .v-remote-event-empty{font-size:9px;color:rgba(255,255,255,.58)}
.vela-roche .v-live-stage.is-remote .v-live-moment{display:none!important}
.vela-roche .v-live-stage.is-remote:after{z-index:1}
.vela-roche .v-replay-card{width:100%;border:0;background:var(--v-card);color:var(--v-text);text-align:left;border-radius:20px;padding:12px;margin-bottom:10px}
.vela-roche .v-replay-cover{height:130px;border-radius:15px;background:linear-gradient(145deg,#e8eef8,#f8f8fa);overflow:hidden;display:grid;place-items:center;font-size:34px;margin-top:9px}
.vela-roche .v-replay-cover img{width:100%;height:100%;object-fit:cover}
.vela-roche .v-replay-actions{display:flex;justify-content:space-between;align-items:center;gap:8px;margin-top:9px}
.vela-roche .v-replay-actions span{font-size:10px;color:var(--v-muted)}
.vela-roche .v-replay-actions button{border:0;background:rgba(255,59,48,.10);color:var(--v-red);border-radius:10px;padding:6px 8px;font-size:9px;font-weight:800}
.vela-roche .v-replay-summary{background:var(--v-card);border-radius:18px;padding:12px;margin-bottom:10px}
.vela-roche .v-replay-timeline{display:grid;gap:8px}
.vela-roche .v-replay-event{background:var(--v-card);border-radius:16px;padding:10px 11px}
.vela-roche .v-replay-event-head{font-size:9px;color:var(--v-muted);font-weight:800;margin-bottom:4px}
.vela-roche .v-replay-event-text{font-size:12px;line-height:1.5;white-space:pre-wrap}
.vela-roche .v-replay-chat{background:var(--v-card);border-radius:18px;padding:10px 12px;margin-top:12px}
.vela-roche .v-replay-chat .v-chatline{border-bottom:1px solid var(--v-line)}
.vela-roche .v-replay-chat .v-chatline:last-child{border-bottom:0}


/* v0.1.16 · unified live timeline */
.vela-roche .v-remote-grid{display:none!important}
.vela-roche .v-live-stage.is-remote .v-live-moment{display:block!important}
.vela-roche .v-live-stage.is-remote:after{z-index:auto}
.vela-roche .v-live-moment{left:14px!important;right:14px!important;bottom:14px!important;max-height:46%!important;padding:9px 10px!important;overflow:auto!important;scrollbar-width:none}
.vela-roche .v-live-moment::-webkit-scrollbar{display:none}
.vela-roche .v-live-timeline{display:grid;gap:8px}
.vela-roche .v-live-timeline-empty{font-size:10px;line-height:1.45;color:rgba(255,255,255,.58);padding:2px 1px}
.vela-roche .v-live-time-entry{padding:0 0 8px;border-bottom:1px solid rgba(255,255,255,.12)}
.vela-roche .v-live-time-entry:last-child{border-bottom:0;padding-bottom:0}
.vela-roche .v-live-time-head{display:flex;align-items:center;gap:7px;min-width:0;margin-bottom:4px}
.vela-roche .v-live-time-pill{flex:0 0 auto;padding:3px 6px;border-radius:7px;background:rgba(142,142,147,.72);color:#fff;font-size:8px;font-weight:850;font-variant-numeric:tabular-nums}
.vela-roche .v-live-time-actor{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:#fff;font-size:10px;font-weight:900}
.vela-roche .v-live-time-action{font-size:10.5px;line-height:1.45;color:rgba(255,255,255,.62);font-style:italic;margin:2px 0}
.vela-roche .v-live-time-speech{font-size:12px;line-height:1.48;color:#fff;margin:2px 0}
.vela-roche .v-live-time-entry .v-translate-btn{color:rgba(255,255,255,.68)!important;margin-top:2px!important}
.vela-roche .v-live-time-entry .v-translation{color:rgba(255,255,255,.82)!important;border-left-color:rgba(255,255,255,.26)!important}


/* v0.1.17 · profile activity / subscriptions / DM relationships */
.vela-roche .v-profile-message{border:0;border-radius:12px;background:var(--v-soft);color:var(--v-text);padding:8px 11px;font-size:10px;font-weight:900;white-space:nowrap}
.vela-roche .v-activity-stack{display:grid;gap:10px}
.vela-roche .v-activity-pin{background:var(--v-card);border:1px solid var(--v-line);border-radius:18px;padding:11px}
.vela-roche .v-activity-pin .v-kicker{display:flex;align-items:center;gap:6px;font-size:9px;font-weight:900;color:var(--v-muted);margin-bottom:7px}
.vela-roche .v-activity-pin.is-scheduled .v-kicker:before{content:"";width:7px;height:7px;border-radius:50%;background:#8e8e93}
.vela-roche .v-activity-pin.is-live .v-kicker{color:var(--v-red)}
.vela-roche .v-activity-pin.is-live .v-kicker:before{content:"";width:7px;height:7px;border-radius:50%;background:var(--v-red);box-shadow:0 0 0 4px rgba(255,59,48,.10)}
.vela-roche .v-activity-pin button{width:100%;border:0;background:none;padding:0;text-align:left;color:inherit}
.vela-roche .v-activity-compose{margin:3px 0 2px}
.vela-roche .v-activity-posts{display:grid;gap:10px;margin-top:2px}
.vela-roche .v-subscription-head{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:10px}
.vela-roche .v-subscription-head h3{font-size:16px;margin:0}.vela-roche .v-subscription-head p{font-size:10px;color:var(--v-muted);margin:3px 0 0}
.vela-roche .v-subscription-head button{border:0;border-radius:11px;background:var(--v-soft);color:var(--v-text);padding:8px 9px;font-size:10px;font-weight:900}
.vela-roche .v-membership-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;align-items:stretch}
.vela-roche .v-membership-card{min-width:0;background:var(--v-card);border:1px solid var(--v-line);border-radius:20px;overflow:hidden;display:flex;flex-direction:column}
.vela-roche .v-membership-card.is-paid{border-color:rgba(255,59,48,.36)}
.vela-roche .v-membership-cover{height:88px;background:linear-gradient(145deg,#e9edf3,#f8f8fa);display:grid;place-items:center;font-size:30px;overflow:hidden}
.vela-roche .v-membership-cover img{width:100%;height:100%;object-fit:cover;display:block}
.vela-roche .v-membership-body{padding:11px;display:flex;flex:1;flex-direction:column}
.vela-roche .v-membership-title{font-size:15px;font-weight:950;line-height:1.2;word-break:break-word}
.vela-roche .v-membership-price{font-size:22px;font-weight:950;margin-top:5px;letter-spacing:-.03em}.vela-roche .v-membership-price small{font-size:10px;font-weight:700;margin-left:2px}
.vela-roche .v-membership-button{width:100%;border:0;border-radius:12px;background:var(--v-blue);color:#fff;padding:9px 8px;font-size:10px;font-weight:900;margin:10px 0}
.vela-roche .v-membership-card.is-paid .v-membership-button{background:var(--v-red)}
.vela-roche .v-membership-button.is-current{background:var(--v-soft)!important;color:var(--v-muted)!important}
.vela-roche .v-membership-intro{font-size:10px;line-height:1.45;color:var(--v-text);font-weight:750;white-space:pre-wrap}
.vela-roche .v-membership-benefits{display:grid;gap:4px;margin-top:8px;font-size:9px;line-height:1.4;color:var(--v-muted)}
.vela-roche .v-membership-benefits div:before{content:"•";margin-right:5px;color:var(--v-text)}
.vela-roche .v-membership-community{margin-top:auto;padding-top:9px;border-top:1px solid var(--v-line);font-size:9px;line-height:1.4;color:var(--v-muted)}
.vela-roche .v-membership-community b{display:block;color:var(--v-text);font-size:10px;margin-top:2px}
.vela-roche .v-your-membership{margin-top:12px;background:var(--v-card);border-radius:18px;border:1px solid var(--v-line);padding:12px}
.vela-roche .v-your-membership-label{font-size:10px;color:var(--v-muted);font-weight:900}.vela-roche .v-your-membership-value{font-size:14px;font-weight:950;margin-top:5px}
.vela-roche .v-your-membership-note{font-size:10px;color:var(--v-muted);margin-top:4px;line-height:1.45}
.vela-roche .v-relation{display:inline-flex;align-items:center;gap:3px;margin-left:5px;padding:3px 6px;border-radius:8px;font-size:8px;font-weight:900;vertical-align:1px}
.vela-roche .v-relation.is-stranger{background:#eeeeF2;color:#74747c}.vela-roche .v-relation.is-free{background:#e8f2ff;color:#007aff}.vela-roche .v-relation.is-paid{background:#fff0f1;color:#ff3b30}.vela-roche .v-relation.is-mutual{background:#ffe9f4;color:#d63384}.vela-roche .v-relation.is-business{background:#fff1d8;color:#946100}
.vela-roche .v-community-section-title{font-size:10px;color:var(--v-muted);font-weight:900;margin:5px 2px 7px}
.vela-roche .v-community-list{background:var(--v-card);border-radius:18px;overflow:hidden;border:1px solid var(--v-line);margin-bottom:12px}
.vela-roche .v-community-entry{width:100%;border:0;border-top:1px solid var(--v-line);background:none;color:inherit;display:flex;align-items:center;gap:10px;padding:11px;text-align:left}.vela-roche .v-community-entry:first-child{border-top:0}
.vela-roche .v-community-entry .v-social-avatar{width:40px;height:40px}.vela-roche .v-community-entry .v-meta{min-width:0;flex:1}.vela-roche .v-community-owner{font-size:9px;color:var(--v-muted);margin-top:3px}.vela-roche .v-community-tier-dot{font-size:8px;font-weight:900;border-radius:8px;padding:4px 6px}.vela-roche .v-community-tier-dot.is-free{background:#e8f2ff;color:#007aff}.vela-roche .v-community-tier-dot.is-paid{background:#fff0f1;color:#ff3b30}
.vela-roche .v-group-chat{display:flex;flex-direction:column;min-height:calc(100vh - var(--v-top-h) - 28px)}
.vela-roche .v-group-lines{flex:1;display:grid;align-content:start;gap:8px}.vela-roche .v-group-line{display:flex;gap:8px;align-items:flex-start}.vela-roche .v-group-line .v-social-avatar{width:30px;height:30px;font-size:10px}.vela-roche .v-group-bubble{max-width:78%;background:var(--v-card);border:1px solid var(--v-line);border-radius:15px;padding:8px 10px}.vela-roche .v-group-bubble b{font-size:9px}.vela-roche .v-group-bubble div{font-size:11px;line-height:1.45;margin-top:2px;white-space:pre-wrap}.vela-roche .v-group-empty{padding:44px 10px;text-align:center;color:var(--v-muted);font-size:11px;line-height:1.55}
.vela-roche .v-group-composer,.vela-roche .v-dm-composer{position:sticky;bottom:0;display:grid;grid-template-columns:1fr 44px;gap:8px;background:var(--v-bg);padding-top:10px}.vela-roche .v-group-composer input,.vela-roche .v-dm-composer input{min-width:0;border:1px solid var(--v-line);background:var(--v-card);color:var(--v-text);border-radius:14px;padding:10px 11px;font-size:16px}.vela-roche .v-group-composer button,.vela-roche .v-dm-composer button{border:0;border-radius:14px;background:#111;color:#fff;font-weight:900}
.vela-roche .v-subscription-editor-grid{display:grid;gap:12px}.vela-roche .v-subscription-editor-tier{background:var(--v-card);border-radius:20px;padding:13px;border:1px solid var(--v-line)}.vela-roche .v-subscription-editor-tier h3{font-size:14px;margin:0 0 11px}.vela-roche .v-subscription-editor-tier.is-paid h3{color:var(--v-red)}
@media (max-width:390px){.vela-roche .v-membership-grid{grid-template-columns:1fr}}


/* v0.1.18 · activity is display-only; own subscription list */
.vela-roche .v-inline-manage{display:flex;align-items:center;justify-content:flex-end;gap:7px;margin-top:9px;padding-top:9px;border-top:1px solid var(--v-line)}
.vela-roche .v-inline-manage button{border:0;border-radius:10px;background:var(--v-soft);color:var(--v-text);padding:7px 9px;font-size:9px;font-weight:900}
.vela-roche .v-inline-manage button.is-danger{background:rgba(255,59,48,.09);color:var(--v-red)}
.vela-roche .v-my-subscriptions{display:grid;gap:10px}
.vela-roche .v-my-sub-card{background:var(--v-card);border:1px solid var(--v-line);border-radius:18px;padding:12px}
.vela-roche .v-my-sub-head{display:flex;align-items:center;gap:10px}
.vela-roche .v-my-sub-head .v-social-avatar{width:44px;height:44px}
.vela-roche .v-my-sub-head .v-meta{min-width:0;flex:1}
.vela-roche .v-my-sub-title{font-size:13px;font-weight:950}
.vela-roche .v-my-sub-handle{font-size:10px;color:var(--v-muted);margin-top:2px}
.vela-roche .v-my-sub-tier{display:inline-flex;align-items:center;gap:4px;border-radius:9px;padding:4px 7px;font-size:8px;font-weight:950;white-space:nowrap}
.vela-roche .v-my-sub-tier.is-free{background:#e8f2ff;color:#007aff}
.vela-roche .v-my-sub-tier.is-paid{background:#fff0f1;color:#ff3b30}
.vela-roche .v-my-sub-detail{margin-top:10px;padding-top:9px;border-top:1px solid var(--v-line);display:grid;gap:4px;font-size:10px;line-height:1.45;color:var(--v-muted)}
.vela-roche .v-my-sub-detail b{color:var(--v-text);font-size:10px}
.vela-roche .v-my-sub-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px}
.vela-roche .v-my-sub-actions button{border:0;border-radius:12px;min-height:38px;font-size:10px;font-weight:900}
.vela-roche .v-my-sub-actions .v-continue-sub{background:var(--v-blue-soft);color:var(--v-blue)}
.vela-roche .v-my-sub-actions .v-cancel-sub{background:rgba(255,59,48,.09);color:var(--v-red)}
.vela-roche .v-own-subscription-tools{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:11px}
.vela-roche .v-own-subscription-tools h3{font-size:16px;margin:0}.vela-roche .v-own-subscription-tools p{font-size:10px;color:var(--v-muted);margin:3px 0 0}
.vela-roche .v-own-subscription-tools button{border:0;border-radius:11px;background:var(--v-soft);color:var(--v-text);padding:8px 9px;font-size:9px;font-weight:900;white-space:nowrap}


/* v0.1.19 · unified DM / community chat */
.vela-roche .v-chat-screen{height:100%;min-height:0;display:flex;flex-direction:column;background:var(--v-bg)}
.vela-roche .v-chat-head{position:sticky;top:0;z-index:5;height:var(--v-top-h);padding:0 11px;display:grid;grid-template-columns:40px minmax(0,1fr) auto;align-items:center;gap:8px;background:rgba(242,242,247,.94);backdrop-filter:blur(18px);border-bottom:1px solid var(--v-line)}
.vela-roche .v-chat-back,.vela-roche .v-chat-headicon{width:36px;height:36px;border:0;border-radius:12px;background:var(--v-card);color:var(--v-text);display:grid;place-items:center;font-size:19px}
.vela-roche .v-chat-headcopy{min-width:0;text-align:center}
.vela-roche .v-chat-headcopy strong{display:block;font-size:13px;line-height:1.15;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.vela-roche .v-chat-headcopy small{display:block;margin-top:3px;font-size:9px;font-weight:650;color:var(--v-muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.vela-roche .v-chat-headactions{display:flex;align-items:center;gap:5px}
.vela-roche .v-chat-headicon{font-size:14px;background:transparent}
.vela-roche .v-chat-headicon.is-edit{font-size:13px;font-weight:900}
.vela-roche .v-chat-topmenu{position:absolute;z-index:10;right:10px;top:calc(var(--v-top-h) - 6px);min-width:142px;padding:6px;background:rgba(255,255,255,.96);backdrop-filter:blur(18px);border:1px solid var(--v-line);border-radius:14px;box-shadow:0 12px 35px rgba(0,0,0,.14);display:none}
.vela-roche .v-chat-topmenu.is-open{display:block}
.vela-roche .v-chat-topmenu button{width:100%;border:0;background:none;border-radius:10px;padding:9px 10px;text-align:left;color:var(--v-red);font-size:10px;font-weight:900}
.vela-roche .v-chat-body{flex:1;min-height:0;overflow:auto;padding:11px 12px 88px;-webkit-overflow-scrolling:touch}
.vela-roche .v-chat-timestamp{text-align:center;color:var(--v-muted);font-size:8.5px;font-weight:750;margin:10px 0 9px}
.vela-roche .v-chat-row{display:flex;align-items:flex-end;gap:7px;margin:7px 0}
.vela-roche .v-chat-row.is-me{justify-content:flex-end}
.vela-roche .v-chat-row .v-social-avatar{width:28px;height:28px;font-size:9px}
.vela-roche .v-chat-stack{max-width:min(78%,430px);min-width:0}
.vela-roche .v-chat-sender{font-size:8px;color:var(--v-muted);font-weight:850;margin:0 0 3px 4px}
.vela-roche .v-chat-row.is-me .v-chat-sender{text-align:right;margin:0 4px 3px 0}
.vela-roche .v-chat-bubble{background:var(--v-card);color:var(--v-text);border:1px solid var(--v-line);border-radius:17px 17px 17px 6px;padding:9px 11px;font-size:12px;line-height:1.5;white-space:pre-wrap;word-break:break-word}
.vela-roche .v-chat-row.is-me .v-chat-bubble{background:#111;color:#fff;border-color:#111;border-radius:17px 17px 6px 17px}
.vela-roche .v-chat-bubble .v-translate-btn{display:block;margin-top:5px;color:var(--v-muted)}
.vela-roche .v-chat-row.is-me .v-chat-bubble .v-translate-btn{color:rgba(255,255,255,.65)}
.vela-roche .v-chat-bubble .v-translation{margin-top:5px;font-size:10px}
.vela-roche .v-chat-row.is-me .v-chat-bubble .v-translation{color:rgba(255,255,255,.78);border-left-color:rgba(255,255,255,.28)}
.vela-roche .v-chat-card{width:min(86%,450px);margin:9px auto;background:var(--v-card);border:1px solid var(--v-line);border-radius:18px;padding:12px;box-shadow:0 4px 16px rgba(0,0,0,.04)}
.vela-roche .v-chat-card-head{display:flex;align-items:center;justify-content:space-between;gap:10px}
.vela-roche .v-chat-card-type{font-size:9px;font-weight:950;color:var(--v-muted)}
.vela-roche .v-chat-card-status{font-size:8px;font-weight:900;border-radius:8px;padding:4px 6px;background:var(--v-soft);color:var(--v-muted)}
.vela-roche .v-chat-card-status.is-ok{background:#e8f7ee;color:#21834a}.vela-roche .v-chat-card-status.is-no{background:#fff0f1;color:var(--v-red)}
.vela-roche .v-chat-card-title{font-size:14px;font-weight:950;line-height:1.3;margin-top:8px}
.vela-roche .v-chat-card-meta{display:grid;gap:4px;margin-top:8px;font-size:10px;line-height:1.45;color:var(--v-muted)}
.vela-roche .v-chat-card-meta b{color:var(--v-text);font-weight:850}
.vela-roche .v-chat-card-actions{display:flex;gap:7px;margin-top:10px}
.vela-roche .v-chat-card-actions button{flex:1;border:0;border-radius:11px;min-height:36px;font-size:9px;font-weight:900;background:var(--v-blue);color:#fff}
.vela-roche .v-chat-card-actions button.is-light{background:var(--v-soft);color:var(--v-text)}
.vela-roche .v-chat-card-actions button.is-danger{background:rgba(255,59,48,.10);color:var(--v-red)}
.vela-roche .v-chat-composer-wrap{position:absolute;z-index:8;left:10px;right:10px;bottom:calc(9px + env(safe-area-inset-bottom,0px));display:flex;align-items:center;gap:6px;padding:6px;background:rgba(255,255,255,.92);backdrop-filter:blur(18px);border:1px solid var(--v-line);border-radius:22px;box-shadow:0 9px 28px rgba(0,0,0,.11)}
.vela-roche .v-chat-composer-wrap input{flex:1;min-width:0;border:0;background:transparent;color:var(--v-text);font-size:16px;padding:7px 8px;outline:none}
.vela-roche .v-chat-composer-wrap button{width:38px;height:38px;border:0;border-radius:16px;display:grid;place-items:center;font-weight:950}
.vela-roche .v-chat-plus{background:var(--v-soft);color:var(--v-text);font-size:20px}.vela-roche .v-chat-summon{background:#fff;color:#111;font-size:17px;box-shadow:inset 0 0 0 1px var(--v-line)}
.vela-roche .v-chat-summon.is-loading{opacity:.5;pointer-events:none}
.vela-roche .v-chat-tool-grid{display:grid;gap:8px}
.vela-roche .v-chat-tool-grid button{width:100%;border:0;border-radius:16px;background:var(--v-card);color:var(--v-text);padding:13px;text-align:left;font-size:12px;font-weight:900;border:1px solid var(--v-line)}
.vela-roche .v-chat-tool-grid button small{display:block;color:var(--v-muted);font-size:9px;font-weight:650;margin-top:3px}
.vela-roche .v-tool-form{display:grid;gap:11px}
.vela-roche .v-tool-note{font-size:9px;line-height:1.5;color:var(--v-muted)}
.vela-roche .v-community-announcement{width:min(92%,500px);margin:5px auto 10px;padding:8px 10px;border-radius:13px;background:var(--v-soft);font-size:9px;line-height:1.45;color:var(--v-muted)}
.vela-roche .v-community-card-msg{width:min(88%,460px);margin:9px auto;border-radius:18px;background:var(--v-card);border:1px solid var(--v-line);padding:12px}
.vela-roche .v-community-card-msg .v-kicker{font-size:8px;color:var(--v-muted);font-weight:950}
.vela-roche .v-community-card-msg h4{font-size:13px;margin:7px 0 5px}.vela-roche .v-community-card-msg p{font-size:10px;line-height:1.45;color:var(--v-muted);margin:0}
.vela-roche .v-poll-options{display:grid;gap:6px;margin-top:9px}.vela-roche .v-poll-options button{border:0;border-radius:11px;background:var(--v-soft);color:var(--v-text);min-height:36px;padding:7px 9px;text-align:left;font-size:10px;font-weight:800;display:flex;justify-content:space-between;gap:8px}
.vela-roche .v-poll-options button.is-voted{background:var(--v-blue-soft);color:var(--v-blue)}
.vela-roche .v-lottery-action{margin-top:9px;width:100%;border:0;border-radius:11px;background:var(--v-blue);color:#fff;min-height:37px;font-size:10px;font-weight:900}
.vela-roche .v-community-edit-grid{display:grid;gap:12px}
.vela-roche .v-community-preview{display:flex;align-items:center;gap:10px;padding:11px;background:var(--v-card);border:1px solid var(--v-line);border-radius:18px}
.vela-roche .v-community-preview .v-social-avatar{width:54px;height:54px}
.vela-roche .v-community-botchecks{display:grid;gap:7px}.vela-roche .v-community-check{display:flex;align-items:center;justify-content:space-between;gap:10px;font-size:10px;padding:8px 0;border-top:1px solid var(--v-line)}.vela-roche .v-community-check:first-child{border-top:0}
.vela-roche .v-appointment-card{margin:18px;background:var(--v-card);border-radius:22px;padding:16px;border:1px solid var(--v-line)}
.vela-roche .v-appointment-card h3{font-size:18px;margin:0 0 7px}.vela-roche .v-appointment-card p{font-size:11px;line-height:1.55;color:var(--v-muted)}
.vela-roche .v-appointment-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:13px}.vela-roche .v-appointment-actions button{border:0;border-radius:13px;min-height:42px;font-size:10px;font-weight:900}.vela-roche .v-appointment-actions .v-go{background:var(--v-blue);color:#fff}.vela-roche .v-appointment-actions .v-later{background:var(--v-soft);color:var(--v-text)}
.vela-roche.is-dark .v-chat-head{background:rgba(16,17,22,.94)}.vela-roche.is-dark .v-chat-topmenu,.vela-roche.is-dark .v-chat-composer-wrap{background:rgba(25,26,32,.94)}


/* v0.1.20 · DM polish / Vela Coin wallet / live economy */
.vela-roche .v-msg .v-mini{border-radius:50%!important}
.vela-roche .v-chat-head{grid-template-columns:40px minmax(0,1fr) auto!important}
.vela-roche .v-chat-headcopy{text-align:left!important}
.vela-roche .v-chat-headcopy strong{font-size:13px!important}
.vela-roche .v-chat-headcopy small{font-size:9px!important}
.vela-roche .v-chat-row .v-social-avatar{border-radius:50%!important}
.vela-roche .v-chat-bubble{position:relative;border-radius:20px 20px 20px 7px!important;padding:9px 12px!important}
.vela-roche .v-chat-row:not(.is-me) .v-chat-bubble:after{content:"";position:absolute;left:-5px;bottom:2px;width:11px;height:12px;background:var(--v-card);clip-path:polygon(100% 0,100% 100%,0 100%);border-bottom-left-radius:8px}
.vela-roche .v-chat-row.is-me .v-chat-bubble{border-radius:20px 20px 7px 20px!important}
.vela-roche .v-chat-row.is-me .v-chat-bubble:after{content:"";position:absolute;right:-5px;bottom:2px;width:11px;height:12px;background:#111;clip-path:polygon(0 0,100% 100%,0 100%);border-bottom-right-radius:8px}
.vela-roche .v-chat-bubble .v-translation{margin-top:7px!important;padding:7px 0 0 0!important;border-left:0!important;border-top:1px solid var(--v-line);font-size:10px!important}
.vela-roche .v-chat-row.is-me .v-chat-bubble .v-translation{border-top-color:rgba(255,255,255,.2)!important}
.vela-roche .v-chat-bubble .v-translate-btn{font-size:9px!important;font-weight:850!important}
.vela-roche .v-role-title{display:inline-flex;align-items:center;padding:2px 5px;border-radius:6px;margin-right:4px;font-size:7px;font-weight:950;vertical-align:1px}
.vela-roche .v-role-title.is-owner{background:#111;color:#fff}.vela-roche .v-role-title.is-bot{background:#e8f2ff;color:#007aff}
.vela-roche .v-chat-card{border-radius:20px!important;box-shadow:none!important}
.vela-roche .v-chat-card.is-business{border-color:rgba(0,122,255,.18)}
.vela-roche .v-chat-card-edit{margin-top:8px;border:0;background:none;color:var(--v-blue);font-size:9px;font-weight:900;padding:2px 0}
.vela-roche .v-chat-card-actions{display:grid;grid-template-columns:1fr 1fr}
.vela-roche .v-chat-card-actions button:first-child:last-child{grid-column:1/-1}
.vela-roche .v-community-bot-options[hidden]{display:none!important}
.vela-roche .v-community-bot-profile{display:grid;grid-template-columns:72px 1fr;gap:10px;align-items:center;margin-top:10px}
.vela-roche .v-community-bot-avatar{width:64px;height:64px;border-radius:50%;background:var(--v-soft);overflow:hidden;display:grid;place-items:center;font-weight:950}
.vela-roche .v-community-bot-avatar img{width:100%;height:100%;object-fit:cover;border-radius:50%}

.vela-roche .v-wallet-assets{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:12px}
.vela-roche .v-wallet-asset{border-radius:22px;padding:16px;min-height:150px;display:flex;flex-direction:column;justify-content:space-between}
.vela-roche .v-wallet-asset.is-coin{background:linear-gradient(145deg,#0f62ff,#69a8ff);color:#fff}
.vela-roche .v-wallet-asset.is-cash{background:linear-gradient(145deg,#16171c,#4d5059);color:#fff}
.vela-roche .v-wallet-asset-label{font-size:10px;font-weight:850;opacity:.72}
.vela-roche .v-wallet-asset-value{font-size:25px;font-weight:950;letter-spacing:-.03em;margin-top:6px}
.vela-roche .v-wallet-asset-sub{font-size:8px;opacity:.68;margin-top:5px;line-height:1.4}
.vela-roche .v-wallet-asset-actions{display:flex;gap:6px;margin-top:12px}
.vela-roche .v-wallet-asset-actions button{flex:1;border:0;border-radius:11px;min-height:34px;background:rgba(255,255,255,.92);color:#111;font-size:9px;font-weight:900}
.vela-roche .v-wallet-account{background:var(--v-card);border:1px solid var(--v-line);border-radius:19px;padding:13px;margin-bottom:12px}
.vela-roche .v-wallet-account-head{display:flex;align-items:center;justify-content:space-between;gap:10px}
.vela-roche .v-wallet-account-head button{border:0;background:var(--v-soft);color:var(--v-text);border-radius:10px;padding:7px 9px;font-size:9px;font-weight:900}
.vela-roche .v-wallet-card-no{font-size:15px;font-weight:900;letter-spacing:.06em;margin-top:7px}
.vela-roche .v-wallet-ledger-head{display:flex;align-items:flex-end;justify-content:space-between;gap:10px}.vela-roche .v-wallet-ledger-head small{font-size:8px;color:var(--v-muted);text-align:right}
.vela-roche .v-tx{align-items:center}.vela-roche .v-tx-amount{font-weight:950;white-space:nowrap}.vela-roche .v-tx-amount.is-coin{color:var(--v-blue)}
.vela-roche .v-wallet-flow-card{background:var(--v-card);border:1px solid var(--v-line);border-radius:22px;padding:14px}
.vela-roche .v-wallet-convert-preview{padding:10px 12px;background:var(--v-soft);border-radius:13px;font-size:10px;color:var(--v-muted);line-height:1.5}


/* v0.2.2 · account cap / live compact cards / bottom icon polish */
.vela-roche .v-nav-icon{width:25px;height:25px;object-fit:contain;display:block;flex:0 0 auto}.vela-roche .v-nav button[data-nav="wallet"] .v-nav-icon{width:23px;height:23px}
.vela-roche .v-nav button{gap:3px!important}
.vela-roche .v-nav .v-publish b{width:36px!important;height:36px!important;border-radius:13px!important;font-size:22px!important;margin-top:0!important;margin-bottom:0}
.vela-roche .v-drawer-clear{width:100%;border:0;background:none;color:#c9362b;padding:13px 4px;text-align:left;font-size:13px;font-weight:850;border-top:1px solid var(--v-line);margin-bottom:8px}
.vela-roche .v-rolecard>.v-author .v-mini,.vela-roche [data-screen="roles"] .v-mini{width:44px!important;height:44px!important;flex:0 0 44px!important;aspect-ratio:1/1!important;border-radius:50%!important;overflow:hidden!important}
.vela-roche [data-screen="roles"] .v-mini img{width:100%!important;height:100%!important;object-fit:cover!important;border-radius:50%!important}
.vela-roche .v-account-delete{background:#fff1f1!important;color:#c9362b!important}
.vela-roche .v-live-quicktools{margin-left:0!important;order:3}
.vela-roche .v-live-quicktool,.vela-roche .v-live-quicktool.is-lottery,.vela-roche .v-live-quicktool.is-ad{height:38px!important;min-width:38px!important;border-radius:19px!important;padding:0 10px!important;background:rgba(18,18,22,.66)!important;color:#fff!important;font-size:9px!important;font-weight:900!important;backdrop-filter:blur(10px)}
.vela-roche .v-gift-rank-trigger{margin-left:auto!important;order:4!important;background:rgba(18,18,22,.66)!important;color:#fff!important}
.vela-roche .v-live-title-row{display:flex;align-items:center;justify-content:space-between;gap:10px}
.vela-roche .v-live-title-row h2{min-width:0;flex:1}
.vela-roche .v-live-connect-btn{flex:0 0 auto;border:0;border-radius:12px;background:var(--v-soft);color:var(--v-text);padding:7px 10px;font-size:9px;font-weight:900;white-space:nowrap}
.vela-roche .v-live-ad-strip{display:none!important}
.vela-roche .v-live-body{padding:7px 8px 8px!important}
.vela-roche .v-live-stage{border-radius:19px!important}
.vela-roche .v-live-info{margin-top:7px!important;padding:9px 11px!important}
.vela-roche .v-chat-panel{flex:0 1 205px!important;min-height:118px!important;max-height:245px!important;margin-top:7px!important;padding:9px 11px!important}
.vela-roche .v-chat-scroll{min-height:45px!important}
.vela-roche .v-live-economy-panel{padding:14px!important}
.vela-roche .v-live-economy-card{width:min(92%,390px)!important;max-height:76%!important}
.vela-roche .v-live-summary-card{width:min(92%,390px);background:var(--v-card);color:var(--v-text);border-radius:24px;padding:15px;box-shadow:0 18px 50px rgba(0,0,0,.22)}
.vela-roche .v-live-summary-card h3{margin:0 0 4px;font-size:16px}
.vela-roche .v-live-summary-card .v-hint{margin-bottom:10px}
.vela-roche .v-live-summary-close{width:100%;margin-top:12px;border:0;border-radius:13px;min-height:40px;background:#111;color:#fff;font-size:10px;font-weight:900}
.vela-roche .v-readonly-link{background:var(--v-soft)!important;color:var(--v-muted)!important}

.vela-roche .v-live-quicktools{margin-left:auto;display:flex;align-items:center;gap:6px;pointer-events:auto}
.vela-roche .v-live-quicktool{height:34px;min-width:34px;border:0;border-radius:17px;padding:0 9px;color:#fff;font-size:9px;font-weight:950;backdrop-filter:blur(10px)}
.vela-roche .v-live-quicktool.is-lottery{background:rgba(52,199,89,.86)}
.vela-roche .v-live-quicktool.is-ad{background:rgba(255,59,48,.86)}
.vela-roche .v-gift-rank-trigger{margin-left:0!important;max-width:36%!important}
.vela-roche .v-live-ad-strip{display:flex;align-items:center;gap:7px;margin-top:8px;padding:8px 10px;border-radius:13px;background:#fff2f1;color:#a92720;font-size:9px;line-height:1.35}
.vela-roche .v-live-ad-strip b{white-space:nowrap}.vela-roche .v-live-ad-strip a{color:inherit;text-decoration:none;font-weight:850;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.vela-roche .v-chat-tip{display:inline-flex;margin-left:5px;padding:2px 5px;border-radius:7px;background:#e8f2ff;color:#007aff;font-size:8px;font-weight:950}
.vela-roche .v-live-economy-panel{position:absolute;z-index:35;inset:0;background:rgba(0,0,0,.28);display:none;align-items:center;justify-content:center;padding:18px}.vela-roche .v-live-economy-panel.is-open{display:flex}
.vela-roche .v-live-economy-card{width:min(92%,430px);max-height:78%;overflow:auto;background:var(--v-card);color:var(--v-text);border-radius:23px;padding:14px;box-shadow:0 18px 50px rgba(0,0,0,.2)}
.vela-roche .v-live-tool-title{display:flex;align-items:center;justify-content:space-between;gap:10px}.vela-roche .v-live-tool-title h3{margin:0;font-size:15px}.vela-roche .v-live-tool-title button{width:34px;height:34px;border:0;border-radius:12px;background:var(--v-soft);color:var(--v-text)}
.vela-roche .v-prize-row{display:grid;grid-template-columns:78px 64px 1fr 74px;gap:6px;margin-top:8px}.vela-roche .v-prize-row input{min-width:0;border:1px solid var(--v-line);background:var(--v-card);color:var(--v-text);border-radius:10px;padding:8px;font-size:11px}
.vela-roche .v-prize-remove{width:100%;border:0;border-radius:10px;background:rgba(255,59,48,.08);color:var(--v-red);padding:7px;font-size:9px;font-weight:900;margin-top:6px}
.vela-roche .v-live-tool-actions{display:flex;gap:8px;margin-top:11px}.vela-roche .v-live-tool-actions button{flex:1;border:0;border-radius:12px;min-height:39px;font-size:10px;font-weight:900}.vela-roche .v-live-tool-actions .is-primary{background:#111;color:#fff}.vela-roche .v-live-tool-actions .is-light{background:var(--v-soft);color:var(--v-text)}
.vela-roche .v-live-summary-grid{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin-top:12px}.vela-roche .v-live-summary-box{background:var(--v-soft);border-radius:15px;padding:11px}.vela-roche .v-live-summary-box small{display:block;color:var(--v-muted);font-size:8px}.vela-roche .v-live-summary-box b{display:block;margin-top:4px;font-size:15px}
@media(max-width:390px){.vela-roche .v-wallet-assets{grid-template-columns:1fr}.vela-roche .v-prize-row{grid-template-columns:1fr 58px}.vela-roche .v-prize-row input:nth-child(3),.vela-roche .v-prize-row input:nth-child(4){grid-column:auto}}

.vela-roche.is-dark{--v-bg:#101116;--v-card:#191a20;--v-text:#f4f4f6;--v-muted:#a6a7b0;--v-line:#2b2d34;--v-soft:#23252c}.vela-roche.is-dark .v-top,.vela-roche.is-dark .v-nav,.vela-roche.is-dark .v-drawer,.vela-roche.is-dark .v-sheet,.vela-roche.is-dark .v-subhead{background:rgba(16,17,22,.96);color:var(--v-text)}.vela-roche.is-dark .v-card,.vela-roche.is-dark .v-userhead,.vela-roche.is-dark .v-identity-card,.vela-roche.is-dark .v-roche-compact,.vela-roche.is-dark .v-linkbox{background:var(--v-card);color:var(--v-text)}.vela-roche.is-dark .v-plainbtn,.vela-roche.is-dark .v-subhead>button:first-child,.vela-roche.is-dark .v-subhead .v-head-action{background:#24262d!important;color:#f4f4f6!important}.vela-roche.is-dark .v-profile-tabs button.is-active{color:#fff}.vela-roche.is-dark .v-profile-tabs button.is-active:after{background:#fff}.vela-roche.is-dark .v-chip{background:#262830;color:#ddd}.vela-roche.is-dark .v-chip.is-active,.vela-roche.is-dark .v-action{background:#f4f4f6;color:#111}.vela-roche.is-dark .v-action.light,.vela-roche.is-dark .v-follow.is-on{background:#292b33;color:#f4f4f6}.vela-roche.is-dark .v-avatar,.vela-roche.is-dark .v-mini{background:#2a2c33;color:#f4f4f6}.vela-roche.is-dark .v-exit{background:#24262d}.vela-roche.is-dark .v-switch:checked{background:#f4f4f6}.vela-roche.is-dark .v-switch:checked:after{background:#111}

.vela-roche .v-subscreen[data-screen="live"]{overflow:hidden!important}
.vela-roche .v-live-screen{height:100%;min-height:0!important;display:flex;flex-direction:column;overflow:hidden;background:var(--v-bg)!important;color:var(--v-text)!important}
.vela-roche .v-live-body{flex:1;min-height:0;display:flex;flex-direction:column;overflow:hidden;padding:10px 12px 10px!important}
.vela-roche .v-live-fixed{flex:0 0 auto;min-height:0}
/* 直播本体占视觉中心：完整 16:9，不再保留额外顶栏 */
.vela-roche .v-live-stage{flex:0 0 auto;aspect-ratio:16/9!important;max-height:none!important;min-height:0!important;position:relative;border-radius:20px!important}
.vela-roche .v-live-info{flex:0 0 auto;padding:11px 13px!important}
.vela-roche .v-live-hud{position:absolute;z-index:6;left:14px;right:14px;top:14px;display:flex;align-items:center;gap:8px;pointer-events:none}
.vela-roche .v-live-hud button,.vela-roche .v-live-hud .v-live-host{pointer-events:auto}
.vela-roche .v-live-exit{width:36px;height:36px;border:0;border-radius:50%;background:rgba(18,18,22,.66);backdrop-filter:blur(10px);color:#fff;font-size:20px;display:grid;place-items:center;flex:0 0 auto}
.vela-roche .v-live-host{display:flex;align-items:center;gap:7px;min-width:0;max-width:48%;padding:4px 8px 4px 4px;border-radius:20px;background:rgba(18,18,22,.62);backdrop-filter:blur(10px);color:#fff}
.vela-roche .v-live-host-avatar{width:29px;height:29px;border-radius:50%;background:rgba(255,255,255,.18);overflow:hidden;display:grid;place-items:center;font-size:9px;font-weight:900;flex:0 0 auto}
.vela-roche .v-live-host-avatar img{width:100%;height:100%;object-fit:cover;display:block}
.vela-roche .v-live-host-copy{min-width:0}.vela-roche .v-live-host-name{font-size:10px;font-weight:900;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.vela-roche .v-live-viewers{font-size:9px;color:rgba(255,255,255,.76);white-space:nowrap}
.vela-roche .v-gift-rank-trigger{margin-left:auto;min-width:0;max-width:42%;height:38px;border:0;border-radius:19px;padding:0 10px;background:rgba(18,18,22,.66);backdrop-filter:blur(10px);color:#fff;font-size:9px;font-weight:900;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.vela-roche .v-live-stage-copy{display:none!important}
.vela-roche .v-live-moment{position:absolute;z-index:3;left:14px;right:14px;bottom:14px;margin:0;padding:9px 11px;border:1px solid rgba(255,255,255,.14);border-radius:15px;background:rgba(15,15,18,.74);backdrop-filter:blur(10px);color:#fff;box-shadow:0 8px 26px rgba(0,0,0,.16);max-height:42%;overflow:auto}
.vela-roche .v-live-moment-label{font-size:9px;font-weight:900;color:rgba(255,255,255,.68);margin-bottom:4px}
.vela-roche .v-live-moment-text{font-size:12px;line-height:1.48;color:#fff}
.vela-roche .v-live-moment .v-translate-btn{color:rgba(255,255,255,.72);margin-top:3px}
.vela-roche .v-live-moment .v-translation{color:rgba(255,255,255,.82);border-left-color:rgba(255,255,255,.28)}
/* 只有聊天室区域滚动，上面的直播画面和标题不动 */
.vela-roche .v-chat-panel{flex:1 1 220px;min-height:150px;display:flex;flex-direction:column;overflow:hidden;margin-bottom:0!important;padding:11px 13px!important}
.vela-roche .v-chat-title{flex:0 0 auto;margin-bottom:4px}
.vela-roche .v-chat-scroll{flex:1 1 auto;min-height:64px;overflow-y:auto;overscroll-behavior:contain;-webkit-overflow-scrolling:touch;padding-right:3px}
.vela-roche .v-composer{flex:0 0 auto;display:grid!important;grid-template-columns:minmax(0,1fr) 38px 38px!important;gap:7px!important;margin-top:7px!important}
.vela-roche .v-composer input{min-height:38px!important;padding:8px 12px!important;border-radius:19px!important}
.vela-roche .v-composer button{width:38px!important;height:38px!important;min-height:38px!important;border-radius:19px!important;padding:0!important;display:grid!important;place-items:center!important;font-size:14px!important}
.vela-roche .v-composer .v-continue-mini{background:#111!important;color:#fff!important;font-size:18px!important;font-weight:900!important}
.vela-roche.is-dark .v-composer .v-continue-mini{background:#f4f4f6!important;color:#111!important}
.vela-roche .v-live-popover{position:absolute;z-index:30;inset:0;display:none;align-items:flex-start;justify-content:flex-end;padding:58px 16px 16px;background:rgba(0,0,0,.12)}
.vela-roche .v-live-popover.is-open{display:flex}.vela-roche .v-live-popover-card{width:min(82%,300px);background:rgba(255,255,255,.98);color:#111;border-radius:18px;padding:12px;box-shadow:0 14px 42px rgba(0,0,0,.2)}
.vela-roche .v-live-popover-title{display:flex;align-items:center;justify-content:space-between;font-size:12px;font-weight:900;margin-bottom:7px}.vela-roche .v-live-popover-title button{border:0;background:none;font-size:17px}
.vela-roche .v-rank-row{display:flex;align-items:center;gap:8px;padding:8px 0;border-top:1px solid #ececf1;font-size:11px}.vela-roche .v-rank-row:first-of-type{border-top:0}.vela-roche .v-rank-no{width:20px;text-align:center;font-weight:900}.vela-roche .v-rank-user{flex:1;min-width:0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.vela-roche .v-rank-amount{font-weight:900}
.vela-roche .v-gift-picker{position:absolute;z-index:31;left:12px;right:12px;bottom:12px;display:none;background:rgba(255,255,255,.98);color:#111;border-radius:20px;padding:12px;box-shadow:0 14px 42px rgba(0,0,0,.2)}.vela-roche .v-gift-picker.is-open{display:block}.vela-roche .v-gift-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:9px}.vela-roche .v-gift-grid button{border:0;background:#f1f1f4;color:#111;border-radius:13px;padding:10px 4px;font-size:11px;font-weight:900}.vela-roche .v-gift-balance{font-size:10px;color:#777}
.vela-roche .v-live-exitmenu{position:absolute;z-index:32;inset:0;display:none;align-items:center;justify-content:center;padding:18px;background:rgba(0,0,0,.28)}.vela-roche .v-live-exitmenu.is-open{display:flex}.vela-roche .v-live-exitcard{width:min(86%,310px);background:#fff;color:#111;border-radius:20px;padding:12px}.vela-roche .v-live-exitcard h3{font-size:14px;margin:2px 2px 10px}.vela-roche .v-live-exitcard button{width:100%;border:0;border-radius:13px;padding:11px;margin-top:7px;font-size:12px;font-weight:900}.vela-roche .v-live-exitcard .v-primary{background:#111;color:#fff}.vela-roche .v-live-exitcard .v-danger{background:#fff0ef;color:#b3261e}.vela-roche .v-live-exitcard .v-cancel{background:#f2f2f5;color:#111}
.vela-roche .v-post-composer{display:grid;grid-template-columns:minmax(0,1fr) 44px;gap:7px;margin-top:8px;padding-top:8px}
.vela-roche .v-post-composer input{width:100%;min-width:0;border:0;border-radius:15px;background:var(--v-soft);color:var(--v-text);padding:11px 12px;font-size:16px;outline:none}
.vela-roche .v-post-composer button{border:0;border-radius:15px;background:#fff;color:#111;min-height:44px;font-weight:900;box-shadow:inset 0 0 0 1px var(--v-line)}
.vela-roche .v-post-composer .v-summon-mini{background:#fff;color:#111;font-size:17px;box-shadow:inset 0 0 0 1px var(--v-line);display:grid;place-items:center}
.vela-roche .v-comment-tools{display:flex;align-items:center;gap:10px;margin-top:5px}
.vela-roche .v-comment-tools button{border:0;background:none;color:var(--v-muted);padding:0;font-size:10px;font-weight:800}.vela-roche .v-comment-tools .v-translate-btn{margin:0!important}
.vela-roche .v-post-footer-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}
.vela-roche .v-post-footer-actions .v-action{margin-top:0!important}
.vela-roche .v-delete-soft{background:#f3f3f6!important;color:#b3261e!important}
.vela-roche.is-dark .v-post-composer button{background:#f4f4f6;color:#111}
.vela-roche.is-dark .v-delete-soft{background:#24262d!important;color:#ff8f86!important}


.vela-roche .v-avatar-wrap.is-live{background:transparent;padding:3px;box-shadow:0 0 0 2px rgba(23,105,210,.9),0 0 0 6px rgba(23,105,210,.14);animation:vLiveBreath 1.8s ease-in-out infinite}
.vela-roche .v-profile-avatar.is-live-profile{box-shadow:0 0 0 3px #fff,0 0 0 6px rgba(23,105,210,.85);animation:vLiveBreathProfile 1.8s ease-in-out infinite}
@keyframes vLiveBreathProfile{0%,100%{box-shadow:0 0 0 3px #fff,0 0 0 6px rgba(23,105,210,.70)}50%{box-shadow:0 0 0 3px #fff,0 0 0 10px rgba(23,105,210,.20)}}
@keyframes vLiveBreath{0%,100%{box-shadow:0 0 0 2px rgba(23,105,210,.82),0 0 0 5px rgba(23,105,210,.10)}50%{box-shadow:0 0 0 2px rgba(23,105,210,1),0 0 0 9px rgba(23,105,210,.22)}}
.vela-roche .v-avatar-wrap.is-live .v-avatar{border:2px solid #fff}
.vela-roche .v-live-cover-bg{position:absolute;z-index:0;inset:0;width:100%;height:100%;object-fit:cover}
.vela-roche .v-live-stage.has-cover:after{z-index:1;background:linear-gradient(rgba(7,20,40,.14),rgba(7,20,40,.46))}
.vela-roche .v-live-stage>.v-live-hud,.vela-roche .v-live-stage>.v-live-stage-copy,.vela-roche .v-live-stage>.v-live-moment{z-index:6}
.vela-roche .v-live-host-controls{display:flex;gap:7px;align-items:center;margin-top:8px;flex-wrap:wrap}
.vela-roche .v-live-host-controls button{border:0;border-radius:10px;background:var(--v-blue-soft);color:var(--v-blue);padding:7px 9px;font-size:10px;font-weight:900}
.vela-roche .v-cohost-pill{display:inline-flex;align-items:center;gap:5px;border-radius:10px;background:var(--v-blue-soft);color:var(--v-blue);padding:6px 8px;font-size:10px;font-weight:850}
.vela-roche .v-live-chat-tools{display:flex;gap:9px;align-items:center;margin-top:4px;min-height:18px}
.vela-roche .v-live-chat-tools button{border:0;background:none;color:var(--v-muted);padding:0!important;margin:0!important;font-size:10px;font-weight:800;line-height:18px;height:18px;display:inline-flex;align-items:center}
.vela-roche .v-live-chat-tools .v-translate-btn{margin:0!important;padding:0!important;line-height:18px;height:18px}
.vela-roche .v-publish-banner{height:190px;border-radius:22px;background:linear-gradient(135deg,#dceaff,#fff);overflow:hidden;display:grid;place-items:center;color:var(--v-blue);font-size:38px;position:relative}
.vela-roche .v-publish-banner img{width:100%;height:100%;object-fit:cover;display:block}
.vela-roche .v-access-badge{display:inline-flex;align-items:center;border-radius:9px;background:#fff0f2;color:#bd2539;padding:4px 7px;font-size:9px;font-weight:900;margin-top:7px}
.vela-roche .v-publish-note{background:var(--v-blue-soft);color:#31577f;border-radius:14px;padding:10px 11px;font-size:11px;line-height:1.55}
.vela-roche .v-primary-red{background:var(--v-red)!important;color:#fff!important}
.vela-roche .v-live-info-head{display:flex;align-items:center;gap:7px;flex-wrap:wrap;margin-bottom:7px}
.vela-roche .v-live-info-head .v-badge{margin:0!important;flex:0 0 auto}
.vela-roche .v-live-info-head .v-cohost-pill{margin:0!important}
.vela-roche .v-upload-row{display:grid;grid-template-columns:1fr auto;gap:8px;align-items:center}
.vela-roche .v-upload-row input[type="file"]{position:absolute;width:1px;height:1px;opacity:0;pointer-events:none}
.vela-roche .v-upload-btn{border:0;border-radius:12px;background:var(--v-blue-soft);color:var(--v-blue);padding:10px 12px;font-size:11px;font-weight:900;white-space:nowrap}
.vela-roche .v-media-preview-actions{display:flex;gap:8px;margin-top:7px}
.vela-roche .v-media-preview-actions button{border:0;border-radius:10px;background:var(--v-soft);color:var(--v-muted);padding:7px 9px;font-size:10px;font-weight:850}
.vela-roche .v-remote-pane{align-items:flex-start!important;padding-top:58px!important}
.vela-roche .v-remote-pane-label{position:relative;z-index:4;background:rgba(15,15,18,.62);backdrop-filter:blur(8px);border-radius:16px;padding:5px 8px 5px 5px}
.vela-roche .v-live-host.is-multi{max-width:62%}
.vela-roche .v-live-host.is-multi .v-live-host-name{max-width:150px}
.vela-roche .v-live-habit-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}


.vela-roche .v-userhead-static{cursor:default!important}
.vela-roche .v-userhead-static:active{transform:none!important}
.vela-roche .v-dm-tabs{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:9px 0 14px}
.vela-roche .v-dm-tabs button{border:0;border-radius:14px;background:#e9eef7;color:#56708e;padding:10px 12px;font-size:12px;font-weight:900}
.vela-roche .v-dm-tabs button.is-active{background:var(--v-blue);color:#fff}
.vela-roche .v-community-hub{display:grid;gap:10px}
.vela-roche .v-community-card{background:var(--v-card);border:1px solid var(--v-line);border-radius:18px;padding:12px}
.vela-roche .v-community-card-head{display:flex;align-items:center;gap:9px}.vela-roche .v-community-card-head .v-meta{flex:1}
.vela-roche .v-community-tier{display:flex;align-items:center;justify-content:space-between;gap:10px;border-top:1px solid var(--v-line);padding:10px 0 0;margin-top:10px;font-size:11px}
.vela-roche .v-community-tier button{border:0;border-radius:11px;background:var(--v-blue-soft);color:var(--v-blue);padding:7px 9px;font-size:10px;font-weight:900}
.vela-roche .v-community-tier button.is-paid{background:#fff0f2;color:var(--v-red)}
.vela-roche .v-publish-sheet-title{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:12px}
.vela-roche .v-publish-sheet-title h3{margin:0;font-size:16px}.vela-roche .v-publish-sheet-title button{border:0;background:none;font-size:18px}
.vela-roche .v-publish-sheet-form{display:grid;gap:11px}
.vela-roche .v-publish-sheet-actions{display:flex;gap:8px;margin-top:12px}.vela-roche .v-publish-sheet-actions button{flex:1}
.vela-roche .v-access-row{display:grid;grid-template-columns:1fr 1fr;gap:9px}
.vela-roche .v-host-console{flex:0 0 auto;border-top:1px solid var(--v-line);padding-top:9px;margin-top:7px}
.vela-roche .v-host-console-top{display:flex;align-items:center;gap:7px;margin-bottom:7px}
.vela-roche .v-host-mode{border:0;border-radius:11px;background:var(--v-soft);color:var(--v-muted);padding:7px 9px;font-size:10px;font-weight:900}
.vela-roche .v-host-mode.is-active{background:var(--v-blue);color:#fff}
.vela-roche .v-host-console-row{display:grid;grid-template-columns:minmax(0,1fr) 44px;gap:7px}
.vela-roche .v-host-console-row input{width:100%;min-width:0;border:0;border-radius:16px;background:var(--v-soft);color:var(--v-text);padding:10px 12px;font-size:16px}
.vela-roche .v-host-console-row button{border:0;border-radius:14px;min-height:42px;background:#111;color:#fff;font-weight:900}
.vela-roche .v-host-console-row .v-summon-host{background:var(--v-blue);color:#fff}
.vela-roche .v-host-console-foot{display:flex;justify-content:space-between;align-items:center;gap:8px;margin-top:7px;font-size:10px;color:var(--v-muted)}
.vela-roche .v-host-console-foot button{border:0;background:none;color:var(--v-blue);font-size:10px;font-weight:900;padding:0}
.vela-roche .v-own-stage-event{margin-bottom:7px;padding-bottom:7px;border-bottom:1px solid rgba(255,255,255,.16)}
.vela-roche .v-own-stage-event-label{font-size:9px;color:rgba(255,255,255,.7);font-weight:900;margin-bottom:3px}
.vela-roche .v-own-stage-event-text{font-size:12px;line-height:1.45;color:#fff}
.vela-roche .v-remote-grid{position:absolute;z-index:2;inset:0;display:grid;grid-template-columns:1fr 1fr;gap:1px;background:rgba(255,255,255,.12);pointer-events:none}
.vela-roche .v-remote-pane{position:relative;background:linear-gradient(145deg,rgba(30,34,43,.92),rgba(85,94,112,.7));display:flex;align-items:flex-end;padding:14px;color:#fff}
.vela-roche .v-remote-pane:nth-child(2){background:linear-gradient(145deg,rgba(52,58,72,.9),rgba(105,115,135,.65))}
.vela-roche .v-remote-pane-label{display:flex;align-items:center;gap:7px;font-size:10px;font-weight:900}
.vela-roche .v-remote-pane-label .v-social-avatar{width:28px;height:28px;border:1px solid rgba(255,255,255,.5)}
.vela-roche .v-live-hud.is-same-room .v-live-host{max-width:68%}
.vela-roche .v-live-participants{display:flex;align-items:center;gap:4px}.vela-roche .v-live-participants .v-live-host-avatar{margin-right:-5px;border:1px solid rgba(255,255,255,.55)}
.vela-roche .v-home-self-label{font-size:9px;color:var(--v-blue);font-weight:900;margin-top:2px}
.vela-roche .v-subscribe-row{display:flex;gap:8px;flex-wrap:wrap;margin-top:11px}
.vela-roche .v-subscribe-row button{border:0;border-radius:12px;padding:8px 10px;font-size:10px;font-weight:900}
.vela-roche .v-sub-free{background:var(--v-blue-soft);color:var(--v-blue)}
.vela-roche .v-sub-paid{background:#fff0f2;color:var(--v-red)}
.vela-roche .v-access-badge.is-free{background:var(--v-blue-soft);color:var(--v-blue)}
.vela-roche .v-access-badge.is-paid{background:#fff0f2;color:var(--v-red)}
.vela-roche .v-access-badge.is-age{box-shadow:inset 0 0 0 1px rgba(189,37,57,.18)}

@media (max-width:360px){.vela-roche .v-main{padding-left:10px;padding-right:10px}.vela-roche .v-live-title{font-size:16px}.vela-roche .v-balance{font-size:30px}}

/* v0.2.2 final live sizing overrides */
.vela-roche .v-live-body{padding:7px 8px 8px!important}
.vela-roche .v-live-stage{border-radius:19px!important}
.vela-roche .v-live-info{margin-top:7px!important;padding:9px 11px!important}
.vela-roche .v-chat-panel{flex:0 1 205px!important;min-height:118px!important;max-height:245px!important;margin-top:7px!important;padding:9px 11px!important}
.vela-roche .v-chat-scroll{min-height:45px!important}
.vela-roche .v-live-quicktools{margin-left:0!important;order:3!important}
.vela-roche .v-live-quicktool,.vela-roche .v-live-quicktool.is-lottery,.vela-roche .v-live-quicktool.is-ad{height:38px!important;min-width:38px!important;border-radius:19px!important;padding:0 10px!important;background:rgba(18,18,22,.66)!important;color:#fff!important}
.vela-roche .v-gift-rank-trigger{margin-left:auto!important;order:4!important;max-width:42%!important}

.vela-roche .v-title-row{display:flex;align-items:center;justify-content:space-between;gap:12px}.vela-roche .v-title-actions{display:flex;align-items:center;gap:8px}.vela-roche .v-icon-btn{width:34px;height:34px;border:0;border-radius:999px;background:#fff;box-shadow:var(--v-shadow);display:grid;place-items:center;padding:0;flex:0 0 auto}.vela-roche .v-icon-btn img{display:block}.vela-roche .v-refresh-icon{width:18px;height:18px;object-fit:contain}.vela-roche .v-plane-icon{width:16px;height:16px;object-fit:contain;display:block}.vela-roche .v-chat-summon,.vela-roche .v-summon-host,.vela-roche [data-action="summon-live"]{display:grid;place-items:center}.vela-roche .v-nav-icon{width:26px!important;height:26px!important}.vela-roche .v-channels .v-avatar-wrap{border-radius:50%!important}.vela-roche .v-channels .v-avatar{border-radius:50%!important}.vela-roche .v-home-title-note{font-size:11px;color:var(--v-muted)}

/* v0.2.3 live layout */
.vela-roche .v-live-body{height:100%;min-height:0;padding:8px 8px 10px!important;display:flex;flex-direction:column;gap:8px!important}.vela-roche .v-live-fixed{flex:0 0 auto}.vela-roche .v-live-stage{min-height:320px;max-height:56vh;overflow:hidden}.vela-roche .v-live-info{margin-top:6px!important;padding:9px 11px!important}.vela-roche .v-chat-panel{flex:1 1 auto!important;min-height:230px!important;max-height:none!important;margin-top:0!important;padding:10px 11px!important;display:flex;flex-direction:column}.vela-roche .v-chat-scroll{flex:1 1 auto;min-height:140px!important}.vela-roche .v-live-hud{display:grid;grid-template-columns:auto 1fr auto auto;grid-template-areas:"exit host quick rank";align-items:start;gap:8px}.vela-roche .v-live-exit{grid-area:exit}.vela-roche .v-live-host{grid-area:host;min-width:0}.vela-roche .v-live-quicktools{grid-area:quick;margin-left:0!important;order:unset!important;display:flex;gap:8px;justify-self:end}.vela-roche .v-gift-rank-trigger{grid-area:rank;margin-left:0!important;order:unset!important;max-width:none!important;justify-self:end}.vela-roche .v-live-title-row{display:flex;align-items:flex-start;justify-content:space-between;gap:10px}.vela-roche .v-live-connect-btn{white-space:nowrap;flex:0 0 auto}.vela-roche .v-host-console-top{justify-content:flex-start}.vela-roche .v-host-console-row .v-summon-host{min-width:44px}.vela-roche .v-tool-form .v-field textarea,.vela-roche .v-ios-group textarea{min-height:92px}.vela-roche .v-live-tool-actions,.vela-roche .v-settings-actions{flex-wrap:wrap}

/* v0.2.4 · multi-account home / identity-aware interaction */
.vela-roche .v-follow-stack{position:absolute;right:-7px;bottom:-7px;display:flex;align-items:center;z-index:5}
.vela-roche .v-follow-mini{width:19px;height:19px;border-radius:50%;overflow:hidden;background:transparent;border:0;display:grid;place-items:center;font-size:7px;font-weight:950;margin-left:-5px;color:#111}
.vela-roche .v-follow-mini:first-child{margin-left:0}.vela-roche .v-follow-mini img{width:100%;height:100%;object-fit:cover;display:block}
.vela-roche .v-follow-more{min-width:19px;padding:0 4px;border-radius:10px}
.vela-roche .v-channel.is-current .v-avatar-wrap{box-shadow:0 0 0 2px var(--v-blue),0 0 0 5px rgba(0,122,255,.12)}
.vela-roche .v-channel-kind{display:block;font-size:8px;color:var(--v-muted);font-weight:700;margin-top:1px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.vela-roche .v-identity-switch{display:flex;align-items:center;gap:7px;margin-top:12px;padding:8px 9px;border-radius:14px;background:var(--v-soft);border:1px solid var(--v-line)}
.vela-roche .v-identity-switch>span{font-size:9px;color:var(--v-muted);font-weight:850;white-space:nowrap}.vela-roche .v-identity-switch select{min-width:0;flex:1;border:0;background:transparent;color:var(--v-text);font-size:11px;font-weight:900;outline:none}
.vela-roche .v-post-composer .v-plane-icon{width:18px;height:18px}.vela-roche .v-chat-summon .v-plane-icon,.vela-roche .v-summon-host .v-plane-icon,.vela-roche [data-action="summon-live"] .v-plane-icon{width:18px;height:18px}
.vela-roche .v-community-identity-note{font-size:9px;color:var(--v-muted);margin:0 0 6px 4px}
.vela-roche .v-wallet-source{display:inline-flex;align-items:center;gap:5px;margin-top:4px;color:var(--v-muted);font-size:9px}.vela-roche .v-wallet-source-avatar{width:18px;height:18px;border-radius:50%;overflow:hidden;background:var(--v-soft);display:grid;place-items:center;font-size:7px;font-weight:900}.vela-roche .v-wallet-source-avatar img{width:100%;height:100%;object-fit:cover;display:block}

.vela-roche .v-composer button[data-action="summon-live"],.vela-roche .v-summon-host,.vela-roche .v-chat-summon,.vela-roche .v-summon-mini{background:#fff!important;color:#111!important;box-shadow:inset 0 0 0 1px var(--v-line)!important}

/* v0.2.7 · DM restore + identity badge/gift refinements */
.vela-roche .v-nav button{font-size:11px!important;gap:5px!important;min-height:60px!important;font-weight:800}
.vela-roche .v-nav-icon{width:32px!important;height:32px!important}
.vela-roche .v-nav .v-publish b{width:50px!important;height:50px!important;border-radius:18px!important;font-size:30px!important;margin-top:-3px!important}
.vela-roche .v-composer-identity{width:40px;height:40px;border-radius:50%;position:relative;display:grid;place-items:center;flex:0 0 auto;overflow:hidden;background:var(--v-soft);box-shadow:inset 0 0 0 1px var(--v-line);cursor:pointer}
.vela-roche .v-composer-identity-avatar{width:100%;height:100%;border-radius:50%;overflow:hidden;display:grid;place-items:center;font-size:9px;font-weight:950;color:var(--v-text)}
.vela-roche .v-composer-identity-avatar img{width:100%;height:100%;object-fit:cover;display:block;border-radius:50%}
.vela-roche .v-composer-identity select{position:absolute;inset:0;width:100%;height:100%;opacity:0;cursor:pointer;border:0;padding:0}
.vela-roche .v-post-composer{grid-template-columns:40px minmax(0,1fr) 44px!important;align-items:center}
.vela-roche .v-chat-composer-wrap .v-composer-identity{width:38px;height:38px}
.vela-roche .v-composer{grid-template-columns:40px minmax(0,1fr) 38px 38px!important;align-items:center}
.vela-roche .v-identity-switch{display:none!important}
.vela-roche .v-chat-row:not(.is-me) .v-chat-bubble{background:#fff!important;color:#111!important;border:1px solid var(--v-line)!important;border-radius:18px!important}
.vela-roche .v-chat-row.is-me .v-chat-bubble{background:var(--v-blue)!important;color:#fff!important;border-color:var(--v-blue)!important;border-radius:18px!important}
.vela-roche .v-chat-row .v-chat-bubble:after{content:none!important;display:none!important}
.vela-roche .v-chat-row.is-me .v-chat-bubble .v-translate-btn{color:rgba(255,255,255,.78)!important}
.vela-roche .v-chat-row.is-me .v-chat-bubble .v-translation{border-top-color:rgba(255,255,255,.28)!important;color:rgba(255,255,255,.86)!important}
.vela-roche .v-profile-actions{display:flex;align-items:center;gap:8px;flex:0 0 auto}
.vela-roche .v-profile-message,.vela-roche .v-profile-follow{min-width:74px;min-height:46px;border:0;border-radius:15px;padding:0 16px;font-size:12px;font-weight:950;display:grid;place-items:center;white-space:nowrap;pointer-events:auto}
.vela-roche .v-profile-message{background:var(--v-soft);color:var(--v-text)}
.vela-roche .v-profile-follow{background:#111;color:#fff}.vela-roche .v-profile-follow.is-on{background:var(--v-blue);color:#fff}
.vela-roche .v-profile-follow-note{margin-top:8px;font-size:10px;color:var(--v-muted);font-weight:850;text-align:right}
.vela-roche .v-profile-followed-ids{margin-top:7px;display:flex;align-items:center;justify-content:flex-end;gap:5px;font-size:9px;color:var(--v-muted);font-weight:800}.vela-roche .v-profile-followed-mini{width:22px;height:22px;border-radius:50%;overflow:hidden;background:var(--v-soft);display:grid;place-items:center;font-size:7px;color:var(--v-text)}.vela-roche .v-profile-followed-mini img{width:100%;height:100%;object-fit:cover;display:block}
.vela-roche .v-membership-card.is-paid{border-color:var(--v-line)!important}
.vela-roche .v-subscription-locked{margin-top:4px;padding:11px 12px;border-radius:14px;background:var(--v-soft);color:var(--v-muted);font-size:10px;line-height:1.55}
.vela-roche .v-subscription-locked b{display:block;color:var(--v-text);font-size:10px;margin-top:5px}.vela-roche .v-subscription-locked b:first-child{margin-top:0}.vela-roche .v-subscription-locked p{margin:3px 0 0}
.vela-roche .v-sub-confirm h3{font-size:16px;margin:6px 0}.vela-roche .v-sub-confirm p{font-size:12px;margin:0;color:var(--v-muted)}.vela-roche .v-sub-confirm-note{margin-top:12px;padding:10px;border-radius:12px;background:var(--v-soft);font-size:11px;line-height:1.55}


/* v0.2.7 UI refinements */
.vela-roche .v-title{justify-content:flex-start!important;gap:13px!important}.vela-roche .v-title small{margin-left:5px!important}
.vela-roche .v-avatar-wrap{padding:0!important;background:transparent!important}.vela-roche .v-avatar-wrap.is-live{padding:3px!important;background:var(--v-blue)!important;box-shadow:none!important}.vela-roche .v-avatar-wrap.is-live>.v-avatar{box-shadow:0 0 0 2px var(--v-bg)}.vela-roche .v-live-dot{display:none!important}.vela-roche .v-channel.is-current .v-avatar-wrap{box-shadow:none!important}
.vela-roche .v-follow-stack{right:-1px!important;bottom:-5px!important;display:flex!important;flex-direction:row-reverse}.vela-roche .v-follow-mini{width:19px!important;height:19px!important;border:0!important;background:transparent!important;margin-left:-7px!important;box-shadow:none!important}.vela-roche .v-follow-mini img{width:100%;height:100%;object-fit:cover;border-radius:50%}
.vela-roche .v-create-grid{gap:9px}.vela-roche .v-create{min-height:58px!important;padding:0 16px!important;display:grid;place-items:center;text-align:center;border-radius:16px!important;font-size:14px}.vela-roche .v-create b{display:none!important}
.vela-roche .v-ios-publish{gap:9px!important}.vela-roche .v-ios-group{border-radius:18px!important}.vela-roche .v-publish-sheet-actions{position:static!important;background:none!important;padding:8px 0 0!important}.vela-roche .v-publish-sheet-actions .v-action{min-height:44px;border-radius:15px!important}
.vela-roche .v-social-head{position:relative}.vela-roche .v-post-more{margin-left:auto;width:34px;height:34px;border:0;border-radius:50%;background:transparent;font-size:22px;line-height:1}.vela-roche .v-post-menu{display:none;position:absolute;right:0;top:38px;z-index:8;background:#fff;border:1px solid var(--v-line);border-radius:14px;padding:5px;box-shadow:0 8px 28px rgba(0,0,0,.12);min-width:118px}.vela-roche .v-post-menu.is-open{display:grid}.vela-roche .v-post-menu button{border:0;background:none;text-align:left;padding:9px 10px;border-radius:10px;font-weight:750}.vela-roche .v-post-menu .is-delete{color:var(--v-red)}
.vela-roche .v-social-media:empty{display:none!important}.vela-roche .v-social-actions{align-items:center}.vela-roche .v-social-actions button{border:0;background:none;color:var(--v-muted);font-size:12px;padding:4px 0}.vela-roche .v-inline-comments{display:none;border-top:1px solid var(--v-line);margin-top:9px;padding-top:5px}.vela-roche .v-inline-comments.is-open{display:block}.vela-roche .v-more-comments{width:100%;border:0;background:none;color:var(--v-muted);font-weight:800;padding:10px 0;text-align:left}
.vela-roche .v-msg{position:relative;overflow:hidden}.vela-roche .v-msg-delete{position:absolute;right:0;top:0;bottom:0;transform:translateX(100%);width:72px;background:var(--v-red);color:#fff;border:0;font-weight:900}.vela-roche .v-msg.is-swipe-open .v-msg-delete{transform:none}

/* v0.2.12 · conversation presence / stranger avatar / recommendation & language controls */
.vela-roche .v-subscreen[data-screen="community-settings"]{transform:none!important;display:flex!important;align-items:center;justify-content:center;padding:18px;background:rgba(20,20,24,.34)!important;opacity:0;visibility:hidden;pointer-events:none;transition:opacity .18s ease!important;overflow:hidden!important}
.vela-roche .v-subscreen[data-screen="community-settings"].is-open{opacity:1;visibility:visible;pointer-events:auto}
.vela-roche .v-community-modal-backdrop{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;padding:18px}
.vela-roche .v-community-modal-card{width:min(94%,430px);max-height:min(78vh,680px);background:var(--v-card);border-radius:24px;box-shadow:0 22px 70px rgba(0,0,0,.22);display:flex;flex-direction:column;overflow:hidden;border:1px solid var(--v-line)}
.vela-roche .v-community-modal-head,.vela-roche .v-community-modal-foot{flex:0 0 auto;background:var(--v-card);padding:13px 14px;display:flex;align-items:center;justify-content:space-between;gap:10px}
.vela-roche .v-community-modal-head{border-bottom:1px solid var(--v-line)}.vela-roche .v-community-modal-foot{border-top:1px solid var(--v-line)}
.vela-roche .v-community-modal-head strong{display:block;font-size:15px}.vela-roche .v-community-modal-head small{display:block;color:var(--v-muted);font-size:10px;margin-top:2px}.vela-roche .v-community-modal-head>button{width:34px;height:34px;border:0;border-radius:12px;background:var(--v-soft);font-size:20px}
.vela-roche .v-community-modal-scroll{flex:1 1 auto;min-height:0;overflow:auto;padding:12px;-webkit-overflow-scrolling:touch}.vela-roche .v-community-modal-scroll .v-card{border:1px solid var(--v-line);margin-bottom:10px}
.vela-roche .v-community-avatar-uploader{display:flex;flex-direction:column;align-items:center;gap:5px;cursor:pointer;flex:0 0 auto}.vela-roche .v-community-avatar-uploader>.v-social-avatar{width:58px;height:58px;border-radius:50%;box-shadow:inset 0 0 0 1px var(--v-line)}.vela-roche .v-community-avatar-uploader>input[type="file"],.vela-roche .v-community-avatar-uploader.is-bot>input[type="file"]{display:none!important}.vela-roche .v-community-avatar-uploader>span{font-size:8px;color:var(--v-muted);font-weight:800}.vela-roche .v-community-avatar-uploader.is-bot{width:48px;height:48px;border-radius:50%;overflow:hidden;display:grid;place-items:center;background:var(--v-soft)}.vela-roche .v-community-avatar-uploader.is-bot>span{width:100%;height:100%;display:grid;place-items:center}.vela-roche .v-community-avatar-uploader.is-bot img{width:100%;height:100%;object-fit:cover;border-radius:50%}
.vela-roche .v-community-modal-foot .v-action{flex:1;min-height:42px}
.vela-roche .v-chat-bubble[data-action="toggle-dm-bubble-translation"]{cursor:pointer}.vela-roche .v-dm-bubble-translation{margin-top:8px;padding-top:8px;border-top:1px solid var(--v-line);font-size:10px;line-height:1.55;color:var(--v-muted);white-space:pre-wrap}.vela-roche .v-chat-row.is-me .v-dm-bubble-translation{border-top-color:rgba(255,255,255,.28);color:rgba(255,255,255,.86)}
.vela-roche .v-msg-swipe{position:relative;overflow:hidden;border-bottom:1px solid var(--v-line);touch-action:pan-y}.vela-roche .v-msg-swipe:last-child{border-bottom:0}.vela-roche .v-msg-swipe .v-msg{width:100%;transition:transform .18s ease;border-bottom:0!important;position:relative;z-index:1;touch-action:pan-y}.vela-roche .v-msg-swipe .v-msg-delete{position:absolute;z-index:2;right:0;top:0;bottom:0;width:72px;transform:translateX(100%);transition:transform .18s ease}.vela-roche .v-msg-swipe.is-swipe-open .v-msg{transform:translateX(-72px)}.vela-roche .v-msg-swipe.is-swipe-open .v-msg-delete{transform:none}
.vela-roche .v-icon-btn.is-generating{width:auto!important;min-width:78px!important;padding:0 11px!important;border-radius:14px!important}.vela-roche .v-generating-label{font-size:10px;font-weight:900;white-space:nowrap}
.vela-roche .v-rec-card{position:relative}.vela-roche .v-rec-remove{position:absolute;right:10px;top:10px;z-index:4;width:32px;height:32px;border:0;border-radius:50%;background:rgba(255,255,255,.9);font-size:18px;display:grid;place-items:center}.vela-roche .v-rec-translation{padding:0 14px 10px}.vela-roche .v-rec-body .v-mini{border-radius:50%;overflow:hidden}.vela-roche .v-rec-body .v-mini img{width:100%;height:100%;object-fit:cover}
.vela-roche .v-home-live-head{display:flex;align-items:center;gap:10px;position:relative}.vela-roche .v-home-live-actions{margin-left:auto}.vela-roche .v-home-live-actions .v-post-menu{top:36px}.vela-roche .v-home-live-stage{width:100%;border:0;background:none;padding:0;text-align:left}

/* v0.2.12 · DM presence, recommendation filters, language control, compact live-end card */
.vela-roche .v-dm-chat-screen .v-chat-row .v-social-avatar{width:36px!important;height:36px!important;font-size:9px!important}
.vela-roche .v-dm-chat-screen .v-chat-row.is-me .v-own-chat-avatar{order:3}
.vela-roche .v-dm-chat-screen .v-chat-stack{max-width:min(76%,430px)!important}
.vela-roche .v-dm-chat-screen .v-chat-bubble{border-radius:20px!important;padding:10px 13px!important;font-size:12px!important;line-height:1.5!important}
.vela-roche .v-dm-chat-screen .v-chat-row:not(.is-me) .v-chat-bubble{background:#f2f2f4!important}
.vela-roche .v-dm-chat-screen .v-chat-row.is-me .v-chat-bubble{background:var(--v-blue)!important}
.vela-roche .v-typing-dots{height:14px;min-width:30px;display:inline-flex;align-items:center;justify-content:center;gap:4px}.vela-roche .v-typing-dots i{width:5px;height:5px;border-radius:50%;background:currentColor;opacity:.35;animation:vTypingPulse 1.15s infinite ease-in-out}.vela-roche .v-typing-dots i:nth-child(2){animation-delay:.16s}.vela-roche .v-typing-dots i:nth-child(3){animation-delay:.32s}@keyframes vTypingPulse{0%,70%,100%{transform:translateY(0);opacity:.3}35%{transform:translateY(-3px);opacity:.9}}
.vela-roche .v-rec-topic-btn{border:0;background:var(--v-soft);color:var(--v-text);border-radius:13px;min-height:36px;padding:0 12px;font-size:10px;font-weight:900;white-space:nowrap}.vela-roche .v-title-actions{display:flex;align-items:center;gap:7px}
.vela-roche .v-rec-topic-note{font-size:10px;line-height:1.55;color:var(--v-muted);margin-bottom:12px}.vela-roche .v-rec-topic-grid{display:flex;flex-wrap:wrap;gap:8px}.vela-roche .v-rec-topic-chip{border:0;border-radius:14px;background:var(--v-soft);color:var(--v-text);padding:9px 12px;font-size:11px;font-weight:850}.vela-roche .v-rec-topic-chip.is-active{background:#111;color:#fff}
.vela-roche .v-language-scale{display:flex;justify-content:space-between;color:var(--v-muted);font-size:8px;font-weight:750;margin-top:4px}
.vela-roche .v-live-summary-card.v-live-summary-compact{width:min(84%,320px);padding:18px 16px;text-align:center}.vela-roche .v-live-summary-card.v-live-summary-compact h3{font-size:17px;margin:0}.vela-roche .v-live-summary-card.v-live-summary-compact p{margin:8px 0 0;color:var(--v-muted);font-size:12px;line-height:1.55}.vela-roche .v-live-summary-card.v-live-summary-compact .v-live-summary-close{min-height:44px;font-size:12px;margin-top:16px}
/* v0.2.13 · centered reminders / confirmations / profile follow */
.vela-roche .v-profile-actions{display:flex;align-items:center;gap:8px;flex:0 0 auto}.vela-roche .v-profile-follow{border:0;border-radius:12px;background:var(--v-soft);color:var(--v-text);padding:8px 11px;font-size:10px;font-weight:900;white-space:nowrap}.vela-roche .v-profile-follow.is-on{background:#111;color:#fff}
.vela-roche .v-subscreen[data-screen="appointment-reminder"],.vela-roche .v-subscreen[data-screen="live-summary"]{transform:none!important;display:flex!important;align-items:center;justify-content:center;padding:18px;background:rgba(20,20,24,.32)!important;opacity:0;visibility:hidden;pointer-events:none;transition:opacity .18s ease!important;overflow:hidden!important}.vela-roche .v-subscreen[data-screen="appointment-reminder"].is-open,.vela-roche .v-subscreen[data-screen="live-summary"].is-open{opacity:1;visibility:visible;pointer-events:auto}
.vela-roche .v-center-modal-backdrop{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;padding:18px}.vela-roche .v-appointment-card,.vela-roche .v-confirm-card{width:min(94%,390px);margin:0!important;background:var(--v-card);border:1px solid var(--v-line);border-radius:24px;padding:17px;box-shadow:0 22px 70px rgba(0,0,0,.22)}.vela-roche .v-appointment-card .v-kicker,.vela-roche .v-confirm-card .v-kicker{margin-bottom:7px}.vela-roche .v-appointment-actions{display:grid!important;grid-template-columns:repeat(3,minmax(0,1fr))!important;gap:7px!important;margin-top:14px}.vela-roche .v-appointment-actions button{min-width:0!important;padding:0 7px!important;white-space:nowrap}.vela-roche .v-appointment-actions .v-today{background:#111;color:#fff}.vela-roche .v-confirm-card h3{font-size:17px;margin:0 0 7px}.vela-roche .v-confirm-card p{font-size:11px;line-height:1.6;color:var(--v-muted);margin:0}.vela-roche .v-confirm-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:15px}.vela-roche .v-confirm-actions button{border:0;border-radius:13px;min-height:42px;font-size:11px;font-weight:900}.vela-roche .v-confirm-actions .is-cancel{background:var(--v-soft);color:var(--v-text)}.vela-roche .v-confirm-actions .is-danger{background:#fff0f1;color:var(--v-red)}

/* v0.2.16 · unified chat bubbles + iOS live publisher */
.vela-roche .v-chat-row{align-items:flex-start!important}
.vela-roche .v-chat-sender{font-size:10px!important;line-height:1.25!important;margin:1px 0 4px 3px!important;color:var(--v-muted)!important;font-weight:800!important}
.vela-roche .v-chat-row.is-me .v-chat-sender{margin:1px 3px 4px 0!important}
.vela-roche .v-chat-row .v-social-avatar{align-self:flex-start!important;margin-top:0!important}
.vela-roche .v-chat-row .v-social-avatar{width:36px!important;height:36px!important;font-size:9px!important}
.vela-roche .v-chat-screen .v-chat-stack{max-width:min(76%,430px)!important}
.vela-roche .v-chat-screen .v-chat-bubble{border-radius:20px!important;padding:10px 13px!important;font-size:12px!important;line-height:1.5!important}
.vela-roche .v-chat-screen .v-chat-row:not(.is-me) .v-chat-bubble{background:#f2f2f4!important}
.vela-roche .v-chat-bubble[data-action="toggle-community-bubble-translation"]{cursor:pointer}
.vela-roche .v-chat-bubble .v-dm-bubble-translation{margin-top:8px;padding-top:8px;border-top:1px solid var(--v-line);font-size:10px;line-height:1.55;color:var(--v-muted);white-space:pre-wrap}
.vela-roche .v-chat-row.is-me .v-dm-bubble-translation{border-top-color:rgba(255,255,255,.28);color:rgba(255,255,255,.86)}
.vela-roche .v-live-publish-group{background:var(--v-card);border-radius:20px;overflow:hidden;box-shadow:0 .5px 0 rgba(60,60,67,.12)}
.vela-roche .v-live-publish-section{padding:13px 14px;border-top:1px solid var(--v-line)}
.vela-roche .v-live-publish-section:first-child{border-top:0}
.vela-roche .v-live-publish-label{font-size:10px;color:var(--v-muted);font-weight:850;margin-bottom:8px}
.vela-roche .v-live-segments{display:grid;grid-auto-flow:column;grid-auto-columns:minmax(0,1fr);gap:4px;padding:3px;background:rgba(118,118,128,.12);border-radius:12px}
.vela-roche .v-live-segments button{border:0;min-height:34px;border-radius:9px;background:transparent;color:var(--v-text);font-size:11px;font-weight:850;padding:0 8px;white-space:nowrap}
.vela-roche .v-live-segments button.is-selected{background:var(--v-card);box-shadow:0 1px 4px rgba(0,0,0,.12)}
.vela-roche .v-live-multi-fields[hidden]{display:none!important}
.vela-roche .v-live-multi-fields{border-top:1px solid var(--v-line)}
.vela-roche .v-live-cohost-list{display:flex;gap:8px;overflow:auto;padding:1px 0 2px;-webkit-overflow-scrolling:touch}
.vela-roche .v-live-cohost-choice{position:relative;flex:0 0 auto}
.vela-roche .v-live-cohost-choice input{position:absolute;opacity:0;pointer-events:none}
.vela-roche .v-live-cohost-choice span{min-width:88px;max-width:130px;min-height:38px;padding:7px 10px;border-radius:12px;background:var(--v-soft);display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:850;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;border:1px solid transparent}
.vela-roche .v-live-cohost-choice input:checked+span{background:var(--v-blue-soft);color:var(--v-blue);border-color:rgba(0,122,255,.22)}
.vela-roche .v-live-cohost-empty{font-size:10px;color:var(--v-muted);padding:5px 1px}
.vela-roche .v-live-publish-group .v-field{padding:12px 14px!important;border-top:1px solid var(--v-line)!important}
.vela-roche .v-live-publish-group .v-field:first-child{border-top:0!important}
.vela-roche .v-live-publish-group .v-field input,.vela-roche .v-live-publish-group .v-field textarea{border:0!important;background:transparent!important;border-radius:0!important;padding:2px 0!important;font-size:16px!important}
.vela-roche .v-live-publish-group .v-field textarea{min-height:74px!important}
/* v0.2.17 · multi appointments / subscription opt-in / post UI polish */
.vela-roche .v-live-segments button.is-disabled,.vela-roche .v-live-segments button:disabled{opacity:.32!important;pointer-events:none!important;box-shadow:none!important}
.vela-roche .v-schedule-entry-hit{width:100%;border:0;background:transparent;color:inherit;padding:0;text-align:left;display:block}
.vela-roche .v-schedule-entry-more{display:block;margin-top:10px;font-size:10px;font-weight:850;color:var(--v-muted)}
.vela-roche .v-schedule-list{display:grid;gap:12px}.vela-roche .v-schedule-list-card{background:var(--v-card);border:1px solid var(--v-line);border-radius:22px;padding:14px;overflow:hidden}.vela-roche .v-schedule-list-cover{height:155px;border-radius:17px;background:var(--v-soft);overflow:hidden;display:grid;place-items:center;margin-bottom:13px}.vela-roche .v-schedule-list-cover img{width:100%;height:100%;object-fit:cover;display:block}.vela-roche .v-schedule-cover-placeholder{width:38px;height:38px;border-radius:50%;background:#111;box-shadow:0 0 0 12px rgba(0,0,0,.035)}
.vela-roche .v-schedule-list-copy h3{font-size:16px;line-height:1.35;margin:5px 0 4px}.vela-roche .v-schedule-card-actions{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:13px}.vela-roche .v-schedule-card-actions button{border:0;border-radius:13px;min-height:42px;font-size:10px;font-weight:900;padding:0 8px}.vela-roche .v-schedule-card-actions .is-primary{background:var(--v-blue);color:#fff}.vela-roche .v-schedule-card-actions .is-light{background:var(--v-soft);color:var(--v-text)}.vela-roche .v-schedule-card-actions .is-danger{background:#fff0f1;color:var(--v-red)}.vela-roche .v-schedule-list-card.is-editing .v-schedule-card-actions{grid-template-columns:1fr 1fr}.vela-roche .v-schedule-edit-grid{display:grid;gap:10px}.vela-roche .v-schedule-edit-grid .v-field,.vela-roche .v-schedule-edit-grid .v-fieldpair{margin:0}
.vela-roche .v-subscription-off{background:var(--v-card);border:1px solid var(--v-line);border-radius:18px;padding:16px}.vela-roche .v-subscription-off b{display:block;font-size:14px}.vela-roche .v-subscription-off span{display:block;margin-top:5px;color:var(--v-muted);font-size:10px;line-height:1.55}.vela-roche [data-role="subscription-plan-fields"][hidden]{display:none!important}.vela-roche .v-subscribed-list-head{margin-top:18px}.vela-roche .v-community-system-note{margin:10px auto;padding:0 10px;text-align:center;color:var(--v-muted);font-size:9px;line-height:1.5;max-width:82%}
.vela-roche .v-post-card{border-radius:22px!important;padding:16px!important}.vela-roche .v-post-card .v-post-head{align-items:flex-start!important;gap:11px}.vela-roche .v-post-card .v-post-head>.v-social-avatar{width:46px!important;height:46px!important;border-radius:14px!important;align-self:flex-start!important;margin-top:0!important}.vela-roche .v-post-card .v-post-head>.v-social-avatar img{border-radius:14px!important}.vela-roche .v-post-author-line{display:flex;align-items:center;gap:5px;flex-wrap:wrap;min-height:20px}.vela-roche .v-post-author-line strong{font-size:14px;line-height:1.2}.vela-roche .v-post-author-handle{font-size:11px;color:var(--v-muted);font-weight:650}.vela-roche .v-post-card .v-access-badge{margin:0!important;padding:3px 6px!important;border-radius:7px!important;font-size:8px!important}.vela-roche .v-post-time{font-size:10px;color:var(--v-muted);margin-top:3px}.vela-roche .v-post-card>.v-social-text{font-size:14px;line-height:1.58;margin-top:13px}.vela-roche .v-post-engagement{gap:18px!important;margin-top:12px!important;padding-top:0;border-top:0}.vela-roche .v-post-engagement button{display:inline-flex!important;align-items:center!important;gap:5px!important;color:var(--v-muted)!important;font-size:10px!important;padding:2px 0!important}.vela-roche .v-post-engagement button svg{width:20px;height:20px;display:block}.vela-roche .v-post-engagement button.is-liked{color:var(--v-red)!important}.vela-roche .v-post-card .v-inline-comments{margin-top:10px;padding-top:8px}.vela-roche .v-post-comments-empty{padding:22px 8px;text-align:center;color:var(--v-muted);font-size:10px;line-height:1.65}.vela-roche .v-post-card .v-comment{align-items:flex-start;padding:11px 0}.vela-roche .v-post-card .v-comment .v-social-avatar{width:34px!important;height:34px!important;border-radius:50%!important}.vela-roche .v-post-card .v-comment-name{font-size:11px!important;font-weight:900}.vela-roche .v-comment-replying{font-size:9px;color:var(--v-muted);margin-top:2px}.vela-roche .v-post-card .v-comment-text{font-size:12px;line-height:1.55;margin-top:3px}.vela-roche .v-comment-tool{border:0;background:none;color:var(--v-muted);font-size:9px;font-weight:800;padding:0}.vela-roche .v-comment-translation{font-size:10px!important;line-height:1.55!important;margin-top:6px!important}.vela-roche .v-post-card .v-more-comments{text-align:left;font-size:10px;padding:10px 0}.vela-roche .v-post-detail-card{margin-bottom:0}.vela-roche .v-post-publish-group .v-field textarea{min-height:105px!important}
.vela-roche .v-readonly-link{background:var(--v-soft)!important;color:var(--v-muted)!important}
.vela-roche .v-appointment-actions .v-today{background:var(--v-red)!important;color:#fff!important}
.vela-roche .v-profile-follow.is-mutual{background:var(--v-red)!important;color:#fff!important}
.vela-roche button.is-generating{pointer-events:none!important;opacity:.72!important}
.vela-roche .v-summon-dots{display:inline-flex;letter-spacing:2px;font-weight:900;line-height:1}
</style>`;
  }

  function mountMarkup(root, state) {
    const identity = state.identities.find(x => x.id === state.viewerIdentityId) || state.identities[0];
    const topH = Math.max(52, Math.min(86, Number(state.appearance?.topbarHeight || 62)));
    const bottomH = Math.max(64, Math.min(100, Number(state.appearance?.bottombarHeight || 78)));
    root.classList.remove("is-dark");
    root.style.setProperty("--v-top-h", `${topH}px`);
    root.style.setProperty("--v-bottom-h", `${bottomH}px`);
    root.innerHTML = `${styles()}
      <header class="v-top">
        <div class="v-brand"><button class="v-plainbtn" data-action="open-drawer">☰</button><span class="v-logo"><img src="${VELA_ICON}" alt=""></span>Vela</div>
      </header>
      <main class="v-main">
        <section class="v-page is-active" data-page="home">
          <div class="v-title-row"><div class="v-title">首页 <small>关注中的频道</small></div><div class="v-title-actions"><button class="v-icon-btn" data-action="refresh-home" aria-label="刷新首页" title="刷新首页">${refreshIconHTML()}</button></div></div>
          <div class="v-channels" data-role="channel-strip"></div>
          <div data-role="schedule"></div>
          <div class="v-filters"><button class="v-chip" data-action="home-filter" data-home-filter="all">全部</button><button class="v-chip" data-action="home-filter" data-home-filter="live">正在直播</button><button class="v-chip" data-action="home-filter" data-home-filter="post">贴文</button></div>
          <div data-role="home-feed"></div>
        </section>
        <section class="v-page" data-page="discover">
          <div class="v-title-row"><div class="v-title">推荐 <small>现在正在播什么</small></div><div class="v-title-actions"><button class="v-rec-topic-btn" data-action="open-recommendation-topics">想看什么</button><button class="v-icon-btn" data-action="refresh-recommended" aria-label="刷新推荐" title="随机刷新推荐">${refreshIconHTML()}</button></div></div>
          <div data-role="recommended"></div>
        </section>
        <section class="v-page" data-page="messages">
          <div class="v-title-row"><div class="v-title">DM <small>私信与订阅社群</small></div><div class="v-title-actions"><button class="v-icon-btn" data-action="refresh-messages" aria-label="刷新消息" title="刷新消息">${refreshIconHTML()}</button></div></div>
          <div class="v-dm-tabs"><button data-action="dm-tab" data-dm-tab="dm">DM</button><button data-action="dm-tab" data-dm-tab="community">社群</button></div>
          <div data-role="messages"></div>
        </section>
        <section class="v-page" data-page="wallet">
          <div class="v-title">钱包 <small>Vela Coin 与结算</small></div>
          <div data-role="wallet-assets"></div>
          <div data-role="wallet-account"></div>
          <div class="v-card v-wallet-ledger"><div class="v-wallet-ledger-head"><b>最近流水</b><small>🔷 为 Vela Coin，现金按当前结算币种显示</small></div><div data-role="transactions"></div></div>
        </section>
      </main>
      <nav class="v-nav">
        <button class="is-active" data-nav="home"><img class="v-nav-icon" src="https://imgbed.heliar.top/i/fV1QV24Idhyq5eLW_7e05df23a9a155c4a103dce04bbf45c9.webp" alt="">首页</button>
        <button data-nav="discover"><img class="v-nav-icon" src="https://imgbed.heliar.top/i/v3qLon2ownzeUSZz_5d47fe37a5795aeda79a43687cdf7509.webp" alt="">推荐</button>
        <button class="v-publish" data-action="open-publish"><b>＋</b>发布</button>
        <button data-nav="messages"><img class="v-nav-icon" src="https://imgbed.heliar.top/i/DA6HCswKkWlxsubp_1096c3944796c4014d0e61524da96691.webp" alt="">DM</button>
        <button data-nav="wallet"><img class="v-nav-icon" src="https://imgbed.heliar.top/i/iGFBlwtDW8QMNSgv_c7a1d5d3edc7d5d7950b866a79f93158.webp" alt="">钱包</button>
      </nav>
      <div class="v-mask" data-role="mask" data-action="close-drawer"></div>
      <aside class="v-drawer" data-role="drawer">
        <div class="v-drawer-main">
          <div class="v-userhead v-userhead-static">
            <div class="v-userav">${avatarHTML(identity?.avatar || "", identity?.displayName || "V")}</div>
            <div class="v-usercopy">
              <b>${escapeHTML(identity?.displayName || "尚未创建 Vela 账号")}</b>
              <span>${escapeHTML(identity?.handle || "先到「身份与账号」关联 Roche 用户资料")}</span>
              <span class="v-identity-chip">${identity ? (identity.accountRole === "alias" ? "副账号" : "主账号") : "未创建"}</span>
            </div>
          </div>

          <div class="v-drawer-section">
            <div class="v-drawer-label">账号与频道</div>
            <button class="v-drawer-item" data-action="open-identities"><span class="v-itemcopy"><span>身份与账号</span></span><span>›</span></button>
            <button class="v-drawer-item" data-action="open-roles"><span class="v-itemcopy"><span>角色与频道管理</span></span><span class="v-itembadge">${safeArray(state.rocheLink?.linkedCharacterIds).length} 已连接&nbsp; ›</span></button>
          </div>

          <div class="v-drawer-section">
            <div class="v-drawer-label">Vela 设置</div>
            <button class="v-drawer-item" data-action="settings-note" data-settings-kind="presets"><span class="v-itemcopy"><span>生成预设</span></span><span>›</span></button>
            <button class="v-drawer-item" data-action="settings-note" data-settings-kind="appearance"><span class="v-itemcopy"><span>外观设置</span></span><span>›</span></button>
            <button class="v-drawer-item" data-action="settings-note" data-settings-kind="platform"><span class="v-itemcopy"><span>平台设置</span></span><span>›</span></button>
            <button class="v-drawer-item" data-action="settings-note" data-settings-kind="memory"><span class="v-itemcopy"><span>记忆互通</span></span><span class="v-itembadge">稍后&nbsp; ›</span></button>
          </div>
        </div>

        <div class="v-drawer-foot">
          <button class="v-drawer-clear" data-action="clear-vela-data">清空插件数据</button>
          <div class="v-version">Vela v${VERSION}</div>
          <button class="v-exit" data-action="exit-vela">退出 Vela</button>
        </div>
      </aside>
      <div class="v-sheetmask" data-role="publish-sheet" data-action="sheet-bg-close">
        <div class="v-sheet"><div class="v-handle"></div><div data-role="publish-sheet-content"></div></div>
      </div>
      <section class="v-subscreen" data-screen="live"></section>
      <section class="v-subscreen" data-screen="channel"></section>
      <section class="v-subscreen" data-screen="message"></section>
      <section class="v-subscreen" data-screen="identities"></section>
      <section class="v-subscreen" data-screen="roles"></section>
      <section class="v-subscreen" data-screen="profile"></section>
      <section class="v-subscreen" data-screen="profile-edit"></section>
      <section class="v-subscreen" data-screen="settings"></section>
      <section class="v-subscreen" data-screen="post"></section>
      <section class="v-subscreen" data-screen="post-editor"></section>
      <section class="v-subscreen" data-screen="schedule-editor"></section>
      <section class="v-subscreen" data-screen="schedule-detail"></section>
      <section class="v-subscreen" data-screen="publish-live"></section>
      <section class="v-subscreen" data-screen="restricted-editor"></section>
      <section class="v-subscreen" data-screen="invite-guest"></section>
      <section class="v-subscreen" data-screen="replay"></section>
      <section class="v-subscreen" data-screen="subscription-settings"></section>
      <section class="v-subscreen" data-screen="community-settings"></section>
      <section class="v-subscreen" data-screen="appointment-reminder"></section>
      <section class="v-subscreen" data-screen="wallet-flow"></section>
      <section class="v-subscreen" data-screen="live-summary"></section>
      <div class="v-toast" data-role="toast"></div>
    `;
  }

  function registerApp() {
    window.RochePlugin.register({
      id: PLUGIN_ID,
      name: "Vela",
      version: VERSION,
      apps: [
        {
          id: APP_ID,
          name: "Vela",
          icon: "chat",
          iconImage: VELA_ICON,
          async mount(container, roche) {
            container.replaceChildren();
            const root = document.createElement("div");
            root.className = "vela-roche";
            container.appendChild(root);

            let state = createDefaultState();
            try {
              const saved = await roche?.storage?.get?.("vela-shell-state");
              if (saved && typeof saved === "object") {
                state = {
                  ...state,
                  ...saved,
                  wallet: { ...state.wallet, ...(saved.wallet || {}), cashBalanceCNY: Number(saved.wallet?.cashBalanceCNY ?? saved.wallet?.balance ?? state.wallet.cashBalanceCNY ?? 0) },
                  roleCommerce: { ...state.roleCommerce, ...(saved.roleCommerce || {}) },
                  businessDeals: { ...state.businessDeals, ...(saved.businessDeals || {}) },
                  generationPreset: { ...state.generationPreset, ...(saved.generationPreset || {}) },
                  appearance: { ...state.appearance, ...(saved.appearance || {}) },
                  platformSettings: { ...state.platformSettings, ...(saved.platformSettings || {}) }
                };
              }
            } catch (err) {
              console.warn("[Vela] failed to load shell state", err);
            }

            state.rocheLink = {
              ownerPersonaId: String(state.rocheLink?.ownerPersonaId || ""),
              linkedCharacterIds: safeArray(state.rocheLink?.linkedCharacterIds).map(id => String(id))
            };
            state.identities = safeArray(state.identities).map(item => ({
              accountRole: String(item?.accountRole || "primary") === "alias" ? "alias" : "primary",
              sourcePersonaId: String(item?.sourcePersonaId || ""),
              mainAccountId: String(item?.mainAccountId || ""),
              linkedToMain: Boolean(item?.linkedToMain),
              knownByCharacters: { ...(item?.knownByCharacters || {}) },
              faceMode: String(item?.faceMode || "mixed"),
              identityVisibility: String(item?.identityVisibility || (item?.isAnonymous ? "hidden" : "partial")),
              contentStyle: "", fanProfile: "", banner: "", followers: 0, following: 0, scheduledLive: null, scheduledLives: [], activeLive: null, live: false, ...item
            }));
            // v0.2.1 fresh installs no longer ship a default Vela identity.
            // Remove only the untouched old shell seed so existing real/custom accounts are preserved.
            state.identities = state.identities.filter(item => !(
              String(item?.id) === "alias-night" &&
              !item?.sourcePersonaId &&
              String(item?.displayName || "") === "Nini" &&
              String(item?.handle || "") === "@night_vela" &&
              Number(item?.followers || 0) === 0 &&
              Number(item?.following || 0) === 0
            ));
            state.channels = safeArray(state.channels).map(item => ({
              accountRole: String(item?.accountRole || "primary") === "alias" ? "alias" : "primary",
              mainChannelId: String(item?.mainChannelId || ""),
              linkedToMain: Boolean(item?.linkedToMain),
              userKnowsAlias: Boolean(item?.userKnowsAlias),
              characterKnowsUserKnows: Boolean(item?.characterKnowsUserKnows),
              faceMode: String(item?.faceMode || "mixed"),
              identityVisibility: String(item?.identityVisibility || "partial"),
              contentStyle: "", fanProfile: "", banner: "", followers: 0, following: 0, ...item
            }));
            if (!state.identities.some(item => String(item.id) === String(state.viewerIdentityId || ""))) {
              state.viewerIdentityId = state.identities[0]?.id || "";
            }
            if (!state.followDefaultsInitialized) {
              const mainIdentity = state.identities.find(item => item.accountRole !== "alias" && String(item.sourcePersonaId || "") === String(state.rocheLink?.ownerPersonaId || ""))
                || state.identities.find(item => item.accountRole !== "alias")
                || state.identities[0];
              if (mainIdentity) {
                const primaryChannels = state.channels.filter(channel => channel.accountRole !== "alias" && Boolean(channel.sourceCharacterId));
                primaryChannels.forEach(channel => {
                  state.follows[`identity:${String(mainIdentity.id)}>channel:${String(channel.id)}`] = true;
                  state.follows[`channel:${String(channel.id)}>identity:${String(mainIdentity.id)}`] = true;
                  channel.followers = Math.max(Number(channel.followers || 0), 1);
                  channel.following = Math.max(Number(channel.following || 0), 1);
                });
                mainIdentity.following = Math.max(Number(mainIdentity.following || 0), primaryChannels.length);
                mainIdentity.followers = Math.max(Number(mainIdentity.followers || 0), primaryChannels.length);
              }
              state.followDefaultsInitialized = true;
            }
            state.appearance = {
              topbarHeight: Math.max(52, Math.min(86, Number(state.appearance?.topbarHeight || 62))),
              bottombarHeight: Math.max(64, Math.min(100, Number(state.appearance?.bottombarHeight || 78)))
            };
            state.wallet = {
              cashBalanceCNY: Number(state.wallet?.cashBalanceCNY ?? state.wallet?.balance ?? 0),
              coinBalance: Number(state.wallet?.coinBalance ?? 0),
              linkedAccount: state.wallet?.linkedAccount && typeof state.wallet.linkedAccount === "object" ? state.wallet.linkedAccount : null,
              settlementCurrency: String(state.wallet?.settlementCurrency || ""),
              fxRates: { CNY: 1, ...(state.wallet?.fxRates || {}) },
              fxUpdatedAt: Number(state.wallet?.fxUpdatedAt || 0),
              subscriptionAccruals: safeArray(state.wallet?.subscriptionAccruals),
              subscriptionPurchases: { ...(state.wallet?.subscriptionPurchases || {}) },
              transactions: safeArray(state.wallet?.transactions).map((tx, index) => {
                if (tx?.cashCNY != null || tx?.coin != null) return { ...tx };
                return { id: tx?.id || `legacy-${index}`, type: "cash", title: tx?.title || "历史流水", note: tx?.note || "", cashCNY: Number(tx?.amount || 0), at: tx?.at || Date.now() };
              })
            };
            state.wallet.transactions = safeArray(state.wallet.transactions).filter(tx => !["tx-demo-business","tx-demo-tip"].includes(String(tx?.id || "")));
            if (state.roleCommerce && typeof state.roleCommerce === "object") delete state.roleCommerce["char-aster"];
            state.roleEarnings = { ...(state.roleEarnings || {}) };
            state.platformSettings = {
              recommendationCount: 3,
              allowRoleAliasesInRecommendations: false,
              allowBusinessDM: true,
              activityLevel: 50,
              strangerInteraction: "normal",
              languagePreference: "overseas_more",
              ...(state.platformSettings || {})
            };
            state.platformSettings.strangerInteraction = ["low","normal","high"].includes(state.platformSettings.strangerInteraction)
              ? state.platformSettings.strangerInteraction : "normal";
            state.platformSettings.languagePreference = LANGUAGE_PREFERENCE_OPTIONS.some(item => item.key === String(state.platformSettings.languagePreference || ""))
              ? String(state.platformSettings.languagePreference) : "overseas_more";
            state.generationPreset = { mode: "default", customText: "", nsfwText: "成熟向内容可以更大胆、更暧昧、更有成人主播或成人社交平台氛围，可包含性感但不露骨的造型、夜生活、付费订阅与成熟向玩笑；不要生成露骨色情细节、明确性行为或色情裸露描写。", ...(state.generationPreset || {}) };
            state.communityPosts = safeArray(state.communityPosts);
            state.liveReplays = safeArray(state.liveReplays);
            state.pendingLiveUserMessage = state.pendingLiveUserMessage && typeof state.pendingLiveUserMessage === "object" ? state.pendingLiveUserMessage : null;
            state.postReplies = { ...(createDefaultState().postReplies || {}), ...(state.postReplies || {}) };
            state.postOverrides = { ...(state.postOverrides || {}) };
            state.liveProgress = { ...(state.liveProgress || {}) };
            state.liveStats = { ...(state.liveStats || {}) };
            state.liveChat = safeArray(state.liveChat);
            state.homeFilter = ["all","live","post"].includes(state.homeFilter) ? state.homeFilter : "all";
            state.dmTab = ["dm","community"].includes(state.dmTab) ? state.dmTab : "dm";
            state.subscriptions = { ...(state.subscriptions || {}) };
            state.subscriptionsByIdentity = { ...(state.subscriptionsByIdentity || {}) };
            state.follows = { ...(state.follows || {}) };
            state.followDefaultsInitialized = Boolean(state.followDefaultsInitialized);
            state.followDefaultsV0213 = Boolean(state.followDefaultsV0213);
            state.appointmentReminderMuteDate = String(state.appointmentReminderMuteDate || "");
            state.dmInteractionIdentity = { ...(state.dmInteractionIdentity || {}) };
            state.liveInteractionIdentity = { ...(state.liveInteractionIdentity || {}) };
            const legacySubscriptionOwner = String(state.viewerIdentityId || state.identities[0]?.id || "");
            if (legacySubscriptionOwner && Object.keys(state.subscriptions).length && !Object.keys(state.subscriptionsByIdentity).length) {
              state.subscriptionsByIdentity[legacySubscriptionOwner] = { ...state.subscriptions };
            }
            state.postInteractionIdentity = { ...(state.postInteractionIdentity || {}) };
            state.communityInteractionIdentity = { ...(state.communityInteractionIdentity || {}) };
            state.homeFeedByChannel = { ...(state.homeFeedByChannel || {}) };
            state.homeActivities = safeArray(state.homeActivities);
            state.liveChatsById = { ...(state.liveChatsById || {}) };
            state.liveChatQueues = { ...(state.liveChatQueues || {}) };
            state.recommendationSamplesSeeded = Boolean(state.recommendationSamplesSeeded);
            state.subscriptionProfiles = { ...(state.subscriptionProfiles || {}) };
            state.communityChats = { ...(state.communityChats || {}) };
            state.communityChatQueues = { ...(state.communityChatQueues || {}) };
            state.communitySettings = { ...(state.communitySettings || {}) };
            state.dmThreads = { ...(state.dmThreads || {}) };
            Object.keys(state.dmThreads).forEach(key => { state.dmThreads[key] = safeArray(state.dmThreads[key]).filter(item => !item?.typing); });
            const removedLegacyDMIds = new Set();
            state.messages = safeArray(state.messages).filter(msg => {
              const id = String(msg?.id || "");
              const name = String(msg?.name || "").trim();
              const handle = String(msg?.handle || "").trim();
              if (["aster-dm","char-aster","char-noir"].includes(id) || ["Aster","Noir"].includes(name)) { removedLegacyDMIds.add(id); return false; }
              const legacyGenericGuest = /^@guest_[a-z0-9]+$/i.test(handle) && ["陌生人","商务联系","骚扰信息"].includes(name);
              if (legacyGenericGuest) { removedLegacyDMIds.add(id); return false; }
              return true;
            });
            removedLegacyDMIds.forEach(id => { if (id) delete state.dmThreads[id]; });
            state.recommendedLives = safeArray(state.recommendedLives).filter(item => {
              if (["rec-mellow","rec-cloud","rec-anon","rec-preview-ko","rec-preview-ja"].includes(String(item?.id || ""))) return false;
              const ownerId = String(item?.ownerId || "");
              if (!ownerId) return true;
              const ch = state.channels.find(row => String(row?.id || "") === ownerId);
              if (!ch) return true;
              if (String(ch.accountRole || "primary") !== "alias") return false;
              return state.platformSettings.allowRoleAliasesInRecommendations === true;
            });
            // Keep Mellow Studio as the single intentional business-demo conversation.
            // Old one-shot contract cards are removed; the demo now starts from a normal DM and negotiates naturally.
            const legacyMellowMsg = state.messages.find(msg => String(msg?.id || "") === "biz-mellow") || null;
            const legacyMellowRows = safeArray(state.dmThreads?.["biz-mellow"]);
            if (legacyMellowMsg || legacyMellowRows.length) {
              if (legacyMellowMsg) {
                legacyMellowMsg.name = "Mellow Studio";
                legacyMellowMsg.handle = "@mellow_studio";
                legacyMellowMsg.badge = "商务联系";
                legacyMellowMsg.strangerKind = "business";
                legacyMellowMsg.external = true;
                legacyMellowMsg.avatar = STRANGER_AVATAR;
                legacyMellowMsg.businessProfile = legacyMellowMsg.businessProfile || "生活方式与香氛品牌；产品方向为居家香氛、空间气味与桌面生活方式。";
                if (!String(legacyMellowMsg.preview || "").trim() || String(legacyMellowMsg.preview || "").includes("Cloud Mist 香氛机合作邀约")) {
                  legacyMellowMsg.preview = "你好，我们是 Mellow Studio，做生活方式和香氛产品。最近有一个直播合作方向想先问问你有没有兴趣了解？";
                }
              }
              const oldMellowThread = legacyMellowRows.filter(item => item && typeof item === "object" && (!String(item?.id || "").startsWith("biz-card-demo") || String(item?.id || "") === "biz-card-demo-0212"));
              if (!oldMellowThread.length && legacyMellowMsg?.preview) {
                oldMellowThread.push({ id:"biz-intro", kind:"text", sender:"other", name:"Mellow Studio", avatar:STRANGER_AVATAR, text:String(legacyMellowMsg.preview), translation:"", at:Date.now()-180000 });
              }
              state.dmThreads["biz-mellow"] = oldMellowThread;
            }
            state.recommendationSamplesSeeded = true;
            state.pendingOwnLiveInput = state.pendingOwnLiveInput && typeof state.pendingOwnLiveInput === "object" ? state.pendingOwnLiveInput : null;
            state.businessDeals = { ...(state.businessDeals || {}) };
            delete state.businessDeals["biz-mellow"];
            state.schedule = {
              visible: false, source: "", messageId: "", dateText: "", title: "", detail: "", type: "",
              ...(state.schedule || {})
            };
            // v0.1.4 以前的 Mellow Studio 日程只是 Shell 占位。升级后默认隐藏，只有真正接受合作才出现。
            if (String(state.schedule?.messageId || "") === "biz-mellow" || state.schedule?.title === "Mellow Studio 合作直播") {
              state.schedule = { visible: false, source: "", messageId: "", dateText: "", title: "", detail: "", type: "" };
            }

            const rocheRuntime = {
              loaded: false,
              error: "",
              activePersona: null,
              personas: [],
              characters: []
            };

            mountMarkup(root, state);

            const q = (sel) => root.querySelector(sel);
            const qa = (sel) => [...root.querySelectorAll(sel)];
            let toastTimer = null;
            const generationBusy = { home:false, recommended:false, messages:false };
            const dmReplyPending = new Set();
            const communityReplyPending = new Set();
            const communityGenerationBusy = new Set();
            const postReplyPending = new Set();
            const liveReplyPending = new Set();
            let lastVelaAIError = "";
            const communityLazyTimers = new Map();
            const liveLazyTimers = new Map();

            const persist = async () => {
              try {
                if (state.liveSession) {
                  const liveKey = String(state.liveSession.id || state.liveSession.ownerId || "live");
                  state.liveChatsById = { ...(state.liveChatsById || {}) };
                  state.liveChatsById[liveKey] = safeArray(state.liveChat).map(item => ({ ...item }));
                }
                await roche?.storage?.set?.("vela-shell-state", {
                  viewerIdentityId: state.viewerIdentityId,
                  rocheLink: state.rocheLink,
                  identities: state.identities,
                  channels: state.channels,
                  recommendedLives: state.recommendedLives,
                  schedule: state.schedule,
                  wallet: state.wallet,
                  roleEarnings: state.roleEarnings,
                  roleCommerce: state.roleCommerce,
                  businessDeals: state.businessDeals,
                  homeFilter: state.homeFilter,
                  dmTab: state.dmTab,
                  subscriptions: state.subscriptions,
                  subscriptionsByIdentity: state.subscriptionsByIdentity,
                  follows: state.follows,
                  followDefaultsInitialized: state.followDefaultsInitialized,
                  appointmentReminderMuteDate: state.appointmentReminderMuteDate,
                  subscriptionProfiles: state.subscriptionProfiles,
                  postInteractionIdentity: state.postInteractionIdentity,
                  communityInteractionIdentity: state.communityInteractionIdentity,
                  dmInteractionIdentity: state.dmInteractionIdentity,
                  liveInteractionIdentity: state.liveInteractionIdentity,
                  homeFeedByChannel: state.homeFeedByChannel,
                  homeActivities: state.homeActivities,
                  liveChatsById: state.liveChatsById,
                  liveChatQueues: state.liveChatQueues,
                  recommendationSamplesSeeded: state.recommendationSamplesSeeded,
                  communityChats: state.communityChats,
                  communityChatQueues: state.communityChatQueues,
                  communitySettings: state.communitySettings,
                  dmThreads: state.dmThreads,
                  messages: state.messages,
                  pendingOwnLiveInput: state.pendingOwnLiveInput,
                  generationPreset: state.generationPreset,
                  appearance: state.appearance,
                  platformSettings: state.platformSettings,
                  communityPosts: state.communityPosts,
                  liveReplays: state.liveReplays,
                  pendingLiveUserMessage: state.pendingLiveUserMessage,
                  postReplies: state.postReplies,
                  postOverrides: state.postOverrides,
                  liveProgress: state.liveProgress,
                  liveStats: state.liveStats,
                  liveChat: state.liveChat
                });
              } catch (err) {
                console.warn("[Vela] failed to persist shell state", err);
              }
            };

            const toast = (message) => {
              const el = q('[data-role="toast"]');
              if (!el) return;
              el.textContent = message;
              el.classList.add("is-show");
              if (toastTimer) clearTimeout(toastTimer);
              toastTimer = setTimeout(() => el.classList.remove("is-show"), 1700);
            };


            const isRocheBotIdentity = (item) => {
              if (!item || typeof item !== "object") return false;
              if (item.isBot === true || item.bot === true) return true;
              const type = `${item.type || ""} ${item.kind || ""} ${item.role || ""}`.toLowerCase();
              if (/\bbot\b/.test(type)) return true;
              const labels = `${item.id || ""} ${item.name || ""} ${item.displayName || ""} ${item.handle || ""}`.toLowerCase();
              return /(^|[\s_\-])bot($|[\s_\-])/.test(labels);
            };

            const getPersonaAccounts = (personaId) => state.identities.filter(item => String(item?.sourcePersonaId || "") === String(personaId || ""));
            const getPersonaMainAccount = (personaId) => getPersonaAccounts(personaId).find(item => item.accountRole !== "alias") || null;
            const getPersonaSubAccount = (personaId) => getPersonaAccounts(personaId).find(item => item.accountRole === "alias") || null;
            const getPersonaAliasAccounts = (personaId) => getPersonaAccounts(personaId).filter(item => item.accountRole === "alias");
            const getCharacterAccounts = (characterId) => state.channels.filter(item => String(item?.sourceCharacterId || "") === String(characterId || ""));
            const getCharacterMainAccount = (characterId) => getCharacterAccounts(characterId).find(item => item.accountRole !== "alias") || null;
            const getCharacterSubAccount = (characterId) => getCharacterAccounts(characterId).find(item => item.accountRole === "alias") || null;
            const getCharacterAliasAccounts = (characterId) => getCharacterAccounts(characterId).filter(item => item.accountRole === "alias");

            const nextUniqueHandle = (baseHandle, collection) => {
              const normalized = normalizeHandle(baseHandle || "") || "@vela";
              const taken = new Set(safeArray(collection).map(item => String(item?.handle || "").toLowerCase()));
              if (!taken.has(normalized.toLowerCase())) return normalized;
              const stem = normalized.replace(/^@/, "").replace(/[^a-zA-Z0-9_.]/g, "") || "vela";
              for (let i = 2; i < 100; i += 1) {
                const candidate = `@${stem}_${i}`;
                if (!taken.has(candidate.toLowerCase())) return candidate;
              }
              return `@${stem}_${Math.random().toString(36).slice(2,6)}`;
            };

            const accountRoleLabel = item => item?.accountRole === "alias" ? "副账号" : "主账号";
            const subLinkLabel = item => item?.accountRole === "alias"
              ? (item?.linkedToMain ? "已关联主账号" : "隐藏小号")
              : "主要账号";

            const syncLinkedChannelSnapshots = async () => {
              const linkedIds = new Set(safeArray(state.rocheLink?.linkedCharacterIds).map(String));
              if (!linkedIds.size) return;

              for (const charId of linkedIds) {
                let char = rocheRuntime.characters.find(x => String(x?.id) === charId) || null;
                try {
                  const full = await roche?.character?.get?.(charId);
                  if (full) char = full;
                } catch (_) {}
                if (!char) continue;

                const channelId = `roche-char-${charId}`;
                const existing = state.channels.find(x => x.id === channelId);
                const displayName = displayProfileName(char);
                const snapshot = {
                  id: channelId,
                  kind: "character",
                  sourceCharacterId: charId,
                  realName: String(char?.name || ""),
                  name: displayName,
                  handle: normalizeHandle(char?.handle || ""),
                  avatar: String(char?.avatar || initials(displayName)),
                  bio: String(char?.bio || ""),
                  banner: "",
                  followers: 0,
                  following: 0,
                  accountRole: "primary",
                  faceMode: "mixed",
                  identityVisibility: "partial",
                  contentStyle: "",
                  fanProfile: "",
                  liveDurationMode: String(existing?.liveDurationMode || "irregular"),
                  liveDurationText: String(existing?.liveDurationText || ""),
                  liveSchedule: String(existing?.liveSchedule || ""),
                  liveAutonomy: String(existing?.liveAutonomy || "flexible"),
                  profileCustomized: false,
                  scheduledLive: existing?.scheduledLive || null,
                  scheduledLives: safeArray(existing?.scheduledLives),
                  live: Boolean(existing?.live),
                  title: String(existing?.title || ""),
                  viewers: Number(existing?.viewers || 0)
                };
                if (existing) {
                  const custom = existing.profileCustomized ? {
                    name: existing.name, handle: existing.handle, avatar: existing.avatar,
                    bio: existing.bio, banner: existing.banner, followers: existing.followers,
                    following: existing.following, accountRole: existing.accountRole, faceMode: existing.faceMode,
                    identityVisibility: existing.identityVisibility, contentStyle: existing.contentStyle,
                    fanProfile: existing.fanProfile, liveDurationMode: existing.liveDurationMode,
                    liveDurationText: existing.liveDurationText, liveSchedule: existing.liveSchedule,
                    liveAutonomy: existing.liveAutonomy, profileCustomized: true
                  } : {};
                  Object.assign(existing, snapshot, custom);
                } else state.channels.push(snapshot);
              }
            };

            const refreshRocheData = async ({ announce = false, sync = true } = {}) => {
              try {
                const [activePersona, personas, characters] = await Promise.all([
                  roche?.persona?.getActiveUserPersona?.(),
                  roche?.persona?.getUserPersonas?.(),
                  roche?.character?.list?.()
                ]);

                rocheRuntime.activePersona = activePersona || null;
                rocheRuntime.personas = safeArray(personas);
                rocheRuntime.characters = safeArray(characters);
                rocheRuntime.loaded = true;
                rocheRuntime.error = "";

                const linkedOwner = rocheRuntime.personas.find(item => String(item?.id || "") === String(state.rocheLink.ownerPersonaId || "")) ||
                  (String(activePersona?.id || "") === String(state.rocheLink.ownerPersonaId || "") ? activePersona : null);
                if (linkedOwner && isRocheBotIdentity(linkedOwner)) state.rocheLink.ownerPersonaId = "";
                if (sync) await syncLinkedChannelSnapshots();
                await persist();
                if (announce) toast("已重新读取 Roche 用户与角色");
                return true;
              } catch (err) {
                rocheRuntime.loaded = true;
                rocheRuntime.error = String(err?.message || err || "读取失败");
                console.warn("[Vela] failed to read Roche persona/characters", err);
                if (announce) toast("读取 Roche 用户或角色失败");
                return false;
              }
            };

            const linkCharacter = async (charId) => {
              const id = String(charId || "");
              if (!id) return;
              let char = rocheRuntime.characters.find(x => String(x?.id) === id) || null;
              try {
                const full = await roche?.character?.get?.(id);
                if (full) char = full;
              } catch (_) {}
              if (!char) {
                toast("没有读取到这个 Roche 角色");
                return;
              }

              if (!state.rocheLink.linkedCharacterIds.includes(id)) {
                state.rocheLink.linkedCharacterIds.push(id);
              }

              // 第一次加入真实 Roche 角色后，移除 Shell 演示角色。
              state.channels = state.channels.filter(ch => !ch.isDemo && !["char-aster", "char-noir"].includes(ch.id));

              const channelId = `roche-char-${id}`;
              const displayName = displayProfileName(char);
              const existing = state.channels.find(x => x.id === channelId);
              const snapshot = {
                id: channelId,
                kind: "character",
                sourceCharacterId: id,
                name: displayName,
                realName: String(char?.name || ""),
                handle: normalizeHandle(char?.handle || ""),
                avatar: String(char?.avatar || initials(displayName)),
                bio: String(char?.bio || ""),
                banner: String(existing?.banner || ""),
                followers: Number(existing?.followers || 0),
                following: Number(existing?.following || 0),
                accountRole: "primary",
                mainChannelId: "",
                linkedToMain: false,
                userKnowsAlias: false,
                characterKnowsUserKnows: false,
                faceMode: String(existing?.faceMode || "mixed"),
                identityVisibility: String(existing?.identityVisibility || "partial"),
                contentStyle: String(existing?.contentStyle || ""),
                fanProfile: String(existing?.fanProfile || ""),
                liveDurationMode: String(existing?.liveDurationMode || "irregular"),
                liveDurationText: String(existing?.liveDurationText || ""),
                liveSchedule: String(existing?.liveSchedule || ""),
                liveAutonomy: String(existing?.liveAutonomy || "flexible"),
                profileCustomized: Boolean(existing?.profileCustomized),
                live: Boolean(existing?.live),
                title: String(existing?.title || ""),
                viewers: Number(existing?.viewers || 0)
              };
              if (existing) Object.assign(existing, snapshot);
              else state.channels.unshift(snapshot);

              if (!state.roleCommerce[id]) {
                state.roleCommerce[id] = { enabled: false, tendency: 35 };
              }
              ensureDefaultMutualFollow(snapshot);

              await persist();
              rerender();
              toast(`${displayName} 已加入 Vela`);
            };

            const unlinkCharacter = async (charId) => {
              const id = String(charId || "");
              state.rocheLink.linkedCharacterIds = state.rocheLink.linkedCharacterIds.filter(x => String(x) !== id);
              state.channels = state.channels.filter(ch => String(ch.sourceCharacterId || "") !== id);
              await persist();
              rerender();
              toast("已从 Vela 移除角色频道");
            };

            const closeDrawer = () => {
              q('[data-role="drawer"]')?.classList.remove("is-open");
              q('[data-role="mask"]')?.classList.remove("is-open");
            };

            const switchPage = (page) => {
              state.activePage = page;
              qa(".v-page").forEach(el => el.classList.toggle("is-active", el.dataset.page === page));
              qa("[data-nav]").forEach(el => el.classList.toggle("is-active", el.dataset.nav === page));
              if (page === "wallet") {
                renderWallet();
                settleWeeklySubscriptions().then(() => renderWallet());
                if (!state.wallet.linkedAccount) setTimeout(() => {
                  if (state.activePage === "wallet" && !state.wallet.linkedAccount) openWalletBind();
                }, 120);
              }
            };

            const followKey = (sourceType, sourceId, targetType, targetId) => `${String(sourceType)}:${String(sourceId)}>${String(targetType)}:${String(targetId)}`;
            const isFollowing = (sourceType, sourceId, targetType, targetId) => Boolean(state.follows?.[followKey(sourceType, sourceId, targetType, targetId)]);
            const setFollowing = (sourceType, sourceId, targetType, targetId, enabled = true) => {
              state.follows = { ...(state.follows || {}) };
              const key = followKey(sourceType, sourceId, targetType, targetId);
              if (enabled) state.follows[key] = true; else delete state.follows[key];
            };
            const getPrimaryViewerIdentity = () => state.identities.find(item => item.accountRole !== "alias" && String(item.sourcePersonaId || "") === String(state.rocheLink?.ownerPersonaId || ""))
              || state.identities.find(item => item.accountRole !== "alias")
              || state.identities[0]
              || null;
            const ensureDefaultMutualFollow = channel => {
              if (!channel || channel.accountRole === "alias") return;
              const identity = getPrimaryViewerIdentity();
              if (!identity) return;
              const alreadyA = isFollowing("identity", identity.id, "channel", channel.id);
              const alreadyB = isFollowing("channel", channel.id, "identity", identity.id);
              setFollowing("identity", identity.id, "channel", channel.id, true);
              setFollowing("channel", channel.id, "identity", identity.id, true);
              if (!alreadyA) { channel.followers = Math.max(0, Number(channel.followers || 0) + 1); identity.following = Math.max(0, Number(identity.following || 0) + 1); }
              if (!alreadyB) { channel.following = Math.max(0, Number(channel.following || 0) + 1); identity.followers = Math.max(0, Number(identity.followers || 0) + 1); }
            };
            const migrateExistingPrimaryMutualFollows = async () => {
              if (state.followDefaultsV0213) return;
              state.channels.filter(channel => channel?.accountRole !== "alias" && Boolean(channel?.sourceCharacterId)).forEach(ensureDefaultMutualFollow);
              state.followDefaultsInitialized = true;
              state.followDefaultsV0213 = true;
              await persist();
            };
            const maybeSilentCharacterFollowAlias = () => {
              if (Math.random() >= 0.04) return;
              const aliases = state.identities.filter(item => item.accountRole === "alias");
              const channels = state.channels.filter(item => Boolean(item.sourceCharacterId));
              if (!aliases.length || !channels.length) return;
              const pairs = [];
              channels.forEach(channel => aliases.forEach(identity => {
                if (!isFollowing("channel", channel.id, "identity", identity.id)) pairs.push({ channel, identity });
              }));
              const pick = pairs[Math.floor(Math.random() * Math.max(1, pairs.length))];
              if (pick) {
                setFollowing("channel", pick.channel.id, "identity", pick.identity.id, true);
                pick.channel.following = Math.max(0, Number(pick.channel.following || 0) + 1);
                pick.identity.followers = Math.max(0, Number(pick.identity.followers || 0) + 1);
              }
            };

            const getIdentitySubscriptions = (identityId = state.viewerIdentityId) => {
              const id = String(identityId || "");
              if (!id) return {};
              state.subscriptionsByIdentity[id] = { ...(state.subscriptionsByIdentity?.[id] || {}) };
              return state.subscriptionsByIdentity[id];
            };

            const getSubscriptionTierFor = (identityId, channelId) => {
              const tier = getIdentitySubscriptions(identityId)?.[String(channelId)] || "none";
              return tier === "paid" ? "paid" : tier === "free" ? "free" : "none";
            };

            const setSubscriptionTierFor = (identityId, channelId, tier) => {
              const id = String(identityId || "");
              const channelKey = String(channelId || "");
              if (!id || !channelKey) return;
              const map = getIdentitySubscriptions(id);
              if (tier === "paid" || tier === "free") map[channelKey] = tier;
              else delete map[channelKey];
              if (id === String(state.viewerIdentityId || "")) state.subscriptions = { ...map };
            };

            const tierAllows = (tier, accessMode = "public") => {
              if (accessMode === "paid") return tier === "paid";
              if (accessMode === "free") return tier === "free" || tier === "paid";
              return true;
            };

            const identitiesWithAccessToChannel = (channelId, accessMode = "public") => state.identities.filter(identity => tierAllows(getSubscriptionTierFor(identity.id, channelId), accessMode));

            const identitiesFollowingChannel = (channelId, accessMode = "public") => state.identities.filter(identity => {
              if (!isFollowing("identity", identity.id, "channel", channelId)) return false;
              return tierAllows(getSubscriptionTierFor(identity.id, channelId), accessMode);
            });

            const followedChannelIds = () => {
              const ids = new Set();
              state.identities.forEach(identity => state.channels.forEach(channel => {
                if (isFollowing("identity", identity.id, "channel", channel.id)) ids.add(String(channel.id));
              }));
              return ids;
            };

            const identityPickerHTML = (identities, selectedId, role, attrs = "") => {
              const list = safeArray(identities).filter(Boolean);
              if (!list.length) return "";
              const selected = list.find(item => String(item.id) === String(selectedId || "")) || list[0];
              return `<label class="v-composer-identity" title="切换发言身份"><span class="v-composer-identity-avatar">${avatarHTML(selected.avatar || "", selected.displayName || selected.handle || "U")}</span><select data-role="${escapeHTML(role)}" ${attrs} aria-label="切换发言身份">${list.map(identity => `<option value="${escapeHTML(identity.id)}" ${String(identity.id) === String(selected.id) ? "selected" : ""}>${escapeHTML(identity.displayName || identity.handle || "我的账号")}</option>`).join("")}</select></label>`;
            };

            const updateIdentityPickerAvatar = select => {
              const identity = state.identities.find(item => String(item.id) === String(select?.value || ""));
              const host = select?.closest?.(".v-composer-identity")?.querySelector?.(".v-composer-identity-avatar");
              if (identity && host) host.innerHTML = avatarHTML(identity.avatar || "", identity.displayName || identity.handle || "U");
            };

            const identityStackHTML = identities => {
              const list = safeArray(identities);
              return list.length ? `<span class="v-follow-stack">${list.map(identity => `<span class="v-follow-mini" title="${escapeHTML(identity.displayName || identity.handle || "我的账号")}">${avatarHTML(identity.avatar || "", identity.displayName || "U")}</span>`).join("")}</span>` : "";
            };

            const renderChannels = () => {
              const host = q('[data-role="channel-strip"]');
              if (!host) return;
              const selfHTML = state.identities.map(identity => {
                const selfLive = Boolean(identity?.activeLive || identity?.live);
                const current = String(identity.id) === String(state.viewerIdentityId || "");
                return `<button class="v-channel ${current ? "is-current" : ""}" data-action="switch-home-identity-profile" data-identity-id="${escapeHTML(identity.id)}">
                  <div class="v-avatar-wrap ${selfLive ? "is-live" : ""}"><div class="v-avatar">${avatarHTML(identity.avatar || "", identity.displayName || "U")}</div>${selfLive ? '<span class="v-live-dot">LIVE</span>' : ''}</div>
                  <div class="v-channel-name">${escapeHTML(identity.displayName || "我")}</div><span class="v-channel-kind">${identity.accountRole === "alias" ? "我的副号" : "我的主号"}</span>
                </button>`;
              }).join("");

              const followed = [...followedChannelIds()].map(id => state.channels.find(ch => String(ch.id) === String(id))).filter(Boolean);
              const chars = followed.map(ch => {
                const followers = identitiesFollowingChannel(ch.id, "public");
                return `<button class="v-channel" data-action="open-channel" data-channel-id="${escapeHTML(ch.id)}">
                  <div class="v-avatar-wrap ${ch.live ? "is-live" : ""}"><div class="v-avatar">${avatarHTML(ch.avatar, ch.name)}</div>${ch.live ? '<span class="v-live-dot">LIVE</span>' : ''}${identityStackHTML(followers)}</div>
                  <div class="v-channel-name">${escapeHTML(ch.name)}</div><span class="v-channel-kind">${ch.accountRole === "alias" ? "角色副号" : "角色账号"}</span>
                </button>`;
              }).join("");

              host.innerHTML = selfHTML + chars + `<button class="v-channel" data-action="open-roles"><div class="v-avatar-wrap"><div class="v-avatar">＋</div></div><div class="v-channel-name">添加角色</div><span class="v-channel-kind">账号管理</span></button>`;
            };


            const pickOne = (arr) => safeArray(arr)[Math.floor(Math.random() * Math.max(1, safeArray(arr).length))] || null;
            const nowHour = () => new Date().getHours();
            const formatRelativeAt = value => {
              const at = Number(value || Date.now());
              const diff = Math.max(0, Date.now() - at);
              const mins = Math.max(1, Math.round(diff / 60000));
              if (mins < 60) return `${mins} 分钟前`;
              if (mins < 1440) return `${Math.floor(mins / 60)} 小时前`;
              const days = Math.floor(mins / 1440);
              return days === 1 ? "昨天" : `${days} 天前`;
            };
            const setRefreshBusy = (kind, busy) => {
              generationBusy[kind] = Boolean(busy);
              const action = kind === "home" ? "refresh-home" : kind === "messages" ? "refresh-messages" : "refresh-recommended";
              const button = q(`[data-action="${action}"]`);
              if (!button) return;
              button.disabled = Boolean(busy);
              button.classList.toggle("is-generating", Boolean(busy));
              button.innerHTML = busy ? '<span class="v-generating-label">生成中…</span>' : refreshIconHTML();
            };
            const languagePreferenceIndex = key => {
              const index = LANGUAGE_PREFERENCE_OPTIONS.findIndex(item => item.key === String(key || ""));
              return index >= 0 ? index : LANGUAGE_PREFERENCE_OPTIONS.findIndex(item => item.key === "overseas_more");
            };
            const languagePreferenceLabel = key => LANGUAGE_PREFERENCE_OPTIONS[languagePreferenceIndex(key)]?.label || "海外较多";
            const languagePreferencePrompt = () => {
              const key = String(state.platformSettings?.languagePreference || "overseas_more");
              if (key === "chinese_main") return "陌生用户以简体中文为主，但仍自然混入繁体中文、日韩英语和东南亚语言。";
              if (key === "chinese_more") return "陌生用户中文比例较高，但不要清一色简中；繁中、日韩、英语和东南亚语言仍应持续出现。";
              if (key === "balanced") return "陌生用户语言全球均衡分布：简中、繁中、韩语、日语、英语、东南亚及其他语言都自然出现。";
              if (key === "overseas_main") return "陌生用户明显以海外语言为主；简体中文只占很少一部分，繁中、韩语、日语、英语和东南亚语言占大多数。";
              return "陌生用户默认海外较多：简体中文偏少；繁中（台湾/香港）、韩语、日语、英语、东南亚及其他语言明显更多。同一批不要全来自同一国家。";
            };
            const translationRulePrompt = () => "只要原文不是简体中文，就提供自然的简体中文翻译；繁体中文（台湾/香港）也必须翻译成简体中文。原文已经是简体中文时 translation 留空。";
            const localeNeedsTranslation = locale => {
              const value = String(locale || "").toLowerCase();
              if (!value) return false;
              return !["zh-cn","zh_cn","zh-hans","zh_hans","zh","cn"].includes(value);
            };
            const textNeedsSimplifiedTranslation = (text, locale = "") => {
              if (localeNeedsTranslation(locale)) return true;
              const value = String(text || "").trim();
              if (!value) return false;
              if (/[ぁ-んァ-ヶ가-힣]/.test(value)) return true;
              if (/[體臺灣與為這個們來時說話開關點後裡讓從還嗎會學氣風門間長歡樂愛見麼沒發現過種樣應實際寫讀買賣]/.test(value)) return true;
              const latin = (value.match(/[A-Za-z]/g) || []).length;
              const han = (value.match(/[一-龥]/g) || []).length;
              return latin >= 3 && latin > han;
            };
            // v0.2.12: generated strangers/fans/business viewers all share one fixed avatar.
            state.messages.forEach(msg => { if (msg?.external || msg?.strangerKind) msg.avatar = STRANGER_AVATAR; });
            state.recommendedLives.forEach(live => {
              if (live?.external || !live?.ownerId) live.avatar = STRANGER_AVATAR;
              live.chatSeed = safeArray(live.chatSeed).map(line => ({ ...line, avatar:STRANGER_AVATAR }));
            });
            Object.entries(state.dmThreads || {}).forEach(([messageId, rows]) => {
              const msg = state.messages.find(item => String(item?.id || "") === String(messageId));
              if (!msg?.external && !msg?.strangerKind) return;
              state.dmThreads[messageId] = safeArray(rows).map(item => item?.sender === "other" ? { ...item, avatar:STRANGER_AVATAR, locale:item.locale || msg.locale || "", needsTranslation:Boolean(item.needsTranslation || (!item.translation && textNeedsSimplifiedTranslation(item.text, item.locale || msg.locale))) } : item);
            });
            Object.keys(state.communityChats || {}).forEach(key => {
              state.communityChats[key] = safeArray(state.communityChats[key]).map(item => item?.kind === "text" && item?.role === "member" && item?.sender !== "user" ? { ...item, avatar:STRANGER_AVATAR } : item);
            });
            Object.keys(state.communityChatQueues || {}).forEach(key => {
              state.communityChatQueues[key] = safeArray(state.communityChatQueues[key]).map(item => item?.role === "member" ? { ...item, avatar:STRANGER_AVATAR } : item);
            });
            const knownAccountAvatars = new Set([...safeArray(state.identities), ...safeArray(state.channels)].map(item => String(item?.avatar || "")).filter(Boolean));
            Object.keys(state.postReplies || {}).forEach(postId => {
              state.postReplies[postId] = safeArray(state.postReplies[postId]).map(reply => {
                if (reply?.isUser || knownAccountAvatars.has(String(reply?.avatar || ""))) return reply;
                return { ...reply, avatar:STRANGER_AVATAR };
              });
            });
            Object.keys(state.liveChatsById || {}).forEach(key => {
              state.liveChatsById[key] = safeArray(state.liveChatsById[key]).map(line => (!line?.isUser && String(line?.user || "") !== "系统") ? { ...line, avatar:STRANGER_AVATAR } : line);
            });
            state.liveChat = safeArray(state.liveChat).map(line => (!line?.isUser && String(line?.user || "") !== "系统") ? { ...line, avatar:STRANGER_AVATAR } : line);
            const ensureMellowBusinessDemo = () => {
              const id = "biz-mellow";
              const preview = "你好，我们是 Mellow Studio，做生活方式和香氛产品。最近有一个直播合作方向想先问问你有没有兴趣了解？";
              let msg = state.messages.find(item => String(item?.id || "") === id) || null;
              if (!msg) {
                msg = {
                  id, avatar:STRANGER_AVATAR, name:"Mellow Studio", handle:"@mellow_studio", badge:"商务联系",
                  preview, unread:1, external:true, strangerKind:"business", locale:"zh-CN",
                  businessProfile:"生活方式与香氛品牌；产品方向为居家香氛、空间气味与桌面生活方式。"
                };
                state.messages.unshift(msg);
              } else {
                msg.avatar = STRANGER_AVATAR;
                msg.name = "Mellow Studio";
                msg.handle = "@mellow_studio";
                msg.badge = "商务联系";
                msg.external = true;
                msg.strangerKind = "business";
                msg.locale = msg.locale || "zh-CN";
                msg.businessProfile = msg.businessProfile || "生活方式与香氛品牌；产品方向为居家香氛、空间气味与桌面生活方式。";
                if (!String(msg.preview || "").trim()) msg.preview = preview;
              }
              state.dmThreads[id] = safeArray(state.dmThreads[id]).filter(item => item && typeof item === "object" && (!String(item?.id || "").startsWith("biz-card-demo") || String(item?.id || "") === "biz-card-demo-0212"));
              if (!state.dmThreads[id].some(item => String(item?.id || "") === "biz-intro")) {
                state.dmThreads[id].unshift({ id:"biz-intro", kind:"text", sender:"other", name:"Mellow Studio", avatar:STRANGER_AVATAR, text:preview, translation:"", locale:"zh-CN", at:Date.now()-180000 });
              }
              if (!state.dmThreads[id].some(item => String(item?.id || "") === "biz-card-demo-0212")) {
                const start = Date.now() + 26 * 60 * 60 * 1000;
                const end = start + 90 * 60 * 1000;
                const card = {
                  id:"biz-card-demo-0212", kind:"business", direction:"incoming", status:"pending", sender:"other",
                  name:"Mellow Studio", avatar:STRANGER_AVATAR, brandName:"Mellow Studio", productName:"Cloud Mist 空间香氛", productPriceCNY:299,
                  title:"Mellow Studio · 直播合作", content:"直播中自然体验产品，介绍香调与使用场景，并完成一次口播和置顶链接展示。", deliverables:"直播中自然体验产品，介绍香调与使用场景，并完成一次口播和置顶链接展示。",
                  totalFeeCNY:3200, cashAmountCNY:3200, depositCNY:800, balanceCNY:2400,
                  scheduledAt:start, endsAt:end, dateText:formatBusinessWindow(start,end), includesLive:true, mode:"online",
                  hostOwnerType:"identity", hostOwnerId:String(state.viewerIdentityId || state.identities[0]?.id || ""), hostLabel:"你的直播间",
                  adCopy:"Mellow Studio Cloud Mist · 给日常空间留一点舒服的气味。", adLink:"https://mellow.example/cloud-mist",
                  at:Date.now()-60000
                };
                state.dmThreads[id].push(card);
                msg.preview = "[商务合作方案]";
              }
              state.dmThreads[id] = state.dmThreads[id].slice(-240);
              return msg;
            };
            const inferChannelLocale = channel => {
              const persona = typeof getChannelPersonaText === "function" ? getChannelPersonaText(channel) : "";
              const source = [channel?.name, channel?.handle, channel?.contentStyle, channel?.fanProfile, channel?.liveSchedule, persona].filter(Boolean).join(" ");
              if (/[가-힣]|韩国|韓國|한국|서울|Seoul/i.test(source)) return "ko";
              if (/[ぁ-んァ-ン]|日本|東京|Tokyo|大阪|Osaka/i.test(source)) return "ja";
              if (/台湾|臺灣|香港|繁體|台北|Taipei|Hong Kong/i.test(source)) return "zh-Hant";
              if (/简体|簡體|中国大陆|中國大陸|北京|上海|中文/i.test(source)) return "zh-CN";
              return pickOne(["ko","ko","ja","en","zh"]) || "ko";
            };
            const liveLocalePack = locale => {
              const packs = {
                ko: {
                  titles:["오늘은 그냥 같이 얘기해요","조금 늦게까지 있을게요","작업하면서 천천히 얘기 중"],
                  category:"Seoul · 일상 토크", translation:"首尔 · 日常闲聊",
                  chat:[
                    { user:"yuna.zip", avatar:STRANGER_AVATAR, text:"오늘 분위기 진짜 좋다", translation:"今天这个氛围真的很好。" },
                    { user:"mii_02", avatar:STRANGER_AVATAR, text:"방금 왔는데 무슨 얘기 중이었어요?", translation:"刚进来，刚才在聊什么呀？" },
                    { user:"sora", avatar:STRANGER_AVATAR, text:"주말에도 방송 켜줘요 ㅠ", translation:"周末也开播吧呜呜。" }
                  ]
                },
                ja: {
                  titles:["少しだけ雑談します","作業しながらゆっくり","今日は静かめに配信"],
                  category:"Tokyo · 雑談", translation:"东京 · 闲聊",
                  chat:[
                    { user:"mio", avatar:STRANGER_AVATAR, text:"今日の雰囲気すごく好き", translation:"今天的氛围特别喜欢。" },
                    { user:"haru_5", avatar:STRANGER_AVATAR, text:"今来た〜何してたの？", translation:"刚进来～刚才在做什么？" },
                    { user:"rinrin", avatar:STRANGER_AVATAR, text:"この時間の配信ありがたい", translation:"这个时间开播太好了。" }
                  ]
                },
                en: {
                  titles:["just hanging out for a bit","late night catch-up","working and chatting"],
                  category:"Global · casual live", translation:"全球 · 日常直播",
                  chat:[
                    { user:"mika", avatar:STRANGER_AVATAR, text:"this is such a nice background stream", translation:"这场直播特别适合当背景听。" },
                    { user:"noah", avatar:STRANGER_AVATAR, text:"just got here, what did I miss?", translation:"刚进来，我错过什么了？" },
                    { user:"jules", avatar:STRANGER_AVATAR, text:"the vibe tonight is so calm", translation:"今晚的氛围好安静。" }
                  ]
                },
                zh: {
                  titles:["随便聊一会儿","边做事边播","今天慢慢聊"],
                  category:"日常聊天", translation:"",
                  chat:[
                    { user:"momo", avatar:STRANGER_AVATAR, text:"刚进来，今天好安静哈哈", translation:"" },
                    { user:"小羽", avatar:STRANGER_AVATAR, text:"这个时间开播刚刚好", translation:"" },
                    { user:"aki", avatar:STRANGER_AVATAR, text:"背景音好舒服", translation:"" }
                  ]
                }
              };
              return packs[locale] || packs.ko;
            };
            const nsfwPresetText = () => String(state.generationPreset?.nsfwText || "").trim();
            const nsfwPresetPrompt = active => active && nsfwPresetText() ? `NSFW / 18+ 自定义风格预设：${nsfwPresetText()}` : "";

            const normalizeRecommendedAIItems = (data, count, allowedAliases = []) => {
              const arr = Array.isArray(data) ? data : [];
              const aliasIds = new Set(allowedAliases.map(item => String(item.id)));
              const usedHandles = new Set(safeArray(state.recommendedLives).map(item => String(item?.handle || "").toLowerCase()).filter(Boolean));
              const out = [];
              for (const raw of arr) {
                if (!raw || typeof raw !== "object" || out.length >= count) continue;
                const source = String(raw.source || "stranger") === "alias" ? "alias" : "stranger";
                const ownerId = source === "alias" && aliasIds.has(String(raw.ownerId || "")) ? String(raw.ownerId) : "";
                if (source === "alias" && !ownerId) continue;
                const alias = ownerId ? allowedAliases.find(item => String(item.id) === ownerId) : null;
                const name = String(alias?.name || raw.name || "").trim();
                const handle = normalizeHandle(alias?.handle || raw.handle || "");
                if (!name || !handle || usedHandles.has(handle.toLowerCase())) continue;
                const title = String(raw.title || "").trim();
                const category = String(raw.category || "").trim();
                if (!title || !category) continue;
                usedHandles.add(handle.toLowerCase());
                const locale = String(raw.locale || "").trim() || detectLiveLanguage({ title, category });
                const at = Date.now() - Math.max(3, Math.min(120, Number(raw.startedMinutesAgo || 20))) * 60 * 1000;
                const chatSeed = safeArray(raw.chatSeed).slice(0, 6).map((item, index) => ({
                  user:String(item?.user || "").trim(),
                  avatar:STRANGER_AVATAR,
                  text:String(item?.text || "").trim(),
                  translation:String(item?.translation || "").trim(),
                  at:at + index
                })).filter(item => item.user && item.text);
                out.push({
                  id:`rec-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,7)}-${out.length}`,
                  external:!alias,
                  locale,
                  name,
                  handle,
                  avatar:String(alias?.avatar || STRANGER_AVATAR),
                  ownerId:alias?.id || "",
                  sourceType:alias ? "role-alias" : "stranger",
                  title,
                  titleTranslation:String(raw.titleTranslation || "").trim(),
                  category,
                  categoryTranslation:String(raw.categoryTranslation || "").trim(),
                  creatorProfile:String(raw.creatorProfile || alias?.contentStyle || "").trim(),
                  nsfw:Boolean(raw.nsfw === true || safeArray(raw.tags).some(tag => String(tag).toUpperCase() === "NSFW")),
                  ageRequirement:String(raw.ageRequirement || "none") === "18+" ? "18+" : "none",
                  accessMode:["public","free","paid"].includes(String(raw.accessMode || "")) ? String(raw.accessMode) : "public",
                  viewers:Math.max(1, Math.round(Number(raw.viewers || 0) || (80 + Math.random()*2400))),
                  live:true,
                  startedAt:at,
                  at,
                  openingAction:String(raw.openingAction || "").trim(),
                  openingSpeech:String(raw.openingSpeech || "").trim(),
                  openingTranslation:String(raw.openingTranslation || "").trim(),
                  chatSeed
                });
              }
              return out.slice(0, count);
            };
            const scheduleAllowsLiveNow = channel => {
              const text = String(channel?.liveSchedule || "").trim();
              const d = new Date();
              const day = d.getDay();
              const hour = d.getHours() + d.getMinutes()/60;
              if (!text) return Math.random() < .26;
              if (/周末|週末/.test(text) && day >= 1 && day <= 5 && !/工作日|平日/.test(text)) return false;
              const dayNames = [[0,"周日"],[1,"周一"],[2,"周二"],[3,"周三"],[4,"周四"],[5,"周五"],[6,"周六"]];
              const mentionedDays = dayNames.filter(([,name]) => text.includes(name)).map(([n]) => n);
              if (mentionedDays.length && !mentionedDays.includes(day) && !(/工作日/.test(text) && day >= 1 && day <= 5)) return false;
              const times = [...text.matchAll(/(\d{1,2}):(\d{2})/g)].map(m => Number(m[1]) + Number(m[2])/60).filter(Number.isFinite);
              if (times.length >= 2) {
                const a = times[0], b = times[1];
                return a <= b ? hour >= a && hour <= b : (hour >= a || hour <= b);
              }
              if (times.length === 1) {
                const delta = Math.min(Math.abs(hour-times[0]), 24-Math.abs(hour-times[0]));
                return delta <= 2.5;
              }
              return /深夜|晚上|夜/.test(text) ? (hour >= 20 || hour < 2) : true;
            };
            const normalizeHomeAIItems = (data, candidates) => {
              const arr = Array.isArray(data) ? data : [];
              const validIds = new Set(candidates.map(ch => String(ch.id)));
              return arr.filter(item => item && validIds.has(String(item.channelId || "")) && ["post","live"].includes(String(item.type || ""))).slice(0, Math.max(1, candidates.length));
            };
            const refreshHomeFeedContent = async () => {
              if (generationBusy.home) return;
              const ids = [...followedChannelIds()];
              const candidates = ids.map(id => state.channels.find(ch => String(ch.id) === String(id))).filter(ch => ch && !ch.isDemo);
              if (!candidates.length) { toast("先用任意账号关注至少一个角色频道"); return; }
              setRefreshBusy("home", true);
              try {
                const now = new Date();
                const summaries = candidates.map(channel => ({
                  channelId:String(channel.id), publicName:channel.name || channel.handle || "Vela Channel", handle:channel.handle || "@vela",
                  accountRole:channel.accountRole || "primary", linkedToMain:Boolean(channel.linkedToMain), mainChannelId:String(channel.mainChannelId || ""),
                  faceMode:channel.faceMode || "mixed", identityVisibility:channel.identityVisibility || "partial", contentStyle:channel.contentStyle || "", fanProfile:channel.fanProfile || "",
                  liveSchedule:channel.liveSchedule || "", liveAutonomy:channel.liveAutonomy || "flexible", subscriptionEnabled:subscriptionEnabled("channel", channel.id), persona:String((typeof getChannelPersonaText === "function" ? getChannelPersonaText(channel) : "") || "").slice(0,1500)
                }));
                const custom = state.generationPreset?.mode === "custom" ? String(state.generationPreset?.customText || "").trim() : "";
                const system = `${VELA_DEFAULT_PRESET}

你正在给 Vela 聚合首页生成“已经被 USER 任一大号/副号关注的角色频道”的新活动。只生成角色频道自己的贴文或当前直播，不得替 USER 发任何内容。一次可以只出现 1~2 个角色，也可以多个，当前没有合适活动时允许返回空数组，不要为了热闹强行让所有角色同时营业。

生成前检查账号风格、露脸方式、主副号公开关联关系和直播习惯。linkedToMain=true 的关联副号可以在自然情况下与主号出现内容延续；未公开关联的小号要当作独立账号，不要用相似内容或线索暴露关系。看得到什么才能讨论什么：不露脸的账号不要让正文、弹幕或评论无依据讨论脸。默认只使用当前 Vela 账号公开网名/频道名/handle；除非账号风格明确说明本名已公开，否则不要让任何外部人知道本名。

直播必须符合当前星期和时间以及 liveSchedule；明显不在习惯直播时间时不要硬生成直播，可以生成贴文或什么都不生成。贴文发布时间可以是今天、昨天或更早的合理时间；直播应是当前仍在进行的近期直播。不同主副号不要机械地同一时间发完全相似内容。
如果账号风格/直播风格明确说明会做面向订阅用户的 18+ 成熟向内容，可以自然地生成这种活动，但不是每次都必须生成。只有 subscriptionEnabled=true 的频道才能输出 accessMode=free 或 paid；否则必须 public。ageRequirement=18+ 只允许搭配 free/paid，public 必须 none。对于你决定标记为 18+ 的条目，应用这段风格预设：${nsfwPresetText() || "保持成熟向但不露骨"}。
角色频道自己的正文、标题和说话语言必须遵循角色账号设定，不受陌生人语言倾向覆盖；只有 chatSeed 里的陌生观众受平台语言倾向影响：${languagePreferencePrompt()}
${translationRulePrompt()}

返回严格 JSON 数组，最多 ${Math.max(1, Math.min(4,candidates.length))} 条。字段：
[{"channelId":"必须从候选 channelId 中选","type":"post|live","publishedAt":"ISO时间","accessMode":"public|free|paid","ageRequirement":"none|18+","text":"贴文正文，live 可空","translation":"按翻译规则填写","title":"直播标题，post 可空","titleTranslation":"按翻译规则填写","category":"直播内容/分类","categoryTranslation":"按翻译规则填写","chatSeed":[{"user":"观众网名","text":"原语言弹幕","translation":"按翻译规则填写"}]}]
直播的 chatSeed 给 2~4 条已有弹幕，让直播间不是空的；语言和昵称按角色地区、人设、粉丝画像自然分布。${custom ? `
用户自定义平台规则：${custom}` : ""}`;
                const aiText = await runVelaAI([{ role:"system", content:system }, { role:"user", content:`当前时间：${now.toString()}
候选频道：${JSON.stringify(summaries)}` }]);
                const parsedHome = extractJSON(aiText);
                if (!Array.isArray(parsedHome)) { toast(`首页生成失败 · ${String(aiText || "").trim() ? "回复格式解析失败" : shortVelaAIError("AI 返回为空")}`); return; }
                const items = normalizeHomeAIItems(parsedHome, candidates);
                let added = 0;
                items.forEach((item,index) => {
                  const channel = candidates.find(ch => String(ch.id) === String(item.channelId));
                  if (!channel) return;
                  let at = Date.parse(String(item.publishedAt || ""));
                  if (!Number.isFinite(at)) at = Date.now() - Math.floor(Math.random()*36*60*60*1000);
                  at = Math.min(Date.now(), Math.max(Date.now()-14*86400000, at));
                  if (String(item.type) === "live") {
                    if (channel.live || !scheduleAllowsLiveNow(channel)) return;
                    channel.live = true;
                    channel.title = String(item.title || "正在直播");
                    channel.titleTranslation = String(item.titleTranslation || "");
                    channel.category = String(item.category || channel.contentStyle || "直播进行中");
                    channel.categoryTranslation = String(item.categoryTranslation || "");
                    channel.startedAt = Math.max(Date.now()-2*60*60*1000, at);
                    channel.viewers = Math.max(8, Math.round((Number(channel.followers || 0) || 500) * (.02 + Math.random()*.05)));
                    const requestedMode = ["free","paid"].includes(String(item.accessMode || "")) && subscriptionEnabled("channel", channel.id) ? String(item.accessMode) : "public";
                    channel.accessMode = requestedMode;
                    channel.ageRequirement = requestedMode !== "public" && String(item.ageRequirement || "") === "18+" ? "18+" : "none";
                    channel.chatSeed = safeArray(item.chatSeed).map(x => ({ user:String(x?.user || "").trim(), avatar:STRANGER_AVATAR, text:String(x?.text || "").trim(), translation:String(x?.translation || "") })).filter(x => x.user && x.text);
                    added += 1;
                    return;
                  }
                  const id = `home-ai-${String(channel.id)}-${at.toString(36)}-${index}-${Math.random().toString(36).slice(2,5)}`;
                  const postText = String(item.text || "").trim(); if (!postText) return;
                  state.communityPosts.push({ id, ownerType:"channel", ownerId:String(channel.id), text:postText, translation:String(item.translation || ""), image:"", time:formatRelativeAt(at), likes:Math.max(0,Math.round((Number(channel.followers||0)||120)*(.02+Math.random()*.08))), accessMode:(["free","paid"].includes(String(item.accessMode || "")) && subscriptionEnabled("channel", channel.id) ? String(item.accessMode) : "public"), ageRequirement:(["free","paid"].includes(String(item.accessMode || "")) && subscriptionEnabled("channel", channel.id) && String(item.ageRequirement || "") === "18+" ? "18+" : "none"), at, generatedByHome:true });
                  state.postReplies[id] = safeArray(state.postReplies[id]);
                  added += 1;
                });
                maybeSilentCharacterFollowAlias();
                await persist();
                renderChannels();
                renderHome();
                toast(added ? `首页已生成 ${added} 条新活动` : "这次没有适合的新动态");
              } finally {
                setRefreshBusy("home", false);
              }
            };
            const recommendationTopicPrompt = topics => safeArray(topics).map(topic => {
              if (topic === "NSFW") return "NSFW=成熟向/深夜/暧昧或成人生活话题氛围，但不得包含色情、露骨性内容或明确性行为描写";
              return topic;
            }).join("、");
            const refreshRecommendedLives = async (topics = []) => {
              if (generationBusy.recommended) return;
              const selectedTopics = safeArray(topics).filter(topic => RECOMMENDATION_TOPICS.includes(topic) && topic !== "随便看看");
              setRefreshBusy("recommended", true);
              try {
                const count = Math.max(1, Math.min(6, Number(state.platformSettings?.recommendationCount || 3)));
                const aliasEnabled = state.platformSettings?.allowRoleAliasesInRecommendations === true;
                const aliasBatch = aliasEnabled && Math.random() < 0.22;
                const aliasCandidates = aliasBatch ? state.channels.filter(ch => !ch?.isDemo && String(ch?.accountRole || "primary") === "alias").sort(() => Math.random()-.5).slice(0, 4) : [];
                const aliasSummary = aliasCandidates.map(ch => ({ ownerId:String(ch.id), publicName:ch.name || ch.handle || "Vela Channel", handle:ch.handle || "", contentStyle:ch.contentStyle || "", fanProfile:ch.fanProfile || "", faceMode:ch.faceMode || "mixed", identityVisibility:ch.identityVisibility || "partial", persona:String(getChannelPersonaText(ch) || "").slice(0,1000) }));
                const custom = state.generationPreset?.mode === "custom" ? String(state.generationPreset?.customText || "").trim() : "";
                const nsfwBatch = selectedTopics.includes("NSFW");
                const topicRule = selectedTopics.length ? `本批用户主动选择的偏好标签：${recommendationTopicPrompt(selectedTopics)}。优先围绕这些方向，但 ${count} 个直播仍要彼此明显不同，不要做成同一个模板。` : "本批没有选择标签，完全随机探索不同题材。";
                const system = `${VELA_DEFAULT_PRESET}

你正在生成 Vela 推荐页的正在直播频道。默认只推荐陌生主播，不得生成 USER 自己的账号，也不得生成任何角色主号。${aliasCandidates.length ? "本轮低概率开放角色副号候选；最多允许 1 条 source=alias，并且 ownerId 必须来自提供的副号候选。其余必须是陌生主播。" : "本轮完全禁止角色账号，全部必须是陌生主播。"}
${topicRule}
${nsfwPresetPrompt(nsfwBatch)}
如果本批选择了 NSFW，请把符合该方向的直播设为 nsfw=true；NSFW 在 Vela 中表示成熟向、成人主播/成人社交平台氛围，可以更大胆、暧昧、性感但不露骨。
陌生人语言规则：${languagePreferencePrompt()}。source=stranger 的主播和陌生观众按这个权重；source=alias 的角色副号本身必须遵循候选角色账号的人设与语言，不得被这个设置改成别的语言，但聊天室里的陌生观众仍可按平台权重自然混合。

陌生主播要像真实平台用户：姓名/网名、handle、地区语言、直播标题、分类、当前动作和已存在聊天室都要彼此一致。不要三个主播都用同一种说话节奏；允许有人安静做自己的事、有人和熟客闲聊、有人刚好在吃东西或工作。聊天室不是 NPC 队列：观众不需要每条都回应主播，允许观众之间接话、跑题、只发 emoji、只发一两个词、有人潜水，不要求每句都有信息量。不要使用固定模板或重复老名字。
${translationRulePrompt()}
返回严格 JSON 数组，恰好 ${count} 条。每条字段：
[{"source":"stranger|alias","ownerId":"alias 时填候选 id，否则空","name":"公开昵称","handle":"@handle","locale":"zh-CN|zh-TW|zh-HK|ko|ja|en|th|vi|id|其他合理语言","creatorProfile":"一句主播风格/地区提示","nsfw":false,"title":"原语言直播标题","titleTranslation":"按翻译规则填写","category":"原语言直播内容/分类","categoryTranslation":"按翻译规则填写","viewers":1234,"startedMinutesAgo":20,"openingAction":"镜头里正在发生的动作","openingSpeech":"主播正在说的话，符合 locale","openingTranslation":"按翻译规则填写","chatSeed":[{"user":"观众网名","text":"原语言弹幕","translation":"按翻译规则填写"}]}]
chatSeed 每场 2~5 条；不同直播不要共用同一批观众名。不要凭空泄露角色本名或后台身份。${custom ? `
用户自定义平台规则：${custom}` : ""}`;
                const aiText = await runVelaAI([{ role:"system", content:system }, { role:"user", content:`当前时间：${new Date().toString()}
允许的角色副号候选：${JSON.stringify(aliasSummary)}` }]);
                const parsedRecommended = extractJSON(aiText);
                const recommendedRows = Array.isArray(parsedRecommended)
                  ? parsedRecommended
                  : safeArray(parsedRecommended?.items || parsedRecommended?.lives || parsedRecommended?.recommendations || parsedRecommended?.data);
                const batch = normalizeRecommendedAIItems(recommendedRows, count, aliasCandidates);
                if (nsfwBatch) batch.forEach(item => { item.nsfw = true; });
                if (!batch.length) { toast(`推荐生成失败 · ${String(aiText || "").trim() ? "没有解析到可用直播" : shortVelaAIError("AI 返回为空")}`); return; }
                state.recommendedLives = [...safeArray(state.recommendedLives), ...batch];
                await persist(); renderRecommended();
                const countNote = batch.length < count ? `（本次有效 ${batch.length}/${count}）` : "";
                toast(selectedTopics.length ? `已按「${selectedTopics.join(" / ")}」新增 ${batch.length} 个推荐直播${countNote}` : `新增 ${batch.length} 个推荐直播${countNote}`);
              } finally {
                setRefreshBusy("recommended", false);
              }
            };
            const refreshDirectMessages = async () => {
              if (generationBusy.messages) return;
              setRefreshBusy("messages", true);
              try {
                // Mellow Studio is the one intentional business-demo contact. If it was removed, DM refresh restores it.
                ensureMellowBusinessDemo();
                const strangerMode = ["low","normal","high"].includes(state.platformSettings?.strangerInteraction) ? state.platformSettings.strangerInteraction : "normal";
                const count = strangerMode === "low" ? 1 : strangerMode === "high" ? 3 : 2;
                const allowBusiness = state.platformSettings?.allowBusinessDM !== false;
                const system = `${VELA_DEFAULT_PRESET}

你正在生成 Vela 私信列表中刚刚出现的陌生联系人。不得冒充 USER，也不得把已有 Roche 角色伪装成陌生人。生成 ${count} 个彼此不同的联系人。可以是普通陌生网友、粉丝或品牌商务联系人；${allowBusiness ? "允许少量商务联系人，但不要每批都有。" : "本轮禁止商务联系人。"} Mellow Studio 是平台保留的单独商务演示联系人，不要在本批重复生成它。
返回严格 JSON 数组：[{"kind":"stranger|fan|business","name":"公开昵称或品牌名","handle":"@handle","badge":"陌生私信|粉丝|商务联系","locale":"zh-CN|zh-TW|zh-HK|ko|ja|en|th|vi|id|其他合理语言","preview":"对方第一条自然私信；品牌先问合作意向，不要一上来发正式合同","translation":"按翻译规则给 preview 的简体中文翻译或空","businessProfile":"仅 business 可写公司主营/品牌风格/产品方向，否则空"}]。${languagePreferencePrompt()} ${translationRulePrompt()}`;
                const aiText = await runVelaAI([{ role:"system", content:system }, { role:"user", content:`当前时间：${new Date().toString()}` }]);
                const data = extractJSON(aiText);
                if (!Array.isArray(data)) {
                  await persist();
                  renderMessages();
                  toast(`Mellow Studio 已保留；DM 生成失败 · ${String(aiText || "").trim() ? "回复格式解析失败" : shortVelaAIError("AI 返回为空")}`);
                  return;
                }
                const existingHandles = new Set(state.messages.map(item => String(item?.handle || "").toLowerCase()).filter(Boolean));
                const batch = [];
                for (const raw of data) {
                  if (batch.length >= count || !raw || typeof raw !== "object") continue;
                  const kind = ["stranger","fan","business"].includes(String(raw.kind)) ? String(raw.kind) : "stranger";
                  if (kind === "business" && !allowBusiness) continue;
                  const name = String(raw.name || "").trim();
                  const handle = normalizeHandle(raw.handle || "");
                  const preview = String(raw.preview || "").trim();
                  if (["陌生人","商务联系","骚扰信息","粉丝"].includes(name)) continue;
                  if (/mellow/i.test(name) || handle.toLowerCase() === "@mellow_studio") continue;
                  if (!name || !handle || !preview || existingHandles.has(handle.toLowerCase())) continue;
                  existingHandles.add(handle.toLowerCase());
                  const id = `dm-${kind}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,7)}-${batch.length}`;
                  const badge = kind === "business" ? "商务联系" : kind === "fan" ? "粉丝" : "陌生私信";
                  const msg = { id, avatar:STRANGER_AVATAR, name, handle, badge, preview, previewTranslation:String(raw.translation || ""), unread:1, external:true, strangerKind:kind, locale:String(raw.locale || ""), businessProfile:String(raw.businessProfile || "") };
                  batch.push(msg);
                  state.dmThreads[id] = [{ id:`seed-${id}`, kind:"text", sender:"other", name, avatar:STRANGER_AVATAR, text:preview, translation:String(raw.translation || ""), locale:msg.locale, needsTranslation:Boolean(!raw.translation && textNeedsSimplifiedTranslation(preview, msg.locale)), at:Date.now() }];
                }
                // Keep the demo contact from being pushed out by the 50-message cap.
                const mellow = state.messages.find(item => String(item?.id || "") === "biz-mellow");
                const others = state.messages.filter(item => String(item?.id || "") !== "biz-mellow");
                state.messages = [...batch, ...(mellow ? [mellow] : []), ...others].slice(0, 50);
                await persist();
                renderMessages();
                toast(batch.length ? `新增 ${batch.length} 条私信` : "Mellow Studio 已保留；这次没有生成新的私信");
              } finally {
                setRefreshBusy("messages", false);
              }
            };

            const renderSchedule = () => {
              const host = q('[data-role="schedule"]');
              if (!state.schedule?.visible) { host.innerHTML = ""; return; }
              const messageId = String(state.schedule.messageId || "");
              host.innerHTML = `<div class="v-schedule"><div class="v-schedule-top"><div><div class="v-kicker">即将开始 · 已加入日程</div><div class="v-schedule-time">${escapeHTML(state.schedule.dateText)}</div></div><span class="v-pill">${escapeHTML(state.schedule.type || "直播")}</span></div><div class="v-schedule-main"><div class="v-schedule-icon">M</div><div class="v-schedule-copy"><b>${escapeHTML(state.schedule.title)}</b><span>${escapeHTML(state.schedule.detail)}</span></div>${messageId ? `<button class="v-small-dark" data-action="open-message" data-message-id="${escapeHTML(messageId)}">查看任务</button>` : ""}</div></div>`;
            };

            const translationHTML = (id, text) => text ? `<button class="v-translate-btn" data-action="toggle-translation" data-translation-target="${escapeHTML(id)}">翻译</button><div class="v-translation" data-translation-id="${escapeHTML(id)}" hidden>${escapeHTML(text)}</div>` : "";
            const postSeedNumber = (id, min, span) => { let h=0; for (const ch of String(id||"post")) h=(h*31+ch.charCodeAt(0))>>>0; return min+(h%span); };
            const postDisplayLikes = post => Math.max(Number(post?.likes || 0), Number(post?.displayLikes || 0), postSeedNumber(post?.id,2,19));
            const postDisplayCommentCount = post => Math.max(safeArray(state.postReplies?.[String(post?.id||"")]).length, Number(post?.displayCommentCount || post?.comments || 0), postSeedNumber(post?.id,2,11));
            const postHeartIconHTML = () => `<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M20.8 4.7c-2-2-5.3-2-7.3 0L12 6.2l-1.5-1.5c-2-2-5.3-2-7.3 0s-2 5.3 0 7.3L12 20.8 20.8 12c2-2 2-5.3 0-7.3Z" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"/></svg>`;
            const postCommentIconHTML = () => `<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M20.5 11.5a8.5 8.5 0 1 1-3-6.5 8.2 8.2 0 0 1 3 6.5Z" fill="none" stroke="currentColor" stroke-width="1.7"/><path d="M7.2 18.4 5.5 21l4-1" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"/></svg>`;

            const renderSocialPostCard = (post, { compact = false, canDelete = false } = {}) => {
              post = { ...post, ...(state.postOverrides?.[String(post?.id || "")] || {}) };
              const id = String(post.id || "post");
              const imageHTML = isImageURL(post.image || "") ? `<button class="v-social-media" data-action="open-post-by-id" data-post-id="${escapeHTML(id)}"><img src="${escapeHTML(post.image)}" alt=""></button>` : "";
              const trId = `post-card-${id}`;
              const mode = String(post.accessMode || "public");
              const age = String(post.ageRequirement || "none");
              const accessLabel = mode === "free" ? `免费订阅${age === "18+" ? " · 18+" : ""}` : mode === "paid" ? `付费订阅${age === "18+" ? " · 18+" : ""}` : (age === "18+" ? "18+" : "");
              const accessClass = mode === "free" ? " is-free" : mode === "paid" ? " is-paid" : (age === "18+" ? " is-age" : "");
              const channelId = String(post.ownerType||"") === "channel" ? String(post.ownerId||"") : "";
              let eligible = channelId ? identitiesWithAccessToChannel(channelId, mode) : state.identities.slice();
              if (!eligible.length) eligible = state.identities.filter(x => String(x.id) === String(state.viewerIdentityId));
              const selected = eligible.find(x => String(x.id) === String(state.postInteractionIdentity?.[id]||"")) || eligible[0];
              if (selected) state.postInteractionIdentity[id] = String(selected.id);
              const picker = identityPickerHTML(eligible, selected?.id, "post-identity-select", `data-post-id="${escapeHTML(id)}"`);
              const menu = `<div class="v-post-menu" data-post-menu="${escapeHTML(id)}"><button data-action="edit-post" data-post-id="${escapeHTML(id)}">编辑</button><button class="is-delete" data-action="delete-any-post" data-post-id="${escapeHTML(id)}">删除本条贴文</button></div>`;
              const badge = accessLabel ? `<span class="v-access-badge${accessClass}">${escapeHTML(accessLabel)}</span>` : "";
              return `<article class="v-card v-home-card v-post-card" data-home-post-card="${escapeHTML(id)}"><div class="v-social-head v-post-head"><div class="v-social-avatar">${avatarHTML(post.avatar || "", post.name || "V")}</div><div class="v-social-copy"><div class="v-post-author-line"><strong>${escapeHTML(post.name || "Vela Channel")}</strong><span class="v-post-author-handle">${escapeHTML(post.handle || "")}</span>${badge}</div><div class="v-post-time">${escapeHTML(post.at ? formatRelativeAt(post.at) : (post.time || "刚刚"))}</div></div><button class="v-post-more" data-action="toggle-post-menu" data-post-id="${escapeHTML(id)}">⋯</button>${menu}</div><div class="v-social-text">${escapeHTML(post.text || "")}</div>${translationHTML(trId, post.translation || "")}${imageHTML}<div class="v-social-actions v-post-engagement"><button data-action="like-post" data-post-id="${escapeHTML(id)}">${postHeartIconHTML()}<span>${postDisplayLikes(post)}</span></button><button data-action="toggle-inline-comments" data-post-id="${escapeHTML(id)}">${postCommentIconHTML()}<span data-inline-count="${escapeHTML(id)}">${postDisplayCommentCount(post)}</span></button></div><div class="v-inline-comments" data-inline-comments="${escapeHTML(id)}"><div class="v-comment-list" data-inline-comment-list="${escapeHTML(id)}"></div><button class="v-more-comments" data-action="summon-post-replies" data-post-id="${escapeHTML(id)}" hidden>再加载 5 条</button><div class="v-post-composer">${picker}<input data-role="inline-post-comment-input" data-post-id="${escapeHTML(id)}" placeholder="留下评论… · Enter发送" maxlength="240"><button class="v-summon-mini" data-action="summon-post-replies" data-post-id="${escapeHTML(id)}">${planeIconHTML()}</button></div></div></article>`;
            };

            const renderHome = () => {
              const host = q('[data-role="home-feed"]');
              const filter = ["all","live","post"].includes(state.homeFilter) ? state.homeFilter : "all";
              qa('[data-home-filter]').forEach(btn => btn.classList.toggle("is-active", btn.dataset.homeFilter === filter));
              const cards = [];

              // All of the user's Vela accounts are visible in the aggregate home.
              state.identities.forEach(identity => {
                if (identity?.activeLive) {
                  const live = identity.activeLive;
                  const stats = getLiveStats(live);
                  const trId = `home-own-live-${String(live.id || identity.id)}`;
                  cards.push({ type:"live", order:Number(live.startedAt || Date.now()), html:`<article class="v-card v-home-card"><button class="v-home-card-hit" data-action="open-own-profile-live" data-identity-id="${escapeHTML(identity.id)}"><div class="v-social-head"><div class="v-social-avatar">${avatarHTML(identity.avatar || "", identity.displayName || "U")}</div><div class="v-social-copy"><div class="v-social-name">${escapeHTML(identity.displayName || "我的频道")} <span style="font-weight:600;color:var(--v-muted)">${escapeHTML(identity.handle || "")}</span></div><div class="v-home-self-label">我的账号直播 · ${live.accessMode === "paid" ? "付费订阅" : live.accessMode === "free" ? "免费订阅" : "公开直播"}${live.ageRequirement === "18+" ? " · 18+" : ""} · ${formatViewers(stats.currentViewers || 0)} 人观看</div></div></div><div class="v-stage"><div class="v-stage-copy"><span class="v-badge">LIVE</span>${live.nsfw === true ? '<span class="v-badge" style="margin-left:6px">NSFW</span>' : ""}<div class="v-live-title">${escapeHTML(live.title || "正在直播")}</div><div class="v-viewers">${escapeHTML(live.category || "直播进行中")}</div></div></div></button>${translationHTML(trId, live.titleTranslation || "")}</article>` });
                }
                state.communityPosts.filter(p => String(p.ownerType || "") === "identity" && String(p.ownerId || "") === String(identity.id)).forEach(post => {
                  const resolved = getPostById(post.id);
                  if (resolved) cards.push({ type:"post", order:Number(post.at || 0), html:renderSocialPostCard(resolved, { canDelete:String(identity.id) === String(state.viewerIdentityId || "") }) });
                });
              });

              // Merge every channel followed by any of the user's accounts. Access follows the account(s) that actually subscribed.
              [...followedChannelIds()].forEach(channelId => {
                const channel = state.channels.find(ch => String(ch.id) === String(channelId));
                if (!channel) return;
                if (channel.live) {
                  const followerIds = identitiesFollowingChannel(channel.id, channel.accessMode || "public");
                  if (followerIds.length) {
                    const liveTrId = `home-live-${String(channel.id)}`;
                    const liveTranslation = [channel.titleTranslation, channel.categoryTranslation].filter(Boolean).join("\n");
                    cards.push({ type:"live", order:Number(channel.startedAt || Date.now()), html:`<article class="v-card v-home-card"><div class="v-home-live-head"><div class="v-social-avatar">${avatarHTML(channel.avatar || "", channel.name || "V")}</div><div class="v-social-copy"><div class="v-social-name">${escapeHTML(channel.name || "频道")} <span style="font-weight:600;color:var(--v-muted)">${escapeHTML(channel.handle || "")}</span></div><div class="v-social-handle">正在直播 · ${channel.accessMode === "paid" ? "付费订阅" : channel.accessMode === "free" ? "免费订阅" : "公开直播"}${channel.ageRequirement === "18+" ? " · 18+" : ""} · ${formatViewers(channel.viewers || 0)} 人观看 · ${escapeHTML(formatRelativeAt(channel.startedAt || Date.now()))}</div></div><div class="v-home-live-actions"><button class="v-post-more" data-action="toggle-home-live-menu" data-channel-id="${escapeHTML(channel.id)}">⋯</button><div class="v-post-menu" data-home-live-menu="${escapeHTML(channel.id)}"><button data-action="edit-generated-live" data-channel-id="${escapeHTML(channel.id)}">编辑直播</button><button class="is-delete" data-action="close-generated-live" data-channel-id="${escapeHTML(channel.id)}">关闭本场直播</button></div></div></div><button class="v-home-live-stage" data-action="open-live" data-live-id="${escapeHTML(channel.id)}"><div class="v-stage"><div class="v-stage-copy"><span class="v-badge">LIVE</span><div class="v-live-title">${escapeHTML(channel.title || "正在直播")}</div><div class="v-viewers">${escapeHTML(channel.category || channel.contentStyle || "频道直播")}</div></div></div></button>${translationHTML(liveTrId, liveTranslation)}</article>` });
                  }
                }
                state.communityPosts.filter(p => String(p.ownerType || "") === "channel" && String(p.ownerId || "") === String(channel.id)).forEach(post => {
                  const mode = String(post.accessMode || "public");
                  if (!identitiesFollowingChannel(channel.id, mode).length) return;
                  const resolved = getPostById(post.id);
                  if (resolved) cards.push({ type:"post", order:Number(post.at || 0), html:renderSocialPostCard(resolved) });
                });
                const snap = state.homeFeedByChannel?.[String(channel.id)];
                if (snap && identitiesFollowingChannel(channel.id, String(snap.accessMode || "public")).length) {
                  cards.push({ type:"post", order:Number(snap.at || 0), html:renderSocialPostCard({ ...snap, name:channel.name, handle:channel.handle, avatar:channel.avatar, comments:safeArray(state.postReplies?.[String(snap.id)]).length }) });
                }
              });

              // No default role/post placeholder: the home only shows real persisted content.
              const visible = (filter === "all" ? cards : cards.filter(item => item.type === filter)).sort((a,b) => Number(b.order || 0) - Number(a.order || 0));
              host.innerHTML = visible.map(item => item.html).join("") || `<div class="v-profile-empty">这里暂时没有内容。</div>`;
            };

            const openRecommendationTopics = () => {
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              if (!sheet || !host) return;
              host.innerHTML = `<div class="v-publish-sheet-title"><h3>想看什么</h3><button data-action="close-publish-sheet">×</button></div><div class="v-rec-topic-note">选一个或多个标签，只影响这一次推荐刷新。直接点推荐页刷新按钮仍然是随机。</div><div class="v-rec-topic-grid">${RECOMMENDATION_TOPICS.map(topic => `<button class="v-rec-topic-chip ${topic === "随便看看" ? "is-active" : ""}" data-action="toggle-recommendation-topic" data-topic="${escapeHTML(topic)}">${escapeHTML(topic)}</button>`).join("")}</div><div class="v-publish-sheet-actions"><button class="v-action light" data-action="close-publish-sheet">取消</button><button class="v-action" data-action="refresh-recommended-topics">按这些刷新</button></div>`;
              sheet.classList.add("is-open");
            };
            const selectedRecommendationTopics = () => {
              const sheet = q('[data-role="publish-sheet"]');
              if (!sheet) return [];
              return [...sheet.querySelectorAll('[data-action="toggle-recommendation-topic"].is-active')].map(btn => String(btn.dataset.topic || "")).filter(topic => topic && topic !== "随便看看");
            };
            const toggleRecommendationTopic = button => {
              const sheet = q('[data-role="publish-sheet"]');
              if (!sheet || !button) return;
              const topic = String(button.dataset.topic || "");
              const chips = [...sheet.querySelectorAll('[data-action="toggle-recommendation-topic"]')];
              if (topic === "随便看看") { chips.forEach(chip => chip.classList.toggle("is-active", chip === button)); return; }
              chips.find(chip => String(chip.dataset.topic || "") === "随便看看")?.classList.remove("is-active");
              button.classList.toggle("is-active");
              if (!chips.some(chip => chip.classList.contains("is-active"))) chips.find(chip => String(chip.dataset.topic || "") === "随便看看")?.classList.add("is-active");
            };

            const renderRecommended = () => {
              const recs = safeArray(state.recommendedLives);
              if (!recs.length) { q('[data-role="recommended"]').innerHTML = `<div class="v-profile-empty">暂时还没有推荐直播，点右上角刷新试试。</div>`; return; }
              q('[data-role="recommended"]').innerHTML = recs.map(live => {
                const trId = `rec-live-${String(live.id || "rec")}`;
                const trText = [live.titleTranslation, live.categoryTranslation].filter(Boolean).join("\n");
                const channel = state.channels.find(item => String(item.id) === String(live.ownerId || ""));
                const follow = channel ? (() => {
                  const followed = isFollowing("identity", state.viewerIdentityId, "channel", channel.id);
                  return `<button class="v-follow ${followed ? "is-on" : ""}" data-action="follow-rec" data-rec-id="${escapeHTML(live.id)}">${followed ? "已关注" : "关注"}</button>`;
                })() : "";
                return `<article class="v-card v-rec-card"><button class="v-rec-remove" data-action="remove-recommended-live" data-rec-id="${escapeHTML(live.id)}" aria-label="删除推荐">×</button><button class="v-rec-cover" data-action="open-recommended-live" data-rec-id="${escapeHTML(live.id)}" style="border:0;width:100%;text-align:left"><div><span class="v-badge">LIVE</span><div class="v-live-title">${escapeHTML(live.title || "正在直播")}</div><div class="v-viewers">${formatViewers(live.viewers)} 人正在观看 · ${escapeHTML(live.category || "Live")}</div></div></button><div class="v-rec-translation">${translationHTML(trId, trText)}</div><div class="v-rec-body"><div class="v-mini">${avatarHTML(live.avatar || "", live.name || live.handle || "V")}</div><div class="v-meta"><div class="v-name">${escapeHTML(live.handle || live.name || "@vela")}</div><div class="v-sub">${escapeHTML(live.name || "Vela Live")} · 点进去继续观看或参与</div></div>${follow}</div></article>`;
              }).join("");
            };

            const openGeneratedLiveEditor = channelId => {
              const channel = state.channels.find(item => String(item.id) === String(channelId || ""));
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              if (!channel || !sheet || !host) return;
              host.innerHTML = `<div class="v-publish-sheet-title"><h3>编辑直播</h3><button data-action="close-publish-sheet">×</button></div><div class="v-tool-form"><div class="v-field"><label>直播标题</label><input data-generated-live-field="title" maxlength="90" value="${escapeHTML(channel.title || "")}"></div><div class="v-field"><label>直播内容 / 分类</label><input data-generated-live-field="category" maxlength="120" value="${escapeHTML(channel.category || channel.contentStyle || "")}"></div><div class="v-field"><label>标题翻译（可留空）</label><input data-generated-live-field="titleTranslation" maxlength="120" value="${escapeHTML(channel.titleTranslation || "")}"></div><button class="v-action" data-action="save-generated-live" data-channel-id="${escapeHTML(channel.id)}">保存</button></div>`;
              sheet.classList.add("is-open");
            };

            const saveGeneratedLiveEdit = async channelId => {
              const channel = state.channels.find(item => String(item.id) === String(channelId || ""));
              const sheet = q('[data-role="publish-sheet"]');
              if (!channel || !sheet) return;
              const read = name => String(sheet.querySelector(`[data-generated-live-field="${name}"]`)?.value || "").trim();
              channel.title = read("title") || channel.title || "正在直播";
              channel.category = read("category") || channel.category || channel.contentStyle || "直播进行中";
              channel.titleTranslation = read("titleTranslation");
              await persist();
              sheet.classList.remove("is-open");
              renderHome(); renderChannels();
              toast("直播信息已保存");
            };

            const closeGeneratedLive = async channelId => {
              const channel = state.channels.find(item => String(item.id) === String(channelId || ""));
              if (!channel) return;
              channel.live = false;
              channel.activeLive = null;
              await persist();
              renderHome(); renderChannels();
              if (q('[data-screen="profile"].is-open')) renderProfileTab("channel", channel.id, "activity");
              toast("这场直播已关闭");
            };

            const getOwnSubscriptionProfile = () => getSubscriptionProfile("identity", state.viewerIdentityId);

            const getDMRelationshipBadge = (msg) => {
              if (/合作|商务/.test(String(msg?.badge || ""))) return { text: String(msg.badge || "商务联系"), cls: "is-business" };
              if (msg?.fanTier === "paid") return { text: `${getOwnSubscriptionProfile().paid.emoji || "◆"} 粉丝`, cls: "is-paid" };
              if (msg?.fanTier === "free") return { text: `${getOwnSubscriptionProfile().free.emoji || "◇"} 粉丝`, cls: "is-free" };
              if (msg?.relationship === "mutual") return { text: "互关", cls: "is-mutual" };
              if (msg?.outboundToChannel && msg?.channelId) {
                const channelId = String(msg.channelId);
                const mutual = state.identities.some(identity => isFollowing("identity", identity.id, "channel", channelId) && isFollowing("channel", channelId, "identity", identity.id));
                if (mutual) return { text: "互关", cls: "is-mutual" };
                const tier = getSubscriptionTierFor(state.viewerIdentityId, channelId);
                if (tier === "paid") return { text: "付费订阅", cls: "is-paid" };
                if (tier === "free") return { text: "免费订阅", cls: "is-free" };
                const followed = state.identities.some(identity => isFollowing("identity", identity.id, "channel", channelId));
                const followsYou = state.identities.some(identity => isFollowing("channel", channelId, "identity", identity.id));
                if (followsYou) return { text: "关注了你", cls: "is-mutual" };
                if (followed) return { text: "已关注", cls: "is-free" };
              }
              return { text: "陌生人", cls: "is-stranger" };
            };

            const communityChatKey = (ownerType, ownerId, tier) => `${String(ownerType)}:${String(ownerId)}:${tier === "paid" ? "paid" : "free"}`;

            const isOwnCommunity = (ownerType, ownerId) => ownerType === "identity" && String(ownerId) === String(state.viewerIdentityId);
            const canEditCommunitySettings = (ownerType, ownerId) => ownerType === "identity"
              ? state.identities.some(item => String(item.id) === String(ownerId))
              : ownerType === "channel"
                ? state.channels.some(item => String(item.id) === String(ownerId))
                : false;
            const canManageCommunity = (ownerType, ownerId) => isOwnCommunity(ownerType, ownerId);

            const getCommunitySettings = (ownerType, ownerId, tier) => {
              const key = communityChatKey(ownerType, ownerId, tier);
              const entity = getProfileEntity(ownerType, ownerId);
              const subCfg = getSubscriptionProfile(ownerType, ownerId)[tier === "paid" ? "paid" : "free"];
              const current = state.communitySettings[key] || {};
              const normalized = {
                avatar: String(current.avatar || entity?.avatar || ""),
                name: String(current.name || subCfg.communityName || `${profileName(entity, ownerType)}社群`),
                announcement: String(current.announcement || ""),
                fanName: String(current.fanName || (tier === "paid" ? "会员" : "粉丝")),
                botEnabled: Boolean(current.botEnabled),
                botName: String(current.botName || "Vela 小助手"),
                botAvatar: String(current.botAvatar || ""),
                botLeadHours: Math.max(1, Math.min(24, Number(current.botLeadHours || 3))),
                botScheduled: current.botScheduled !== false,
                botLive: current.botLive !== false,
                botLottery: current.botLottery !== false,
                fanProfile: String(current.fanProfile || entity?.fanProfile || "喜欢主播日常、直播讨论与轻松聊天。"),
                chatStyle: String(current.chatStyle || "自然闲聊、直播讨论、偶尔接梗，不要求每个人都围着主播。")
              };
              state.communitySettings[key] = normalized;
              return normalized;
            };

            const stableHash = (value) => {
              let h = 2166136261;
              const text = String(value || "");
              for (let i = 0; i < text.length; i += 1) {
                h ^= text.charCodeAt(i);
                h = Math.imul(h, 16777619);
              }
              return Math.abs(h >>> 0);
            };

            const getCommunityStats = (ownerType, ownerId, tier) => {
              const entity = getProfileEntity(ownerType, ownerId);
              const followers = Math.max(24, Number(entity?.followers || 0));
              const seed = stableHash(communityChatKey(ownerType, ownerId, tier));
              const ratio = (tier === "paid" ? 0.055 : 0.18) + (seed % 14) / 100;
              const members = Math.max(tier === "paid" ? 8 : 16, Math.round(followers * ratio));
              const activity = Math.max(4, Math.min(98, Number(state.platformSettings?.activityLevel || 50)));
              const timeSeed = Math.floor(Date.now() / 60000);
              const activeRatio = 0.018 + (activity / 100) * 0.06;
              const active = Math.max(1, Math.min(members, Math.round(members * activeRatio + ((seed + timeSeed) % 7))));
              return { members, active };
            };

            const renderMessages = () => {
              syncIncomingFanSubscriptions?.();
              const host = q('[data-role="messages"]');
              qa('[data-dm-tab]').forEach(btn => btn.classList.toggle("is-active", btn.dataset.dmTab === state.dmTab));

              if (state.dmTab === "community") {
                // One own paid fan group for the currently active Vela account.
                const ownGroups = [];
                const identity = state.identities.find(item => String(item.id) === String(state.viewerIdentityId));
                if (identity && subscriptionEnabled("identity", identity.id)) ownGroups.push({ ownerType:"identity", ownerId:identity.id, tier:"paid", entity:identity, identityIds:[identity.id] });

                // Character/other creator groups only exist for paid subscribers. Merge duplicate membership across big/small accounts.
                const joinedMap = new Map();
                state.identities.forEach(userIdentity => {
                  Object.entries(getIdentitySubscriptions(userIdentity.id)).forEach(([channelId, tier]) => {
                    if (tier !== "paid") return;
                    const ch = state.channels.find(item => String(item.id) === String(channelId));
                    if (!ch || !subscriptionEnabled("channel", ch.id)) return;
                    const key = communityChatKey("channel", ch.id, "paid");
                    const item = joinedMap.get(key) || { ownerType:"channel", ownerId:ch.id, tier:"paid", entity:ch, identityIds:[] };
                    if (!item.identityIds.includes(String(userIdentity.id))) item.identityIds.push(String(userIdentity.id));
                    joinedMap.set(key, item);
                  });
                });
                const joinedGroups = [...joinedMap.values()];

                const rowHTML = group => {
                  const cfg = getCommunitySettings(group.ownerType, group.ownerId, "paid");
                  const stats = getCommunityStats(group.ownerType, group.ownerId, "paid");
                  const followers = safeArray(group.identityIds).map(id => state.identities.find(x => String(x.id) === String(id))).filter(Boolean);
                  return `<button class="v-community-entry" data-action="open-community-chat" data-owner-type="${escapeHTML(group.ownerType)}" data-owner-id="${escapeHTML(group.ownerId)}" data-tier="paid"><div class="v-social-avatar" style="position:relative">${avatarHTML(cfg.avatar || group.entity?.avatar || "", cfg.name || "V")}</div><div class="v-meta"><div class="v-name">${escapeHTML(cfg.name)}</div><div class="v-community-owner">${stats.members.toLocaleString("zh-CN")} 人 · ${stats.active.toLocaleString("zh-CN")} 人正在聊天${group.ownerType === "channel" && followers.length ? ` · ${followers.length} 个我的身份可发言` : ""}</div></div><span class="v-community-tier-dot is-paid">会员</span></button>`;
                };

                host.innerHTML = `<div class="v-community-section-title">我的社群</div><div class="v-community-list">${ownGroups.map(rowHTML).join("") || '<div class="v-profile-empty">还没有自己的会员社群。</div>'}</div><div class="v-community-section-title">我加入的社群</div>${joinedGroups.length ? `<div class="v-community-list">${joinedGroups.map(rowHTML).join("")}</div>` : `<div class="v-profile-empty">还没有加入其他主播的会员社群。<br>付费订阅后才会解锁对应社群。</div>`}`;
                return;
              }

              host.innerHTML = `<div class="v-card">${state.messages.map(msg => {
                const rel = getDMRelationshipBadge(msg);
                return `<div class="v-msg-swipe" data-dm-row="${escapeHTML(msg.id)}" data-message-id="${escapeHTML(msg.id)}"><button type="button" class="v-msg" data-action="open-message" data-message-id="${escapeHTML(msg.id)}"><div class="v-mini">${avatarHTML(msg.avatar, msg.name)}</div><div class="v-meta"><div class="v-name">${escapeHTML(msg.name)}<span class="v-relation ${rel.cls}">${escapeHTML(rel.text)}</span></div><div class="v-preview">${escapeHTML(msg.preview || "暂无消息")}</div></div>${msg.unread ? `<span class="v-unread">${msg.unread}</span>` : ""}</button><button type="button" class="v-msg-delete" data-action="delete-dm-conversation" data-message-id="${escapeHTML(msg.id)}">删除</button></div>`;
              }).join("")}</div>`;
            };

            const COIN_CNY_RATE = 0.1; // 🔷100 = ¥10

            const detectSettlementCurrency = () => {
              const configured = String(state.wallet?.settlementCurrency || "").toUpperCase();
              if (["CNY","USD","JPY","KRW","HKD","EUR"].includes(configured)) return configured;
              const locale = String(navigator?.language || "zh-CN").toLowerCase();
              if (locale.includes("ja")) return "JPY";
              if (locale.includes("ko")) return "KRW";
              if (locale.includes("hk")) return "HKD";
              if (locale.includes("us") || locale.startsWith("en")) return "USD";
              if (locale.includes("de") || locale.includes("fr") || locale.includes("it") || locale.includes("es") || locale.includes("nl")) return "EUR";
              return "CNY";
            };

            const currencySymbol = code => ({ CNY:"¥", USD:"$", JPY:"¥", KRW:"₩", HKD:"HK$", EUR:"€" }[String(code)] || String(code));

            const walletCurrency = () => {
              const currency = detectSettlementCurrency();
              state.wallet.settlementCurrency = currency;
              return currency;
            };

            const cnyToCurrency = (cny, currency = walletCurrency()) => {
              const rate = Number(state.wallet?.fxRates?.[currency] || (currency === "CNY" ? 1 : 0));
              return currency === "CNY" ? Number(cny || 0) : Number(cny || 0) * (rate || 1);
            };

            const currencyToCNY = (value, currency = walletCurrency()) => {
              const rate = Number(state.wallet?.fxRates?.[currency] || (currency === "CNY" ? 1 : 0));
              return currency === "CNY" ? Number(value || 0) : Number(value || 0) / (rate || 1);
            };

            const formatCash = (cny, { signed = false } = {}) => {
              const currency = walletCurrency();
              const converted = cnyToCurrency(cny, currency);
              const digits = ["JPY","KRW"].includes(currency) ? 0 : 2;
              const sign = signed ? (converted > 0 ? "+" : converted < 0 ? "-" : "") : (converted < 0 ? "-" : "");
              const abs = Math.abs(converted);
              return `${sign}${currencySymbol(currency)}${abs.toLocaleString("zh-CN", { minimumFractionDigits: digits, maximumFractionDigits: digits })}`;
            };

            const formatCoin = (value, { signed = false } = {}) => {
              const n = Number(value || 0);
              const sign = signed ? (n > 0 ? "+" : n < 0 ? "-" : "") : (n < 0 ? "-" : "");
              return `${sign}🔷 ${Math.abs(Math.round(n)).toLocaleString("zh-CN")}`;
            };

            let fxLoading = false;
            const loadWalletFxRates = async ({ force = false } = {}) => {
              const stale = Date.now() - Number(state.wallet?.fxUpdatedAt || 0) > 6 * 60 * 60 * 1000;
              if (fxLoading || (!force && !stale)) return;
              fxLoading = true;
              try {
                const res = await fetch("https://api.frankfurter.app/latest?from=CNY");
                if (!res.ok) throw new Error(`FX ${res.status}`);
                const data = await res.json();
                const rates = { CNY: 1 };
                ["USD","JPY","KRW","HKD","EUR"].forEach(code => {
                  if (Number(data?.rates?.[code]) > 0) rates[code] = Number(data.rates[code]);
                });
                state.wallet.fxRates = { ...(state.wallet.fxRates || {}), ...rates };
                state.wallet.fxUpdatedAt = Date.now();
                await persist();
                if (state.activePage === "wallet") renderWallet();
              } catch (err) {
                // Offline fallback only; live values will refresh again when the endpoint is reachable.
                state.wallet.fxRates = {
                  CNY: 1,
                  USD: Number(state.wallet.fxRates?.USD || 0.149),
                  JPY: Number(state.wallet.fxRates?.JPY || 21.9),
                  KRW: Number(state.wallet.fxRates?.KRW || 207),
                  HKD: Number(state.wallet.fxRates?.HKD || 1.16),
                  EUR: Number(state.wallet.fxRates?.EUR || 0.128)
                };
              } finally {
                fxLoading = false;
              }
            };

            const walletTxHTML = tx => {
              const cash = tx.cashCNY != null ? Number(tx.cashCNY || 0) : null;
              const coin = tx.coin != null ? Number(tx.coin || 0) : null;
              const amount = coin != null ? formatCoin(coin, { signed: true }) : formatCash(cash || 0, { signed: true });
              const cls = coin != null ? " is-coin" : "";
              const dateText = tx.at ? new Date(Number(tx.at)).toLocaleDateString("zh-CN", { month:"numeric", day:"numeric" }) : "";
              const identity = state.identities.find(item => String(item.id) === String(tx.identityId || tx.ownerIdentityId || ""));
              const sourceHTML = identity ? `<span class="v-wallet-source"><span class="v-wallet-source-avatar">${avatarHTML(identity.avatar || "", identity.displayName || "U")}</span>${escapeHTML(identity.displayName || identity.handle || "账号")}</span>` : "";
              return `<div class="v-tx"><span>${escapeHTML(tx.title || "流水")}<small>${escapeHTML(tx.note || "")}${dateText ? ` · ${escapeHTML(dateText)}` : ""}</small>${sourceHTML}</span><b class="v-tx-amount${cls}">${escapeHTML(amount)}</b></div>`;
            };

            const renderWallet = () => {
              const assetHost = q('[data-role="wallet-assets"]');
              const accountHost = q('[data-role="wallet-account"]');
              const txHost = q('[data-role="transactions"]');
              if (!assetHost || !accountHost || !txHost) return;
              const currency = walletCurrency();
              const cashCNY = Number(state.wallet.cashBalanceCNY || 0);
              const coin = Math.max(0, Number(state.wallet.coinBalance || 0));
              assetHost.innerHTML = `<div class="v-wallet-assets"><section class="v-wallet-asset is-coin"><div><div class="v-wallet-asset-label">Vela Coin</div><div class="v-wallet-asset-value">🔷 ${Math.round(coin).toLocaleString("zh-CN")}</div><div class="v-wallet-asset-sub">打赏、礼物、订阅都使用 Vela Coin</div></div><div class="v-wallet-asset-actions"><button data-action="wallet-recharge-coin">充值 Coin</button><button data-action="wallet-cashout-coin">兑换提现</button></div></section><section class="v-wallet-asset is-cash"><div><div class="v-wallet-asset-label">可提现余额 · ${escapeHTML(currency)}</div><div class="v-wallet-asset-value">${escapeHTML(formatCash(cashCNY))}</div><div class="v-wallet-asset-sub">商务直接入账；订阅每周一自动结算</div></div><div class="v-wallet-asset-actions"><button data-action="wallet-withdraw">提现</button><button data-action="wallet-refresh-fx">更新汇率</button></div></section></div>`;

              const account = state.wallet.linkedAccount;
              accountHost.innerHTML = account
                ? `<div class="v-wallet-account"><div class="v-wallet-account-head"><div><div class="v-wallet-asset-label">提现账户</div><div class="v-name">${escapeHTML(account.bankName || "Vela Bank")}</div></div><button data-action="wallet-edit-account">管理</button></div><div class="v-wallet-card-no">${escapeHTML(account.first4 || "0000")} ·••• ·••• ${escapeHTML(account.last4 || "0000")}</div><div class="v-hint" style="margin-top:6px">交易 PIN 已设置 · 仅用于 Vela 模拟结算</div></div>`
                : `<button class="v-wallet-account" style="width:100%;text-align:left;color:inherit" data-action="wallet-bind-account"><div class="v-wallet-account-head"><div><div class="v-wallet-asset-label">提现账户</div><div class="v-name">绑定后才能提现 / 使用账户充值</div></div><b>＋</b></div><div class="v-hint" style="margin-top:7px">Vela 模拟账户，请勿填写真实银行卡号或真实银行密码。</div></button>`;

              txHost.innerHTML = safeArray(state.wallet.transactions).slice(0, 80).map(walletTxHTML).join("") || '<div class="v-profile-empty">还没有流水。</div>';
              loadWalletFxRates();
            };

            const hashWalletPin = async pin => {
              const value = String(pin || "");
              try {
                if (crypto?.subtle && typeof TextEncoder !== "undefined") {
                  const bytes = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(`vela-wallet:${value}`));
                  return [...new Uint8Array(bytes)].map(b => b.toString(16).padStart(2, "0")).join("");
                }
              } catch (_) {}
              let h = 2166136261;
              for (let i = 0; i < value.length; i += 1) { h ^= value.charCodeAt(i); h = Math.imul(h, 16777619); }
              return `fallback-${Math.abs(h >>> 0)}`;
            };

            const openWalletBind = () => {
              const account = state.wallet.linkedAccount || {};
              openScreen("wallet-flow", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="wallet-flow">‹</button><div class="v-meta"><strong>${state.wallet.linkedAccount ? "管理提现账户" : "绑定提现账户"}</strong><div class="v-hint">Vela 模拟结算账户</div></div><button class="v-head-action" data-action="save-wallet-account">保存</button></header><div class="v-subbody"><div class="v-wallet-flow-card"><div class="v-editform"><div class="v-field"><label>银行名称</label><input data-wallet-field="bankName" maxlength="40" value="${escapeHTML(account.bankName || "")}" placeholder="例如 Vela Bank"></div><div class="v-fieldpair"><div class="v-field"><label>账号前四位</label><input data-wallet-field="first4" inputmode="numeric" maxlength="4" value="${escapeHTML(account.first4 || "")}" placeholder="1234"></div><div class="v-field"><label>账号后四位</label><input data-wallet-field="last4" inputmode="numeric" maxlength="4" value="${escapeHTML(account.last4 || "")}" placeholder="5678"></div></div><div class="v-field"><label>Vela 交易 PIN · 6 位</label><input data-wallet-field="pin" type="password" inputmode="numeric" maxlength="6" placeholder="${state.wallet.linkedAccount ? "不修改可留空" : "仅用于 Vela 模拟验证"}"></div></div><div class="v-publish-note" style="margin-top:12px">不要输入真实银行卡完整号码或真实银行密码。这里只保存银行名称、前四位、后四位以及不可逆的 Vela PIN 摘要。</div></div></div>`);
            };

            const saveWalletAccount = async () => {
              const screen = q('[data-screen="wallet-flow"]');
              if (!screen) return;
              const read = name => String(screen.querySelector(`[data-wallet-field="${name}"]`)?.value || "").trim();
              const bankName = read("bankName");
              const first4 = read("first4").replace(/\D/g, "").slice(0, 4);
              const last4 = read("last4").replace(/\D/g, "").slice(0, 4);
              const pin = read("pin").replace(/\D/g, "").slice(0, 6);
              if (!bankName || first4.length !== 4 || last4.length !== 4) { toast("请填写银行名称和账号前 / 后四位"); return; }
              if (!state.wallet.linkedAccount && pin.length !== 6) { toast("请设置 6 位 Vela 交易 PIN"); return; }
              if (pin && pin.length !== 6) { toast("Vela 交易 PIN 必须是 6 位"); return; }
              const old = state.wallet.linkedAccount || {};
              state.wallet.linkedAccount = { bankName, first4, last4, pinHash: pin ? await hashWalletPin(pin) : old.pinHash || "" };
              await persist();
              closeScreen("wallet-flow");
              renderWallet();
              toast("提现账户已保存");
            };

            const verifyWalletPin = async pin => {
              const account = state.wallet.linkedAccount;
              if (!account?.pinHash) return false;
              return (await hashWalletPin(String(pin || "").replace(/\D/g, "").slice(0, 6))) === account.pinHash;
            };

            const coinCNYValue = coin => Number(coin || 0) * COIN_CNY_RATE;

            const openWalletCoinRecharge = () => {
              if (!state.wallet.linkedAccount) { openWalletBind(); return; }
              openScreen("wallet-flow", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="wallet-flow">‹</button><div class="v-meta"><strong>充值 Vela Coin</strong><div class="v-hint">🔷100 = ¥10 基准价值</div></div></header><div class="v-subbody"><div class="v-wallet-flow-card"><div class="v-editform"><div class="v-field"><label>充值数量</label><input data-wallet-recharge-field="coin" type="number" min="10" step="10" value="100"></div><div class="v-field"><label>付款方式</label><select data-wallet-recharge-field="source"><option value="cash">使用可提现余额</option><option value="account">使用已绑定模拟账户</option></select></div><div class="v-field"><label>Vela 交易 PIN</label><input data-wallet-recharge-field="pin" type="password" inputmode="numeric" maxlength="6" placeholder="6 位 PIN"></div></div><div class="v-wallet-convert-preview" style="margin-top:10px">Coin 充值只发生在 Vela 模拟钱包中，不会发起真实银行卡交易。</div><button class="v-action" style="margin-top:12px;width:100%" data-action="confirm-wallet-coin-recharge">确认充值</button></div></div>`);
            };

            const confirmWalletCoinRecharge = async () => {
              const screen = q('[data-screen="wallet-flow"]');
              const coin = Math.max(0, Math.round(Number(screen?.querySelector('[data-wallet-recharge-field="coin"]')?.value || 0)));
              const source = String(screen?.querySelector('[data-wallet-recharge-field="source"]')?.value || "cash");
              const pin = String(screen?.querySelector('[data-wallet-recharge-field="pin"]')?.value || "");
              if (coin < 10) { toast("至少充值 🔷10"); return; }
              if (!(await verifyWalletPin(pin))) { toast("Vela 交易 PIN 不正确"); return; }
              const costCNY = coinCNYValue(coin);
              if (source === "cash") {
                if (Number(state.wallet.cashBalanceCNY || 0) < costCNY) { toast("可提现余额不足"); return; }
                state.wallet.cashBalanceCNY -= costCNY;
                state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-cash2coin`, type:"cash", title:"余额购买 Coin", note:`兑换 ${formatCoin(coin)}`, cashCNY:-costCNY, identityId:state.viewerIdentityId, at:Date.now() });
              } else {
                state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-account2coin`, type:"coin", title:"模拟账户充值", note:`${state.wallet.linkedAccount.bankName} · ${state.wallet.linkedAccount.first4}••••${state.wallet.linkedAccount.last4}`, coin, identityId:state.viewerIdentityId, at:Date.now() });
              }
              state.wallet.coinBalance = Number(state.wallet.coinBalance || 0) + coin;
              if (source === "cash") state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-coinin`, type:"coin", title:"Coin 充值", note:"由可提现余额兑换", coin, identityId:state.viewerIdentityId, at:Date.now() });
              await persist();
              closeScreen("wallet-flow");
              renderWallet();
            };

            const openWalletCoinCashout = () => {
              if (!state.wallet.linkedAccount) { openWalletBind(); return; }
              openScreen("wallet-flow", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="wallet-flow">‹</button><div class="v-meta"><strong>Coin 兑换</strong><div class="v-hint">转入可提现余额</div></div></header><div class="v-subbody"><div class="v-wallet-flow-card"><div class="v-editform"><div class="v-field"><label>兑换 Coin</label><input data-wallet-cashout-field="coin" type="number" min="10" step="10" value="100"></div><div class="v-field"><label>Vela 交易 PIN</label><input data-wallet-cashout-field="pin" type="password" inputmode="numeric" maxlength="6" placeholder="6 位 PIN"></div></div><div class="v-wallet-convert-preview" style="margin-top:10px">🔷100 = ¥10 CNY 基准；钱包会按当前汇率显示你的结算币种。</div><button class="v-action" style="margin-top:12px;width:100%" data-action="confirm-wallet-coin-cashout">兑换到余额</button></div></div>`);
            };

            const confirmWalletCoinCashout = async () => {
              const screen = q('[data-screen="wallet-flow"]');
              const coin = Math.max(0, Math.round(Number(screen?.querySelector('[data-wallet-cashout-field="coin"]')?.value || 0)));
              const pin = String(screen?.querySelector('[data-wallet-cashout-field="pin"]')?.value || "");
              if (coin < 10 || Number(state.wallet.coinBalance || 0) < coin) { toast("Coin 余额不足"); return; }
              if (!(await verifyWalletPin(pin))) { toast("Vela 交易 PIN 不正确"); return; }
              const cashCNY = coinCNYValue(coin);
              state.wallet.coinBalance -= coin;
              state.wallet.cashBalanceCNY = Number(state.wallet.cashBalanceCNY || 0) + cashCNY;
              state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-coinout`, type:"coin", title:"Coin 兑换", note:`转入 ${formatCash(cashCNY)}`, coin:-coin, identityId:state.viewerIdentityId, at:Date.now() });
              state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-cashin`, type:"cash", title:"Coin 兑换入账", note:`来自 ${formatCoin(coin)}`, cashCNY, identityId:state.viewerIdentityId, at:Date.now() });
              await persist();
              closeScreen("wallet-flow");
              renderWallet();
            };

            const openWalletWithdraw = () => {
              if (!state.wallet.linkedAccount) { openWalletBind(); return; }
              openScreen("wallet-flow", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="wallet-flow">‹</button><div class="v-meta"><strong>提现</strong><div class="v-hint">${escapeHTML(state.wallet.linkedAccount.bankName || "提现账户")}</div></div></header><div class="v-subbody"><div class="v-wallet-flow-card"><div class="v-editform"><div class="v-field"><label>提现金额 · ${escapeHTML(walletCurrency())}</label><input data-wallet-withdraw-field="amount" type="number" min="0.01" step="0.01" placeholder="${escapeHTML(formatCash(state.wallet.cashBalanceCNY || 0))}"></div><div class="v-field"><label>Vela 交易 PIN</label><input data-wallet-withdraw-field="pin" type="password" inputmode="numeric" maxlength="6"></div></div><div class="v-publish-note" style="margin-top:10px">这是 Vela 模拟提现，不会产生真实银行转账。</div><button class="v-action" style="margin-top:12px;width:100%" data-action="confirm-wallet-withdraw">确认提现</button></div></div>`);
            };

            const confirmWalletWithdraw = async () => {
              const screen = q('[data-screen="wallet-flow"]');
              const amountLocal = Math.max(0, Number(screen?.querySelector('[data-wallet-withdraw-field="amount"]')?.value || 0));
              const pin = String(screen?.querySelector('[data-wallet-withdraw-field="pin"]')?.value || "");
              const cny = currencyToCNY(amountLocal);
              if (!amountLocal || cny > Number(state.wallet.cashBalanceCNY || 0)) { toast("可提现余额不足"); return; }
              if (!(await verifyWalletPin(pin))) { toast("Vela 交易 PIN 不正确"); return; }
              state.wallet.cashBalanceCNY -= cny;
              state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-withdraw`, type:"cash", title:"提现", note:`${state.wallet.linkedAccount.bankName} · ${state.wallet.linkedAccount.first4}••••${state.wallet.linkedAccount.last4}`, cashCNY:-cny, identityId:state.viewerIdentityId, at:Date.now() });
              await persist();
              closeScreen("wallet-flow");
              renderWallet();
              toast("Vela 模拟提现已完成");
            };

            const recordSubscriptionAccrual = (ownerType, ownerId, coinAmount, note = "") => {
              const amount = Math.max(0, Math.round(Number(coinAmount || 0)));
              if (!amount) return;
              if (ownerType === "identity" && String(ownerId) === String(state.viewerIdentityId)) {
                state.wallet.subscriptionAccruals.push({ id:`subacc-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,6)}`, ownerType, ownerId, coinAmount:amount, note, at:Date.now(), settled:false });
              } else if (ownerType === "channel") {
                const earning = state.roleEarnings[ownerId] || (state.roleEarnings[ownerId] = { coin:0, cashCNY:0, subscriptionCoin:0, liveHistory:[] });
                earning.subscriptionCoin = Number(earning.subscriptionCoin || 0) + amount;
              }
            };

            const syncIncomingFanSubscriptions = () => {
              const identity = state.identities.find(item => String(item.id) === String(state.viewerIdentityId));
              if (!identity) return;
              const coinPrice = Math.max(1, Math.round(Number(getSubscriptionProfile("identity", identity.id).paid.coinPrice || 0)));
              let changed = false;
              state.messages.forEach(msg => {
                if (msg?.fanTier !== "paid" || msg.subscriptionAccrued) return;
                msg.subscriptionAccrued = true;
                msg.subscriptionCoinSnapshot = coinPrice;
                state.wallet.subscriptionAccruals.push({
                  id:`subacc-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,6)}`,
                  ownerType:"identity",
                  ownerId:identity.id,
                  subscriberId:String(msg.id || ""),
                  coinAmount:coinPrice,
                  note:`${msg.name || "付费粉丝"} · 订阅价快照 🔷${coinPrice}`,
                  at:Number(msg.subscriptionStartedAt || Date.now()),
                  settled:false
                });
                changed = true;
              });
              if (changed) persist();
            };

            const mondayStart = dateValue => {
              const date = new Date(dateValue);
              date.setHours(0,0,0,0);
              const day = date.getDay();
              const diff = day === 0 ? -6 : 1 - day;
              date.setDate(date.getDate() + diff);
              return date.getTime();
            };

            const settleWeeklySubscriptions = async () => {
              const cutoff = mondayStart(Date.now());
              const pending = safeArray(state.wallet.subscriptionAccruals).filter(item => !item.settled && Number(item.at || 0) < cutoff);
              if (!pending.length) return;
              const coin = pending.reduce((sum, item) => sum + Number(item.coinAmount || 0), 0);
              const cashCNY = coinCNYValue(coin);
              pending.forEach(item => { item.settled = true; item.settledAt = Date.now(); });
              state.wallet.cashBalanceCNY = Number(state.wallet.cashBalanceCNY || 0) + cashCNY;
              const ownerIds = [...new Set(pending.filter(item => item.ownerType === "identity").map(item => String(item.ownerId || "")).filter(Boolean))];
              const ownerNames = ownerIds.map(id => state.identities.find(item => String(item.id) === id)?.displayName || "").filter(Boolean);
              state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-weeklysub`, type:"cash", title:"订阅周结算", note:`本周订阅收入 ${formatCoin(coin)} · 无平台抽成${ownerNames.length ? ` · 来自 ${ownerNames.join(" / ")}` : ""}`, cashCNY, identityId:ownerIds.length === 1 ? ownerIds[0] : "", at:Date.now() });
              await persist();
            };

            const openScreen = (name, html) => {
              const screen = q(`[data-screen="${name}"]`);
              if (!screen) return;
              screen.innerHTML = html;
              screen.classList.add("is-open");
            };

            const closeScreen = (name) => {
              q(`[data-screen="${name}"]`)?.classList.remove("is-open");
            };

            const detectLiveLanguage = (live) => {
              const explicit = String(live?.language || live?.lang || "").toLowerCase();
              if (explicit.startsWith("ko")) return "ko";
              if (explicit.startsWith("ja")) return "ja";
              if (explicit.startsWith("en")) return "en";
              if (/^zh[-_](tw|hk|hant)/.test(explicit)) return "zh-Hant";
              if (explicit.startsWith("zh")) return "zh-CN";
              const sample = `${live?.title || ""} ${live?.category || ""}`;
              if (/[가-힣]/.test(sample)) return "ko";
              if (/[ぁ-んァ-ン]/.test(sample)) return "ja";
              if (/^[\x00-\x7F\s\p{P}]+$/u.test(sample) && /[A-Za-z]/.test(sample)) return "en";
              return "zh";
            };

            const getLiveProgress = (live) => {
              const id = String(live?.id || "live");
              if (!state.liveProgress[id]) {
                const lang = detectLiveLanguage(live);
                state.liveProgress[id] = {
                  step: 0,
                  text: String(live?.openingSpeech || live?.moment || ""),
                  translation: String(live?.openingTranslation || live?.momentTranslation || ""),
                  openingAction: String(live?.openingAction || ""),
                  language: lang,
                  timeline: [],
                  participantEvents: {}
                };
              }
              const progress = state.liveProgress[id];
              progress.timeline = safeArray(progress.timeline);
              progress.participantEvents = { ...(progress.participantEvents || {}) };
              return progress;
            };

            const isOwnLiveSession = (live) => {
              const id = String(live?.id || "");
              return id === "own-live" || id.startsWith("own-scheduled-") || live?.ownerType === "identity" || live?.isOwn === true;
            };

            const getLiveGuests = (live) => {
              const direct = safeArray(live?.participants).map(item => ({
                id: String(item?.id || ""),
                name: String(item?.name || item?.handle || "嘉宾"),
                handle: String(item?.handle || ""),
                avatar: String(item?.avatar || initials(item?.name || "嘉宾"))
              })).filter(item => item.id);
              if (direct.length) return direct.slice(0, 3);
              if (live?.cohostId) {
                const ch = state.channels.find(item => String(item.id) === String(live.cohostId));
                return [{ id: String(live.cohostId), name: String(ch?.name || live.cohostName || "嘉宾"), handle: String(ch?.handle || live.cohostHandle || ""), avatar: String(ch?.avatar || live.cohostAvatar || initials(ch?.name || live.cohostName || "嘉宾")) }];
              }
              return [];
            };

            const getLiveParticipants = (live) => {
              const host = { id: `host:${String(live?.ownerId || live?.id || "host")}`, name: String(live?.name || live?.handle || "主播"), handle: String(live?.handle || ""), avatar: String(live?.avatar || initials(live?.name || "主播")), isUser: Boolean(isOwnLiveSession(live)) };
              return [host, ...getLiveGuests(live)].slice(0, 4);
            };

            const getLiveStats = (live) => {
              const id = String(live?.id || "live");
              if (!state.liveStats[id]) {
                const followers = Math.max(0, Number(live?.followers || 0));
                const activity = Math.max(0, Math.min(100, Number(state.platformSettings?.activityLevel ?? 50)));
                const followerBase = followers > 0 ? Math.max(3, Math.round(followers * (0.025 + activity * 0.00018))) : 0;
                const seedViewers = Math.max(1, Number(live?.viewers || followerBase || 36));
                const baseViewers = followerBase || seedViewers;
                const currentViewers = followers > 0 ? Math.max(1, Math.round((baseViewers + seedViewers) / 2)) : Math.round(seedViewers);
                const seededGifts = [];
                state.liveStats[id] = {
                  baseViewers,
                  currentViewers,
                  peakViewers: currentViewers,
                  heat: 50,
                  userGiftTotal: 0,
                  tipReceivedCoin: 0,
                  giftReceivedCoin: seededGifts.reduce((sum, item) => sum + Number(item.amount || 0), 0),
                  gifts: seededGifts,
                  lottery: null,
                  ad: live?.business ? { ...live.business } : null
                };
              }
              return state.liveStats[id];
            };

            const getGiftRanking = (live) => {
              const stats = getLiveStats(live);
              const identity = state.identities.find(x => x.id === state.viewerIdentityId) || state.identities[0];
              const ownHandle = String(identity?.handle || identity?.displayName || "@user");
              const map = new Map();
              safeArray(stats.gifts).forEach(item => map.set(String(item.user), (map.get(String(item.user)) || 0) + Number(item.amount || 0)));
              if (Number(stats.userGiftTotal || 0) > 0) map.set(ownHandle, Number(stats.userGiftTotal || 0));
              return [...map.entries()].map(([user, amount]) => ({ user, amount })).sort((a,b) => b.amount - a.amount);
            };

            const renderLiveHUD = () => {
              const live = state.liveSession;
              if (!live) return;
              const stats = getLiveStats(live);
              const viewers = q('[data-screen="live"] [data-role="live-viewers"]');
              if (viewers) viewers.textContent = `${formatViewers(stats.currentViewers || 0)} 人正在观看`;
              const trigger = q('[data-screen="live"] [data-role="gift-rank-trigger"]');
              const ranking = getGiftRanking(live);
              if (trigger) trigger.textContent = ranking.length ? `👑 ${ranking[0].user} · 🔷${Math.round(ranking[0].amount)}` : "🎁 礼物榜";
              const rows = q('[data-screen="live"] [data-role="gift-rank-rows"]');
              if (rows) {
                const identity = state.identities.find(x => x.id === state.viewerIdentityId) || state.identities[0];
                const ownHandle = String(identity?.handle || identity?.displayName || "@user");
                rows.innerHTML = ranking.slice(0,8).map((item,index) => `<div class="v-rank-row"><span class="v-rank-no">${index < 3 ? ["👑","2","3"][index] : index + 1}</span><span class="v-rank-user">${escapeHTML(item.user)}${item.user === ownHandle ? " · 你" : ""}</span><span class="v-rank-amount">🔷${Math.round(item.amount).toLocaleString("zh-CN")}</span></div>`).join("") || '<div class="v-hint">本场还没有人送礼物。</div>';
              }
              const balance = q('[data-screen="live"] [data-role="gift-balance"]');
              if (balance) balance.textContent = `Vela Coin 余额 🔷${Math.round(Number(state.wallet?.coinBalance || 0)).toLocaleString("zh-CN")}`;
            };

            const advanceViewerCount = (live, { heatBoost = 0 } = {}) => {
              const stats = getLiveStats(live);
              stats.heat = Math.max(0, Math.min(100, Number(stats.heat || 50) + Number(heatBoost || 0) - 1));
              const base = Math.max(1, Number(stats.baseViewers || stats.currentViewers || 1));
              const current = Math.max(1, Number(stats.currentViewers || base));
              const swing = Math.max(1, Math.round(current * 0.055));
              const heatPull = ((stats.heat - 50) / 50) * Math.max(1, Math.round(base * 0.025));
              const delta = Math.round((Math.random() - 0.48) * swing * 2 + heatPull);
              const low = Math.max(1, Math.round(base * 0.58));
              const high = Math.max(low + 1, Math.round(base * 1.85));
              stats.currentViewers = Math.max(low, Math.min(high, current + delta));
              stats.peakViewers = Math.max(Number(stats.peakViewers || 0), stats.currentViewers);
              return stats.currentViewers;
            };

            const formatLiveElapsed = (at, live) => {
              const start = Number(live?.startedAt || 0) || Math.max(0, Number(at || Date.now()) - 1000);
              const total = Math.max(0, Math.floor((Number(at || Date.now()) - start) / 1000));
              const hours = Math.floor(total / 3600);
              const minutes = Math.floor((total % 3600) / 60);
              const seconds = total % 60;
              return `${hours}:${String(minutes).padStart(2, "0")}:${String(seconds).padStart(2, "0")}`;
            };

            const normalizeLiveTimelineEvent = (event) => {
              const mode = String(event?.mode || "");
              const text = String(event?.text || "");
              return {
                ...event,
                action: String(event?.action || (mode === "action" ? text : "")),
                speech: String(event?.speech || (mode !== "action" ? text : "")),
                translation: String(event?.translation || ""),
                at: Number(event?.at || Date.now())
              };
            };

            const pushLiveTimelineEvent = (progress, event) => {
              progress.timeline = safeArray(progress.timeline);
              const next = normalizeLiveTimelineEvent(event);
              const lastIndex = progress.timeline.length - 1;
              const last = lastIndex >= 0 ? normalizeLiveTimelineEvent(progress.timeline[lastIndex]) : null;
              const sameActor = last && String(last.actorId || "") === String(next.actorId || "");
              const closeInTime = last && Math.abs(Number(next.at || 0) - Number(last.at || 0)) <= 15000;
              const complementary = last && ((next.action && !last.action) || (next.speech && !last.speech));
              if (sameActor && closeInTime && complementary) {
                progress.timeline[lastIndex] = {
                  ...last,
                  action: last.action || next.action,
                  speech: last.speech || next.speech,
                  translation: last.translation || next.translation,
                  at: last.at || next.at
                };
              } else {
                progress.timeline.push(next);
              }
              progress.timeline = progress.timeline.slice(-120);
            };

            const renderLiveMoment = () => {
              const live = state.liveSession;
              const host = q('[data-screen="live"] [data-role="live-moment"]');
              if (!live || !host) return;
              const progress = getLiveProgress(live);
              let rows = safeArray(progress.timeline).map(normalizeLiveTimelineEvent).slice(-12);

              if (!rows.length && !isOwnLiveSession(live) && progress.text) {
                rows = [{
                  actorType: "character",
                  actorId: String(live.id || "character"),
                  actorName: String(live.name || live.handle || "主播"),
                  action: String(progress.openingAction || ""),
                  speech: String(progress.text || ""),
                  translation: String(progress.translation || ""),
                  at: Number(live.startedAt || Date.now())
                }];
              }

              host.innerHTML = `<div class="v-live-timeline">${rows.length ? rows.map((event, index) => {
                const trId = `live-timeline-${String(live.id || "live")}-${Number(event.at || 0)}-${index}`;
                const actor = String(event.actorName || (event.actorType === "user" ? live.name || "user" : "主播"));
                return `<div class="v-live-time-entry"><div class="v-live-time-head"><span class="v-live-time-pill">${escapeHTML(formatLiveElapsed(event.at, live))}</span><span class="v-live-time-actor">${escapeHTML(actor)}</span></div>${event.action ? `<div class="v-live-time-action">${escapeHTML(event.action)}</div>` : ""}${event.speech ? `<div class="v-live-time-speech">${escapeHTML(event.speech)}</div>${translationHTML(trId, event.translation || "")}` : ""}</div>`;
              }).join("") : `<div class="v-live-timeline-empty">直播内容会按时间记录在这里。</div>`}</div>`;
              requestAnimationFrame(() => { host.scrollTop = host.scrollHeight; });
            };

            let liveLotteryTimer = null;

            const acceptedBusinessCards = () => {
              const rows = [];
              Object.entries(state.dmThreads || {}).forEach(([messageId, thread]) => {
                safeArray(thread).forEach(item => {
                  if (item?.kind === "business" && item?.status === "accepted") rows.push({ messageId, card:item });
                });
              });
              return rows;
            };

            const livePrizeRowHTML = (item = {}, index = 0) => `<div class="v-prize-row" data-prize-row><input data-prize-field="name" maxlength="20" value="${escapeHTML(item.name || `${["一","二","三","四","五"][index] || index + 1}等奖`)}" placeholder="奖项"><input data-prize-field="count" type="number" min="1" max="100" value="${Math.max(1, Number(item.count || 1))}" placeholder="人数"><input data-prize-field="content" maxlength="100" value="${escapeHTML(item.content || "")}" placeholder="奖品内容"><input data-prize-field="value" type="number" min="0" value="${Math.max(0, Number(item.value || 0))}" placeholder="估值"><button class="v-prize-remove" data-action="remove-live-prize-row">删除这一档</button></div>`;

            const closeLiveEconomyPanel = () => q('[data-screen="live"] [data-role="live-economy-panel"]')?.classList.remove("is-open");

            const openLiveLotteryPanel = () => {
              const live = state.liveSession;
              if (!live || !isOwnLiveSession(live)) return;
              const stats = getLiveStats(live);
              const lottery = stats.lottery;
              const panel = q('[data-screen="live"] [data-role="live-economy-panel"]');
              if (!panel) return;
              if (lottery?.status === "active") {
                const left = Math.max(0, Number(lottery.endsAt || 0) - Date.now());
                panel.innerHTML = `<div class="v-live-economy-card"><div class="v-live-tool-title"><h3>本场抽奖 · 进行中</h3><button data-action="close-live-economy">×</button></div><div class="v-wallet-convert-preview" style="margin-top:10px">剩余约 ${Math.max(0, Math.ceil(left/60000))} 分钟 · ${safeArray(lottery.prizes).length} 档奖项</div>${safeArray(lottery.prizes).map(item => `<div class="v-row"><div><b>${escapeHTML(item.name)}</b><small>${escapeHTML(item.content || "奖品")} · ${Number(item.count || 1)} 人</small></div><span>${item.value ? `¥${Number(item.value).toLocaleString("zh-CN")}` : ""}</span></div>`).join("")}<div class="v-live-tool-actions"><button class="is-light" data-action="resolve-live-lottery">立即开奖</button><button class="is-primary" data-action="close-live-economy">返回直播</button></div></div>`;
              } else if (lottery?.status === "ended") {
                panel.innerHTML = `<div class="v-live-economy-card"><div class="v-live-tool-title"><h3>本场抽奖 · 已开奖</h3><button data-action="close-live-economy">×</button></div>${safeArray(lottery.results).map(item => `<div class="v-row"><div><b>${escapeHTML(item.prizeName)}</b><small>${escapeHTML(item.winners.join("、") || "暂无参与者")}</small></div></div>`).join("")}<div class="v-live-tool-actions"><button class="is-primary" data-action="new-live-lottery">再开一轮</button></div></div>`;
              } else {
                panel.innerHTML = `<div class="v-live-economy-card"><div class="v-live-tool-title"><h3>设置本场抽奖</h3><button data-action="close-live-economy">×</button></div><div class="v-field" style="margin-top:10px"><label>开奖倒计时 · 分钟</label><input data-live-lottery-minutes type="number" min="1" max="120" value="5"></div><div data-role="live-prize-rows">${livePrizeRowHTML({name:"一等奖"},0)}${livePrizeRowHTML({name:"二等奖"},1)}</div><button class="v-action light" style="width:100%;margin-top:8px" data-action="add-live-prize-row">＋ 添加奖项</button><div class="v-live-tool-actions"><button class="is-primary" data-action="save-live-lottery">开始倒计时</button></div></div>`;
              }
              panel.classList.add("is-open");
            };

            const saveLiveLottery = async () => {
              const live = state.liveSession;
              const panel = q('[data-screen="live"] [data-role="live-economy-panel"]');
              if (!live || !panel || !isOwnLiveSession(live)) return;
              const minutes = Math.max(1, Math.min(120, Number(panel.querySelector('[data-live-lottery-minutes]')?.value || 5)));
              const prizes = [...panel.querySelectorAll('[data-prize-row]')].map(row => ({
                name:String(row.querySelector('[data-prize-field="name"]')?.value || "奖项").trim(),
                count:Math.max(1, Math.min(100, Number(row.querySelector('[data-prize-field="count"]')?.value || 1))),
                content:String(row.querySelector('[data-prize-field="content"]')?.value || "").trim(),
                value:Math.max(0, Number(row.querySelector('[data-prize-field="value"]')?.value || 0))
              })).filter(item => item.name);
              if (!prizes.length) { toast("至少保留一个奖项"); return; }
              const stats = getLiveStats(live);
              stats.lottery = { id:`lottery-${Date.now().toString(36)}`, status:"active", startedAt:Date.now(), endsAt:Date.now()+minutes*60000, prizes, results:[] };
              await persist();
              if (liveLotteryTimer) clearTimeout(liveLotteryTimer);
              liveLotteryTimer = setTimeout(() => resolveLiveLottery(), minutes*60000);
              openLiveLotteryPanel();
              toast(`抽奖已开始 · ${minutes} 分钟后开奖`);
            };

            const resolveLiveLottery = async () => {
              const live = state.liveSession;
              if (!live) return;
              const stats = getLiveStats(live);
              const lottery = stats.lottery;
              if (!lottery || lottery.status !== "active") return;
              const pool = [...new Set(state.liveChat.map(line => String(line.user || "")).filter(name => name && name !== "系统" && name !== (state.identities.find(x => x.id === state.viewerIdentityId)?.handle || "")))];
              let cursor = 0;
              lottery.results = safeArray(lottery.prizes).map(prize => {
                const winners = [];
                for (let i = 0; i < Number(prize.count || 1) && pool.length; i += 1) {
                  winners.push(pool[(cursor + i) % pool.length]);
                }
                cursor += Number(prize.count || 1);
                return { prizeName:prize.name, content:prize.content, winners };
              });
              lottery.status = "ended";
              lottery.endedAt = Date.now();
              const summary = lottery.results.map(item => `${item.prizeName}：${item.winners.join("、") || "暂无参与者"}`).join("；");
              state.liveChat.push({ user:"系统", avatar:"🎉", text:`本场抽奖结果：${summary}`, translation:"", at:Date.now() });
              const ownerType = live.ownerType || (isOwnLiveSession(live) ? "identity" : "channel");
              const ownerId = live.ownerId || live.id;
              pushCommunityBotEvent(ownerType, ownerId, "lottery_result", { title:"本场抽奖结果", text:summary });
              await persist();
              renderLiveChatLines();
              openLiveLotteryPanel();
            };

            const openLiveAdPanel = () => {
              const live = state.liveSession;
              if (!live || !isOwnLiveSession(live)) return;
              const stats = getLiveStats(live);
              const panel = q('[data-screen="live"] [data-role="live-economy-panel"]');
              if (!panel) return;
              const current = stats.ad || live.ad || null;
              const contract = live.business?.sourceCardId ? live.business : null;
              if (contract) {
                const total = Number(contract.totalFeeCNY ?? contract.cashAmountCNY ?? 0);
                const deposit = Number(contract.depositCNY || 0);
                const balance = Number(contract.balanceCNY ?? Math.max(0, total - deposit));
                panel.innerHTML = `<div class="v-live-economy-card"><div class="v-live-tool-title"><h3>本场商务广告</h3><button data-action="close-live-economy">×</button></div><div class="v-chat-card-meta" style="margin-top:10px"><div>品牌 / 产品 <b>${escapeHTML(`${contract.brandName || "品牌"} · ${contract.productName || "合作商品"}`)}</b></div><div>合作费用 <b>${escapeHTML(formatCash(total))}</b></div><div>定金 / 尾款 <b>${escapeHTML(`${formatCash(deposit)} / ${formatCash(balance)}`)}</b></div>${contract.adCopy ? `<div>广告词 <b>${escapeHTML(contract.adCopy)}</b></div>` : ""}${contract.adLink ? `<div>广告链接 <b>${escapeHTML(contract.adLink)}</b></div>` : ""}</div><div class="v-publish-note" style="margin-top:10px">这张广告只属于当前预约商务直播，不能挂到其他临时直播。进入约定场次后会自动启用。</div><div class="v-live-tool-actions"><button class="is-primary" data-action="close-live-economy">返回直播</button></div></div>`;
                panel.classList.add("is-open");
                return;
              }
              const locked = Boolean(current?.active && current?.adLink);
              panel.innerHTML = `<div class="v-live-economy-card"><div class="v-live-tool-title"><h3>本场广告</h3><button data-action="close-live-economy">×</button></div><div class="v-field" style="margin-top:10px"><label>品牌 / 产品</label><input data-live-ad-field="label" maxlength="100" value="${escapeHTML(current?.label || current?.productName || current?.brandName || "")}" placeholder="品牌或产品名称" ${current?.active ? "readonly" : ""}></div><div class="v-field"><label>广告词</label><textarea data-live-ad-field="copy" maxlength="600" ${current?.active ? "readonly" : ""}>${escapeHTML(current?.adCopy || "")}</textarea></div><div class="v-field"><label>广告链接</label><input class="${locked ? "v-readonly-link" : ""}" data-live-ad-field="link" value="${escapeHTML(current?.adLink || "")}" placeholder="https://..." ${locked ? "readonly" : ""}></div><div class="v-publish-note" style="margin-top:9px">普通直播可以手动设置本场广告，但已接受的商务合同不会出现在这里；合同广告只能从对应预约直播进入。</div><div class="v-live-tool-actions"><button class="is-light" data-action="clear-live-ad">${current?.active ? "结束投放" : "本场不投放"}</button><button class="is-primary" data-action="save-live-ad" ${current?.active ? "disabled" : ""}>${current?.active ? "投放中" : "开始投放广告"}</button></div></div>`;
              panel.classList.add("is-open");
            };

            const saveLiveAd = async () => {
              const live = state.liveSession;
              const panel = q('[data-screen="live"] [data-role="live-economy-panel"]');
              if (!live || !panel) return;
              if (live.business?.sourceCardId) { toast("商务合同广告会随约定直播自动启用"); return; }
              const read = name => String(panel.querySelector(`[data-live-ad-field="${name}"]`)?.value || "").trim();
              const existing = getLiveStats(live).ad || {};
              if (existing.active) { toast("本场广告已经在投放"); return; }
              const ad = { ...existing, label:read("label"), adCopy:read("copy"), adLink:read("link"), active:true, startedAt:Date.now(), manual:true };
              if (!ad.label && !ad.adCopy && !ad.adLink) { toast("先填写广告内容"); return; }
              getLiveStats(live).ad = ad;
              state.liveSession.ad = { ...ad };
              await persist(); closeLiveEconomyPanel(); openLive({ ...state.liveSession }); toast("广告已开始投放");
            };

            const clearLiveAd = async () => {
              const live = state.liveSession;
              if (!live) return;
              const stats = getLiveStats(live);
              if (stats.ad?.active) stats.ad = { ...stats.ad, active:false, endedAt:Date.now() };
              else stats.ad = null;
              state.liveSession.ad = stats.ad ? { ...stats.ad } : null;
              await persist();
              closeLiveEconomyPanel();
              openLive({ ...state.liveSession });
              toast(stats.ad ? "本场广告投放已结束" : "本场不投放广告");
            };

            const openLive = (live) => {
              if (state.liveSession) {
                const previousKey = String(state.liveSession.id || state.liveSession.ownerId || "live");
                state.liveChatsById = { ...(state.liveChatsById || {}) };
                state.liveChatsById[previousKey] = safeArray(state.liveChat).map(item => ({ ...item }));
              }
              state.liveSession = { ...live };
              const currentLive = state.liveSession;
              const currentLiveKey = String(currentLive.id || currentLive.ownerId || "live");
              state.liveChatsById = { ...(state.liveChatsById || {}) };
              const seededChat = safeArray(state.liveChatsById[currentLiveKey]).length
                ? safeArray(state.liveChatsById[currentLiveKey])
                : safeArray(currentLive.chatSeed);
              state.liveChat = seededChat.map(item => ({ ...item }));
              state.liveChatsById[currentLiveKey] = state.liveChat.map(item => ({ ...item }));
              state.liveChatQueues[currentLiveKey] = safeArray(state.liveChatQueues?.[currentLiveKey]);
              flushLiveChatQueue(currentLiveKey);
              armLiveLazyTimer(currentLiveKey);
              const title = currentLive.title || "正在直播";
              const hostName = currentLive.name || currentLive.handle || "主播";
              const liveTrId = `live-title-${String(currentLive.id || "live")}`;
              const liveTranslation = [currentLive.titleTranslation, currentLive.categoryTranslation].filter(Boolean).join("\n");
              getLiveProgress(currentLive);
              getLiveStats(currentLive);
              const own = isOwnLiveSession(currentLive);
              const managedChannel = !own && state.channels.some(ch => String(ch.id) === String(currentLive.ownerId || currentLive.id || ""));
              const canEndLive = own || managedChannel;
              const liveStats = getLiveStats(currentLive);
              if (!liveStats.ad && currentLive.business) liveStats.ad = { ...currentLive.business };
              const guests = getLiveGuests(currentLive);
              const mode = currentLive.mode || (guests.length ? (currentLive.cohostMode === "inperson" ? "same-room" : "remote") : "solo");
              currentLive.mode = mode;
              currentLive.participants = guests;
              const participants = getLiveParticipants(currentLive);
              const coverHTML = isImageURL(currentLive.cover || "") ? `<img class="v-live-cover-bg" src="${escapeHTML(currentLive.cover)}" alt="">` : "";
              const multiHost = participants.length > 1;
              const participantAvatars = participants.map(p => `<div class="v-live-host-avatar">${avatarHTML(p.avatar || "", p.name || "V")}</div>`).join("");
              const participantNames = participants.map(p => p.name || "参与者").join(" + ");
              const participantsHUD = multiHost
                ? `<div class="v-live-participants">${participantAvatars}</div><div class="v-live-host-copy"><div class="v-live-host-name">${escapeHTML(participantNames)}</div><div class="v-live-viewers" data-role="live-viewers"></div></div>`
                : `<div class="v-live-host-avatar">${avatarHTML(currentLive.avatar || "", hostName)}</div><div class="v-live-host-copy"><div class="v-live-host-name">${escapeHTML(hostName)}</div><div class="v-live-viewers" data-role="live-viewers"></div></div>`;
              const accessLabel = currentLive.accessMode === "free" ? "免费订阅" : currentLive.accessMode === "paid" ? "付费订阅" : "公开直播";
              const ageLabel = currentLive.ageRequirement === "18+" ? " · 18+" : "";
              const nsfwLabel = currentLive.nsfw === true ? " · NSFW" : "";
              const modeLabel = guests.length ? ` · ${mode === "same-room" ? "线下同镜" : "线上联播"}` : "";
              const liveInteractionKey = String(currentLive.id || currentLive.ownerId || "live");
              const liveChannelId = String(currentLive.ownerId || currentLive.id || "");
              let liveEligibleIdentities = liveChannelId ? identitiesWithAccessToChannel(liveChannelId, currentLive.accessMode || "public") : state.identities.slice();
              if (!liveEligibleIdentities.length) liveEligibleIdentities = state.identities.filter(item => String(item.id) === String(state.viewerIdentityId));
              const liveRemembered = String(state.liveInteractionIdentity?.[liveInteractionKey] || "");
              const liveSelectedIdentity = liveEligibleIdentities.find(item => String(item.id) === liveRemembered) || liveEligibleIdentities.find(item => String(item.id) === String(state.viewerIdentityId)) || liveEligibleIdentities[0];
              if (liveSelectedIdentity) state.liveInteractionIdentity[liveInteractionKey] = String(liveSelectedIdentity.id);
              const liveIdentityPicker = identityPickerHTML(liveEligibleIdentities, liveSelectedIdentity?.id, "live-identity-select", `data-live-key="${escapeHTML(liveInteractionKey)}"`);
              const viewerComposer = `<div class="v-composer">${liveIdentityPicker}<input data-role="live-input" placeholder="回车发送弹幕…" maxlength="120"><button data-action="open-gift-picker" aria-label="送礼物" title="送礼物">🎁</button><button class="${liveReplyPending.has(liveInteractionKey) ? "is-generating" : ""}" data-action="summon-live" aria-label="召唤 / 继续直播" title="召唤 / 继续直播" ${liveReplyPending.has(liveInteractionKey) ? "disabled" : ""}>${liveReplyPending.has(liveInteractionKey) ? '<span class="v-summon-dots">•••</span>' : planeIconHTML()}</button></div>`;
              const activeAd = liveStats.ad || currentLive.ad || currentLive.business || null;
              const quickTools = own ? `<div class="v-live-quicktools"><button class="v-live-quicktool is-lottery" data-action="open-live-lottery">抽奖</button><button class="v-live-quicktool is-ad" data-action="open-live-ad">${activeAd?.active ? "AD · 投放中" : "AD"}</button></div>` : "";
              const adStrip = "";
              const connectButton = own ? `<button class="v-live-connect-btn" data-action="open-invite-guest">与…连线${guests.length ? ` · ${participants.length}/4` : ""}</button>` : "";
              const hostConsole = `<div class="v-host-console"><div class="v-host-console-top"><button class="v-host-mode is-active" data-action="set-host-input-mode" data-host-input-mode="speech">说话</button><button class="v-host-mode" data-action="set-host-input-mode" data-host-input-mode="action">动作</button></div><div class="v-host-console-row"><input data-role="host-live-input" placeholder="输入你在直播里说的话… · Enter发送" maxlength="500"><button class="v-summon-host ${liveReplyPending.has(liveInteractionKey) ? "is-generating" : ""}" data-action="summon-host-live" aria-label="召唤 / 继续" ${liveReplyPending.has(liveInteractionKey) ? "disabled" : ""}>${liveReplyPending.has(liveInteractionKey) ? '<span class="v-summon-dots">•••</span>' : planeIconHTML()}</button></div><div class="v-host-console-foot"><span>发送只记录 user 的说话 / 动作，不自动召唤。</span></div></div>`;

              openScreen("live", `<div class="v-live-screen"><div class="v-live-body"><div class="v-live-fixed"><div class="v-live-stage ${coverHTML ? "has-cover" : ""}">${coverHTML}<div class="v-live-hud ${mode === "same-room" ? "is-same-room" : ""}"><button class="v-live-exit" data-action="open-live-exit" aria-label="退出">×</button><div class="v-live-host ${multiHost ? "is-multi" : ""}">${participantsHUD}</div>${quickTools}<button class="v-gift-rank-trigger" data-action="toggle-gift-rank" data-role="gift-rank-trigger">🎁 礼物榜</button></div><div class="v-live-moment" data-role="live-moment"></div></div><div class="v-live-info"><div class="v-live-info-head"><span class="v-badge">LIVE</span><span class="v-cohost-pill">${escapeHTML(accessLabel + ageLabel + nsfwLabel + modeLabel)}</span></div><div class="v-live-title-row"><h2>${escapeHTML(title)}</h2>${connectButton}</div><p>${escapeHTML(currentLive.category || "直播进行中")}</p>${translationHTML(liveTrId, liveTranslation)}${adStrip}</div></div><div class="v-chat-panel"><div class="v-chat-title">聊天室</div><div class="v-chat-scroll" data-role="live-chat-scroll"><div data-role="live-chat-lines"></div></div>${own ? hostConsole : viewerComposer}</div></div><div class="v-live-popover" data-role="gift-rank-panel"><div class="v-live-popover-card"><div class="v-live-popover-title"><span>本场礼物榜</span><button data-action="close-gift-rank">×</button></div><div data-role="gift-rank-rows"></div></div></div><div class="v-gift-picker" data-role="gift-picker"><div class="v-live-popover-title"><span>送礼物</span><button data-action="close-gift-picker">×</button></div><div class="v-gift-balance" data-role="gift-balance"></div><div class="v-gift-grid"><button data-action="send-live-gift" data-gift-amount="50">❤️ 🔷50</button><button data-action="send-live-gift" data-gift-amount="200">💕 🔷200</button><button data-action="send-live-gift" data-gift-amount="500">💖 🔷500</button><button data-action="send-live-gift" data-gift-amount="1000">🛍️ 🔷1000</button><button data-action="send-live-gift" data-gift-amount="2888">🎁 🔷2888</button><button data-action="send-live-gift" data-gift-amount="5888">🎉 🔷5888</button><button data-action="send-live-gift" data-gift-amount="9888">❤️‍🔥 🔷9888</button><button data-action="send-live-gift" data-gift-amount="18888">💎 🔷18888</button></div></div><div class="v-live-economy-panel" data-role="live-economy-panel"></div><div class="v-live-exitmenu" data-role="live-exit-menu"><div class="v-live-exitcard"><h3>${canEndLive ? "退出或结束直播" : "退出直播间？"}</h3>${own ? '<button class="v-primary" data-action="hang-own-live">退出直播间</button><button class="v-danger" data-action="end-current-live">结束直播</button>' : managedChannel ? '<button class="v-primary" data-action="leave-live">退出直播间</button><button class="v-danger" data-action="end-current-live">结束直播</button>' : '<button class="v-primary" data-action="leave-live">退出直播间</button>'}<button class="v-cancel" data-action="close-live-exit">取消</button></div></div></div>`);
              renderLiveMoment();
              renderLiveChatLines();
              renderLiveHUD();
            };

            const flushLiveChatQueue = liveKey => {
              state.liveChatQueues[liveKey] = safeArray(state.liveChatQueues?.[liveKey]);
              const now = Date.now();
              const due = state.liveChatQueues[liveKey].filter(item => Number(item?.plannedAt || 0) <= now);
              if (!due.length) return 0;
              const ids = new Set(due.map(item => String(item.id || "")));
              state.liveChatQueues[liveKey] = state.liveChatQueues[liveKey].filter(item => !ids.has(String(item?.id || "")));
              due.forEach(item => state.liveChat.push({ ...item, at:Number(item.plannedAt || now), plannedAt:undefined }));
              state.liveChat = state.liveChat.slice(-120);
              state.liveChatsById[liveKey] = state.liveChat.map(item => ({ ...item }));
              return due.length;
            };
            const armLiveLazyTimer = liveKey => {
              if (liveLazyTimers.has(liveKey)) clearTimeout(liveLazyTimers.get(liveKey));
              const next = safeArray(state.liveChatQueues?.[liveKey]).map(item => Number(item?.plannedAt || 0)).filter(at => at > Date.now()).sort((a,b)=>a-b)[0];
              if (!next) { liveLazyTimers.delete(liveKey); return; }
              const timer = setTimeout(async () => {
                liveLazyTimers.delete(liveKey);
                const moved = flushLiveChatQueue(liveKey);
                if (moved) { await persist(); if (state.liveSession && String(state.liveSession.id || state.liveSession.ownerId || "live") === liveKey) renderLiveChatLines(); }
                armLiveLazyTimer(liveKey);
              }, Math.max(350, next-Date.now()));
              liveLazyTimers.set(liveKey, timer);
            };

            const renderLiveChatLines = () => {
              const host = q('[data-screen="live"] [data-role="live-chat-lines"]');
              if (!host) return;
              const rows = state.liveChat.slice(-40);
              const offset = Math.max(0, state.liveChat.length - rows.length);
              host.innerHTML = rows.map((line, index) => {
                const absoluteIndex = offset + index;
                const trId = `live-chat-${absoluteIndex}-${String(line.user || "viewer")}`;
                const translateTool = line.translation ? `<button class="v-translate-btn" data-action="toggle-translation" data-translation-target="${escapeHTML(trId)}">翻译</button>` : "";
                const translationBody = line.translation ? `<div class="v-translation" data-translation-id="${escapeHTML(trId)}" hidden>${escapeHTML(line.translation)}</div>` : "";
                return `<div class="v-chatline"><div class="v-social-avatar">${avatarHTML(line.avatar || initials(line.user || "V"), line.user || "V")}</div><div class="v-chatline-body"><div class="v-chatline-head">${escapeHTML(line.user)}${Number(line.tipCoin || 0) > 0 ? `<span class="v-chat-tip">打赏 🔷${Number(line.tipCoin).toLocaleString("zh-CN")}</span>` : ""}</div><div class="v-chatline-text">${escapeHTML(line.text)}</div><div class="v-live-chat-tools">${translateTool}<button data-action="delete-live-chat" data-chat-index="${absoluteIndex}">删除</button></div>${translationBody}</div></div>`;
              }).join("");
              const scroll = q('[data-screen="live"] [data-role="live-chat-scroll"]');
              if (scroll) requestAnimationFrame(() => { scroll.scrollTop = scroll.scrollHeight; });
            };

            const sendLiveChatOnly = async (textValue = "") => {
              const live = state.liveSession;
              if (!live) return "";
              const input = q('[data-screen="live"] [data-role="live-input"]');
              const textValueClean = String(textValue || input?.value || "").trim();
              if (!textValueClean) return "";
              const liveKey = String(live.id || live.ownerId || "live");
              const selectedIdentityId = String(q('[data-screen="live"] [data-role="live-identity-select"]')?.value || state.liveInteractionIdentity?.[liveKey] || state.viewerIdentityId || "");
              const identity = state.identities.find(x => String(x.id) === selectedIdentityId) || state.identities.find(x => String(x.id) === String(state.viewerIdentityId)) || state.identities[0];
              if (identity) state.liveInteractionIdentity[liveKey] = String(identity.id);
              state.liveChat.push({ user: identity?.handle || "@user", avatar: identity?.avatar || "U", text: textValueClean, translation: "", isUser: true, identityId: identity?.id || "" });
              state.pendingLiveUserMessage = { liveId: String(live.id || "live"), text: textValueClean, identityId: identity?.id || "", at: Date.now() };
              if (input) input.value = "";
              await persist();
              renderLiveChatLines();
              return textValueClean;
            };

            const summonLive = async () => {
              const live = state.liveSession;
              if (!live) return;
              const liveKey = String(live.id || live.ownerId || "live");
              if (liveReplyPending.has(liveKey)) return;
              const inputBeforeBusy = q('[data-screen="live"] [data-role="live-input"]');
              const typed = String(inputBeforeBusy?.value || "").trim();
              liveReplyPending.add(liveKey);
              openLive({ ...live });
              try {
              let userText = "";
              if (typed) userText = await sendLiveChatOnly(typed);
              if (!userText && state.pendingLiveUserMessage && String(state.pendingLiveUserMessage.liveId || "") === String(live.id || "")) {
                userText = String(state.pendingLiveUserMessage.text || "");
              }
              state.pendingLiveUserMessage = null;
              await advanceLive({ fromUser: Boolean(userText), userText });
              } finally { liveReplyPending.delete(liveKey); if (state.liveSession && String(state.liveSession.id || state.liveSession.ownerId || "live") === liveKey) openLive({ ...state.liveSession }); }
            };

            const deleteLiveChat = async (indexValue) => {
              const index = Number(indexValue);
              if (!Number.isInteger(index) || index < 0 || index >= state.liveChat.length) return;
              state.liveChat.splice(index, 1);
              await persist();
              renderLiveChatLines();
              toast("聊天室消息已删除");
            };

            const setHostInputMode = (mode) => {
              const screen = q('[data-screen="live"]');
              const normalized = mode === "action" ? "action" : "speech";
              screen?.querySelectorAll('[data-host-input-mode]').forEach(btn => btn.classList.toggle("is-active", btn.dataset.hostInputMode === normalized));
              const input = screen?.querySelector('[data-role="host-live-input"]');
              if (input) {
                input.dataset.mode = normalized;
                input.placeholder = normalized === "action" ? "输入你在镜头里的动作…" : "输入你在直播里说的话…";
              }
            };

            const sendHostStageInput = async () => {
              const live = state.liveSession;
              if (!live || !isOwnLiveSession(live)) return;
              const input = q('[data-screen="live"] [data-role="host-live-input"]');
              const text = String(input?.value || "").trim();
              if (!text) { toast("先输入说话或动作"); return; }
              const mode = String(input?.dataset.mode || "speech") === "action" ? "action" : "speech";
              const progress = getLiveProgress(live);
              const at = Date.now();
              progress.userStageEvent = { text, mode, at };
              pushLiveTimelineEvent(progress, {
                actorType: "user",
                actorId: String(live.ownerId || state.viewerIdentityId || "user"),
                actorName: String(live.name || "user"),
                action: mode === "action" ? text : "",
                speech: mode === "speech" ? text : "",
                translation: "",
                at
              });
              state.pendingOwnLiveInput = { liveId: String(live.id || "live"), text, mode, at };
              if (input) input.value = "";
              await persist();
              renderLiveMoment();
            };

            const summonHostLive = async () => {
              const live = state.liveSession;
              if (!live || !isOwnLiveSession(live)) return;
              const liveKey = String(live.id || live.ownerId || "live");
              if (liveReplyPending.has(liveKey)) return;
              liveReplyPending.add(liveKey);
              openLive({ ...live });
              try {
              const pending = state.pendingOwnLiveInput && String(state.pendingOwnLiveInput.liveId || "") === String(live.id || "") ? state.pendingOwnLiveInput : null;
              const text = pending ? String(pending.text || "") : "";
              state.pendingOwnLiveInput = null;
              await advanceLive({ fromUser: Boolean(text), userText: text });
              } finally { liveReplyPending.delete(liveKey); if (state.liveSession && String(state.liveSession.id || state.liveSession.ownerId || "live") === liveKey) openLive({ ...state.liveSession }); }
            };

            const advanceLive = async ({ fromUser = false, userText = "" } = {}) => {
              const live = state.liveSession;
              if (!live) return;
              const progress = getLiveProgress(live);
              const own = isOwnLiveSession(live);
              const lang = progress.language || detectLiveLanguage(live);
              const channel = state.channels.find(x => String(x.id) === String(live.ownerId || live.id || "")) || null;
              const persona = channel ? String(getChannelPersonaText(channel) || "").slice(0, 1800) : "";
              const guests = getLiveGuests(live);
              const recentTimeline = safeArray(progress.timeline).slice(-10).map(event => ({ actorName:event.actorName || "", action:event.action || "", speech:event.speech || "" }));
              const recentChat = safeArray(state.liveChat).slice(-16).map(line => ({ user:line.user || "", text:line.text || "" }));
              const activity = Math.max(0, Math.min(100, Number(state.platformSettings?.activityLevel ?? 50)));
              const expectedChat = activity < 30 ? 5 : activity > 75 ? 10 : 7;
              const allowedActors = own
                ? guests.map(g => ({ id:String(g.id), name:String(g.name || "嘉宾") }))
                : [{ id:String(live.ownerId || live.id || "host"), name:String(live.name || live.handle || "主播") }, ...guests.map(g => ({ id:String(g.id), name:String(g.name || "嘉宾") }))];
              const userContext = String(userText || "").trim();
              const system = `${VELA_DEFAULT_PRESET}

你正在继续一场已经进行中的 Vela 直播，不是重新开播。直播标题：${live.title || ""}；内容：${live.category || ""}；主要语言：${lang}；主播公开名：${live.name || live.handle || "主播"}。${live.creatorProfile ? `陌生主播风格：${live.creatorProfile}。` : ""}${persona ? `角色设定：${persona}` : ""}
${own ? "这是一场 USER 自己开的直播：绝对不能替 USER 说话或做动作。只能让嘉宾和观众继续；若没有嘉宾，events 可以为空。" : "主播与嘉宾可以自然继续动作和说话。"}
允许发言/动作的非 USER 主体：${JSON.stringify(allowedActors)}。
最近直播时间线：${JSON.stringify(recentTimeline)}
最近聊天室：${JSON.stringify(recentChat)}
${nsfwPresetPrompt(live.ageRequirement === "18+" || live.nsfw === true)}
${fromUser && userContext ? `USER 刚刚在直播里输入了：${userContext}。主播/嘉宾可以看到，也可以按人设自然略过。` : "USER 本轮没有新增输入，只是继续观看。"}

陌生观众语言倾向：${languagePreferencePrompt()}\n${translationRulePrompt()}\n返回严格 JSON 对象：{"endLive":false,"events":[{"actorId":"必须来自允许主体 id","actorName":"显示名","action":"自然动作，可空","speech":"实际说的话，可空；使用主要语言","translation":"按翻译规则填写"}],"chat":[{"user":"各不相同的观众网名","text":"原语言弹幕","translation":"按翻译规则填写","tipCoin":0,"giftCoin":0}]}。\n${own ? "这是 USER 自己的直播，endLive 必须为 false，不能替 USER 决定下播。" : "这是非 USER 主播的直播；如果当前话题和直播进度已经自然收尾，可以低概率设置 endLive=true，让主播自己结束直播，不要每几轮就结束。"}\n要求：events 0~${Math.max(1, Math.min(3, allowedActors.length))} 条，chat 约 ${expectedChat} 条。Vela 会把本批聊天室消息放进懒加载队列，像真实直播弹幕一样逐条滚出来，所以每条都应能独立成立，不要写成必须瞬间连续显示的脚本；不要复用固定网名或固定动作模板；giftCoin/tipCoin 只能偶尔出现，数值应自然。`;
              const aiText = await runVelaAI([{ role:"system", content:system }, { role:"user", content:"继续这一场直播。" }]);
              const data = extractJSON(aiText);
              if (!data || typeof data !== "object" || Array.isArray(data)) { toast("直播继续生成失败，请再试一次"); return; }
              const validActorIds = new Set(allowedActors.map(item => String(item.id)));
              const events = safeArray(data.events).slice(0, 3).filter(event => validActorIds.has(String(event?.actorId || "")) && (String(event?.action || "").trim() || String(event?.speech || "").trim()));
              const chat = safeArray(data.chat).slice(0, 12).map((item,index) => ({
                user:String(item?.user || "").trim(), avatar:STRANGER_AVATAR, text:String(item?.text || "").trim(), translation:String(item?.translation || "").trim(), tipCoin:Math.max(0, Math.round(Number(item?.tipCoin || 0))), giftCoin:Math.max(0, Math.round(Number(item?.giftCoin || 0))), at:Date.now()+index
              })).filter(item => item.user && item.text);
              if (!events.length && !chat.length) { toast("直播继续生成失败，请再试一次"); return; }
              progress.step = Number(progress.step || 0) + 1;
              progress.language = lang;
              events.forEach((event,index) => {
                const actor = allowedActors.find(item => String(item.id) === String(event.actorId));
                const normalized = { actorType:"character", actorId:String(event.actorId), actorName:String(event.actorName || actor?.name || "主播"), action:String(event.action || "").trim(), speech:String(event.speech || "").trim(), translation:String(event.translation || "").trim(), at:Date.now()+index };
                pushLiveTimelineEvent(progress, normalized);
                if (normalized.speech || normalized.action) { progress.text = normalized.speech || normalized.action; progress.translation = normalized.translation || ""; }
              });
              const liveKey = String(live.id || live.ownerId || "live");
              state.liveChatQueues[liveKey] = safeArray(state.liveChatQueues?.[liveKey]);
              let cursor = Date.now() + 550;
              chat.forEach((item,index) => { if (index > 0) cursor += Math.round((0.8 + Math.random()*2.7) * 1000); const {giftCoin,...row} = item; state.liveChatQueues[liveKey].push({ ...row, id:`liveq-${Date.now().toString(36)}-${index}-${Math.random().toString(36).slice(2,5)}`, plannedAt:cursor }); });
              const stats = getLiveStats(live);
              stats.tipReceivedCoin = Number(stats.tipReceivedCoin || 0) + chat.reduce((sum,item) => sum + Number(item.tipCoin || 0), 0);
              chat.forEach(item => {
                const giftCoin = Math.max(0, Number(item.giftCoin || 0));
                if (!giftCoin) return;
                stats.gifts.push({ user:item.user, amount:giftCoin });
                stats.giftReceivedCoin = Number(stats.giftReceivedCoin || 0) + giftCoin;
                state.liveChat.push({ user:"系统", avatar:"🎁", text:`${item.user} 送出礼物 · 🔷${giftCoin}`, translation:"", at:Date.now()+10+Math.random() });
              });
              advanceViewerCount(live, { heatBoost: fromUser ? 2 : 0 });
              flushLiveChatQueue(liveKey); armLiveLazyTimer(liveKey); renderLiveMoment(); renderLiveChatLines(); renderLiveHUD(); await persist();
              if (!own && data.endLive === true) { await endCurrentLive({ automatic:true }); }
            };

            const getPostById = (postId) => {
              const id = String(postId || "");
              const snapshotEntry = Object.entries(state.homeFeedByChannel || {}).find(([, post]) => String(post?.id || "") === id);
              if (snapshotEntry) {
                const [channelId, snapshot] = snapshotEntry;
                const channel = state.channels.find(ch => String(ch.id) === String(channelId));
                return {
                  ...snapshot,
                  ...(state.postOverrides?.[id] || {}),
                  ownerType:"channel",
                  ownerId:String(channelId),
                  name:channel?.name || channel?.handle || "Vela Channel",
                  handle:channel?.handle || "@vela",
                  avatar:channel?.avatar || "V",
                  comments:safeArray(state.postReplies?.[id]).length
                };
              }
              const saved = state.communityPosts.find(p => String(p.id) === id);
              if (!saved) return null;
              const entity = getProfileEntity(saved.ownerType || "identity", saved.ownerId || "");
              return {
                ...saved,
                ...(state.postOverrides?.[id] || {}),
                name: profileName(entity, saved.ownerType || "identity"),
                handle: entity?.handle || "@vela",
                avatar: entity?.avatar || "V",
                comments: safeArray(state.postReplies?.[id]).length
              };
            };

            const renderPostComments = (postId) => {
              const id = String(postId || "");
              const host = q(`[data-inline-comment-list="${CSS.escape(id)}"]`) || q('[data-screen="post"] [data-role="post-comments"]');
              if (!host) return;
              const replies = safeArray(state.postReplies?.[id]);
              host.innerHTML = replies.length ? replies.map((reply, index) => {
                const trId = `post-comment-${id}-${String(reply.id || index)}`;
                const translateTool = reply.translation ? `<button class="v-comment-tool" data-action="toggle-translation" data-translation-target="${escapeHTML(trId)}">翻译</button>` : "";
                const translationBody = reply.translation ? `<div class="v-translation v-comment-translation" data-translation-id="${escapeHTML(trId)}" hidden>${escapeHTML(reply.translation)}</div>` : "";
                return `<div class="v-comment"><div class="v-social-avatar">${avatarHTML(reply.avatar || initials(reply.user || "V"), reply.user || "V")}</div><div class="v-comment-body"><div class="v-comment-name">${escapeHTML(reply.user || "viewer")}</div>${reply.replyTo ? `<div class="v-comment-replying">回复 ${escapeHTML(reply.replyTo)}</div>` : ""}<div class="v-comment-text">${escapeHTML(reply.text || "")}</div><div class="v-comment-tools"><button class="v-comment-tool" data-action="reply-post-comment" data-post-id="${escapeHTML(id)}" data-comment-index="${index}">回复</button>${translateTool}<button class="v-comment-tool" data-action="delete-post-comment" data-post-id="${escapeHTML(id)}" data-comment-index="${index}">删除</button></div>${translationBody}</div></div>`;
              }).join("") : "";
              const post = getPostById(id);
              const displayCount = post ? postDisplayCommentCount(post) : replies.length;
              const counter = q('[data-screen="post"] [data-role="post-comment-count"]');
              if (counter) counter.textContent = String(displayCount);
              const inlineCounter=q(`[data-inline-count="${CSS.escape(id)}"]`); if(inlineCounter) inlineCounter.textContent=String(displayCount);
              const moreButtons = qa(`[data-action="summon-post-replies"][data-post-id="${CSS.escape(id)}"].v-more-comments`);
              moreButtons.forEach(btn => { btn.hidden = replies.length === 0; btn.textContent = "再加载 5 条"; });
            };

            const openPostDetail = (postId) => {
              const post = getPostById(postId);
              if (!post) return;
              const imageHTML = isImageURL(post.image || "") ? `<img src="${escapeHTML(post.image)}" alt="">` : "";
              const trId = `post-detail-${String(post.id)}`;
              const saved = state.communityPosts.find(p => String(p.id) === String(post.id));
              const canDeletePost = Boolean(saved && saved.ownerType === "identity" && String(saved.ownerId || "") === String(state.viewerIdentityId || ""));
              const mode = String(post.accessMode || "public");
              const age = String(post.ageRequirement || "none");
              const accessLabel = mode === "free" ? `免费订阅${age === "18+" ? " · 18+" : ""}` : mode === "paid" ? `付费订阅${age === "18+" ? " · 18+" : ""}` : (age === "18+" ? "18+" : "");
              const accessClass = mode === "free" ? " is-free" : mode === "paid" ? " is-paid" : (age === "18+" ? " is-age" : "");
              const channelId = String(post.ownerType || "") === "channel" ? String(post.ownerId || "") : "";
              let eligible = channelId ? identitiesWithAccessToChannel(channelId, mode) : state.identities.slice();
              if (!eligible.length) eligible = state.identities.filter(x => String(x.id) === String(state.viewerIdentityId));
              const rememberedId = String(state.postInteractionIdentity?.[String(post.id)] || "");
              const selectedIdentity = eligible.find(x => String(x.id) === rememberedId) || eligible.find(x => String(x.id) === String(state.viewerIdentityId)) || eligible[0];
              if (selectedIdentity) state.postInteractionIdentity[String(post.id)] = String(selectedIdentity.id);
              const identityPicker = identityPickerHTML(eligible, selectedIdentity?.id, "post-identity-select", `data-post-id="${escapeHTML(post.id)}"`);
              const badge = accessLabel ? `<span class="v-access-badge${accessClass}">${escapeHTML(accessLabel)}</span>` : "";
              openScreen("post", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="post">‹</button><div class="v-meta"><strong>贴文</strong><div class="v-hint">${escapeHTML(post.name || "频道")}</div></div></header><div class="v-subbody"><article class="v-card v-post-card v-post-detail-card"><div class="v-social-head v-post-head"><div class="v-social-avatar">${avatarHTML(post.avatar || "", post.name || "V")}</div><div class="v-social-copy"><div class="v-post-author-line"><strong>${escapeHTML(post.name || "频道")}</strong><span class="v-post-author-handle">${escapeHTML(post.handle || "")}</span>${badge}</div><div class="v-post-time">${escapeHTML(post.at ? formatRelativeAt(post.at) : (post.time || "刚刚"))}</div></div><button class="v-post-more" data-action="toggle-post-menu" data-post-id="${escapeHTML(post.id)}">⋯</button><div class="v-post-menu" data-post-menu="${escapeHTML(post.id)}"><button data-action="edit-post" data-post-id="${escapeHTML(post.id)}">编辑</button>${canDeletePost ? `<button class="is-delete" data-action="delete-community-post" data-post-id="${escapeHTML(post.id)}">删除本条贴文</button>` : ""}</div></div><div class="v-social-text">${escapeHTML(post.text || "")}</div>${translationHTML(trId, post.translation || "")}${imageHTML ? `<div class="v-social-media">${imageHTML}</div>` : ""}<div class="v-social-actions v-post-engagement"><button data-action="like-post" data-post-id="${escapeHTML(post.id)}">${postHeartIconHTML()}<span>${postDisplayLikes(post)}</span></button><button type="button">${postCommentIconHTML()}<span data-role="post-comment-count">${postDisplayCommentCount(post)}</span></button></div><div class="v-comment-list" data-role="post-comments"></div><button class="v-more-comments" data-action="summon-post-replies" data-post-id="${escapeHTML(post.id)}" hidden>再加载 5 条</button><div class="v-post-composer">${identityPicker}<input data-role="post-comment-input" data-post-id="${escapeHTML(post.id)}" placeholder="写下评论… · Enter发送" maxlength="240"><button class="v-summon-mini" data-action="summon-post-replies" data-post-id="${escapeHTML(post.id)}" aria-label="召唤回复" title="召唤回复">${planeIconHTML()}</button></div></article></div>`);
              renderPostComments(post.id);
            };

            const openEditPostEditor = (postId) => {
              const post = getPostById(postId);
              if (!post) return;
              openScreen("post-editor", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="post-editor">‹</button><div class="v-meta"><strong>编辑贴文</strong><div class="v-hint">${escapeHTML(post.name || "Vela Channel")}</div></div><button class="v-head-action" data-action="save-post-edit" data-post-id="${escapeHTML(post.id)}">保存</button></header><div class="v-subbody"><div class="v-card"><div class="v-field"><label>贴文内容</label><textarea data-post-edit-field="text" maxlength="1000" placeholder="写点什么……">${escapeHTML(post.text || "")}</textarea></div><div class="v-field" style="margin-top:12px"><label>配图 URL（可留空）</label><input data-post-edit-field="image" value="${escapeHTML(post.image || "")}" placeholder="https://..."></div><div class="v-field" style="margin-top:12px"><label>折叠翻译（可留空）</label><textarea data-post-edit-field="translation" maxlength="1000" placeholder="没有翻译可以留空">${escapeHTML(post.translation || "")}</textarea></div><p class="v-hint">编辑只修改这条 Vela 贴文，不会清空原有点赞、评论或发布时间。</p></div></div>`);
            };

            const saveEditedPost = async (postId) => {
              const id = String(postId || "");
              const screen = q('[data-screen="post-editor"]');
              const text = String(screen?.querySelector('[data-post-edit-field="text"]')?.value || "").trim();
              const image = String(screen?.querySelector('[data-post-edit-field="image"]')?.value || "").trim();
              const translation = String(screen?.querySelector('[data-post-edit-field="translation"]')?.value || "").trim();
              if (!text) { toast("贴文内容不能为空"); return; }
              state.postOverrides[id] = { ...(state.postOverrides[id] || {}), text, image, translation };
              await persist();
              closeScreen("post-editor");
              renderHome();
              openPostDetail(id);
              toast("贴文已更新");
            };

            const getProfileEntity = (ownerType, id) => {
              if (ownerType === "identity") return state.identities.find(x => String(x.id) === String(id)) || null;
              return state.channels.find(x => String(x.id) === String(id)) || null;
            };

            const scheduleIdentityKey = item => String(item?.scheduleId || (item?.sourceMessageId && item?.sourceCardId ? `card:${item.sourceMessageId}:${item.sourceCardId}` : ""));
            const scheduledLiveList = entity => {
              if (!entity) return [];
              let rows = safeArray(entity.scheduledLives).filter(item => item && typeof item === "object");
              if (entity.scheduledLive && typeof entity.scheduledLive === "object") {
                const legacy = { ...entity.scheduledLive };
                if (!legacy.scheduleId) legacy.scheduleId = scheduleIdentityKey(legacy) || `schedule-${String(entity.id || "host")}-${Number(legacy.scheduledAt || Date.now()).toString(36)}`;
                const legacyKey = scheduleIdentityKey(legacy);
                if (!rows.some(item => scheduleIdentityKey(item) === legacyKey)) rows.push(legacy);
              }
              const seen = new Set();
              rows = rows.map((item,index) => ({ ...item, scheduleId:String(item.scheduleId || scheduleIdentityKey(item) || `schedule-${String(entity.id || "host")}-${Number(item.scheduledAt || Date.now()).toString(36)}-${index}`) }))
                .filter(item => { const key=scheduleIdentityKey(item); if(seen.has(key)) return false; seen.add(key); return true; })
                .sort((a,b) => Number(a.scheduledAt || Number.MAX_SAFE_INTEGER) - Number(b.scheduledAt || Number.MAX_SAFE_INTEGER));
              entity.scheduledLives = rows;
              const now = Date.now();
              entity.scheduledLive = rows.find(item => !Number(item.scheduledAt || 0) || !Number(item.endsAt || 0) || Number(item.endsAt || 0) >= now) || null;
              return rows;
            };
            const upsertScheduledLive = (entity, item) => {
              if (!entity || !item) return null;
              const rows = scheduledLiveList(entity);
              const normalized = { ...item, scheduleId:String(item.scheduleId || scheduleIdentityKey(item) || `schedule-${String(entity.id || "host")}-${Date.now().toString(36)}`) };
              const key = scheduleIdentityKey(normalized);
              const index = rows.findIndex(row => scheduleIdentityKey(row) === key);
              if (index >= 0) rows[index] = normalized; else rows.push(normalized);
              entity.scheduledLives = rows.sort((a,b)=>Number(a.scheduledAt || Number.MAX_SAFE_INTEGER)-Number(b.scheduledAt || Number.MAX_SAFE_INTEGER));
              scheduledLiveList(entity);
              return normalized;
            };
            const removeScheduledLive = (entity, scheduleId) => {
              if (!entity) return null;
              const rows = scheduledLiveList(entity);
              const index = rows.findIndex(item => String(item.scheduleId || "") === String(scheduleId || ""));
              if (index < 0) return null;
              const [removed] = rows.splice(index,1);
              entity.scheduledLives = rows; entity.scheduledLive = null; scheduledLiveList(entity);
              return removed;
            };
            const nearestScheduledLive = entity => {
              const now = Date.now();
              return scheduledLiveList(entity).find(item => !Number(item.scheduledAt || 0) || !Number(item.endsAt || 0) || Number(item.endsAt || 0) >= now) || null;
            };

            const profileName = (entity, ownerType) => ownerType === "identity"
              ? String(entity?.displayName || "Vela User")
              : String(entity?.name || "频道");

            const subscriptionProfileKey = (ownerType, id) => `${String(ownerType)}:${String(id)}`;

            const getSubscriptionProfile = (ownerType, id) => {
              const key = subscriptionProfileKey(ownerType, id);
              const entity = getProfileEntity(ownerType, id);
              const name = profileName(entity, ownerType);
              const current = state.subscriptionProfiles?.[key] || {};
              const normalized = {
                enabled: current.enabled === true,
                free: {
                  emoji: "🌊",
                  name: "支持者",
                  price: 0,
                  cover: "",
                  intro: "关注频道动态，解锁免费订阅贴文与直播推送。",
                  benefits: ["获得粉丝身份"],
                  ...(current.free || {})
                },
                paid: {
                  emoji: "💎",
                  name: "会员",
                  coinPrice: 500,
                  cover: "",
                  intro: "支持喜欢的主播，解锁付费订阅内容与社群。",
                  benefits: ["获得付费粉丝身份", "加入订阅社群", "获得主播第一手资讯"],
                  communityName: `${name} · 会员社群`,
                  ...(current.paid || {})
                }
              };
              normalized.free.intro = "关注频道动态，解锁免费订阅贴文与直播推送。";
              normalized.free.benefits = ["获得粉丝身份"];
              normalized.paid.intro = "支持喜欢的主播，解锁付费订阅内容与社群。";
              normalized.paid.benefits = ["获得付费粉丝身份", "加入订阅社群", "获得主播第一手资讯"];
              delete normalized.free.communityName;
              if (normalized.paid.coinPrice == null) {
                normalized.paid.coinPrice = current.paid?.price != null ? Math.max(1, Math.round(Number(current.paid.price || 0) * 10)) : 500;
              }
              normalized.paid.coinPrice = Math.max(1, Math.round(Number(normalized.paid.coinPrice || 500)));
              delete normalized.paid.price;
              state.subscriptionProfiles[key] = normalized;
              return normalized;
            };
            const subscriptionEnabled = (ownerType, id) => getSubscriptionProfile(ownerType, id).enabled === true;

            const renderMembershipCard = (ownerType, entity, tier, cfg, currentTier) => {
              const paid = tier === "paid";
              const cover = isImageURL(cfg.cover || "") ? `<img src="${escapeHTML(cfg.cover)}" alt="">` : escapeHTML(cfg.emoji || (paid ? "💎" : "🌊"));
              const benefits = safeArray(cfg.benefits).filter(Boolean).map(item => `<div>${escapeHTML(item)}</div>`).join("");
              let button = "";
              if (ownerType === "channel") {
                const isCurrent = paid ? currentTier === "paid" : (currentTier === "free" || currentTier === "paid");
                const label = paid ? (currentTier === "paid" ? "当前订阅" : currentTier === "free" ? "升级付费" : "付费订阅") : (isCurrent ? "已解锁" : "免费订阅");
                button = `<button class="v-membership-button ${isCurrent ? "is-current" : ""}" data-action="subscribe-channel" data-channel-id="${escapeHTML(entity.id)}" data-tier="${tier}">${label}</button>`;
              }
              return `<article class="v-membership-card ${paid ? "is-paid" : ""}"><div class="v-membership-cover">${cover}</div><div class="v-membership-body"><div class="v-membership-title">${escapeHTML(cfg.name || (paid ? "会员" : "支持者"))}</div><div class="v-membership-price">${paid ? `🔷${Number(cfg.coinPrice || 0).toLocaleString("zh-CN")}` : "免费"}${paid ? "<small>/月</small>" : ""}</div>${button}<div class="v-membership-intro">${escapeHTML(cfg.intro || "")}</div>${benefits ? `<div class="v-membership-benefits">${benefits}</div>` : ""}${paid ? `<div class="v-membership-community">会员社群<b>${escapeHTML(cfg.communityName || "会员社群")}</b></div>` : ""}</div></article>`;
            };

            const renderProfileTab = (ownerType, id, tab = "activity") => {
              const entity = getProfileEntity(ownerType, id);
              const host = q('[data-screen="profile"] [data-role="profile-tab-body"]');
              if (!entity || !host) return;
              if (tab === "live" || tab === "posts") tab = "activity";
              qa('[data-screen="profile"] [data-profile-tab]').forEach(btn => btn.classList.toggle('is-active', btn.dataset.profileTab === tab));

              if (tab === "activity") {
                const blocks = [];

                const nearestAppointment = nearestScheduledLive(entity);
                if (nearestAppointment) {
                  const scheduledInner = `<div class="v-kicker">置顶 · 最近预约</div><div class="v-live-title">${escapeHTML(nearestAppointment.title || "预约直播")}</div><div class="v-sub" style="margin-top:6px">${escapeHTML(nearestAppointment.dateText || "未设置时间")}${nearestAppointment.sourceType ? ` · 来自${nearestAppointment.sourceType === "business" ? "商务合作" : "直播邀约"}` : ""}</div>`;
                  blocks.push(`<article class="v-activity-pin is-scheduled"><button class="v-schedule-entry-hit" data-action="open-scheduled-live" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(entity.id)}">${scheduledInner}<span class="v-schedule-entry-more">查看全部预约 ›</span></button></article>`);
                }

                if (ownerType === "identity" && entity.activeLive) {
                  const active = entity.activeLive;
                  const stats = getLiveStats(active);
                  blocks.push(`<article class="v-activity-pin is-live"><button data-action="open-own-profile-live" data-identity-id="${escapeHTML(entity.id)}"><div class="v-kicker">正在直播</div><div class="v-live-title">${escapeHTML(active.title || "正在直播")}</div><div class="v-sub" style="margin-top:6px">${formatViewers(stats.currentViewers || 0)} 人正在观看 · ${escapeHTML(active.category || "公开直播")}</div></button><div class="v-inline-manage"><button data-action="open-own-profile-live" data-identity-id="${escapeHTML(entity.id)}">进入直播</button></div></article>`);
                } else if (entity.live) {
                  blocks.push(`<article class="v-activity-pin is-live"><button data-action="open-live" data-live-id="${escapeHTML(entity.id)}"><div class="v-kicker">正在直播</div><div class="v-live-title">${escapeHTML(entity.title || "正在直播")}</div><div class="v-sub" style="margin-top:6px">${formatViewers(entity.viewers || 0)} 人正在观看</div></button></article>`);
                }

                const ownPosts = state.communityPosts.filter(p => String(p.ownerType || "") === String(ownerType) && String(p.ownerId || "") === String(id)).map(p => getPostById(p.id)).filter(Boolean);
                const snap = ownerType === "channel" ? state.homeFeedByChannel?.[String(id)] : null;
                const sample = ownerType === "channel" && snap ? getPostById(snap.id) : null;
                const postItems = [sample, ...ownPosts].filter(Boolean).filter(post => {
                  if (ownerType !== "channel") return true;
                  const mode = String(post.accessMode || "public");
                  return mode === "public" || identitiesWithAccessToChannel(id, mode).length > 0;
                }).sort((a,b) => Number(b?.at || 0) - Number(a?.at || 0));
                if (postItems.length) {
                  const cards = postItems.map(post => {
                    const isOwnedUserPost = ownerType === "identity"
                      && String(post.ownerType || "identity") === "identity"
                      && String(post.ownerId || id) === String(state.viewerIdentityId || "");
                    return renderSocialPostCard(
                      { ...post, comments: safeArray(state.postReplies?.[String(post.id)]).length },
                      { canDelete: isOwnedUserPost }
                    );
                  }).join("");
                  blocks.push(`<div class="v-activity-posts">${cards}</div>`);
                }

                host.innerHTML = blocks.length
                  ? `<div class="v-activity-stack">${blocks.join("")}</div>`
                  : `<div class="v-profile-empty">这里还没有发布过动态。</div>`;
                return;
              }

              if (tab === "replays") {
                const replayItems = state.liveReplays.filter(item => String(item.ownerType || "") === String(ownerType) && String(item.ownerId || "") === String(id));
                host.innerHTML = replayItems.length ? replayItems.map(item => `<article class="v-replay-card"><button style="all:unset;display:block;width:100%;cursor:pointer" data-action="open-replay" data-replay-id="${escapeHTML(item.id)}"><div class="v-name">${escapeHTML(item.title || "直播回放")}</div><div class="v-sub">${escapeHTML(item.endedText || "刚刚结束")} · ${escapeHTML(item.topic || "直播")}</div><div class="v-replay-cover">${isImageURL(item.cover || "") ? `<img src="${escapeHTML(item.cover)}" alt="">` : "▶"}</div></button><div class="v-replay-actions"><span>最高 ${formatViewers(item.peakViewers || 0)} 人观看 · ${safeArray(item.participants).length + 1} 人出镜</span><button data-action="delete-replay" data-replay-id="${escapeHTML(item.id)}">删除回放</button></div></article>`).join("") : `<div class="v-profile-empty">还没有直播回放。</div>`;
                return;
              }

              if (ownerType === "identity") {
                const subscribed = Object.entries(getIdentitySubscriptions(id))
                  .filter(([, tier]) => tier === "free" || tier === "paid")
                  .map(([channelId, tier]) => {
                    const channel = state.channels.find(ch => String(ch.id) === String(channelId));
                    return channel ? { channel, tier } : null;
                  })
                  .filter(Boolean);

                const cards = subscribed.map(({ channel, tier }) => {
                  const cfg = getSubscriptionProfile("channel", channel.id);
                  const tierCfg = cfg[tier];
                  const paid = tier === "paid";
                  const label = paid
                    ? `${escapeHTML(tierCfg.emoji || "💎")} ${escapeHTML(tierCfg.name || "付费订阅")}`
                    : `${escapeHTML(tierCfg.emoji || "🌊")} ${escapeHTML(tierCfg.name || "免费订阅")}`;
                  return `<article class="v-my-sub-card"><div class="v-my-sub-head"><div class="v-social-avatar">${avatarHTML(channel.avatar || "", channel.name || "V")}</div><div class="v-meta"><div class="v-my-sub-title">${escapeHTML(channel.name || channel.handle || "频道")}</div><div class="v-my-sub-handle">${escapeHTML(channel.handle || "")}</div></div><span class="v-my-sub-tier ${paid ? "is-paid" : "is-free"}">${label}</span></div><div class="v-my-sub-detail"><div>当前方案 <b>${escapeHTML(tierCfg.name || (paid ? "付费订阅" : "免费订阅"))}${paid ? ` · 🔷${Number(tierCfg.coinPrice || 0).toLocaleString("zh-CN")}/月` : ""}</b></div>${paid ? `<div>会员社群 <b>${escapeHTML(tierCfg.communityName || "会员社群")}</b></div>` : `<div>权限 <b>免费限定贴文 / 直播</b></div>`}</div><div class="v-my-sub-actions"><button class="v-continue-sub" data-action="continue-subscription" data-channel-id="${escapeHTML(channel.id)}">继续订阅</button><button class="v-cancel-sub" data-action="cancel-subscription" data-channel-id="${escapeHTML(channel.id)}">取消订阅</button></div></article>`;
                }).join("");

                const ownCfg = getSubscriptionProfile("identity", id);
                const ownPlan = ownCfg.enabled
                  ? `<div class="v-membership-grid v-own-plan-grid">${renderMembershipCard("identity", entity, "free", ownCfg.free, "none")}${renderMembershipCard("identity", entity, "paid", ownCfg.paid, "none")}</div>`
                  : `<div class="v-subscription-off"><b>当前未开启订阅计划</b><span>这个账号现在是普通频道，不会显示订阅档位或会员社群。</span></div>`;
                host.innerHTML = `<div class="v-own-subscription-tools"><div><h3>我的频道方案</h3><p>${ownCfg.enabled ? "订阅计划已开启。" : "默认关闭；需要时再开启。"}</p></div><button data-action="open-subscription-settings" data-owner-type="identity" data-owner-id="${escapeHTML(id)}">${ownCfg.enabled ? "管理方案" : "开启订阅计划"}</button></div>${ownPlan}<div class="v-own-subscription-tools v-subscribed-list-head"><div><h3>我的订阅</h3><p>这里显示你已经订阅的主播。</p></div></div>${cards ? `<div class="v-my-subscriptions">${cards}</div>` : `<div class="v-profile-empty">还没有订阅任何主播。<br>去喜欢的主播主页「订阅」里选择方案。</div>`}`;
                return;
              }

              const cfg = getSubscriptionProfile(ownerType, id);
              const currentTier = getSubscriptionTierFor(state.viewerIdentityId, id);
              const canEdit = Boolean(entity.sourceCharacterId);
              if (!cfg.enabled) {
                host.innerHTML = `<div class="v-subscription-head"><div><h3>订阅</h3><p>这个频道目前没有开启订阅计划。</p></div>${canEdit ? `<button data-action="open-subscription-settings" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(id)}">开启订阅计划</button>` : ""}</div><div class="v-subscription-off"><b>普通频道</b><span>开启订阅计划后，才会出现免费订阅 / 付费订阅档位与会员社群。</span></div>`;
                return;
              }
              let currentText = "暂未订阅";
              let currentNote = "关注关系与订阅关系彼此独立；只有付费订阅才会加入会员社群。";
              if (currentTier === "paid") {
                currentText = `${cfg.paid.emoji || "💎"} ${cfg.paid.name || "付费订阅"}`;
                currentNote = `你已解锁全部订阅内容，并加入 ${cfg.paid.communityName || "会员社群"}。`;
              } else if (currentTier === "free") {
                currentText = `${cfg.free.emoji || "🌊"} ${cfg.free.name || "免费订阅"}`;
                currentNote = "你已解锁免费订阅贴文与直播推送；会员社群仍需付费订阅。";
              }
              host.innerHTML = `<div class="v-subscription-head"><div><h3>订阅</h3><p>选择这个主播的订阅方案。</p></div>${canEdit ? `<button data-action="open-subscription-settings" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(id)}">编辑方案</button>` : ""}</div><div class="v-membership-grid">${renderMembershipCard(ownerType, entity, "free", cfg.free, currentTier)}${renderMembershipCard(ownerType, entity, "paid", cfg.paid, currentTier)}</div><div class="v-your-membership"><div class="v-your-membership-label">你的订阅</div><div class="v-your-membership-value">${escapeHTML(currentText)}</div><div class="v-your-membership-note">${escapeHTML(currentNote)}</div></div>`;
            };

            const openCommunityPostEditor = (ownerType, ownerId) => {
              const entity = getProfileEntity(ownerType, ownerId);
              if (!entity) return;
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              if (!sheet || !host) return;
              const segments = (field, items, selected, disabledValues = []) => `<div class="v-live-segments v-post-segments" data-post-choice-group="${escapeHTML(field)}">${items.map(([value,label]) => `<button type="button" class="${value === selected ? "is-selected" : ""} ${disabledValues.includes(value) ? "is-disabled" : ""}" data-action="choose-post-option" data-post-field="${escapeHTML(field)}" data-post-value="${escapeHTML(value)}" ${disabledValues.includes(value) ? "disabled" : ""}>${escapeHTML(label)}</button>`).join("")}</div><input type="hidden" data-post-field="${escapeHTML(field)}" value="${escapeHTML(selected)}">`;
              host.innerHTML = `<div class="v-publish-sheet-title"><h3>发布贴文</h3><button data-action="render-publish-menu">‹</button></div><div class="v-ios-publish"><div class="v-ios-media-card"><div class="v-ios-media-preview" data-role="post-image-preview"><div class="v-ios-media-placeholder"><span>可选配图</span></div></div><div class="v-ios-media-actions"><label class="v-ios-media-picker">选择照片<input type="file" accept="image/*" data-post-image-file></label><button class="v-ios-media-remove" data-action="clear-post-image">移除</button></div><input type="hidden" data-post-field="image" value=""><div class="v-ios-url"><label>图片 URL（备用）</label><input data-post-image-url placeholder="https://..."></div></div><div class="v-live-publish-group v-post-publish-group"><div class="v-field"><label>贴文内容</label><textarea data-post-field="text" maxlength="1000" placeholder="写点什么……"></textarea></div><div class="v-live-publish-section"><div class="v-live-publish-label">可见范围</div>${segments("accessMode", [["public","公开"],["free","免费订阅"],["paid","付费订阅"]], "public")}</div><div class="v-live-publish-section"><div class="v-live-publish-label">年龄限制</div>${segments("ageRequirement", [["none","无限制"],["18+","18+"]], "none", ["18+"])}</div></div><div class="v-publish-note">公开贴文不能开启 18+；免费订阅或付费订阅内容可选择 18+。发布后会同步到当前主页和首页内容流。</div></div><div class="v-publish-sheet-actions"><button class="v-action light" data-action="render-publish-menu">返回</button><button class="v-action" data-action="save-community-post" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(ownerId)}">发布</button></div>`;
              sheet.classList.add("is-open");
            };

            const saveCommunityPost = async (ownerType, ownerId) => {
              const screen = q('[data-role="publish-sheet"]');
              const text = String(screen?.querySelector('[data-post-field="text"]')?.value || "").trim();
              const image = String(screen?.querySelector('[data-post-field="image"]')?.value || "").trim();
              const accessModeRaw = String(screen?.querySelector('[data-post-field="accessMode"]')?.value || "public");
              const accessMode = ["public","free","paid"].includes(accessModeRaw) ? accessModeRaw : "public";
              const requestedAge = String(screen?.querySelector('[data-post-field="ageRequirement"]')?.value || "none") === "18+" ? "18+" : "none";
              const ageRequirement = accessMode === "public" ? "none" : requestedAge;
              if (!text) { toast("先写一点贴文内容"); return; }
              const id = `post-${Date.now().toString(36)}`;
              state.communityPosts.unshift({ id, ownerType, ownerId, kind:"post", accessMode, ageRequirement, text, image, translation:"", time:"刚刚", at:Date.now(), likes:postSeedNumber(id,3,18), displayCommentCount:postSeedNumber(id,2,10) });
              state.postReplies[id] = [];
              await persist();
              q('[data-role="publish-sheet"]')?.classList.remove("is-open");
              openProfile(ownerType, ownerId);
              renderProfileTab(ownerType, ownerId, "activity");
              toast("贴文已发布，并关联到当前主页");
            };

            const sendPostComment = async (postId) => {
              const id = String(postId || "");
              const input = q(`[data-role="inline-post-comment-input"][data-post-id="${CSS.escape(id)}"]`) || q('[data-screen="post"] [data-role="post-comment-input"]');
              const textValue = String(input?.value || "").trim();
              if (!textValue) { toast("先写一点评论"); return; }
              const selectedIdentityId = String(q(`[data-role="post-identity-select"][data-post-id="${CSS.escape(id)}"]`)?.value || q('[data-screen="post"] [data-role="post-identity-select"]')?.value || state.postInteractionIdentity?.[id] || state.viewerIdentityId || "");
              const identity = state.identities.find(x => String(x.id) === selectedIdentityId) || state.identities.find(x => String(x.id) === String(state.viewerIdentityId)) || state.identities[0];
              if (identity) state.postInteractionIdentity[id] = String(identity.id);
              const current = safeArray(state.postReplies[id]);
              const replyTo = String(input?.dataset?.replyTo || "").trim();
              current.push({
                id: `comment-${Date.now().toString(36)}`,
                user: identity?.handle || identity?.displayName || "@user",
                avatar: identity?.avatar || initials(identity?.displayName || "U"),
                text: textValue,
                replyTo,
                translation: "",
                isUser: true,
                identityId: identity?.id || state.viewerIdentityId
              });
              state.postReplies[id] = current;
              if (input) {
                input.value = "";
                delete input.dataset.replyTo;
                input.placeholder = input.dataset.defaultPlaceholder || (input.matches('[data-role="post-comment-input"]') ? "写下评论… · Enter发送" : "留下评论… · Enter发送");
              }
              await persist();
              renderPostComments(id);
              if (!q(`[data-inline-comments="${CSS.escape(id)}"].is-open`)) renderHome();
            };

            const replyToPostComment = (postId, indexValue) => {
              const id = String(postId || "");
              const index = Number(indexValue);
              const reply = safeArray(state.postReplies[id])[index];
              if (!reply) return;
              const input = q(`[data-role="inline-post-comment-input"][data-post-id="${CSS.escape(id)}"]`) || q('[data-screen="post"] [data-role="post-comment-input"]');
              if (!input) return;
              if (!input.dataset.defaultPlaceholder) input.dataset.defaultPlaceholder = input.placeholder || "留下评论… · Enter发送";
              input.dataset.replyTo = String(reply.user || "");
              input.placeholder = `回复 ${String(reply.user || "评论")}… · Enter发送`;
              input.focus();
            };

            const deletePostComment = async (postId, indexValue) => {
              const id = String(postId || "");
              const index = Number(indexValue);
              const current = safeArray(state.postReplies[id]);
              if (!Number.isInteger(index) || index < 0 || index >= current.length) return;
              current.splice(index, 1);
              state.postReplies[id] = current;
              await persist();
              renderPostComments(id);
              if (!q(`[data-inline-comments="${CSS.escape(id)}"].is-open`)) renderHome();
              toast("评论已删除");
            };

            const deleteAnyPost = async (postId) => {
              const id=String(postId||"");
              state.communityPosts = state.communityPosts.filter(p => String(p.id)!==id);
              Object.keys(state.homeFeedByChannel||{}).forEach(key => { if(String(state.homeFeedByChannel[key]?.id||"")===id) delete state.homeFeedByChannel[key]; });
              delete state.postReplies[id]; delete state.postOverrides[id];
              await persist(); renderHome(); toast("贴文已删除");
            };

            const deleteCommunityPost = async (postId) => {
              const id = String(postId || "");
              const index = state.communityPosts.findIndex(p => String(p.id) === id && p.ownerType === "identity" && String(p.ownerId || "") === String(state.viewerIdentityId || ""));
              if (index < 0) return;
              const removed = state.communityPosts.splice(index, 1)[0];
              delete state.postReplies[id];
              delete state.postOverrides[id];
              await persist();
              closeScreen("post");
              openProfile(removed.ownerType || "identity", removed.ownerId || state.viewerIdentityId);
              renderProfileTab(removed.ownerType || "identity", removed.ownerId || state.viewerIdentityId, "activity");
              toast("贴文已删除");
            };

            const summonPostReplies = async (postId) => {
              const id = String(postId || "");
              if (!id || postReplyPending.has(id)) return;
              const post = getPostById(id);
              if (!post) return;
              postReplyPending.add(id);
              const setBusy = busy => { qa(`[data-action="summon-post-replies"][data-post-id="${CSS.escape(id)}"]`).forEach(btn => { btn.classList.toggle("is-generating", busy); btn.disabled = busy; if (btn.classList.contains("v-summon-mini")) btn.innerHTML = busy ? '<span class="v-summon-dots">•••</span>' : planeIconHTML(); }); };
              setBusy(true);
              try {
              const ownerChannel = post?.ownerType === "channel" ? state.channels.find(ch => String(ch.id) === String(post.ownerId || "")) : null;
              const persona = ownerChannel ? String(getChannelPersonaText(ownerChannel) || "").slice(0,1600) : "";
              const current = safeArray(state.postReplies[id]);
              const locale = ownerChannel ? inferChannelLocale(ownerChannel) : "global";
              const history = current.slice(-15).map(item => ({ user:item.user || "", text:item.text || "", replyTo:item.replyTo || "", isUser:Boolean(item.isUser) }));
              const system = `${VELA_DEFAULT_PRESET}

你正在给一条 Vela 贴文继续生成评论区互动。贴文作者：${post.name || post.handle || "Vela 用户"}；作者语言/地区倾向：${locale}；贴文：${post.text || ""}。${persona ? `角色设定：${persona}` : ""}
已有最新评论：${JSON.stringify(history)}
${nsfwPresetPrompt(String(post.ageRequirement || "") === "18+")}
陌生评论者语言倾向：${languagePreferencePrompt()}\n${translationRulePrompt()}\n返回严格 JSON 数组，恰好 5 条：[{"role":"viewer|owner","user":"viewer 时填自然网名；owner 可空","text":"评论原语言","translation":"按翻译规则填写","replyTo":"可空；需要回复已有评论时写对方公开名"}]。评论者网名和语言要有差异，不要复用固定模板；owner 只能是贴文作者本人，且不必每批出现。绝对不能替 USER 发评论。`;
              const aiText = await runVelaAI([{ role:"system", content:system }, { role:"user", content:"继续生成下一批 5 条评论。" }]);
              const data = extractJSON(aiText);
              if (!Array.isArray(data)) { toast(`评论生成失败 · ${String(aiText || "").trim() ? "回复格式解析失败" : shortVelaAIError("AI 返回为空")}`); return; }
              const additions = [];
              for (const item of data) {
                if (additions.length >= 5 || !item || typeof item !== "object") continue;
                const ownerReply = String(item.role || "viewer") === "owner" && post.ownerType === "channel";
                const text = String(item.text || "").trim();
                if (!text) continue;
                const user = ownerReply ? String(post.handle || post.name || "主播") : String(item.user || "").trim();
                if (!user) continue;
                additions.push({ id:`reply-${Date.now().toString(36)}-${additions.length}-${Math.random().toString(36).slice(2,5)}`, user, avatar:ownerReply ? String(post.avatar || initials(post.name || "V")) : STRANGER_AVATAR, text, translation:String(item.translation || "").trim(), replyTo:String(item.replyTo || "").trim(), isUser:false });
              }
              if (additions.length !== 5) { toast(`评论生成失败 · 只解析到 ${additions.length}/5 条有效评论`); return; }
              state.postReplies[id] = [...current, ...additions];
              await persist();
              renderPostComments(id);
              if (!q(`[data-inline-comments="${CSS.escape(id)}"].is-open`)) renderHome();
              toast("已追加 5 条评论");
              } catch (err) { toast(`评论处理失败 · ${String(err?.message || err || "未知错误").replace(/\s+/g," ").slice(0,100)}`); } finally { postReplyPending.delete(id); setBusy(false); }
            };


            const readLocalImage = (file) => new Promise((resolve, reject) => {
              if (!file) return resolve("");
              if (!String(file.type || "").startsWith("image/")) return reject(new Error("请选择图片文件"));
              if (Number(file.size || 0) > 8 * 1024 * 1024) return reject(new Error("图片太大，请选择 8MB 以内的图片"));
              const reader = new FileReader();
              reader.onload = () => resolve(String(reader.result || ""));
              reader.onerror = () => reject(new Error("读取图片失败"));
              reader.readAsDataURL(file);
            });

            const setPublishImagePreview = (kind, value) => {
              const selector = kind === "live" ? '[data-role="live-cover-preview"]' : '[data-role="post-image-preview"]';
              const preview = q(`[data-role="publish-sheet"] ${selector}`);
              if (!preview) return;
              preview.innerHTML = isImageURL(value) ? `<img src="${escapeHTML(value)}" alt="">` : `<div class="v-ios-media-placeholder"><span>${kind === "live" ? "直播封面" : "可选配图"}</span></div>`;
            };

            const renderPublishMenu = () => {
              const host = q('[data-role="publish-sheet-content"]');
              if (!host) return;
              host.innerHTML = `<div class="v-publish-sheet-title"><h3>发布</h3><button data-action="close-publish-sheet">×</button></div><div class="v-create-grid"><button class="v-create" data-action="open-live-publisher">开始直播</button><button class="v-create" data-action="open-publish-post">发布贴文</button></div>`;
            };

            const openLivePublisher = () => {
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              const identity = state.identities.find(x => String(x.id) === String(state.viewerIdentityId)) || state.identities[0];
              if (!identity || !host) return;
              const mutualRoles = state.channels.filter(ch => state.identities.some(identity => isFollowing("identity", identity.id, "channel", ch.id) && isFollowing("channel", ch.id, "identity", identity.id)));
              const cohostChoices = mutualRoles.length
                ? mutualRoles.map(ch => `<label class="v-live-cohost-choice"><input type="checkbox" data-live-cohost-id="${escapeHTML(ch.id)}"><span>${escapeHTML(ch.name || ch.handle || "角色")}</span></label>`).join("")
                : `<div class="v-live-cohost-empty">目前没有与你互关、可邀请一起直播的角色。</div>`;
              const segments = (field, items, selected, disabledValues = []) => `<div class="v-live-segments" data-live-choice-group="${escapeHTML(field)}">${items.map(([value,label]) => `<button type="button" class="${value === selected ? "is-selected" : ""} ${disabledValues.includes(value) ? "is-disabled" : ""}" data-action="choose-live-option" data-live-field="${escapeHTML(field)}" data-live-value="${escapeHTML(value)}" ${disabledValues.includes(value) ? "disabled" : ""}>${escapeHTML(label)}</button>`).join("")}</div><input type="hidden" data-live-publish-field="${escapeHTML(field)}" value="${escapeHTML(selected)}">`;
              host.innerHTML = `<div class="v-publish-sheet-title"><h3>开始直播</h3><button data-action="render-publish-menu">‹</button></div><div class="v-ios-publish"><div class="v-ios-media-card"><div class="v-ios-media-preview" data-role="live-cover-preview"><div class="v-ios-media-placeholder"><span>直播封面</span></div></div><div class="v-ios-media-actions"><label class="v-ios-media-picker">选择照片<input type="file" accept="image/*" data-live-cover-file></label><button class="v-ios-media-remove" data-action="clear-live-cover">移除</button></div><input type="hidden" data-live-publish-field="cover" value=""><div class="v-ios-url"><label>封面 URL（备用）</label><input data-live-cover-url placeholder="https://..."></div></div><div class="v-live-publish-group"><div class="v-field"><label>直播标题</label><input data-live-publish-field="title" maxlength="90" placeholder="给这场直播起个标题"></div><div class="v-field"><label>直播内容</label><textarea data-live-publish-field="topic" maxlength="500" placeholder="今天准备播什么、聊什么"></textarea></div><div class="v-live-publish-section"><div class="v-live-publish-label">直播类型</div>${segments("liveType", [["video","视频直播"],["voice","语音直播"]], "video")}</div><div class="v-live-publish-section"><div class="v-live-publish-label">直播方式</div>${segments("broadcastStyle", [["solo","单人直播"],["multi","多人直播"]], "solo")}</div><div class="v-live-multi-fields" data-role="multi-live-fields" hidden><div class="v-live-publish-section"><div class="v-live-publish-label">合作方式</div>${segments("cohostPlan", [["online","线上"],["same-room","线下"]], "online")}</div><div class="v-live-publish-section"><div class="v-live-publish-label">和谁一起播</div><div class="v-live-cohost-list">${cohostChoices}</div><div class="v-hint" style="margin-top:7px">仅显示与你任一账号互关的角色；最多选择 3 位。</div></div></div><div class="v-live-publish-section"><div class="v-live-publish-label">观看权限</div>${segments("accessMode", [["public","公开"],["free","免费订阅"],["paid","付费订阅"]], "public")}</div><div class="v-live-publish-section"><div class="v-live-publish-label">年龄限制</div>${segments("ageRequirement", [["none","无限制"],["18+","18+"]], "none", ["18+"])}</div></div></div><div class="v-publish-sheet-actions"><button class="v-action light" data-action="render-publish-menu">取消</button><button class="v-action" data-action="start-prepared-live">开始直播</button></div>`;
              sheet.classList.add("is-open");
            };

            const startPreparedLive = async () => {
              const identity = state.identities.find(x => String(x.id) === String(state.viewerIdentityId)) || state.identities[0];
              const screen = q('[data-role="publish-sheet"]');
              if (!identity || !screen) return;
              const get = name => String(screen.querySelector(`[data-live-publish-field="${name}"]`)?.value || "").trim();
              const title = get("title"), topic = get("topic"), cover = get("cover");
              if (!title || !topic) { toast("先填写直播标题和内容"); return; }
              const liveType = get("liveType") === "voice" ? "voice" : "video";
              const broadcastStyle = get("broadcastStyle") === "multi" ? "multi" : "solo";
              const cohostPlan = get("cohostPlan") === "same-room" ? "same-room" : "online";
              const accessMode = ["public","free","paid"].includes(get("accessMode")) ? get("accessMode") : "public";
              const requestedAge = get("ageRequirement") === "18+" ? "18+" : "none";
              const ageRequirement = accessMode === "public" ? "none" : requestedAge;
              const selectedIds = broadcastStyle === "multi" ? [...screen.querySelectorAll('[data-live-cohost-id]:checked')].map(o => String(o.dataset.liveCohostId || "")).filter(Boolean).slice(0,3) : [];
              const mutual = ch => state.identities.some(i => isFollowing("identity", i.id, "channel", ch.id) && isFollowing("channel", ch.id, "identity", i.id));
              const participants = selectedIds.map(id => state.channels.find(ch => String(ch.id) === id)).filter(ch => ch && mutual(ch)).map(ch => ({ id:String(ch.id), name:String(ch.name || ch.handle || "嘉宾"), handle:String(ch.handle || ""), avatar:String(ch.avatar || initials(ch.name || "嘉宾")) }));
              const mode = participants.length ? (cohostPlan === "same-room" ? "same-room" : "remote") : "solo";
              const live = { id:`own-live-${identity.id}-${Date.now().toString(36)}`, ownerType:"identity", ownerId:identity.id, isOwn:true, name:identity.displayName || "我的频道", handle:identity.handle || "@me", avatar:identity.avatar || "U", followers:Number(identity.followers || 0), title, category:topic, liveType, broadcastStyle, cohostPlan, cover, viewers:0, mode, participants, accessMode, ageRequirement, startedAt:Date.now() };
              identity.live = true; identity.title = title; identity.activeLive = { ...live }; identity.scheduledLive = null;
              state.liveProgress[live.id] = undefined; state.liveStats[live.id] = undefined; state.liveChat = []; state.pendingLiveUserMessage = null; state.pendingOwnLiveInput = null;
              pushCommunityBotEvent("identity", identity.id, "live", { title:live.title, text:"直播已经开始。" });
              await persist(); q('[data-role="publish-sheet"]')?.classList.remove("is-open"); renderHome(); renderChannels(); openLive(live);
            };

            const openInviteGuest = () => {
              const live = state.liveSession;
              if (!live || !isOwnLiveSession(live)) return;
              const guests = getLiveGuests(live);
              if (guests.length >= 3) { toast("同时出镜最多 4 人"); return; }
              const used = new Set(guests.map(g => String(g.id)));
              const roles = state.channels.filter(ch => !used.has(String(ch.id)) && state.identities.some(i => isFollowing("identity", i.id, "channel", ch.id) && isFollowing("channel", ch.id, "identity", i.id)));
              const rows = roles.map(ch => `<option value="${escapeHTML(ch.id)}">${escapeHTML(ch.name || ch.handle || "角色")}</option>`).join("");
              const fixedMode = guests.length ? (live.mode === "same-room" ? "inperson" : "online") : "";
              const panel = q('[data-screen="live"] [data-role="live-economy-panel"]');
              if (!panel) return;
              panel.innerHTML = `<div class="v-live-economy-card"><div class="v-live-tool-title"><h3>与…连线</h3><button data-action="close-live-economy">×</button></div><div class="v-field" style="margin-top:10px"><label>选择互关角色</label><select data-invite-field="cohostId"><option value="">请选择</option>${rows}</select></div><div class="v-field"><label>连线方式</label><select data-invite-field="cohostMode" ${fixedMode ? "disabled" : ""}><option value="online" ${fixedMode === "online" ? "selected" : ""}>线上连线</option><option value="inperson" ${fixedMode === "inperson" ? "selected" : ""}>线下同镜</option></select></div><div class="v-live-tool-actions"><button class="is-light" data-action="close-live-economy">取消</button><button class="is-primary" data-action="confirm-invite-guest">发起连线</button></div></div>`;
              panel.classList.add("is-open");
            };

            const confirmInviteGuest = async () => {
              const live = state.liveSession;
              if (!live || !isOwnLiveSession(live)) return;
              const panel = q('[data-screen="live"] [data-role="live-economy-panel"]');
              const cohostId = String(panel?.querySelector('[data-invite-field="cohostId"]')?.value || "");
              const selectedMode = String(panel?.querySelector('[data-invite-field="cohostMode"]')?.value || "online");
              const cohost = state.channels.find(ch => String(ch.id) === cohostId);
              const mutual = cohost && state.identities.some(i => isFollowing("identity", i.id, "channel", cohost.id) && isFollowing("channel", cohost.id, "identity", i.id));
              if (!cohost || !mutual) { toast("先选择一个互关角色"); return; }
              const guests = getLiveGuests(live);
              if (guests.some(g => String(g.id) === String(cohost.id))) { toast("这个角色已经在直播里"); return; }
              if (guests.length >= 3) { toast("同时出镜最多 4 人"); return; }
              const nextMode = guests.length ? live.mode : (selectedMode === "inperson" ? "same-room" : "remote");
              const nextGuest = { id:String(cohost.id), name:String(cohost.name || cohost.handle || "嘉宾"), handle:String(cohost.handle || ""), avatar:String(cohost.avatar || initials(cohost.name || "嘉宾")) };
              live.participants = [...guests, nextGuest].slice(0,3); live.mode = nextMode; live.cohostMode = nextMode === "same-room" ? "inperson" : "online";
              const identity = state.identities.find(x => String(x.id) === String(live.ownerId || state.viewerIdentityId));
              if (identity?.activeLive) identity.activeLive = { ...identity.activeLive, participants:live.participants.map(x => ({...x})), mode:live.mode, cohostMode:live.cohostMode };
              await persist(); closeLiveEconomyPanel(); openLive(live); toast(`已与 ${cohost.name || "嘉宾"} 连线 · ${live.participants.length + 1}/4 人`);
            };

            const scheduleInputValue = value => {
              const ms = Number(value || 0); if (!ms) return "";
              const d = new Date(ms - new Date(ms).getTimezoneOffset() * 60000);
              return d.toISOString().slice(0,16);
            };
            const openScheduledLiveEditor = (identityId, scheduleId = "") => {
              openScheduledLiveDetail("identity", identityId, scheduleId || String(nearestScheduledLive(state.identities.find(x => String(x.id) === String(identityId)))?.scheduleId || ""));
            };
            const saveScheduledLive = async (identityId, scheduleId = "") => {
              const identity = state.identities.find(x => String(x.id) === String(identityId));
              if (!identity) return;
              const card = q(`[data-screen="schedule-detail"] [data-schedule-edit-card="${CSS.escape(String(scheduleId || ""))}"]`);
              if (!card) return;
              const read = name => String(card.querySelector(`[data-schedule-field="${name}"]`)?.value || "").trim();
              const title = read("title"), startRaw = read("scheduledAt"), endRaw = read("endsAt"), cover = read("cover");
              if (!title || !startRaw) { toast("先填写直播标题和开始时间"); return; }
              const start = new Date(startRaw).getTime();
              let end = endRaw ? new Date(endRaw).getTime() : start + 2*3600000;
              if (!Number.isFinite(start)) { toast("直播时间格式不正确"); return; }
              if (!Number.isFinite(end) || end <= start) end = start + 2*3600000;
              const rows = scheduledLiveList(identity);
              const item = rows.find(row => String(row.scheduleId || "") === String(scheduleId || ""));
              if (!item) return;
              item.title = title; item.scheduledAt = start; item.endsAt = end; item.cover = cover;
              item.dateText = item.sourceType === "business" ? formatBusinessWindow(start,end) : `${formatDateTextFromInput(start)}–${new Date(end).toLocaleTimeString("zh-CN",{hour:"2-digit",minute:"2-digit",hour12:false})}`;
              if (item.business) { item.business.scheduledAt = start; item.business.endsAt = end; }
              identity.scheduledLive = null; scheduledLiveList(identity);
              await persist(); armAppointmentTimer(); armCommunityBotTimers?.();
              openScheduledLiveDetail("identity", identity.id);
              renderProfileTab("identity", identity.id, "activity");
              toast("直播预约已保存");
            };
            const openScheduledLiveDetail = (ownerType = "identity", ownerId = "", editingScheduleId = "") => {
              if (ownerId === "" && ownerType && ownerType !== "identity" && ownerType !== "channel") { ownerId = ownerType; ownerType = "identity"; }
              const entity = getProfileEntity(ownerType, ownerId);
              if (!entity) return;
              const rows = scheduledLiveList(entity);
              const canEdit = ownerType === "identity";
              const cards = rows.map(item => {
                const editing = canEdit && String(item.scheduleId || "") === String(editingScheduleId || "");
                if (editing) {
                  return `<article class="v-schedule-list-card is-editing" data-schedule-edit-card="${escapeHTML(item.scheduleId)}"><div class="v-schedule-edit-grid"><div class="v-field"><label>直播标题</label><input data-schedule-field="title" maxlength="80" value="${escapeHTML(item.title || "")}"></div><div class="v-fieldpair"><div class="v-field"><label>开始时间</label><input type="datetime-local" data-schedule-field="scheduledAt" value="${escapeHTML(scheduleInputValue(item.scheduledAt))}"></div><div class="v-field"><label>结束时间</label><input type="datetime-local" data-schedule-field="endsAt" value="${escapeHTML(scheduleInputValue(item.endsAt))}"></div></div><div class="v-field"><label>封面 URL（可留空）</label><input data-schedule-field="cover" value="${escapeHTML(item.cover || "")}" placeholder="https://..."></div></div><div class="v-schedule-card-actions"><button class="is-light" data-action="cancel-scheduled-live-inline" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(entity.id)}">取消</button><button class="is-primary" data-action="save-scheduled-live-inline" data-identity-id="${escapeHTML(entity.id)}" data-schedule-id="${escapeHTML(item.scheduleId)}">保存</button></div></article>`;
                }
                const coverHTML = isImageURL(item.cover || "") ? `<img src="${escapeHTML(item.cover)}" alt="">` : `<div class="v-schedule-cover-placeholder">●</div>`;
                const source = item.sourceType === "business" ? "商务合作" : item.sourceType === "live_invite" ? "直播邀约" : "预约直播";
                return `<article class="v-schedule-list-card"><div class="v-schedule-list-cover">${coverHTML}</div><div class="v-schedule-list-copy"><div class="v-kicker">${escapeHTML(source)}</div><h3>${escapeHTML(item.title || "预约直播")}</h3><div class="v-sub">${escapeHTML(item.dateText || "时间待定")}</div></div><div class="v-schedule-card-actions"><button class="is-primary" data-action="start-scheduled-live-item" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(entity.id)}" data-schedule-id="${escapeHTML(item.scheduleId)}">开始直播</button>${canEdit ? `<button class="is-light" data-action="edit-scheduled-live-inline" data-owner-type="identity" data-owner-id="${escapeHTML(entity.id)}" data-schedule-id="${escapeHTML(item.scheduleId)}">编辑</button><button class="is-danger" data-action="delete-scheduled-live-from-list" data-identity-id="${escapeHTML(entity.id)}" data-schedule-id="${escapeHTML(item.scheduleId)}">删除</button>` : ""}</div></article>`;
              }).join("");
              openScreen("schedule-detail", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="schedule-detail">‹</button><div class="v-meta"><strong>预约直播</strong><div class="v-hint">${escapeHTML(profileName(entity, ownerType))} · ${rows.length} 场</div></div></header><div class="v-subbody"><div class="v-schedule-list">${cards || '<div class="v-profile-empty">还没有预约直播。</div>'}</div></div>`);
            };

            const openProfile = (ownerType, id) => {
              const entity = getProfileEntity(ownerType, id);
              if (!entity) return;
              const name = profileName(entity, ownerType);
              const handle = normalizeHandle(entity.handle || "") || "@vela";
              const banner = String(entity.banner || "");
              const bannerHTML = isImageURL(banner) ? `<img src="${escapeHTML(banner)}" alt="">` : "";
              const viewer = state.identities.find(item => String(item.id) === String(state.viewerIdentityId)) || state.identities[0];
              const viewerFollows = ownerType === "channel" && viewer ? isFollowing("identity", viewer.id, "channel", entity.id) : false;
              const channelFollows = ownerType === "channel" && viewer ? isFollowing("channel", entity.id, "identity", viewer.id) : false;
              const followLabel = viewerFollows && channelFollows ? "互关" : viewerFollows ? "已关注" : channelFollows ? "关注了你" : "关注";
              const profileActions = ownerType === "channel" ? `<div class="v-profile-actions"><button class="v-profile-follow ${viewerFollows ? "is-on" : ""} ${viewerFollows && channelFollows ? "is-mutual" : ""}" data-action="toggle-profile-follow" data-channel-id="${escapeHTML(entity.id)}">${escapeHTML(followLabel)}</button><button class="v-profile-message" data-action="message-channel" data-channel-id="${escapeHTML(entity.id)}">私信</button></div>` : "";
              openScreen("profile", `<div class="v-profile-shell"><header class="v-subhead"><button data-action="close-screen" data-screen-name="profile">‹</button><div class="v-meta"><strong>${escapeHTML(name)}</strong><div class="v-hint">${ownerType === "identity" ? "我的 Vela 主页" : "角色频道主页"}</div></div><button class="v-head-action" data-action="edit-profile" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(entity.id)}">编辑主页</button></header><div class="v-profile-banner">${bannerHTML}</div><div class="v-profile-info"><div class="v-profile-avatar ${entity.live || entity.activeLive ? "is-live-profile" : ""}">${avatarHTML(entity.avatar || "", name)}</div><div class="v-profile-main"><div class="v-profile-maincopy"><h2>${escapeHTML(name)}</h2><div class="v-profile-handle">${escapeHTML(handle)}</div></div>${profileActions}</div><div class="v-profile-stats"><span><b>${Number(entity.followers || 0).toLocaleString("zh-CN")}</b> 粉丝</span><span><b>${Number(entity.following || 0).toLocaleString("zh-CN")}</b> 关注</span></div>${entity.bio ? `<div class="v-profile-bio">${escapeHTML(entity.bio)}</div>` : ""}</div><div class="v-profile-tabs"><button class="is-active" data-action="profile-tab" data-profile-tab="activity" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(entity.id)}">动态</button><button data-action="profile-tab" data-profile-tab="replays" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(entity.id)}">回放</button><button data-action="profile-tab" data-profile-tab="subscriptions" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(entity.id)}">订阅</button></div><div class="v-profile-tabbody" data-role="profile-tab-body"></div></div>`);
              renderProfileTab(ownerType, entity.id, "activity");
            };

            const openUserProfile = () => {
              if (!state.viewerIdentityId || !state.identities.some(item => String(item.id) === String(state.viewerIdentityId))) {
                openIdentities();
                toast("先创建一个 Vela 账号");
                return;
              }
              openProfile("identity", state.viewerIdentityId);
            };

            const openChannel = (channel) => openProfile("channel", channel.id);

            const openProfileEditor = (ownerType, id) => {
              const entity = getProfileEntity(ownerType, id);
              if (!entity) return;
              const name = profileName(entity, ownerType);
              const accountRole = entity.accountRole === "alias" ? "alias" : "primary";
              const faceMode = String(entity.faceMode || "mixed");
              const mainEntity = accountRole === "alias"
                ? (ownerType === "identity"
                    ? (state.identities.find(item => String(item.id) === String(entity.mainAccountId || "")) || getPersonaMainAccount(entity.sourcePersonaId))
                    : (state.channels.find(item => String(item.id) === String(entity.mainChannelId || "")) || getCharacterMainAccount(entity.sourceCharacterId)))
                : null;
              const linkedToMain = accountRole === "alias" ? Boolean(entity.linkedToMain) : false;
              const visibility = linkedToMain && mainEntity
                ? String(mainEntity.identityVisibility || "partial")
                : String(entity.identityVisibility || (entity.isAnonymous ? "hidden" : "partial"));
              const opt = (value, current, label) => `<option value="${value}" ${current === value ? "selected" : ""}>${label}</option>`;

              let knowledgeHTML = "";
              if (accountRole === "alias" && !linkedToMain && ownerType === "identity") {
                const mainCharacters = safeArray(state.rocheLink?.linkedCharacterIds).map(charId => getCharacterMainAccount(charId)).filter(Boolean);
                const rows = mainCharacters.map(channel => {
                  const charId = String(channel.sourceCharacterId || channel.id || "");
                  const checked = Boolean(entity.knownByCharacters?.[charId]);
                  return `<label class="v-community-check"><span>${escapeHTML(channel.name || channel.handle || "角色")} 知道这是我的隐藏副账号</span><input class="v-switch" type="checkbox" data-known-character-id="${escapeHTML(charId)}" ${checked ? "checked" : ""}></label>`;
                }).join("");
                knowledgeHTML = `<div class="v-card" data-role="hidden-sub-knowledge"><b>隐藏副账号 · 知情关系</b><p class="v-hint">只记录谁已经知道这个小号属于你；其他角色默认不知道，之后可以在互动里自然发现。</p>${rows || '<div class="v-hint">当前没有已加入 Vela 的 Roche 角色。</div>'}</div>`;
              } else if (accountRole === "alias" && !linkedToMain && ownerType === "channel") {
                knowledgeHTML = `<div class="v-card" data-role="hidden-sub-knowledge"><b>隐藏副账号 · User 知情关系</b><p class="v-hint">这个小号底层属于 ${escapeHTML(name)}，但角色可以不知道 User 已经识破。</p><div class="v-community-botchecks"><label class="v-community-check"><span>User 已经知道这是角色的小号</span><input class="v-switch" type="checkbox" data-edit-flag="userKnowsAlias" ${entity.userKnowsAlias ? "checked" : ""}></label><label class="v-community-check"><span>角色知道 User 已经识破</span><input class="v-switch" type="checkbox" data-edit-flag="characterKnowsUserKnows" ${entity.characterKnowsUserKnows ? "checked" : ""}></label></div></div>`;
              }

              const roleCommerceCfg = ownerType === "channel" && entity.sourceCharacterId
                ? (state.roleCommerce[String(entity.sourceCharacterId)] || { enabled:false, tendency:35 })
                : null;

              openScreen("profile-edit", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="profile-edit">‹</button><div class="v-meta"><strong>编辑主页</strong><div class="v-hint">${escapeHTML(name)} · Vela 账号设置</div></div><button class="v-head-action" data-action="save-profile" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(entity.id)}">保存</button></header><div class="v-subbody">
                <div class="v-card"><div class="v-editform">
                  <div class="v-field"><label>主页背景图片 URL</label><input data-edit-field="banner" value="${escapeHTML(entity.banner || "")}" placeholder="https://..."></div>
                  <div class="v-field"><label>头像 URL / 文字占位</label><input data-edit-field="avatar" value="${escapeHTML(entity.avatar || "")}" placeholder="https://... 或 NN"></div>
                  <div class="v-field"><label>昵称</label><input data-edit-field="name" value="${escapeHTML(name)}" maxlength="40"></div>
                  <div class="v-field"><label>账号 @ID</label><input data-edit-field="handle" value="${escapeHTML(entity.handle || "")}" maxlength="40" placeholder="@vela_name"></div>
                  <div class="v-fieldpair"><div class="v-field"><label>粉丝数</label><input data-edit-field="followers" type="number" min="0" step="1" value="${Number(entity.followers || 0)}"></div><div class="v-field"><label>关注数</label><input data-edit-field="following" type="number" min="0" step="1" value="${Number(entity.following || 0)}"></div></div>
                  <div class="v-field"><label>签名 / 简介</label><textarea data-edit-field="bio" maxlength="300" placeholder="写点主页介绍…">${escapeHTML(entity.bio || "")}</textarea></div>
                </div></div>

                <div class="v-card"><b>账号风格</b><p class="v-hint">账号定位和露脸是两回事。主账号可以不露脸；副账号也可以露脸。</p><div class="v-editform">
                  <div class="v-setting-row"><div class="v-meta"><div class="v-name">${accountRole === "alias" ? "副账号" : "主账号"}</div><div class="v-sub">${accountRole === "alias" ? (linkedToMain ? "当前公开关联主账号" : "当前作为隐藏小号运行") : "这份 Roche 人设在 Vela 的主要账号"}</div></div></div>
                  ${accountRole === "alias" && mainEntity ? `<div class="v-field"><label>与主账号的关系</label><select data-edit-field="linkedToMain">${opt("yes",linkedToMain ? "yes" : "no","关联主账号 · 公开附属账号")}${opt("no",linkedToMain ? "yes" : "no","不关联主账号 · 隐藏小号")}</select></div>` : ""}
                  <div class="v-fieldpair"><div class="v-field"><label>露脸方式</label><select data-edit-field="faceMode">${opt("show",faceMode,"露脸")}${opt("hidden",faceMode,"不露脸")}${opt("mixed",faceMode,"偶尔露脸 / 不固定")}</select></div><div class="v-field"><label>现实身份公开程度</label><select data-edit-field="identityVisibility" ${linkedToMain && mainEntity ? "disabled" : ""}>${opt("public",visibility,"公开")}${opt("partial",visibility,"半公开 / 留有线索")}${opt("hidden",visibility,"隐藏现实身份")}</select>${linkedToMain && mainEntity ? `<div class="v-hint" style="margin-top:5px">已关联主账号，因此这里跟随主账号的公开程度；副账号仍可单独设置露脸方式。</div>` : ""}</div></div>
                  <div class="v-field"><label>主要直播 / 频道内容</label><textarea data-edit-field="contentStyle" maxlength="800" placeholder="例如：深夜聊天、做饭、游戏、舞蹈、工作室日常……">${escapeHTML(entity.contentStyle || "")}</textarea></div>
                  <div class="v-field"><label>粉丝画像 / 社群氛围</label><textarea data-edit-field="fanProfile" maxlength="800" placeholder="例如：老粉较多、弹幕爱开玩笑、语言混合、对主播很熟……">${escapeHTML(entity.fanProfile || "")}</textarea></div>
                </div></div>

                ${knowledgeHTML}
                ${ownerType === "channel" && roleCommerceCfg ? `<div class="v-card"><b>商业合作</b><p class="v-hint">商业合作与接单倾向现在放在角色自己的主页设置里，不再放在角色管理页。</p><div class="v-community-botchecks"><label class="v-community-check"><span>允许角色自主接商业合作</span><input class="v-switch" type="checkbox" data-edit-flag="commerceEnabled" ${roleCommerceCfg.enabled ? "checked" : ""}></label></div><div class="v-setting-row"><div class="v-meta"><div class="v-name">自主接单倾向 · <span data-role="profile-commerce-value">${Number(roleCommerceCfg.tendency || 0)}%</span></div><div class="v-sub">作为 AI 判断参考，不是硬概率。</div></div></div><input class="v-range" data-edit-field="commerceTendency" type="range" min="0" max="100" step="5" value="${Number(roleCommerceCfg.tendency || 0)}"></div>` : ""}
                ${ownerType === "channel" ? `<div class="v-card"><b>直播习惯</b><p class="v-hint">这是角色频道的直播习惯提示。后面 AI 决定什么时候开播、播多久、何时自然结束时会参考这里。</p><div class="v-editform"><div class="v-live-habit-grid"><div class="v-field"><label>通常直播时长</label><select data-edit-field="liveDurationMode">${opt("irregular",String(entity.liveDurationMode || "irregular"),"不定时")}${opt("30m",String(entity.liveDurationMode || "irregular"),"通常约 30 分钟")}${opt("60m",String(entity.liveDurationMode || "irregular"),"通常约 1 小时")}${opt("120m",String(entity.liveDurationMode || "irregular"),"通常约 2 小时")}${opt("custom",String(entity.liveDurationMode || "irregular"),"自定义")}</select></div><div class="v-field"><label>角色自主程度</label><select data-edit-field="liveAutonomy">${opt("character",String(entity.liveAutonomy || "flexible"),"主要由角色自己决定")}${opt("flexible",String(entity.liveAutonomy || "flexible"),"参考习惯但灵活")}${opt("user",String(entity.liveAutonomy || "flexible"),"主要由 user 控制")}</select></div></div><div class="v-field"><label>自定义时长 / 补充</label><input data-edit-field="liveDurationText" value="${escapeHTML(entity.liveDurationText || "")}" placeholder="例如：通常 1～2 小时，聊得开心会延长"></div><div class="v-field"><label>习惯直播时间</label><textarea data-edit-field="liveSchedule" maxlength="500" placeholder="例如：周五、周六 22:00–01:00；工作日深夜偶尔播">${escapeHTML(entity.liveSchedule || "")}</textarea></div></div></div>` : ""}
                <p class="v-hint">这些账号设定只保存在 Vela，不会反向修改 Roche 原始人设资料。</p>
              </div>`);
            };

            const saveProfileEditor = async (ownerType, id) => {
              const entity = getProfileEntity(ownerType, id);
              if (!entity) return;
              const screen = q('[data-screen="profile-edit"]');
              const get = (field) => screen?.querySelector(`[data-edit-field="${field}"]`)?.value ?? "";
              const flag = (field) => Boolean(screen?.querySelector(`[data-edit-flag="${field}"]`)?.checked);
              const name = String(get("name") || "").trim() || profileName(entity, ownerType);
              const handle = normalizeHandle(get("handle"));
              const followers = Math.max(0, Math.floor(Number(get("followers") || 0)));
              const following = Math.max(0, Math.floor(Number(get("following") || 0)));
              if (ownerType === "identity") entity.displayName = name;
              else entity.name = name;
              entity.handle = handle;
              entity.avatar = String(get("avatar") || "").trim() || initials(name);
              entity.banner = String(get("banner") || "").trim();
              entity.bio = String(get("bio") || "").trim();
              entity.followers = Number.isFinite(followers) ? followers : 0;
              entity.following = Number.isFinite(following) ? following : 0;
              entity.accountRole = entity.accountRole === "alias" ? "alias" : "primary";
              entity.faceMode = ["show","hidden","mixed"].includes(get("faceMode")) ? get("faceMode") : "mixed";

              let mainEntity = null;
              if (entity.accountRole === "alias") {
                mainEntity = ownerType === "identity"
                  ? (state.identities.find(item => String(item.id) === String(entity.mainAccountId || "")) || getPersonaMainAccount(entity.sourcePersonaId))
                  : (state.channels.find(item => String(item.id) === String(entity.mainChannelId || "")) || getCharacterMainAccount(entity.sourceCharacterId));
                entity.linkedToMain = mainEntity ? get("linkedToMain") === "yes" : false;
                if (ownerType === "identity") entity.mainAccountId = String(mainEntity?.id || "");
                else entity.mainChannelId = String(mainEntity?.id || "");
              } else {
                entity.linkedToMain = false;
              }

              entity.identityVisibility = entity.accountRole === "alias" && entity.linkedToMain && mainEntity
                ? String(mainEntity.identityVisibility || "partial")
                : (["public","partial","hidden"].includes(get("identityVisibility")) ? get("identityVisibility") : "partial");
              entity.contentStyle = String(get("contentStyle") || "").trim();
              entity.fanProfile = String(get("fanProfile") || "").trim();

              if (ownerType === "identity") {
                if (entity.accountRole === "alias" && !entity.linkedToMain) {
                  entity.knownByCharacters = {};
                  screen?.querySelectorAll("[data-known-character-id]").forEach(input => {
                    if (input.checked) entity.knownByCharacters[String(input.dataset.knownCharacterId || "")] = true;
                  });
                } else if (entity.accountRole === "alias") {
                  entity.knownByCharacters = {};
                }
                entity.isAnonymous = entity.identityVisibility === "hidden";
                if (entity.accountRole === "primary") {
                  state.identities.forEach(sub => {
                    if (sub.accountRole === "alias" && sub.linkedToMain && String(sub.sourcePersonaId || "") === String(entity.sourcePersonaId || "")) {
                      sub.identityVisibility = entity.identityVisibility;
                    }
                  });
                }
              }

              if (ownerType === "channel") {
                if (entity.accountRole === "alias" && !entity.linkedToMain) {
                  entity.userKnowsAlias = flag("userKnowsAlias");
                  entity.characterKnowsUserKnows = entity.userKnowsAlias ? flag("characterKnowsUserKnows") : false;
                } else {
                  entity.userKnowsAlias = false;
                  entity.characterKnowsUserKnows = false;
                }
                if (entity.accountRole === "primary") {
                  state.channels.forEach(sub => {
                    if (sub.accountRole === "alias" && sub.linkedToMain && String(sub.sourceCharacterId || "") === String(entity.sourceCharacterId || "")) {
                      sub.identityVisibility = entity.identityVisibility;
                    }
                  });
                }
                entity.liveDurationMode = ["irregular","30m","60m","120m","custom"].includes(get("liveDurationMode")) ? get("liveDurationMode") : "irregular";
                entity.liveDurationText = String(get("liveDurationText") || "").trim();
                entity.liveSchedule = String(get("liveSchedule") || "").trim();
                entity.liveAutonomy = ["character","flexible","user"].includes(get("liveAutonomy")) ? get("liveAutonomy") : "flexible";
                entity.profileCustomized = true;
                if (entity.sourceCharacterId) {
                  const cfg = state.roleCommerce[String(entity.sourceCharacterId)] || (state.roleCommerce[String(entity.sourceCharacterId)] = { enabled:false, tendency:35 });
                  cfg.enabled = flag("commerceEnabled");
                  cfg.tendency = Math.max(0, Math.min(100, Number(get("commerceTendency") || 0)));
                }
              }
              await persist();
              mountMarkup(root, state);
              rerender();
              switchPage(state.activePage || "home");
              openProfile(ownerType, id);
              toast("主页已保存");
            };

            const openReplayDetail = (replayId) => {
              const replay = state.liveReplays.find(item => String(item.id) === String(replayId));
              if (!replay) return;
              const timeline = safeArray(replay.timeline);
              const chat = safeArray(replay.chat);
              const participants = safeArray(replay.participants);
              const modeLabel = replay.mode === "remote" ? "线上联播" : replay.mode === "same-room" ? "线下同镜" : "单人直播";
              const eventHTML = timeline.length ? timeline.map((rawEvent, index) => {
                const event = normalizeLiveTimelineEvent(rawEvent);
                const trId = `replay-event-${String(replay.id)}-${index}`;
                const actor = String(event.actorName || (event.actorType === "user" ? "user" : "角色"));
                return `<div class="v-replay-event"><div class="v-live-time-head"><span class="v-live-time-pill">${escapeHTML(formatLiveElapsed(event.at, replay))}</span><span class="v-live-time-actor" style="color:var(--v-text)">${escapeHTML(actor)}</span></div>${event.action ? `<div class="v-live-time-action" style="color:var(--v-muted)">${escapeHTML(event.action)}</div>` : ""}${event.speech ? `<div class="v-live-time-speech" style="color:var(--v-text)">${escapeHTML(event.speech)}</div>${translationHTML(trId, event.translation || "")}` : ""}</div>`;
              }).join("") : `<div class="v-profile-empty">这场回放还没有保存逐轮内容。</div>`;
              const chatHTML = chat.length ? chat.map((line, index) => {
                const trId = `replay-chat-${String(replay.id)}-${index}`;
                return `<div class="v-chatline"><div class="v-social-avatar">${avatarHTML(line.avatar || initials(line.user || "V"), line.user || "V")}</div><div class="v-chatline-body"><div class="v-chatline-head">${escapeHTML(line.user || "viewer")}</div><div class="v-chatline-text">${escapeHTML(line.text || "")}</div>${translationHTML(trId, line.translation || "")}</div></div>`;
              }).join("") : `<div class="v-profile-empty">这场回放没有保存聊天室内容。</div>`;
              openScreen("replay", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="replay">‹</button><div class="v-meta"><strong>直播回放</strong><div class="v-hint">${escapeHTML(replay.endedText || "已结束")}</div></div><button class="v-head-action" style="color:var(--v-red)!important" data-action="delete-replay" data-replay-id="${escapeHTML(replay.id)}">删除</button></header><div class="v-subbody"><div class="v-replay-summary"><div class="v-name">${escapeHTML(replay.title || "直播回放")}</div><div class="v-sub" style="margin-top:4px">${escapeHTML(replay.topic || "直播")} · ${escapeHTML(modeLabel)} · 最高 ${formatViewers(replay.peakViewers || 0)} 人观看</div>${participants.length ? `<div class="v-sub" style="margin-top:5px">参与：${escapeHTML(participants.map(p => p.name || "嘉宾").join("、"))}</div>` : ""}</div><div class="v-title" style="font-size:15px;margin-top:4px">直播内容</div><div class="v-replay-timeline">${eventHTML}</div><div class="v-title" style="font-size:15px;margin-top:14px">聊天室</div><div class="v-replay-chat">${chatHTML}</div></div>`);
            };

            const deleteReplay = async (replayId) => {
              const index = state.liveReplays.findIndex(item => String(item.id) === String(replayId));
              if (index < 0) return;
              if (typeof window.confirm === "function" && !window.confirm("删除这条直播回放？")) return;
              const removed = state.liveReplays.splice(index, 1)[0];
              await persist();
              if (q('[data-screen="replay"]')?.classList.contains("is-open")) closeScreen("replay");
              const profile = q('[data-screen="profile"]');
              if (profile?.classList.contains("is-open")) renderProfileTab(removed.ownerType || "identity", removed.ownerId || state.viewerIdentityId, "replays");
              toast("回放已删除");
            };

            const endCurrentLive = async ({ automatic = false } = {}) => {
              const live = state.liveSession;
              if (!live) return;
              const stats = getLiveStats(live);
              stats.ended = true;
              stats.endedAt = Date.now();
              let replayOwnerType = "";
              let replayOwnerId = "";
              const own = isOwnLiveSession(live);
              const userEligible = own || Boolean(live.userParticipant);
              const guests = getLiveGuests(live);
              const collab = Boolean(guests.length || live.userParticipant);
              const userShare = userEligible ? (collab ? 0.5 : 1) : 0;

              if (own) {
                const identity = state.identities.find(x => String(x.id) === String(live.ownerId || state.viewerIdentityId));
                if (identity) {
                  identity.live = false;
                  identity.activeLive = null;
                  replayOwnerType = "identity";
                  replayOwnerId = identity.id;
                }
              } else {
                const channel = state.channels.find(x => String(x.id) === String(live.id) || String(x.id) === String(live.ownerId || ""));
                if (channel) {
                  channel.live = false;
                  channel.viewers = 0;
                  replayOwnerType = "channel";
                  replayOwnerId = channel.id;
                }
                const rec = state.recommendedLives.find(x => String(x.id) === String(live.id));
                if (rec) rec.live = false;
              }

              const tipCoin = Math.max(0, Math.round(Number(stats.tipReceivedCoin || 0)));
              const giftCoin = Math.max(0, Math.round(Number(stats.giftReceivedCoin || safeArray(stats.gifts).reduce((sum,item) => sum + Number(item.amount || 0), 0))));
              const totalCoin = tipCoin + giftCoin;
              const ad = stats.ad || live.ad || live.business || null;
              const businessContract = Boolean(ad?.sourceMessageId && ad?.sourceCardId);
              const businessCashCNY = businessContract ? 0 : Math.max(0, Number(ad?.cashAmountCNY || 0));
              const userCoin = Math.round(totalCoin * userShare);
              const userCashCNY = businessCashCNY * userShare;

              if (userEligible && userCoin > 0) {
                state.wallet.coinBalance = Number(state.wallet.coinBalance || 0) + userCoin;
                const tipShare = Math.round(tipCoin * userShare);
                const giftShare = Math.max(0, userCoin - tipShare);
                if (tipShare) state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-tips`, type:"coin", title:"直播打赏", note:`${live.title || "本场直播"}${collab ? " · 联播分成 50%" : ""}`, coin:tipShare, identityId:(replayOwnerType === "identity" ? replayOwnerId : state.viewerIdentityId), at:Date.now() });
                if (giftShare) state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-gifts`, type:"coin", title:"直播礼物", note:`${live.title || "本场直播"}${collab ? " · 联播分成 50%" : ""}`, coin:giftShare, identityId:(replayOwnerType === "identity" ? replayOwnerId : state.viewerIdentityId), at:Date.now() });
              }
              if (userEligible && userCashCNY > 0) {
                state.wallet.cashBalanceCNY = Number(state.wallet.cashBalanceCNY || 0) + userCashCNY;
                state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-biz`, type:"cash", title:"商务合作", note:`${ad?.brandName || ad?.label || "本场关联广告"}${collab ? " · 联播分成 50%" : ""}`, cashCNY:userCashCNY, identityId:(replayOwnerType === "identity" ? replayOwnerId : state.viewerIdentityId), at:Date.now() });
              }

              const charIds = new Set();
              if (replayOwnerType === "channel" && replayOwnerId) charIds.add(String(replayOwnerId));
              guests.forEach(g => { if (state.channels.some(ch => String(ch.id) === String(g.id))) charIds.add(String(g.id)); });
              const charPoolCoin = userEligible && collab ? Math.max(0, totalCoin - userCoin) : (userEligible ? 0 : totalCoin);
              const charPoolCash = userEligible && collab ? Math.max(0, businessCashCNY - userCashCNY) : (userEligible ? 0 : businessCashCNY);
              const charCount = Math.max(1, charIds.size);
              charIds.forEach(charId => {
                const earning = state.roleEarnings[charId] || (state.roleEarnings[charId] = { coin:0, cashCNY:0, subscriptionCoin:0, liveHistory:[] });
                const coinPart = Math.round(charPoolCoin / charCount);
                const cashPart = charPoolCash / charCount;
                earning.coin = Number(earning.coin || 0) + coinPart;
                earning.cashCNY = Number(earning.cashCNY || 0) + cashPart;
                earning.liveHistory = safeArray(earning.liveHistory);
                earning.liveHistory.unshift({ liveId:live.id, title:live.title || "直播", coin:coinPart, cashCNY:cashPart, at:Date.now(), collab });
                earning.liveHistory = earning.liveHistory.slice(0, 50);
              });

              if (replayOwnerType && replayOwnerId) {
                const progress = getLiveProgress(live);
                state.liveReplays.unshift({
                  id: `replay-${Date.now().toString(36)}`,
                  ownerType: replayOwnerType,
                  ownerId: replayOwnerId,
                  title: live.title || "直播回放",
                  topic: live.category || "直播",
                  cover: live.cover || "",
                  mode: live.mode || "solo",
                  participants: guests.map(item => ({ ...item })),
                  accessMode: live.accessMode || "public",
                  ageRequirement: live.ageRequirement || "none",
                  startedAt: Number(live.startedAt || Date.now()),
                  peakViewers: Number(stats.peakViewers || stats.currentViewers || 0),
                  gifts: safeArray(stats.gifts).map(item => ({ ...item })),
                  tipCoin,
                  giftCoin,
                  businessCashCNY,
                  ad: ad ? { ...ad } : null,
                  lottery: stats.lottery ? JSON.parse(JSON.stringify(stats.lottery)) : null,
                  timeline: safeArray(progress.timeline).map(item => ({ ...item })),
                  chat: safeArray(state.liveChat).map(item => ({ ...item })),
                  endedAt: Date.now(),
                  endedText: "刚刚结束"
                });
              }

              let businessTailPaid = 0;
              if (ad?.sourceMessageId && ad?.sourceCardId) {
                const contractCard = findDMCard(ad.sourceMessageId, ad.sourceCardId);
                if (contractCard?.kind === "business") {
                  const payIdentityId = String(live.ownerType === "identity" ? live.ownerId : (state.viewerIdentityId || ""));
                  businessTailPaid = recordBusinessPayment(contractCard, "balance", payIdentityId, ad.sourceMessageId);
                  contractCard.status = "completed"; contractCard.completedAt = Date.now(); contractCard.liveEndedAt = Date.now();
                  const msg = state.messages.find(m => String(m.id) === String(ad.sourceMessageId)); if (msg) msg.preview = "[商务合作已完成]";
                }
              }

              state.liveSession = null;
              state.pendingLiveUserMessage = null;
              state.pendingOwnLiveInput = null;
              await persist();
              renderHome();
              renderChannels();
              renderWallet();

              const panel = q('[data-screen="live"] [data-role="live-economy-panel"]');
              const strangerEnded = !own && replayOwnerType !== "channel";
              if (panel) {
                const heading = strangerEnded ? "主播已结束直播" : "当前直播已结束";
                const summary = own ? "本次收益已结算到钱包。" : replayOwnerType === "channel" ? "本次收益已结算到后台。" : "本场直播已经结束。";
                panel.innerHTML = `<div class="v-live-summary-card v-live-summary-compact"><h3>${escapeHTML(heading)}</h3><p>${escapeHTML(summary)}</p><button class="v-live-summary-close" data-action="close-live-summary-card">确定</button></div>`;
                panel.classList.add("is-open");
              } else {
                closeScreen("live");
                if (!automatic) toast(strangerEnded ? "主播已结束直播" : "当前直播已结束");
              }
            };

            const openSettingsNote = (kind) => {
              if (kind === "presets") {
                const mode = String(state.generationPreset?.mode || "default");
                openScreen("settings", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="settings">‹</button><div class="v-meta"><strong>生成预设</strong><div class="v-hint">Vela 默认规则会一直生效；普通自定义与 18+ / NSFW 预设分开注入。</div></div><button class="v-head-action" data-action="save-generation-settings">保存</button></header><div class="v-subbody"><div class="v-settings-group"><h3>Vela 默认生成规则</h3><div class="v-preset-box">${escapeHTML(VELA_DEFAULT_PRESET)}</div></div><div class="v-settings-group"><h3>普通自定义</h3><div class="v-field"><label>使用方式</label><select data-setting-field="presetMode"><option value="default" ${mode === "default" ? "selected" : ""}>只使用 Vela 默认预设</option><option value="custom" ${mode === "custom" ? "selected" : ""}>默认预设 + 我的补充</option></select></div><div class="v-field" style="margin-top:12px"><label>自定义补充（可留空）</label><textarea data-setting-field="customPreset" maxlength="5000" placeholder="例如：希望弹幕更安静、直播节奏更慢……">${escapeHTML(state.generationPreset?.customText || "")}</textarea></div></div><div class="v-settings-group"><h3>NSFW / 18+ 预设</h3><p class="v-settings-note">只注入到带 18+ 标签的 user / char 直播与贴文互动，以及推荐页中带 NSFW 标签的直播和聊天室。不会影响普通内容。</p><div class="v-field"><label>成熟向生成风格</label><textarea data-setting-field="nsfwPreset" maxlength="5000" placeholder="写下你希望的成熟向主播、聊天室、评论区风格……">${escapeHTML(state.generationPreset?.nsfwText || "")}</textarea></div></div></div>`);
                return;
              }
              if (kind === "appearance") {
                const ap = state.appearance || {};
                openScreen("settings", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="settings">‹</button><div class="v-meta"><strong>外观设置</strong><div class="v-hint">调整 Vela 顶栏与底栏占用比例</div></div><button class="v-head-action" data-action="save-appearance-settings">保存</button></header><div class="v-subbody"><div class="v-settings-group"><h3>界面比例</h3><div class="v-setting-row"><div class="v-meta"><div class="v-name">顶栏高度 · <span data-role="topbar-value">${Number(ap.topbarHeight || 62)}px</span></div></div></div><input class="v-range" data-setting-field="topbarHeight" type="range" min="52" max="86" step="1" value="${Number(ap.topbarHeight || 62)}"><div class="v-setting-row"><div class="v-meta"><div class="v-name">底栏高度 · <span data-role="bottombar-value">${Number(ap.bottombarHeight || 78)}px</span></div></div></div><input class="v-range" data-setting-field="bottombarHeight" type="range" min="64" max="100" step="1" value="${Number(ap.bottombarHeight || 78)}"><div class="v-settings-actions"><button class="v-action light" data-action="reset-appearance">恢复默认</button></div></div></div>`);
                return;
              }
              if (kind === "platform") {
                const ps = state.platformSettings || {};
                const stranger = ["low","normal","high"].includes(ps.strangerInteraction) ? ps.strangerInteraction : "normal";
                const langPref = LANGUAGE_PREFERENCE_OPTIONS.some(item => item.key === String(ps.languagePreference || "")) ? String(ps.languagePreference) : "overseas_more";
                openScreen("settings", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="settings">‹</button><div class="v-meta"><strong>平台设置</strong><div class="v-hint">控制 Vela 世界整体运行方式</div></div><button class="v-head-action" data-action="save-platform-settings">保存</button></header><div class="v-subbody"><div class="v-settings-group"><div class="v-setting-row"><div class="v-meta"><div class="v-name">平台活跃度 · <span data-role="activity-value">${Number(ps.activityLevel ?? 50)}%</span></div><div class="v-sub">影响直播人数、聊天室、社群在线与网友互动密度</div></div></div><input class="v-range" data-setting-field="activityLevel" type="range" min="0" max="100" step="5" value="${Number(ps.activityLevel ?? 50)}"><div class="v-field" style="margin-top:14px"><label>陌生互动频率</label><select data-setting-field="strangerInteraction"><option value="low" ${stranger === "low" ? "selected" : ""}>少</option><option value="normal" ${stranger === "normal" ? "selected" : ""}>正常</option><option value="high" ${stranger === "high" ? "selected" : ""}>多</option></select></div><div class="v-setting-row"><div class="v-meta"><div class="v-name">允许陌生商务邀约</div><div class="v-sub">关闭后不会新生成陌生品牌合作邀约</div></div><input class="v-switch" data-setting-field="allowBusinessDM" type="checkbox" ${ps.allowBusinessDM !== false ? "checked" : ""}></div><div class="v-setting-row"><div class="v-meta"><div class="v-name">内容语言倾向 · <span data-role="language-preference-value">${escapeHTML(languagePreferenceLabel(langPref))}</span></div><div class="v-sub">只调整陌生主播、陌生私信、粉丝聊天与网友评论的语言权重；不会覆盖 user / char 自己的语言设定。</div></div></div><input class="v-range" data-setting-field="languagePreference" type="range" min="0" max="4" step="1" value="${languagePreferenceIndex(langPref)}"><div class="v-language-scale"><span>中文为主</span><span>全球均衡</span><span>海外为主</span></div><div class="v-field" style="margin-top:14px"><label>推荐页每批直播数量</label><input data-setting-field="recommendationCount" type="number" min="1" max="6" step="1" value="${Number(ps.recommendationCount || 3)}"></div><div class="v-setting-row"><div class="v-meta"><div class="v-name">推荐中出现角色副号</div><div class="v-sub">默认关闭；开启后副号只低概率进入候选，角色主号仍不会被推荐。</div></div><input class="v-switch" data-setting-field="allowRoleAliasesInRecommendations" type="checkbox" ${ps.allowRoleAliasesInRecommendations === true ? "checked" : ""}></div></div></div>`);
                return;
              }
              openScreen("settings", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="settings">‹</button><div class="v-meta"><strong>记忆互通</strong><div class="v-hint">暂时保留入口</div></div></header><div class="v-subbody"><div class="v-card"><p class="v-settings-note" style="margin:0">这一项先保留入口，后续用于主聊天记忆、重要直播事件回传，以及角色对匿名账号的认知与 Vela 近期事件同步。</p></div></div>`);
            };

            const saveGenerationSettings = async () => {
              const screen = q('[data-screen="settings"]');
              const mode = screen?.querySelector('[data-setting-field="presetMode"]')?.value === "custom" ? "custom" : "default";
              const customText = String(screen?.querySelector('[data-setting-field="customPreset"]')?.value || "").trim();
              const nsfwText = String(screen?.querySelector('[data-setting-field="nsfwPreset"]')?.value || "").trim();
              state.generationPreset = { mode, customText, nsfwText };
              await persist(); toast("生成预设已保存");
            };

            const saveAppearanceSettings = async () => {
              const screen = q('[data-screen="settings"]');
              const topbarHeight = Math.max(52, Math.min(86, Number(screen?.querySelector('[data-setting-field="topbarHeight"]')?.value || 62)));
              const bottombarHeight = Math.max(64, Math.min(100, Number(screen?.querySelector('[data-setting-field="bottombarHeight"]')?.value || 78)));
              state.appearance = { topbarHeight, bottombarHeight };
              root.style.setProperty("--v-top-h", `${topbarHeight}px`); root.style.setProperty("--v-bottom-h", `${bottombarHeight}px`);
              await persist(); toast("界面比例已保存");
            };

            const savePlatformSettings = async () => {
              const screen = q('[data-screen="settings"]');
              const recommendationCount = Math.max(1, Math.min(6, Math.floor(Number(screen?.querySelector('[data-setting-field="recommendationCount"]')?.value || 3))));
              const allowRoleAliasesInRecommendations = Boolean(screen?.querySelector('[data-setting-field="allowRoleAliasesInRecommendations"]')?.checked);
              const allowBusinessDM = Boolean(screen?.querySelector('[data-setting-field="allowBusinessDM"]')?.checked);
              const activityLevel = Math.max(0, Math.min(100, Number(screen?.querySelector('[data-setting-field="activityLevel"]')?.value ?? 50)));
              const strangerInteraction = ["low","normal","high"].includes(String(screen?.querySelector('[data-setting-field="strangerInteraction"]')?.value || "normal")) ? String(screen.querySelector('[data-setting-field="strangerInteraction"]').value) : "normal";
              const langIndex = Math.max(0, Math.min(4, Math.round(Number(screen?.querySelector('[data-setting-field="languagePreference"]')?.value ?? languagePreferenceIndex(state.platformSettings?.languagePreference)))));
              const languagePreference = LANGUAGE_PREFERENCE_OPTIONS[langIndex]?.key || "overseas_more";
              state.platformSettings = { ...(state.platformSettings || {}), recommendationCount, allowRoleAliasesInRecommendations, allowBusinessDM, activityLevel, strangerInteraction, languagePreference };
              await persist(); renderRecommended(); renderMessages(); toast("平台设置已保存");
            };

            const openSubscriptionSettings = (ownerType, ownerId) => {
              const entity = getProfileEntity(ownerType, ownerId); if (!entity) return;
              const cfg = getSubscriptionProfile(ownerType, ownerId);
              openScreen("subscription-settings", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="subscription-settings">‹</button><div class="v-meta"><strong>订阅设置</strong><div class="v-hint">${escapeHTML(profileName(entity, ownerType))}</div></div><button class="v-head-action" data-action="save-subscription-settings" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(ownerId)}">保存</button></header><div class="v-subbody"><div class="v-settings-group v-sub-plan-toggle"><div class="v-setting-row"><div class="v-meta"><div class="v-name">开启订阅计划</div><div class="v-sub">关闭时这个账号就是普通频道，不显示订阅档位或会员社群。</div></div><input class="v-switch" data-sub-field="enabled" data-action="toggle-subscription-plan-editor" type="checkbox" ${cfg.enabled ? "checked" : ""}></div></div><div class="v-subscription-editor-grid" data-role="subscription-plan-fields" ${cfg.enabled ? "" : "hidden"}><section class="v-subscription-editor-tier"><h3>免费订阅</h3><div class="v-editform"><div class="v-fieldpair"><div class="v-field"><label>粉丝 Emoji</label><input data-sub-field="freeEmoji" maxlength="4" value="${escapeHTML(cfg.free.emoji || "🌊")}"></div><div class="v-field"><label>方案名称</label><input data-sub-field="freeName" maxlength="24" value="${escapeHTML(cfg.free.name || "支持者")}"></div></div><div class="v-field"><label>卡片封面 URL（可空）</label><input data-sub-field="freeCover" value="${escapeHTML(cfg.free.cover || "")}" placeholder="https://..."></div><div class="v-publish-note">关注频道动态，解锁免费订阅贴文与直播推送。<br>权益：获得粉丝身份。</div></div></section><section class="v-subscription-editor-tier is-paid"><h3>付费订阅</h3><div class="v-editform"><div class="v-fieldpair"><div class="v-field"><label>粉丝 Emoji</label><input data-sub-field="paidEmoji" maxlength="4" value="${escapeHTML(cfg.paid.emoji || "💎")}"></div><div class="v-field"><label>方案名称</label><input data-sub-field="paidName" maxlength="24" value="${escapeHTML(cfg.paid.name || "会员")}"></div></div><div class="v-fieldpair"><div class="v-field"><label>月费 · Vela Coin</label><input data-sub-field="paidCoinPrice" type="number" min="1" max="999999" step="1" value="${Number(cfg.paid.coinPrice || 500)}"></div><div class="v-field"><label>卡片封面 URL（可空）</label><input data-sub-field="paidCover" value="${escapeHTML(cfg.paid.cover || "")}" placeholder="https://..."></div></div><div class="v-field"><label>对应群聊名称</label><input data-sub-field="paidCommunity" maxlength="40" value="${escapeHTML(cfg.paid.communityName || "")}"></div><div class="v-publish-note">支持喜欢的主播，解锁付费订阅内容与社群。<br>权益：获得付费粉丝身份 · 加入订阅社群 · 获得主播第一手资讯。</div></div></section></div></div>`);
            };

            const saveSubscriptionSettings = async (ownerType, ownerId) => {
              const entity=getProfileEntity(ownerType,ownerId), screen=q('[data-screen="subscription-settings"]'); if(!entity||!screen) return;
              const read=name=>String(screen.querySelector(`[data-sub-field="${name}"]`)?.value||"").trim();
              const current=getSubscriptionProfile(ownerType,ownerId);
              const wasEnabled = current.enabled === true;
              const enabled = Boolean(screen.querySelector('[data-sub-field="enabled"]')?.checked);
              state.subscriptionProfiles[subscriptionProfileKey(ownerType,ownerId)]={
                enabled,
                free:{...current.free,emoji:read("freeEmoji")||"🌊",name:read("freeName")||"支持者",cover:read("freeCover"),intro:"关注频道动态，解锁免费订阅贴文与直播推送。",benefits:["获得粉丝身份。"]},
                paid:{...current.paid,emoji:read("paidEmoji")||"💎",name:read("paidName")||"会员",coinPrice:Math.max(1,Math.min(999999,Math.round(Number(read("paidCoinPrice")||500)))),cover:read("paidCover"),intro:"支持喜欢的主播，解锁付费订阅内容与社群。",benefits:["获得付费粉丝身份","加入订阅社群","获得主播第一手资讯"],communityName:read("paidCommunity")||`${profileName(entity,ownerType)} · 会员社群`}
              };
              if (!wasEnabled && enabled) {
                const key = communityChatKey(ownerType, ownerId, "paid");
                // A newly opened paid community starts clean; old demo/ambient chat must not leak back in.
                state.communityChats[key] = [{ id:`system-sub-open-${Date.now().toString(36)}`, kind:"system", eventType:"subscription_opened", text:"您已开通付费订阅社群", at:Date.now() }];
                state.communityChatQueues[key] = [];
              }
              await persist(); closeScreen("subscription-settings"); openProfile(ownerType,ownerId); renderProfileTab(ownerType,ownerId,"subscriptions"); renderMessages(); toast(enabled ? "订阅方案已保存" : "订阅计划已关闭");
            };

            const formatChatTimestamp = (value) => {
              const date = new Date(Number(value || Date.now()));
              const now = new Date();
              const sameDay = date.getFullYear() === now.getFullYear() && date.getMonth() === now.getMonth() && date.getDate() === now.getDate();
              const time = date.toLocaleTimeString("zh-CN", { hour: "2-digit", minute: "2-digit", hour12: false });
              return sameDay ? `今天 ${time}` : `${date.getMonth() + 1}月${date.getDate()}日 ${time}`;
            };

            const threadTimeHTML = (rows, index) => {
              const current = Number(rows[index]?.at || Date.now());
              const prev = index > 0 ? Number(rows[index - 1]?.at || 0) : 0;
              return index === 0 || current - prev > 10 * 60 * 1000
                ? `<div class="v-chat-timestamp">${escapeHTML(formatChatTimestamp(current))}</div>`
                : "";
            };

            const renderChatBubble = (item, { group = false, threadKey = "", threadType = "dm" } = {}) => {
              const mine = item.sender === "user" || item.isUser;
              const trId = `chat-${String(item.id || item.at || Math.random())}`;
              const role = item.role === "owner" || item.sender === "owner" ? `<span class="v-role-title is-owner">博主</span>` : item.role === "bot" || item.sender === "bot" ? `<span class="v-role-title is-bot">机器人</span>` : "";
              const title = group && !item.typing ? `<div class="v-chat-sender">${role}${escapeHTML(item.name || (mine ? "你" : "成员"))}</div>` : "";
              const longpress = item.id && threadKey && !item.typing ? ` data-longpress="delete-chat-item" data-thread-type="${escapeHTML(threadType)}" data-thread-key="${escapeHTML(threadKey)}" data-item-id="${escapeHTML(item.id)}"` : "";
              const canTranslateDM = threadType === "dm" && !mine && Boolean(item.translation || item.needsTranslation || textNeedsSimplifiedTranslation(item.text, item.locale));
              const canTranslateCommunity = threadType === "community" && !mine && Boolean(item.translation);
              const bubbleTranslation = (canTranslateDM || canTranslateCommunity)
                ? `<div class="v-dm-bubble-translation" data-translation-id="${escapeHTML(trId)}" hidden>${escapeHTML(item.translation || "")}</div>`
                : "";
              const bubbleAction = canTranslateDM
                ? ` data-action="toggle-dm-bubble-translation" data-translation-target="${escapeHTML(trId)}" data-message-id="${escapeHTML(threadKey)}" data-item-id="${escapeHTML(item.id || "")}"`
                : canTranslateCommunity
                  ? ` data-action="toggle-community-bubble-translation" data-translation-target="${escapeHTML(trId)}" data-community-key="${escapeHTML(threadKey)}" data-item-id="${escapeHTML(item.id || "")}"`
                  : "";
              const regularTranslation = threadType !== "dm" && threadType !== "community" && item.translation ? translationHTML(trId, item.translation) : "";
              const bubbleText = item.typing ? '<span class="v-typing-dots"><i></i><i></i><i></i></span>' : escapeHTML(item.text || "");
              const incomingAvatar = !mine ? `<div class="v-social-avatar">${avatarHTML(item.avatar || "", item.name || "V")}</div>` : "";
              const ownAvatar = mine && threadType === "dm" ? `<div class="v-social-avatar v-own-chat-avatar">${avatarHTML(item.avatar || "", item.name || "U")}</div>` : "";
              return `<div class="v-chat-row ${mine ? "is-me" : ""}"${longpress}>${incomingAvatar}<div class="v-chat-stack">${title}<div class="v-chat-bubble"${bubbleAction}>${bubbleText}${bubbleTranslation}${regularTranslation}</div></div>${ownAvatar}</div>`;
            };

            const statusLabel = status => status === "completed" ? "已完成" : status === "accepted" ? "已接受" : status === "declined" ? "已婉拒" : status === "cancelled" ? "已取消" : status === "filled" ? "已填写" : status === "revising" ? "修改确认中" : "待确认";
            const hasScheduleForCard = (messageId, cardId) => {
              const rows = safeArray(state.dmThreads?.[String(messageId)]);
              const card = rows.find(item => String(item?.id || "") === String(cardId || ""));
              const matches = entity => scheduledLiveList(entity).some(item => String(item?.sourceMessageId || "") === String(messageId || "") && String(item?.sourceCardId || "") === String(cardId || ""));
              if (card?.hostOwnerType === "identity") {
                const activeIdentityId = String(state.dmInteractionIdentity?.[String(messageId)] || state.viewerIdentityId || state.identities[0]?.id || "");
                const identity = state.identities.find(item => String(item.id) === activeIdentityId);
                return Boolean(identity && matches(identity));
              }
              return [...state.identities, ...state.channels].some(matches);
            };

            const renderDMToolCard = (item, messageId) => {
              const incoming = item.direction === "incoming";
              const status = String(item.status || "pending");
              if (item.kind === "live_invite") {
                return `<div class="v-chat-card" data-longpress="delete-chat-item" data-thread-type="dm" data-thread-key="${escapeHTML(messageId)}" data-item-id="${escapeHTML(item.id)}"><div class="v-chat-card-head"><span class="v-chat-card-type">直播邀约</span><span class="v-chat-card-status ${status === "accepted" ? "is-ok" : status === "declined" ? "is-no" : ""}">${statusLabel(status)}</span></div><div class="v-chat-card-title">${escapeHTML(item.title || "一起直播")}</div><div class="v-chat-card-meta"><div>时间 <b>${escapeHTML(item.dateText || "待定")}</b></div><div>内容 <b>${escapeHTML(item.content || "一起聊聊")}</b></div><div>方式 <b>${item.mode === "same-room" ? "线下同镜" : "线上联播"}</b></div><div>主场 <b>${escapeHTML(item.hostLabel || "待确认")}</b></div></div>${status === "pending" && incoming ? `<div class="v-chat-card-actions"><button data-action="accept-dm-card" data-message-id="${escapeHTML(messageId)}" data-card-id="${escapeHTML(item.id)}">接受</button><button class="is-light" data-action="decline-dm-card" data-message-id="${escapeHTML(messageId)}" data-card-id="${escapeHTML(item.id)}">婉拒</button></div>` : status === "pending" ? `<div class="v-chat-card-actions"><button class="is-light" data-action="cancel-dm-card" data-message-id="${escapeHTML(messageId)}" data-card-id="${escapeHTML(item.id)}">取消邀约</button></div>` : ""}</div>`;
              }
              if (item.kind === "business") {
                const amounts = businessAmounts(item);
                const money = amounts.total > 0 ? formatCash(amounts.total) : "金额待确认";
                const longpress = ` data-longpress="delete-chat-item" data-thread-type="dm" data-thread-key="${escapeHTML(messageId)}" data-item-id="${escapeHTML(item.id)}"`;
                const price = Number(item.productPriceCNY || 0) > 0 ? `<div>商品售价 <b>${escapeHTML(formatCash(Number(item.productPriceCNY)))}</b></div>` : "";
                const pay = amounts.total > 0 ? `<div>定金 <b>${escapeHTML(formatCash(amounts.deposit))}${item.depositPaid ? " · 已到账" : ""}</b></div><div>尾款 <b>${escapeHTML(formatCash(amounts.balance))}${item.balancePaid ? " · 已到账" : ""}</b></div>` : "";
                return `<div class="v-chat-card is-business"${longpress}><div class="v-chat-card-head"><span class="v-chat-card-type">商务合作 · ${escapeHTML(item.brandName || "品牌")}</span><span class="v-chat-card-status ${status === "accepted" || status === "completed" ? "is-ok" : status === "declined" ? "is-no" : ""}">${statusLabel(status)}</span></div><div class="v-chat-card-title">${escapeHTML(item.title || item.productName || "直播合作方案")}</div><div class="v-chat-card-meta"><div>产品 / 项目 <b>${escapeHTML(item.productName || item.title || "合作项目")}</b></div>${price}<div>合作内容 <b>${escapeHTML(item.content || item.deliverables || "")}</b></div><div>直播时间 <b>${escapeHTML(item.dateText || formatBusinessWindow(item.scheduledAt, item.endsAt))}</b></div><div>直播主场 <b>${escapeHTML(item.hostLabel || "待确认")}</b></div><div>合作总费用 <b>${escapeHTML(money)}</b></div>${pay}${item.adCopy ? `<div>广告词 <b>${escapeHTML(item.adCopy)}</b></div>` : ""}${item.adLink ? `<div>广告链接 <b>${escapeHTML(item.adLink)}</b></div>` : ""}</div>${status === "pending" && incoming ? `<button class="v-chat-card-edit" data-action="edit-business-card" data-message-id="${escapeHTML(messageId)}" data-card-id="${escapeHTML(item.id)}">需要改动？编辑后回传给对方确认</button><div class="v-chat-card-actions"><button data-action="accept-dm-card" data-message-id="${escapeHTML(messageId)}" data-card-id="${escapeHTML(item.id)}">接受并预定直播</button><button class="is-light" data-action="decline-dm-card" data-message-id="${escapeHTML(messageId)}" data-card-id="${escapeHTML(item.id)}">婉拒直播</button></div>` : status === "accepted" && item.includesLive && !hasScheduleForCard(messageId, item.id) ? `<div class="v-chat-card-actions"><button data-action="recreate-business-schedule" data-message-id="${escapeHTML(messageId)}" data-card-id="${escapeHTML(item.id)}">重新创建预约直播</button></div>` : ""}</div>`;
              }
              if (item.kind === "business_revision") {
                const longpress = ` data-longpress="delete-chat-item" data-thread-type="dm" data-thread-key="${escapeHTML(messageId)}" data-item-id="${escapeHTML(item.id)}"`;
                return `<div class="v-chat-card"${longpress}><div class="v-chat-card-head"><span class="v-chat-card-type">你回传的合作修改</span><span class="v-chat-card-status">${status === "accepted" ? "对方已确认" : "等待对方确认"}</span></div><div class="v-chat-card-title">${escapeHTML(item.title || "合作修改")}</div><div class="v-chat-card-meta"><div>直播时间 <b>${escapeHTML(item.dateText || "待定")}</b></div><div>合作内容 <b>${escapeHTML(item.content || "")}</b></div><div>合作费用 <b>${escapeHTML(Number(item.cashAmountCNY || 0) > 0 ? formatCash(item.cashAmountCNY) : "待确认")}</b></div></div></div>`;
              }
              if (item.kind === "winner_form") {
                return `<div class="v-chat-card" data-longpress="delete-chat-item" data-thread-type="dm" data-thread-key="${escapeHTML(messageId)}" data-item-id="${escapeHTML(item.id)}"><div class="v-chat-card-head"><span class="v-chat-card-type">中奖信息填写</span><span class="v-chat-card-status ${status === "filled" ? "is-ok" : ""}">${statusLabel(status)}</span></div><div class="v-chat-card-title">${escapeHTML(item.prize || "中奖礼物")}</div><div class="v-chat-card-meta"><div>${escapeHTML(item.note || "请填写领奖所需的信息。")}</div>${item.reply ? `<div>已提交 <b>${escapeHTML(item.reply)}</b></div>` : ""}</div>${status === "pending" && incoming ? `<div class="v-chat-card-actions"><button data-action="fill-winner-card" data-message-id="${escapeHTML(messageId)}" data-card-id="${escapeHTML(item.id)}">填写信息</button></div>` : ""}</div>`;
              }
              return "";
            };

            const renderDirectThread = (messageId) => {
              const msg = state.messages.find(item => String(item.id) === String(messageId));
              if (!msg) return;
              state.dmThreads[messageId] = safeArray(state.dmThreads[messageId]).filter(item => item && typeof item === "object");
              const rows = state.dmThreads[messageId];
              const channel = state.channels.find(ch => String(ch.id) === String(msg.channelId || ""));
              const handle = String(channel?.handle || msg.handle || (msg.name ? `@${String(msg.name).toLowerCase().replace(/\s+/g, "_")}` : "@vela"));
              const rowHTML = rows.map((item, index) => `${threadTimeHTML(rows, index)}${item.kind && item.kind !== "text" ? renderDMToolCard(item, messageId) : renderChatBubble(item, { threadKey: messageId, threadType: "dm" })}`).join("");
              const rememberedIdentity = String(state.dmInteractionIdentity?.[String(messageId)] || "");
              const selectedIdentity = state.identities.find(item => String(item.id) === rememberedIdentity) || state.identities.find(item => String(item.id) === String(state.viewerIdentityId)) || state.identities[0];
              if (selectedIdentity) state.dmInteractionIdentity[String(messageId)] = String(selectedIdentity.id);
              const identityPicker = identityPickerHTML(state.identities, selectedIdentity?.id, "dm-identity-select", `data-message-id="${escapeHTML(messageId)}"`);
              openScreen("message", `<div class="v-chat-screen v-dm-chat-screen"><header class="v-chat-head"><button class="v-chat-back" data-action="close-screen" data-screen-name="message">‹</button><div class="v-chat-headcopy"><strong>${escapeHTML(msg.name || "Vela ID")}</strong><small>${escapeHTML(handle)}</small></div><div class="v-chat-headactions"><button class="v-chat-headicon" data-action="toggle-chat-topmenu" aria-label="更多" title="更多">⋯</button></div><div class="v-chat-topmenu" data-role="chat-topmenu"><button data-action="clear-direct-chat" data-message-id="${escapeHTML(messageId)}">清除聊天记录</button></div></header><div class="v-chat-body" data-role="chat-body">${rowHTML || '<div class="v-group-empty">还没有消息。<br>Enter 只发送你的消息，小飞机才召唤对方。</div>'}</div><div class="v-chat-composer-wrap">${identityPicker}<input data-role="dm-input" data-message-id="${escapeHTML(messageId)}" placeholder="发消息… · Enter发送" maxlength="600"><button class="v-chat-plus" data-action="open-dm-plus" data-message-id="${escapeHTML(messageId)}">＋</button><button class="v-chat-summon ${dmReplyPending.has(String(messageId)) ? "is-generating" : ""}" data-action="summon-direct-message" data-message-id="${escapeHTML(messageId)}" ${dmReplyPending.has(String(messageId)) ? "disabled" : ""}>${dmReplyPending.has(String(messageId)) ? '<span class="v-summon-dots">•••</span>' : planeIconHTML()}</button></div></div>`);
              requestAnimationFrame(() => {
                const body = q('[data-screen="message"] [data-role="chat-body"]');
                if (body) body.scrollTop = body.scrollHeight;
              });
            };

            const ensureDMThreadSeed = (msg) => {
              state.dmThreads[msg.id] = safeArray(state.dmThreads[msg.id]);
              if (!state.dmThreads[msg.id].length && msg.preview && !msg.outboundToChannel) {
                state.dmThreads[msg.id].push({
                  id: `seed-${msg.id}`,
                  kind: "text",
                  sender: "other",
                  name: msg.name || "对方",
                  avatar: msg.avatar || "",
                  text: msg.preview,
                  translation: String(msg.previewTranslation || ""),
                  locale: String(msg.locale || ""),
                  needsTranslation: Boolean(!msg.previewTranslation && textNeedsSimplifiedTranslation(msg.preview, msg.locale)),
                  at: Date.now() - 2 * 60 * 1000
                });
              }
            };

            const openMessage = (id) => {
              const messageId = String(id || "");
              const msg = state.messages.find(x => String(x.id) === messageId);
              if (!msg) { toast("这条私信暂时无法打开"); return; }
              ensureDMThreadSeed(msg);
              msg.unread = 0;
              try {
                renderDirectThread(msg.id);
              } catch (err) {
                console.warn("[Vela] failed to open DM thread", err, msg);
                toast("私信打开失败，请重新进入后再试");
                return;
              }
              persist();
            };

            const messageChannel = async (channelId) => {
              const channel = state.channels.find(ch => String(ch.id) === String(channelId));
              if (!channel) return;
              let msg = state.messages.find(item => String(item.channelId || "") === String(channel.id) && item.outboundToChannel);
              if (!msg) {
                msg = { id: `channel-dm-${String(channel.id)}`, avatar: channel.avatar || "", name: channel.name || channel.handle || "频道", handle: channel.handle || "", badge: "", channelId: channel.id, outboundToChannel: true, preview: "", unread: 0 };
                state.messages.unshift(msg);
              }
              state.dmThreads[msg.id] = safeArray(state.dmThreads[msg.id]);
              await persist();
              renderMessages();
              closeScreen("profile");
              renderDirectThread(msg.id);
            };

            const sendDirectMessage = async (messageId) => {
              const msg = state.messages.find(item => String(item.id) === String(messageId));
              const input = q('[data-screen="message"] [data-role="dm-input"]');
              const text = String(input?.value || "").trim();
              if (!msg || !text) return;
              const selectedIdentityId = String(q('[data-screen="message"] [data-role="dm-identity-select"]')?.value || state.dmInteractionIdentity?.[String(messageId)] || state.viewerIdentityId || "");
              const identity = state.identities.find(item => String(item.id) === selectedIdentityId) || state.identities.find(item => String(item.id) === String(state.viewerIdentityId)) || state.identities[0];
              if (identity) state.dmInteractionIdentity[String(messageId)] = String(identity.id);
              state.dmThreads[messageId] = safeArray(state.dmThreads[messageId]);
              state.dmThreads[messageId].push({ id: `dm-${Date.now().toString(36)}`, kind: "text", sender: "user", name: identity?.displayName || identity?.handle || "user", avatar: identity?.avatar || "", text, translation: "", at: Date.now(), isUser: true, identityId: identity?.id || "" });
              state.dmThreads[messageId] = state.dmThreads[messageId].slice(-240);
              msg.preview = text;
              if (input) input.value = "";
              await persist();
              renderMessages();
              renderDirectThread(messageId);
            };

            const extractJSON = (text) => {
              const raw = String(text || "").trim().replace(/^```(?:json)?\s*/i, "").replace(/\s*```$/i, "");
              try { return JSON.parse(raw); } catch (_) {}
              const first = raw.indexOf("{"), last = raw.lastIndexOf("}");
              if (first >= 0 && last > first) {
                try { return JSON.parse(raw.slice(first, last + 1)); } catch (_) {}
              }
              const a = raw.indexOf("["), b = raw.lastIndexOf("]");
              if (a >= 0 && b > a) {
                try { return JSON.parse(raw.slice(a, b + 1)); } catch (_) {}
              }
              return null;
            };

            const normalizeVelaAIText = result => {
              if (result == null) return "";
              if (typeof result === "string") return result;
              if (typeof result?.text === "string") return result.text;
              if (typeof result?.content === "string") return result.content;
              if (typeof result?.message === "string") return result.message;
              if (typeof result?.output === "string") return result.output;
              if (typeof result?.output_text === "string") return result.output_text;
              if (Array.isArray(result?.content)) return result.content.map(part => typeof part === "string" ? part : String(part?.text || part?.content || "")).join("");
              if (typeof result?.message?.content === "string") return result.message.content;
              if (typeof result?.choices?.[0]?.message?.content === "string") return result.choices[0].message.content;
              return "";
            };
            const isTransientVelaAIError = err => /temporarily unavailable|try again later|overloaded|rate limit|status 429|status 502|status 503|status 504/i.test(String(err?.message || err || ""));
            const shortVelaAIError = fallback => String(lastVelaAIError || fallback || "未知错误").replace(/\s+/g," ").trim().slice(0,120);
            const runVelaAI = async (messages) => {
              lastVelaAIError = "";
              if (typeof roche?.ai?.chat !== "function") {
                lastVelaAIError = "roche.ai.chat 不可用";
                return "";
              }
              let lastError = null;
              for (let attempt = 0; attempt < 3; attempt += 1) {
                try {
                  const result = await roche.ai.chat({ messages, temperature: 0.85 });
                  const text = normalizeVelaAIText(result);
                  if (!String(text || "").trim()) {
                    lastVelaAIError = "Roche AI 已返回，但 result.text / content 为空";
                    return "";
                  }
                  return String(text);
                } catch (err) {
                  lastError = err;
                  lastVelaAIError = String(err?.message || err || "AI 接口调用失败");
                  console.warn(`[Vela] Roche AI call failed · attempt ${attempt + 1}`, err);
                  if (!isTransientVelaAIError(err) || attempt >= 2) break;
                  await new Promise(resolve => setTimeout(resolve, 700 * (attempt + 1)));
                }
              }
              if (lastError && isTransientVelaAIError(lastError)) lastVelaAIError = `Roche 当前模型暂时不可用，已自动重试 2 次 · ${String(lastError?.message || lastError)}`;
              return "";
            };

            const getChannelPersonaText = (channel) => {
              if (!channel?.sourceCharacterId) return "";
              const char = rocheRuntime.characters.find(item => String(item?.id) === String(channel.sourceCharacterId));
              return String(char?.persona || char?.prompt || char?.description || char?.bio || "");
            };


            const businessAmounts = card => {
              const total = Math.max(0, Number(card?.totalFeeCNY ?? card?.cashAmountCNY ?? 0));
              const deposit = Math.max(0, Math.min(total, Number(card?.depositCNY ?? (total ? Math.round(total * .25) : 0))));
              const balance = Math.max(0, total - deposit);
              return { total, deposit, balance };
            };

            const formatBusinessWindow = (start, end) => {
              const a = Number(start || 0), b = Number(end || 0);
              if (!a) return "时间待定";
              const d = new Date(a), e = new Date(b || a + 2 * 3600000);
              return `${d.getMonth()+1}/${d.getDate()} · ${String(d.getHours()).padStart(2,"0")}:${String(d.getMinutes()).padStart(2,"0")}–${String(e.getHours()).padStart(2,"0")}:${String(e.getMinutes()).padStart(2,"0")}`;
            };

            const recordBusinessPayment = (card, kind, identityId, messageId) => {
              if (!card || card.kind !== "business") return 0;
              const { deposit, balance } = businessAmounts(card);
              const isDeposit = kind === "deposit";
              const flag = isDeposit ? "depositPaid" : "balancePaid";
              if (card[flag]) return 0;
              const amount = isDeposit ? deposit : balance;
              card[flag] = true; card[`${flag}At`] = Date.now();
              if (amount > 0) {
                state.wallet.cashBalanceCNY = Number(state.wallet.cashBalanceCNY || 0) + amount;
                state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-${isDeposit ? "bizdep" : "bizbal"}`, type:"cash", title:isDeposit ? "商务定金" : "商务尾款", note:`${card.brandName || "品牌"} · ${card.productName || card.title || "合作"}`, cashCNY:amount, identityId:String(identityId || state.viewerIdentityId || ""), messageId:String(messageId || ""), cardId:String(card.id || ""), at:Date.now() });
              }
              return amount;
            };

            const createScheduleFromCard = async (messageId, card) => {
              if (!(card.kind === "live_invite" || (card.kind === "business" && card.includesLive))) return;
              let host = getProfileEntity(card.hostOwnerType, card.hostOwnerId);
              if (!host && card.hostOwnerType === "identity") {
                const fallbackId = String(state.dmInteractionIdentity?.[String(messageId)] || state.viewerIdentityId || state.identities[0]?.id || "");
                host = state.identities.find(item => String(item.id) === fallbackId) || null;
                if (host) { card.hostOwnerId = String(host.id); card.hostLabel = `${host.displayName || host.handle || "你的账号"}的直播间`; }
              }
              if (!host) return;
              const msg = state.messages.find(item => String(item.id) === String(messageId));
              const peerChannel = state.channels.find(ch => String(ch.id) === String(msg?.channelId || ""));
              const viewerId = String(card.hostOwnerType === "identity" ? card.hostOwnerId : (state.dmInteractionIdentity?.[String(messageId)] || state.viewerIdentityId || ""));
              const viewer = state.identities.find(item => String(item.id) === viewerId) || state.identities.find(item => String(item.id) === String(state.viewerIdentityId));
              const participants = [viewer ? { ownerType:"identity", id:viewer.id, name:viewer.displayName || viewer.handle || "user" } : null, peerChannel ? { ownerType:"channel", id:peerChannel.id, name:peerChannel.name || peerChannel.handle || "频道" } : null].filter(Boolean);
              const amounts = businessAmounts(card);
              const start = Number(card.scheduledAt || 0);
              const rawEnd = Number(card.endsAt || 0);
              const end = start ? (rawEnd > start ? rawEnd : start + 2 * 3600000) : rawEnd;
              card.endsAt = end;
              card.dateText = card.kind === "business" ? formatBusinessWindow(start, end) : String(card.dateText || "时间待定");
              if (card.kind === "business") { card.scheduleDeletedAt = 0; card.scheduleLinkedAt = Date.now(); }
              const scheduleId = `card:${String(messageId)}:${String(card.id || "")}`;
              // One DM card owns one appointment, but each host can have many different appointments.
              [...state.identities, ...state.channels].forEach(entity => {
                if (entity === host) return;
                const rows = scheduledLiveList(entity).filter(item => !(String(item.sourceMessageId || "") === String(messageId) && String(item.sourceCardId || "") === String(card.id || "")));
                entity.scheduledLives = rows; entity.scheduledLive = null; scheduledLiveList(entity);
              });
              const appointment = {
                scheduleId, title:String(card.title || (card.kind === "business" ? "商务合作直播" : "联播预约")), dateText:String(card.dateText || "时间待定"), scheduledAt:start, endsAt:end, cover:String(card.cover || ""), content:String(card.content || ""), mode:String(card.mode || "online"), sourceMessageId:String(messageId), sourceCardId:String(card.id), sourceType:String(card.kind), hostOwnerType:String(card.hostOwnerType), hostOwnerId:String(card.hostOwnerId), participantIds:participants.map(p => p.id), participants,
                business: card.kind === "business" ? { brandName:String(card.brandName || ""), productName:String(card.productName || ""), productPriceCNY:Math.max(0,Number(card.productPriceCNY || 0)), totalFeeCNY:amounts.total, cashAmountCNY:amounts.total, depositCNY:amounts.deposit, balanceCNY:amounts.balance, deliverables:String(card.deliverables || card.content || ""), adCopy:String(card.adCopy || ""), adLink:String(card.adLink || ""), sourceMessageId:String(messageId), sourceCardId:String(card.id || ""), scheduledAt:start, endsAt:end } : null
              };
              upsertScheduledLive(host, appointment);
              await persist(); armAppointmentTimer(); armCommunityBotTimers?.();
            };

            const findDMCard = (messageId, cardId) => {
              const rows = safeArray(state.dmThreads?.[String(messageId)]);
              return rows.find(item => String(item.id) === String(cardId));
            };

            const acceptDMCard = async (messageId, cardId) => {
              const card = findDMCard(messageId, cardId);
              if (!card || card.status === "accepted" || card.status === "completed") return;
              if (card.kind === "business" && !Number(card.scheduledAt || 0)) { toast("这张合作卡还没有确定直播时间"); return; }
              card.status = "accepted"; card.acceptedAt = Date.now();
              if (card.hostOwnerType === "identity") {
                const activeIdentityId = String(state.dmInteractionIdentity?.[String(messageId)] || state.viewerIdentityId || state.identities[0]?.id || "");
                const activeIdentity = state.identities.find(item => String(item.id) === activeIdentityId) || state.identities[0];
                if (activeIdentity) {
                  card.hostOwnerId = String(activeIdentity.id);
                  card.hostLabel = `${activeIdentity.displayName || activeIdentity.handle || "你的账号"}的直播间`;
                }
              }
              if (card.kind === "business") {
                const identityId = String(card.hostOwnerType === "identity" ? card.hostOwnerId : (state.dmInteractionIdentity?.[String(messageId)] || state.viewerIdentityId || ""));
                recordBusinessPayment(card, "deposit", identityId, messageId);
              }
              await createScheduleFromCard(messageId, card); await persist(); renderWallet(); renderHome(); renderChannels(); renderDirectThread(messageId);
              toast(card.kind === "live_invite" ? "已接受直播邀约，预约已关联到主场主页" : "合作已接受，定金已结算并生成预约直播");
            };

            const declineDMCard = async (messageId, cardId) => {
              const card = findDMCard(messageId, cardId);
              if (!card) return;
              card.status = "declined";
              await persist();
              renderDirectThread(messageId);
            };

            const cancelDMCard = async (messageId, cardId) => {
              const card = findDMCard(messageId, cardId);
              if (!card) return;
              card.status = "cancelled";
              await persist();
              renderDirectThread(messageId);
            };

            const summonDirectMessage = async (messageId) => {
              messageId = String(messageId || "");
              const msg = state.messages.find(item => String(item.id) === messageId);
              if (!msg || dmReplyPending.has(messageId)) return;
              dmReplyPending.add(messageId);
              state.dmThreads[messageId] = safeArray(state.dmThreads[messageId]);
              const typingId = `dm-typing-${Date.now().toString(36)}`;
              state.dmThreads[messageId].push({ id:typingId, kind:"text", sender:"other", name:msg.name || "对方", avatar:msg.external || msg.strangerKind ? STRANGER_AVATAR : (msg.avatar || ""), text:"", translation:"", typing:true, at:Date.now() });
              state.dmThreads[messageId] = state.dmThreads[messageId].slice(-240);
              // Typing is a transient UI state: show it immediately, but never persist it as chat history.
              renderDirectThread(messageId);
              const removeTyping = () => { state.dmThreads[messageId] = safeArray(state.dmThreads[messageId]).filter(item => String(item?.id || "") !== typingId); };
              try {
                const thread = safeArray(state.dmThreads[messageId]).filter(item => !item?.typing);
                const channel = state.channels.find(ch => String(ch.id) === String(msg.channelId || ""));
                const persona = getChannelPersonaText(channel);
                const history = thread.slice(-18).map(item => {
                  if (item.kind && item.kind !== "text") {
                    if (item.kind === "business" || item.kind === "business_revision") return `[${item.kind}] brand=${item.brandName || ""}; product=${item.productName || ""}; productPriceCNY=${Number(item.productPriceCNY || 0)}; title=${item.title || ""}; content=${item.content || ""}; date=${item.dateText || ""}; start=${Number(item.scheduledAt || 0)}; end=${Number(item.endsAt || 0)}; totalFeeCNY=${Number(item.totalFeeCNY ?? item.cashAmountCNY ?? 0)}; depositCNY=${Number(item.depositCNY || 0)}; balanceCNY=${Number(item.balanceCNY || 0)}; host=${item.hostLabel || ""}; adCopy=${item.adCopy || ""}; adLink=${item.adLink || ""}; status=${item.status || "pending"}`;
                    if (item.kind === "live_invite") return `[live_invite] title=${item.title || ""}; content=${item.content || ""}; date=${item.dateText || ""}; mode=${item.mode || ""}; host=${item.hostLabel || ""}; status=${item.status || "pending"}`;
                    return `[${item.kind}] ${item.title || item.content || item.prize || ""} / status=${item.status || "pending"}`;
                  }
                  return `${item.sender === "user" ? "USER" : "OTHER"}: ${item.text || ""}`;
                }).join("\n");
                const pendingOutgoing = thread.filter(item => item.direction === "outgoing" && item.status === "pending" && item.kind === "live_invite").slice(-1)[0];
                const system = `${VELA_DEFAULT_PRESET}\n\n你正在模拟 Vela 私信中 USER 之外的对方。绝对禁止替 USER 发言、行动、做决定。Enter 只发送 USER 自己写的内容，小飞机召唤时即使 USER 没有新消息，对方也可以自然继续说自己的事，不需要强行围着 USER。对方身份：${msg.name || "对方"} ${channel?.handle || msg.handle || ""}。对方类型：${msg.badge || msg.strangerKind || "普通私信"}。${msg.businessProfile ? `商务资料：${msg.businessProfile}。` : ""}${persona ? `角色设定：${persona}` : ""}\n${msg.external || msg.strangerKind ? `陌生用户语言倾向：${languagePreferencePrompt()}` : "这是角色账号私信：必须遵循角色自身语言与人设，不受平台陌生人语言倾向覆盖。"}\n${translationRulePrompt()}

请返回严格 JSON。普通消息：
{"type":"message","messages":[{"text":"对方实际发出的消息1","translation":"按翻译规则填写"},{"text":"可选的第2条","translation":"按翻译规则填写"}],"text":"兼容单条消息时可填写","translation":"兼容单条翻译","acceptCardId":""}
普通私信一次允许对方自然连续说 1~3 条短消息；如果输出多条，用 messages 数组。Vela 会在生成完成后一起显示，不做逐条懒加载。
平台陌生互动频率：${state.platformSettings?.strangerInteraction || "normal"}。
只有在非常自然、符合身份和上下文时，才可以主动发直播邀约。商务合作只能由“对方 / 品牌方”主动提出，USER 不能自己生成正式商务卡。${state.platformSettings?.allowBusinessDM === false ? "当前平台设置已关闭陌生商务邀约：除非这个会话已经存在明确合作上下文，否则不得主动创建新的 business 卡。" : ""}
直播邀约：
{"type":"live_invite","title":"标题","content":"直播内容","dateText":"时间文字","scheduledAt":"YYYY-MM-DDTHH:mm","mode":"online|same-room","host":"counterpart|user","text":"可选附言","translation":"按翻译规则填写"}
如果你是品牌 / 商务联系人：先像真实工作人员一样正常聊天，说明公司主营、品牌和产品并询问 USER 是否有合作意向。USER 没明确同意时可以继续回答疑问、谈需求和价格，不要急着甩正式卡。只有品牌、商品、直播要求、商品售价（若适用）、合作总费用、定金/尾款、明确的开始与结束时间都大致谈妥后，才发正式商务卡。定金 + 尾款应等于合作总费用：
{"type":"business","brandName":"公司或品牌","productName":"产品","productPriceCNY":299,"title":"合作标题","content":"直播要求与交付内容","totalFeeCNY":3200,"depositCNY":800,"balanceCNY":2400,"scheduledAt":"YYYY-MM-DDTHH:mm","endsAt":"YYYY-MM-DDTHH:mm","host":"user|counterpart","adCopy":"广告词","adLink":"链接","text":"可选附言","translation":"按翻译规则填写"}
如果最近出现 [business_revision]，你可以继续讨论；若同意 USER 的修改，必须重新发一张新的 business 卡，内容采用双方最后确认的版本。
如果对方自然同意 USER 已经发出的待确认卡，可以在普通 message JSON 里把 acceptCardId 写成卡片 ID：${pendingOutgoing?.id || "无待确认卡"}。不要自动替 USER 接受任何东西。`;
                const aiText = await runVelaAI([{ role:"system", content:system }, { role:"user", content:`最近私信：\n${history || "目前还没有聊天。请对方自然开启或继续话题。"}` }]);
                if (!String(aiText || "").trim()) { removeTyping(); renderDirectThread(messageId); toast(`对方生成失败 · ${shortVelaAIError("AI 返回为空")}`); return; }
                const data = extractJSON(aiText);
                if (!data || typeof data !== "object" || Array.isArray(data)) { removeTyping(); renderDirectThread(messageId); toast(`对方生成失败 · 回复格式解析失败 · ${String(aiText || "").replace(/\s+/g," ").slice(0,72)}`); return; }
                removeTyping();
                if (data.acceptCardId && pendingOutgoing && String(data.acceptCardId) === String(pendingOutgoing.id)) { pendingOutgoing.status = "accepted"; await createScheduleFromCard(messageId, pendingOutgoing); }
                const otherAvatar = msg.external || msg.strangerKind ? STRANGER_AVATAR : (msg.avatar || "");
                if (data.type === "live_invite" || data.type === "business") {
                  const incoming = { id:`dmcard-${Date.now().toString(36)}`, kind:data.type === "business" ? "business" : "live_invite", direction:"incoming", status:"pending", sender:"other", name:msg.name || "对方", avatar:otherAvatar, brandName:String(data.brandName || (data.type === "business" ? msg.name || "品牌" : "")), productName:String(data.productName || ""), title:String(data.title || (data.type === "business" ? "合作方案" : "一起直播")), content:String(data.content || ""), productPriceCNY:data.type === "business" ? Math.max(0,Number(data.productPriceCNY || 0)) : 0, totalFeeCNY:data.type === "business" ? Math.max(0,Number(data.totalFeeCNY ?? data.cashAmountCNY ?? 0)) : 0, cashAmountCNY:data.type === "business" ? Math.max(0,Number(data.totalFeeCNY ?? data.cashAmountCNY ?? 0)) : 0, depositCNY:data.type === "business" && data.depositCNY != null ? Math.max(0,Number(data.depositCNY || 0)) : undefined, balanceCNY:data.type === "business" && data.balanceCNY != null ? Math.max(0,Number(data.balanceCNY || 0)) : undefined, deliverables:String(data.content || ""), adCopy:String(data.adCopy || ""), adLink:String(data.adLink || ""), mode:data.mode === "same-room" ? "same-room" : "online", includesLive:true, scheduledAt:data.scheduledAt && !Number.isNaN(new Date(data.scheduledAt).getTime()) ? new Date(data.scheduledAt).getTime() : 0, endsAt:data.endsAt && !Number.isNaN(new Date(data.endsAt).getTime()) ? new Date(data.endsAt).getTime() : 0, dateText:"", at:Date.now() };
                  if (incoming.kind === "business") { if (incoming.scheduledAt && (!incoming.endsAt || incoming.endsAt <= incoming.scheduledAt)) incoming.endsAt = incoming.scheduledAt + 2*3600000; const amounts=businessAmounts(incoming); incoming.totalFeeCNY=amounts.total; incoming.cashAmountCNY=amounts.total; incoming.depositCNY=amounts.deposit; incoming.balanceCNY=amounts.balance; incoming.dateText=formatBusinessWindow(incoming.scheduledAt,incoming.endsAt); } else incoming.dateText=String(data.dateText || (incoming.scheduledAt ? formatDateTextFromInput(data.scheduledAt) : "时间待定"));
                  if (String(data.host) === "counterpart" && channel) { incoming.hostOwnerType="channel"; incoming.hostOwnerId=channel.id; incoming.hostLabel=`${channel.name || "对方"}的直播间`; } else { incoming.hostOwnerType="identity"; incoming.hostOwnerId=state.viewerIdentityId; incoming.hostLabel="你的直播间"; }
                  state.dmThreads[messageId].push(incoming);
                  if (data.text) state.dmThreads[messageId].push({ id:`dm-${Date.now().toString(36)}-after`, kind:"text", sender:"other", name:msg.name || "对方", avatar:otherAvatar, text:String(data.text), translation:String(data.translation || ""), locale:String(msg.locale || ""), needsTranslation:Boolean(!data.translation && textNeedsSimplifiedTranslation(String(data.text || ""), msg.locale)), at:Date.now()+1 });
                } else {
                  const messageRows = safeArray(data.messages).slice(0,3).map(item => ({ text:String(item?.text || "").trim(), translation:String(item?.translation || "").trim() })).filter(item => item.text);
                  if (!messageRows.length && String(data.text || "").trim()) messageRows.push({ text:String(data.text || "").trim(), translation:String(data.translation || "").trim() });
                  if (!messageRows.length) { await persist(); renderDirectThread(messageId); toast("对方这次没有生成有效消息"); return; }
                  messageRows.forEach((row,index) => state.dmThreads[messageId].push({ id:`dm-${Date.now().toString(36)}-${index}`, kind:"text", sender:"other", name:msg.name || "对方", avatar:otherAvatar, text:row.text, translation:row.translation, locale:String(msg.locale || ""), needsTranslation:Boolean(!row.translation && textNeedsSimplifiedTranslation(row.text, msg.locale)), at:Date.now()+index }));
                  if (!data.text) data.text = messageRows.map(row => row.text).join(" ");
                }
                state.dmThreads[messageId] = state.dmThreads[messageId].slice(-240);
                msg.preview = String(data.text || data.title || msg.preview || "");
                await persist(); renderMessages(); renderDirectThread(messageId);
              } catch (err) {
                console.warn("[Vela] DM reply processing failed", err);
                removeTyping();
                await persist();
                try { renderDirectThread(messageId); } catch (renderErr) { console.warn("[Vela] DM recovery render failed", renderErr); }
                toast(`私信处理失败 · ${String(err?.message || err || "未知错误").replace(/\s+/g," ").slice(0,110)}`);
              } finally {
                removeTyping();
                dmReplyPending.delete(messageId);
                if (q('[data-screen="message"] [data-role="dm-input"]')?.dataset?.messageId === String(messageId)) renderDirectThread(messageId);
              }
            };

            const translateDMBubble = async (messageId, itemId, translationTarget) => {
              const rows = safeArray(state.dmThreads?.[String(messageId)]);
              const item = rows.find(row => String(row?.id || "") === String(itemId || ""));
              if (!item || item.translation || !String(item.text || "").trim()) return false;
              const raw = await runVelaAI([{ role:"system", content:"把给定文本翻译成自然、准确的简体中文。即使原文是繁体中文，也转换为简体中文。只返回严格 JSON：{\"translation\":\"...\"}。" }, { role:"user", content:String(item.text || "") }]);
              const data = extractJSON(raw);
              const translation = String(data?.translation || "").trim();
              if (!translation) { toast("翻译生成失败，请再试一次"); return false; }
              item.translation = translation; item.needsTranslation = false;
              await persist(); renderDirectThread(messageId);
              requestAnimationFrame(() => { const el=qa('[data-translation-id]').find(node => String(node.dataset.translationId || "") === String(translationTarget || "")); if (el) el.removeAttribute("hidden"); });
              return true;
            };

            const openDMPlus = (messageId) => {
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              if (!sheet || !host) return;
              host.innerHTML = `<div class="v-publish-sheet-title"><h3>发送到私信</h3><button data-action="close-publish-sheet">×</button></div><div class="v-chat-tool-grid"><button data-action="open-dm-tool-form" data-message-id="${escapeHTML(messageId)}" data-tool-type="live_invite">直播邀约<small>你和对方都可以发直播邀约</small></button><button data-action="open-dm-tool-form" data-message-id="${escapeHTML(messageId)}" data-tool-type="winner_form">中奖信息填写<small>直播抽奖后让中奖粉丝填写领奖信息</small></button></div>`;
              sheet.classList.add("is-open");
            };


            const dateInputDefault = () => {
              const date = new Date(Date.now() + 60 * 60 * 1000);
              const local = new Date(date.getTime() - date.getTimezoneOffset() * 60000);
              return local.toISOString().slice(0, 16);
            };

            const formatDateTextFromInput = value => {
              if (!value) return "时间待定";
              const date = new Date(value);
              if (Number.isNaN(date.getTime())) return String(value);
              return `${date.getMonth() + 1}/${date.getDate()} · ${String(date.getHours()).padStart(2,"0")}:${String(date.getMinutes()).padStart(2,"0")}`;
            };

            const openDMToolForm = (messageId, toolType) => {
              const msg = state.messages.find(item => String(item.id) === String(messageId));
              if (!msg) return;
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              const channel = state.channels.find(ch => String(ch.id) === String(msg.channelId || ""));
              if (!sheet || !host) return;
              const hostOptions = `<option value="user">我的直播间</option>${channel ? `<option value="counterpart">${escapeHTML(channel.name || "对方")}的直播间</option>` : ""}`;

              if (toolType === "live_invite") {
                host.innerHTML = `<div class="v-publish-sheet-title"><h3>直播邀约</h3><button data-action="open-dm-plus" data-message-id="${escapeHTML(messageId)}">‹</button></div><div class="v-tool-form"><div class="v-field"><label>直播时间</label><input type="datetime-local" data-dm-tool-field="scheduledAt" value="${dateInputDefault()}"></div><div class="v-field"><label>直播标题</label><input data-dm-tool-field="title" maxlength="90" placeholder="一起播什么？"></div><div class="v-field"><label>直播内容</label><textarea data-dm-tool-field="content" maxlength="500" placeholder="大概聊什么、做什么"></textarea></div><div class="v-fieldpair"><div class="v-field"><label>联播方式</label><select data-dm-tool-field="mode"><option value="online">线上联播</option><option value="same-room">线下同镜</option></select></div><div class="v-field"><label>主场频道</label><select data-dm-tool-field="host">${hostOptions}</select></div></div><div class="v-tool-note">接受后才会生成预约直播，并自动挂到主场主播的主页。</div><button class="v-action" data-action="send-dm-tool-card" data-message-id="${escapeHTML(messageId)}" data-tool-type="live_invite">发送邀约</button></div>`;
              } else if (toolType === "business") {
                toast("商务合作由品牌 / 对方发起");
                openDMPlus(messageId);
                return;
              } else {
                host.innerHTML = `<div class="v-publish-sheet-title"><h3>中奖信息填写</h3><button data-action="open-dm-plus" data-message-id="${escapeHTML(messageId)}">‹</button></div><div class="v-tool-form"><div class="v-field"><label>奖品</label><input data-dm-tool-field="prize" maxlength="90" placeholder="中奖礼物"></div><div class="v-field"><label>填写说明</label><textarea data-dm-tool-field="note" maxlength="350" placeholder="例如：请填写领奖联系方式和收件备注"></textarea></div><button class="v-action" data-action="send-dm-tool-card" data-message-id="${escapeHTML(messageId)}" data-tool-type="winner_form">发送填写卡</button></div>`;
              }
              sheet.classList.add("is-open");
            };

            const sendDMToolCard = async (messageId, toolType) => {
              if (toolType === "business") { toast("商务合作只能由品牌 / 对方发起"); return; }
              const msg = state.messages.find(item => String(item.id) === String(messageId));
              const sheet = q('[data-role="publish-sheet"]');
              if (!msg || !sheet) return;
              const read = name => String(sheet.querySelector(`[data-dm-tool-field="${name}"]`)?.value || "").trim();
              const channel = state.channels.find(ch => String(ch.id) === String(msg.channelId || ""));
              const card = {
                id: `dmcard-${Date.now().toString(36)}`,
                kind: toolType,
                direction: "outgoing",
                status: "pending",
                sender: "user",
                name: state.identities.find(i => String(i.id) === String(state.viewerIdentityId))?.displayName || "user",
                avatar: state.identities.find(i => String(i.id) === String(state.viewerIdentityId))?.avatar || "",
                at: Date.now()
              };
              if (toolType === "live_invite" || toolType === "business") {
                const title = read("title"), content = read("content");
                if (!title || !content) { toast("先把标题和内容填完整"); return; }
                card.title = title;
                card.content = content;
                card.includesLive = toolType === "live_invite" ? true : read("includesLive") !== "no";
                card.mode = read("mode") === "same-room" ? "same-room" : "online";
                const dateValue = read("scheduledAt");
                card.scheduledAt = dateValue ? new Date(dateValue).getTime() : 0;
                card.dateText = formatDateTextFromInput(dateValue);
                const hostChoice = read("host");
                if (hostChoice === "counterpart" && channel) {
                  card.hostOwnerType = "channel";
                  card.hostOwnerId = channel.id;
                  card.hostLabel = `${channel.name || "对方"}的直播间`;
                } else {
                  card.hostOwnerType = "identity";
                  card.hostOwnerId = state.viewerIdentityId;
                  card.hostLabel = "你的直播间";
                }
              } else {
                card.prize = read("prize") || "中奖礼物";
                card.note = read("note") || "请填写领奖所需的信息。";
              }
              state.dmThreads[messageId] = safeArray(state.dmThreads[messageId]);
              state.dmThreads[messageId].push(card);
              msg.preview = toolType === "live_invite" ? "[直播邀约]" : toolType === "business" ? "[商务合作]" : "[中奖信息填写]";
              sheet.classList.remove("is-open");
              await persist();
              renderMessages();
              renderDirectThread(messageId);
            };

            const localDateTimeValue = ms => {
              const value = Number(ms || 0); if (!value) return "";
              const d = new Date(value - new Date().getTimezoneOffset()*60000); return d.toISOString().slice(0,16);
            };

            const openBusinessCardEditor = (messageId, cardId) => {
              const card = findDMCard(messageId, cardId), msg = state.messages.find(item => String(item.id) === String(messageId));
              if (!card || card.kind !== "business" || !msg) return;
              const sheet = q('[data-role="publish-sheet"]'), host = q('[data-role="publish-sheet-content"]');
              const channel = state.channels.find(ch => String(ch.id) === String(msg.channelId || "")); if (!sheet || !host) return;
              const hostCurrent = card.hostOwnerType === "channel" ? "counterpart" : "user";
              const amounts = businessAmounts(card);
              host.innerHTML = `<div class="v-publish-sheet-title"><h3>修改合作卡</h3><button data-action="close-publish-sheet">×</button></div><div class="v-tool-form"><div class="v-field"><label>品牌 / 公司</label><input data-business-edit="brandName" value="${escapeHTML(card.brandName || msg.name || "")}" maxlength="60"></div><div class="v-field"><label>产品 / 项目</label><input data-business-edit="productName" value="${escapeHTML(card.productName || "")}" maxlength="90"></div><div class="v-field"><label>商品售价 · CNY（可空）</label><input type="number" min="0" step="1" data-business-edit="productPriceCNY" value="${Number(card.productPriceCNY || 0)}"></div><div class="v-field"><label>合作标题</label><input data-business-edit="title" value="${escapeHTML(card.title || "")}" maxlength="100"></div><div class="v-field"><label>直播要求 / 交付内容</label><textarea data-business-edit="content" maxlength="800">${escapeHTML(card.content || card.deliverables || "")}</textarea></div><div class="v-fieldpair"><div class="v-field"><label>开始时间</label><input type="datetime-local" data-business-edit="scheduledAt" value="${localDateTimeValue(card.scheduledAt) || dateInputDefault()}"></div><div class="v-field"><label>结束时间</label><input type="datetime-local" data-business-edit="endsAt" value="${localDateTimeValue(card.endsAt)}"></div></div><div class="v-fieldpair"><div class="v-field"><label>合作总费用 · CNY</label><input type="number" min="0" step="1" data-business-edit="totalFeeCNY" value="${amounts.total}"></div><div class="v-field"><label>直播主场</label><select data-business-edit="host"><option value="user" ${hostCurrent === "user" ? "selected" : ""}>我的直播间</option>${channel ? `<option value="counterpart" ${hostCurrent === "counterpart" ? "selected" : ""}>${escapeHTML(channel.name || "对方")}的直播间</option>` : ""}</select></div></div><div class="v-fieldpair"><div class="v-field"><label>定金</label><input type="number" min="0" step="1" data-business-edit="depositCNY" value="${amounts.deposit}"></div><div class="v-field"><label>尾款</label><input type="number" min="0" step="1" data-business-edit="balanceCNY" value="${amounts.balance}"></div></div><div class="v-field"><label>广告词</label><textarea data-business-edit="adCopy" maxlength="500">${escapeHTML(card.adCopy || "")}</textarea></div><div class="v-field"><label>广告链接</label><input class="v-readonly-link" data-business-edit="adLink" value="${escapeHTML(card.adLink || "")}" readonly></div><div class="v-tool-note">修改后会回传给品牌确认；对方同意后应重新发最终合作卡。</div><button class="v-action" data-action="send-business-revision" data-message-id="${escapeHTML(messageId)}" data-card-id="${escapeHTML(cardId)}">把修改发给对方确认</button></div>`;
              sheet.classList.add("is-open");
            };

            const sendBusinessRevision = async (messageId, cardId) => {
              const original = findDMCard(messageId, cardId), msg = state.messages.find(item => String(item.id) === String(messageId)), sheet = q('[data-role="publish-sheet"]');
              if (!original || original.kind !== "business" || !msg || !sheet) return;
              const read = name => String(sheet.querySelector(`[data-business-edit="${name}"]`)?.value || "").trim();
              const channel = state.channels.find(ch => String(ch.id) === String(msg.channelId || ""));
              const startValue = read("scheduledAt"), endValue = read("endsAt");
              const scheduledAt = startValue ? new Date(startValue).getTime() : 0;
              let endsAt = endValue ? new Date(endValue).getTime() : 0; if (scheduledAt && (!endsAt || endsAt <= scheduledAt)) endsAt = scheduledAt + 2*3600000;
              const total = Math.max(0, Number(read("totalFeeCNY") || 0)), deposit = Math.max(0, Math.min(total, Number(read("depositCNY") || 0))), balance = Math.max(0, total - deposit);
              const hostChoice = read("host"); original.status = "revising";
              const revision = { id:`bizrev-${Date.now().toString(36)}`, kind:"business_revision", direction:"outgoing", status:"pending", sender:"user", name:state.identities.find(i => String(i.id) === String(state.viewerIdentityId))?.displayName || "user", brandName:read("brandName"), productName:read("productName"), productPriceCNY:Math.max(0,Number(read("productPriceCNY")||0)), title:read("title") || original.title || "合作修改", content:read("content"), deliverables:read("content"), totalFeeCNY:total, cashAmountCNY:total, depositCNY:Math.min(total,deposit), balanceCNY:balance, adCopy:read("adCopy"), adLink:String(original.adLink || ""), scheduledAt, endsAt, dateText:formatBusinessWindow(scheduledAt,endsAt), hostOwnerType:hostChoice === "counterpart" && channel ? "channel" : "identity", hostOwnerId:hostChoice === "counterpart" && channel ? channel.id : state.viewerIdentityId, hostLabel:hostChoice === "counterpart" && channel ? `${channel.name || "对方"}的直播间` : "你的直播间", at:Date.now() };
              state.dmThreads[messageId] = safeArray(state.dmThreads[messageId]); state.dmThreads[messageId].push(revision); msg.preview = "[合作修改已回传]"; sheet.classList.remove("is-open"); await persist(); renderMessages(); renderDirectThread(messageId);
            };

            const fillWinnerCard = (messageId, cardId) => {
              const card = findDMCard(messageId, cardId);
              if (!card) return;
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              if (!sheet || !host) return;
              host.innerHTML = `<div class="v-publish-sheet-title"><h3>填写中奖信息</h3><button data-action="close-publish-sheet">×</button></div><div class="v-tool-form"><div class="v-field"><label>领奖联系方式 / 备注</label><textarea data-winner-reply maxlength="500" placeholder="填写虚拟领奖信息或备注"></textarea></div><button class="v-action" data-action="save-winner-reply" data-message-id="${escapeHTML(messageId)}" data-card-id="${escapeHTML(cardId)}">提交</button></div>`;
              sheet.classList.add("is-open");
            };

            const saveWinnerReply = async (messageId, cardId) => {
              const card = findDMCard(messageId, cardId);
              const input = q('[data-role="publish-sheet"] [data-winner-reply]');
              const value = String(input?.value || "").trim();
              if (!card || !value) { toast("先填写信息"); return; }
              card.reply = value;
              card.status = "filled";
              q('[data-role="publish-sheet"]')?.classList.remove("is-open");
              await persist();
              renderDirectThread(messageId);
            };

            const clearDirectChat = async (messageId) => {
              if (typeof window.confirm === "function" && !window.confirm("清除这段私信的全部聊天记录？")) return;
              state.dmThreads[messageId] = [];
              const msg = state.messages.find(item => String(item.id) === String(messageId));
              if (msg) msg.preview = "";
              await persist();
              renderMessages();
              renderDirectThread(messageId);
            };

            const flushCommunityChatQueue = key => {
              state.communityChats[key] = safeArray(state.communityChats[key]);
              state.communityChatQueues[key] = safeArray(state.communityChatQueues[key]);
              const now = Date.now();
              const due = state.communityChatQueues[key].filter(item => Number(item?.plannedAt || 0) <= now);
              if (!due.length) return 0;
              const dueIds = new Set(due.map(item => String(item.id)));
              state.communityChatQueues[key] = state.communityChatQueues[key].filter(item => !dueIds.has(String(item?.id || "")));
              due.forEach(item => state.communityChats[key].push({ ...item, at:Number(item.plannedAt || now), plannedAt:undefined }));
              state.communityChats[key] = state.communityChats[key].slice(-300);
              return due.length;
            };
            const armCommunityLazyTimer = key => {
              if (communityLazyTimers.has(key)) clearTimeout(communityLazyTimers.get(key));
              const next = safeArray(state.communityChatQueues?.[key]).map(item => Number(item?.plannedAt || 0)).filter(at => at > Date.now()).sort((a,b)=>a-b)[0];
              if (!next) { communityLazyTimers.delete(key); return; }
              const delay = Math.max(500, Math.min(2147480000, next - Date.now()));
              const timer = setTimeout(async () => {
                communityLazyTimers.delete(key);
                const moved = flushCommunityChatQueue(key);
                if (moved) await persist();
                const currentKey = String(q('[data-screen="message"] [data-role="community-message-input"]')?.dataset?.communityKey || "");
                if (currentKey === key) { const [ownerType, ownerId, tier] = key.split(":"); openCommunityChat(ownerType, ownerId, tier); }
                armCommunityLazyTimer(key);
              }, delay);
              communityLazyTimers.set(key, timer);
            };
            const ensureCommunityAmbientBatch = async key => {
              if (communityGenerationBusy.has(key)) return;
              state.communityChatQueues[key] = safeArray(state.communityChatQueues[key]);
              if (state.communityChatQueues[key].length >= 5) { armCommunityLazyTimer(key); return; }
              const [ownerType, ownerId, tier] = String(key).split(":");
              const entity = getProfileEntity(ownerType, ownerId); if (!entity) return;
              const cfg = getCommunitySettings(ownerType, ownerId, tier);
              communityGenerationBusy.add(key);
              try {
                const history = safeArray(state.communityChats[key]).slice(-22).filter(item => item?.kind === "text").map(item => `${item.sender === "user" ? "USER" : item.name || "成员"}: ${item.text || ""}`).join("\n");
                const localeHint = ownerType === "channel" ? inferChannelLocale(entity) : "global";
                const system = `${VELA_DEFAULT_PRESET}\n\n你在为 Vela 订阅社群预生成一批会在未来慢慢出现的粉丝闲聊。绝对禁止替 USER 发言。群成员不是 NPC 队列，他们可以互相聊天、跑题、接梗、发很短的话或 emoji，也可以聊吃饭、天气、工作、学校、游戏、音乐、最近看到的东西；不要所有消息都围着主播。偶尔两三个人围绕一个小话题接几句，然后自然换话题。${ownerType === "channel" ? "角色频道群主可以极少量自然参与，但不能每批都出现。" : "这是 USER 自己的社群，绝对不要输出 owner 角色，只能生成粉丝成员。"}\n群主：${profileName(entity, ownerType)}；社群主要地区倾向：${localeHint}；粉丝画像：${cfg.fanProfile}；聊天倾向：${cfg.chatStyle}。\n陌生粉丝语言倾向：${languagePreferencePrompt()}\n${translationRulePrompt()}\n返回严格 JSON 数组，恰好 16 条：[{"role":"member|owner","name":"member 时填自然昵称；owner 可空","text":"消息原文","translation":"按翻译规则填写"}]。同一个成员不要连续刷很多条。`;
                const aiText = await runVelaAI([{ role:"system", content:system }, { role:"user", content:`最近群聊：\n${history || "群里刚建立，成员可以自然开启话题。"}` }]);
                const data = extractJSON(aiText); if (!Array.isArray(data)) return;
                const now = Date.now();
                let cursor = now - Math.floor(6 + Math.random()*8) * 60000;
                const queue = [];
                data.slice(0,16).forEach((item,index) => {
                  const ownerReply = ownerType === "channel" && item?.role === "owner" && index > 3;
                  const textValue = String(item?.text || "").trim(); if (!textValue) return;
                  cursor += (index < 4 ? 30000 + Math.random()*90000 : 90000 + Math.random()*6*60000);
                  queue.push({ id:`ambient-${Date.now().toString(36)}-${index}-${Math.random().toString(36).slice(2,5)}`, kind:"text", sender:ownerReply ? "owner" : "other", role:ownerReply ? "owner" : "member", name:ownerReply ? profileName(entity, ownerType) : String(item?.name || cfg.fanName || "member"), avatar:ownerReply ? String(entity.avatar || "") : STRANGER_AVATAR, text:textValue, translation:String(item?.translation || ""), plannedAt:cursor });
                });
                state.communityChatQueues[key].push(...queue);
                flushCommunityChatQueue(key);
                await persist();
                const currentKey = String(q('[data-screen="message"] [data-role="community-message-input"]')?.dataset?.communityKey || "");
                if (currentKey === key) openCommunityChat(ownerType, ownerId, tier);
                armCommunityLazyTimer(key);
              } finally { communityGenerationBusy.delete(key); }
            };

            const openCommunityChat = (ownerType, ownerId, tier) => {
              const entity = getProfileEntity(ownerType, ownerId);
              if (!entity) return;
              if (!subscriptionEnabled(ownerType, ownerId)) { toast("这个频道还没有开启付费订阅社群"); return; }
              tier = "paid";
              const key = communityChatKey(ownerType, ownerId, "paid");
              const own = isOwnCommunity(ownerType, ownerId);
              const canManage = canManageCommunity(ownerType, ownerId);
              const canEdit = canEditCommunitySettings(ownerType, ownerId);
              const cfg = getCommunitySettings(ownerType, ownerId, "paid");
              const stats = getCommunityStats(ownerType, ownerId, "paid");
              state.communityChats[key] = safeArray(state.communityChats[key]);
              const caughtUp = flushCommunityChatQueue(key);
              if (caughtUp) void persist();
              const rows = state.communityChats[key];
              const rowHTML = rows.map((item, index) => `${threadTimeHTML(rows, index)}${renderCommunityItem(item, key, own)}`).join("");
              let eligibleIdentities = own
                ? state.identities.filter(identity => String(identity.id) === String(ownerId))
                : state.identities.filter(identity => getSubscriptionTierFor(identity.id, ownerId) === "paid");
              if (!own && !eligibleIdentities.length) { toast("需要付费订阅后才能进入会员社群"); return; }
              if (!eligibleIdentities.length) eligibleIdentities = state.identities.filter(identity => String(identity.id) === String(state.viewerIdentityId));
              const remembered = String(state.communityInteractionIdentity?.[key] || "");
              const selectedIdentity = eligibleIdentities.find(x => String(x.id) === remembered) || eligibleIdentities.find(x => String(x.id) === String(state.viewerIdentityId)) || eligibleIdentities[0];
              if (selectedIdentity) state.communityInteractionIdentity[key] = String(selectedIdentity.id);
              const identityPicker = identityPickerHTML(eligibleIdentities, selectedIdentity?.id, "community-identity-select", `data-community-key="${escapeHTML(key)}"`);
              openScreen("message", `<div class="v-chat-screen"><header class="v-chat-head"><button class="v-chat-back" data-action="close-screen" data-screen-name="message">‹</button><div class="v-chat-headcopy"><strong>${escapeHTML(cfg.name)} · ${stats.members.toLocaleString("zh-CN")}人</strong><small>${stats.active.toLocaleString("zh-CN")}人正在聊天</small></div><div class="v-chat-headactions">${canEdit ? `<button class="v-chat-headicon is-edit" data-action="edit-community" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(ownerId)}" data-tier="paid">编辑</button>` : ""}<button class="v-chat-headicon" data-action="toggle-chat-topmenu" aria-label="更多" title="更多">⋯</button></div><div class="v-chat-topmenu" data-role="chat-topmenu"><button data-action="clear-community-chat" data-community-key="${escapeHTML(key)}" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(ownerId)}" data-tier="paid">${canManage ? "清空全部聊天记录" : "清空本地聊天记录"}</button></div></header><div class="v-chat-body" data-role="chat-body">${cfg.announcement ? `<div class="v-community-announcement">公告 · ${escapeHTML(cfg.announcement)}</div>` : ""}${rowHTML || '<div class="v-group-empty">群聊已经建立。<br>Enter 只发送你的消息，小飞机会让群成员自己交流。</div>'}</div><div class="v-chat-composer-wrap">${identityPicker}<input data-role="community-message-input" data-community-key="${escapeHTML(key)}" placeholder="发消息… · Enter发送" maxlength="600">${canManage ? `<button class="v-chat-plus" data-action="open-community-plus" data-community-key="${escapeHTML(key)}">＋</button>` : ""}<button class="v-chat-summon ${communityReplyPending.has(key) ? "is-generating" : ""}" data-action="summon-community-chat" data-community-key="${escapeHTML(key)}" ${communityReplyPending.has(key) ? "disabled" : ""}>${communityReplyPending.has(key) ? '<span class="v-summon-dots">•••</span>' : planeIconHTML()}</button></div></div>`);
              requestAnimationFrame(() => {
                const body = q('[data-screen="message"] [data-role="chat-body"]');
                if (body) body.scrollTop = body.scrollHeight;
              });
              armCommunityLazyTimer(key);
            };

            const renderCommunityItem = (item, key, own) => {
              if (item.kind === "system") {
                return `<div class="v-community-system-note">${escapeHTML(item.text || "系统提示")}</div>`;
              }
              if (item.kind === "poll") {
                const total = safeArray(item.votes).reduce((sum, n) => sum + Number(n || 0), 0);
                return `<div class="v-community-card-msg" data-longpress="delete-chat-item" data-thread-type="community" data-thread-key="${escapeHTML(key)}" data-item-id="${escapeHTML(item.id)}"><div class="v-kicker">群内投票</div><h4>${escapeHTML(item.question || "投票")}</h4><p>${escapeHTML(item.note || "")}</p><div class="v-poll-options">${safeArray(item.options).map((opt, index) => {
                  const count = Number(item.votes?.[index] || 0);
                  const voted = safeArray(item.myVotes).includes(index);
                  return `<button class="${voted ? "is-voted" : ""}" data-action="vote-community-poll" data-community-key="${escapeHTML(key)}" data-card-id="${escapeHTML(item.id)}" data-option-index="${index}" ${own ? "disabled" : ""}><span>${escapeHTML(opt)}</span><span>${count}${total ? ` · ${Math.round(count / total * 100)}%` : ""}</span></button>`;
                }).join("")}</div></div>`;
              }
              if (item.kind === "lottery") {
                const ended = item.status === "ended";
                return `<div class="v-community-card-msg" data-longpress="delete-chat-item" data-thread-type="community" data-thread-key="${escapeHTML(key)}" data-item-id="${escapeHTML(item.id)}"><div class="v-kicker">${item.botPosted ? "🤖 机器人 · 群内抽奖" : "群内抽奖"}</div><h4>${escapeHTML(item.prize || "抽奖")}</h4><p>${escapeHTML(item.note || "")}<br>${Number(item.winners || 1)} 人中奖 · ${Number(item.joinedCount || 0)} 人已参与${ended ? `<br>中奖：${escapeHTML(safeArray(item.results).join("、") || "暂无")}` : ""}</p>${own ? (ended ? `<button class="v-lottery-action" disabled>已开奖</button>` : `<button class="v-lottery-action" data-action="resolve-community-lottery" data-community-key="${escapeHTML(key)}" data-card-id="${escapeHTML(item.id)}">开奖并公布结果</button>`) : `<button class="v-lottery-action" data-action="join-community-lottery" data-community-key="${escapeHTML(key)}" data-card-id="${escapeHTML(item.id)}" ${ended ? "disabled" : ""}>${ended ? "已开奖" : item.joined ? "已参与" : "参与抽奖"}</button>`}</div>`;
              }
              if (item.kind === "bot_card") {
                const label = item.eventType === "scheduled" ? "📅 预约直播" : item.eventType === "live" ? "🔴 已开播" : item.eventType === "lottery_result" ? "🎉 抽奖结果" : "🤖 社群助手";
                return `<div class="v-community-card-msg" data-longpress="delete-chat-item" data-thread-type="community" data-thread-key="${escapeHTML(key)}" data-item-id="${escapeHTML(item.id)}"><div class="v-social-head" style="margin-bottom:8px"><div class="v-social-avatar">${avatarHTML(item.botAvatar || "", item.botName || "BOT")}</div><div class="v-meta"><div class="v-name"><span class="v-role-title is-bot">机器人</span>${escapeHTML(item.botName || "Vela 小助手")}</div><div class="v-sub">${escapeHTML(label)}</div></div></div><h4>${escapeHTML(item.title || "频道通知")}</h4><p>${escapeHTML(item.text || item.dateText || "")}</p></div>`;
              }
              return renderChatBubble(item, { group: true, threadKey: key, threadType: "community" });
            };

            const sendCommunityMessage = async (key) => {
              const input = q('[data-screen="message"] [data-role="community-message-input"]');
              const text = String(input?.value || "").trim();
              if (!text) return;
              const selectedIdentityId = String(q('[data-screen="message"] [data-role="community-identity-select"]')?.value || state.communityInteractionIdentity?.[key] || state.viewerIdentityId || "");
              const identity = state.identities.find(item => String(item.id) === selectedIdentityId) || state.identities.find(item => String(item.id) === String(state.viewerIdentityId)) || state.identities[0];
              if (identity) state.communityInteractionIdentity[key] = String(identity.id);
              state.communityChats[key] = safeArray(state.communityChats[key]);
              state.communityChats[key].push({ id: `group-${Date.now().toString(36)}`, kind: "text", sender: "user", role: (String(key).split(":")[0] === "identity" && String(key).split(":")[1] === String(identity?.id || "")) ? "owner" : "member", name: identity?.displayName || identity?.handle || "user", avatar: identity?.avatar || "", text, translation: "", at: Date.now(), isUser: true, identityId: identity?.id || "" });
              state.communityChats[key] = state.communityChats[key].slice(-300);
              if (input) input.value = "";
              await persist();
              const [ownerType, ownerId, tier] = String(key).split(":");
              openCommunityChat(ownerType, ownerId, tier);
            };


            const summonCommunityChat = async (key) => {
              key = String(key || "");
              if (!key || communityReplyPending.has(key)) return;
              const [ownerType, ownerId, tier] = key.split(":");
              const entity = getProfileEntity(ownerType, ownerId);
              if (!entity || !subscriptionEnabled(ownerType, ownerId)) return;
              communityReplyPending.add(key);
              const typingToken = `community-typing-${Date.now().toString(36)}`;
              const body = q('[data-screen="message"] [data-role="chat-body"]');
              if (body && String(q('[data-screen="message"] [data-role="community-message-input"]')?.dataset?.communityKey || "") === key) {
                body.insertAdjacentHTML("beforeend", `<div data-community-typing="${escapeHTML(typingToken)}">${renderChatBubble({ id:typingToken, kind:"text", sender:"other", name:"", avatar:STRANGER_AVATAR, text:"", typing:true, at:Date.now() }, { group:true, threadKey:key, threadType:"community" })}</div>`);
                requestAnimationFrame(() => { body.scrollTop = body.scrollHeight; });
              }
              const removeTyping = () => q(`[data-community-typing="${CSS.escape(typingToken)}"]`)?.remove();
              try {
                const cfg = getCommunitySettings(ownerType, ownerId, tier);
                const rows = safeArray(state.communityChats[key]);
                const history = rows.slice(-22).map(item => item.kind === "text" ? `${item.sender === "user" ? "USER" : item.name || "成员"}: ${item.text || ""}` : `[${item.kind}] ${item.question || item.prize || item.title || item.text || ""}`).join("\n");
                const activity = Math.max(0, Math.min(100, Number(state.platformSettings?.activityLevel ?? 50)));
                const localeHint = ownerType === "channel" ? inferChannelLocale(entity) : "global";
                const replayRows = safeArray(state.liveReplays)
                  .filter(item => String(item.ownerType || "") === String(ownerType) && String(item.ownerId || "") === String(ownerId))
                  .sort((a,b)=>Number(b.endedAt || 0)-Number(a.endedAt || 0))
                  .slice(0,5)
                  .map(item => ({
                    title:String(item.title || ""), topic:String(item.topic || ""), startedAt:Number(item.startedAt || 0), endedAt:Number(item.endedAt || 0),
                    moments:safeArray(item.timeline).slice(-6).map(x => String(x?.speech || x?.action || x?.text || "")).filter(Boolean)
                  }));
                const replayRule = replayRows.length
                  ? `可引用的真实近期直播回放（仅限这些事实）：${JSON.stringify(replayRows)}。如果提到过去某场直播、日期或具体发生的事，只能依据这 1~5 场真实回放，不得补写不存在的历史。`
                  : "当前账号没有任何可验证的直播回放。禁止说‘昨天直播’、‘上周直播’、‘前天那场’、具体周几直播或虚构具体直播内容；也不要暗示成员一定看过这个账号以前的直播。可以聊当前社群、贴文、日常话题。";
                const system = `${VELA_DEFAULT_PRESET}

你正在模拟 Vela 付费订阅社群中除 USER 之外的群成员。绝对禁止替 USER 发言、行动、投票或决定。小飞机表示“让群里继续聊”，这次后台生成一批 10 条候选消息，但这些消息会由 Vela 慢慢显示，不要把 10 条写成连续问答脚本。成员可以互相聊天、跑题、接梗、发短句或 emoji，不必围着 USER。当前社群主要语言/地区倾向：${localeHint}。
${replayRule}
陌生粉丝语言倾向：${languagePreferencePrompt()}
${translationRulePrompt()}
群主：${profileName(entity, ownerType)}
粉丝称呼：${cfg.fanName}
粉丝画像：${cfg.fanProfile}
聊天内容倾向：${cfg.chatStyle}
平台活跃度：${activity}%。
返回严格 JSON 数组，恰好 10 条，例如：
[{"role":"member","name":"成员昵称","text":"消息","translation":"按翻译规则填写"}]
${ownerType === "channel" ? '角色群主可以偶尔自然参与，role=owner；但不能每轮都出现。' : '这是 USER 自己的社群：禁止输出 role=owner，不能替 USER 说话。'}成员风格要有差异，同一个成员不要连续刷屏。`;
                const aiText = await runVelaAI([{ role:"system", content:system }, { role:"user", content:`最近群聊：\n${history || "群里刚建立，可以从轻松的日常话题自然开始。"}` }]);
                if (!String(aiText || "").trim()) { toast(`社群生成失败 · ${shortVelaAIError("AI 返回为空")}`); return; }
                const data = extractJSON(aiText);
                if (!Array.isArray(data)) { toast(`社群生成失败 · 回复格式解析失败 · ${String(aiText || "").replace(/\s+/g," ").slice(0,72)}`); return; }
                state.communityChatQueues[key] = safeArray(state.communityChatQueues[key]);
                const now = Date.now();
                let cursor = now + 1200;
                const queue = [];
                data.slice(0,10).forEach((item,index) => {
                  if (!item || typeof item !== "object") return;
                  const ownerReply = ownerType === "channel" && item?.role === "owner";
                  const text = String(item?.text || "").trim();
                  if (!text) return;
                  if (index > 0) cursor += Math.round((index < 4 ? 2.5 + Math.random()*5 : 7 + Math.random()*14) * 1000);
                  queue.push({
                    id:`group-ai-${Date.now().toString(36)}-${index}-${Math.random().toString(36).slice(2,5)}`,
                    kind:"text", sender:ownerReply ? "owner" : "other", role:ownerReply ? "owner" : "member",
                    name:ownerReply ? profileName(entity, ownerType) : String(item?.name || cfg.fanName || "member"),
                    avatar:ownerReply ? String(entity.avatar || "") : STRANGER_AVATAR,
                    text, translation:String(item?.translation || ""), plannedAt:cursor
                  });
                });
                if (!queue.length) { toast("社群生成失败 · 没有有效消息"); return; }
                state.communityChatQueues[key].push(...queue);
                await persist();
                removeTyping();
                armCommunityLazyTimer(key);
                const moved = flushCommunityChatQueue(key);
                if (moved) { await persist(); openCommunityChat(ownerType, ownerId, tier); }
              } catch (err) {
                console.warn("[Vela] community reply processing failed", err);
                toast(`社群处理失败 · ${String(err?.message || err || "未知错误").replace(/\s+/g," ").slice(0,110)}`);
              } finally {
                removeTyping();
                communityReplyPending.delete(key);
                if (q('[data-screen="message"] [data-role="community-message-input"]')?.dataset?.communityKey === key) openCommunityChat(ownerType, ownerId, tier);
              }
            };

            const openCommunityPlus = (key) => {
              const [ownerType, ownerId, tier] = String(key).split(":");
              if (!isOwnCommunity(ownerType, ownerId)) return;
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              if (!sheet || !host) return;
              host.innerHTML = `<div class="v-publish-sheet-title"><h3>群内工具</h3><button data-action="close-publish-sheet">×</button></div><div class="v-chat-tool-grid"><button data-action="open-community-tool-form" data-community-key="${escapeHTML(key)}" data-tool-type="lottery">群内抽奖<small>发布奖品、人数和参与说明</small></button><button data-action="open-community-tool-form" data-community-key="${escapeHTML(key)}" data-tool-type="poll">群内投票<small>让大家帮你决定下次播什么</small></button></div>`;
              sheet.classList.add("is-open");
            };

            const openCommunityToolForm = (key, toolType) => {
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              if (!sheet || !host) return;
              if (toolType === "lottery") {
                host.innerHTML = `<div class="v-publish-sheet-title"><h3>群内抽奖</h3><button data-action="open-community-plus" data-community-key="${escapeHTML(key)}">‹</button></div><div class="v-tool-form"><div class="v-field"><label>奖品</label><input data-community-tool-field="prize" maxlength="90" placeholder="奖品名称"></div><div class="v-field"><label>中奖人数</label><input data-community-tool-field="winners" type="number" min="1" max="100" value="1"></div><div class="v-field"><label>参与说明</label><textarea data-community-tool-field="note" maxlength="400" placeholder="参与截止、规则、备注"></textarea></div><button class="v-action" data-action="publish-community-tool" data-community-key="${escapeHTML(key)}" data-tool-type="lottery">发布抽奖</button></div>`;
              } else {
                host.innerHTML = `<div class="v-publish-sheet-title"><h3>群内投票</h3><button data-action="open-community-plus" data-community-key="${escapeHTML(key)}">‹</button></div><div class="v-tool-form"><div class="v-field"><label>投票问题</label><input data-community-tool-field="question" maxlength="120" placeholder="下次播什么？"></div><div class="v-field"><label>选项（每行一个，2~6项）</label><textarea data-community-tool-field="options" maxlength="420" placeholder="夜间杂谈&#10;游戏直播&#10;一起看电影"></textarea></div><div class="v-field"><label>投票说明</label><textarea data-community-tool-field="note" maxlength="260" placeholder="可留空"></textarea></div><button class="v-action" data-action="publish-community-tool" data-community-key="${escapeHTML(key)}" data-tool-type="poll">发布投票</button></div>`;
              }
              sheet.classList.add("is-open");
            };

            const publishCommunityTool = async (key, toolType) => {
              const sheet = q('[data-role="publish-sheet"]');
              if (!sheet) return;
              const read = name => String(sheet.querySelector(`[data-community-tool-field="${name}"]`)?.value || "").trim();
              state.communityChats[key] = safeArray(state.communityChats[key]);
              if (toolType === "lottery") {
                const prize = read("prize");
                if (!prize) { toast("先填写奖品"); return; }
                state.communityChats[key].push({ id: `lottery-${Date.now().toString(36)}`, kind: "lottery", sender: "owner", botPosted: false, prize, winners: Math.max(1, Math.min(100, Number(read("winners") || 1))), note: read("note"), joinedCount: 0, joined: false, status:"active", at: Date.now() });
              } else {
                const question = read("question");
                const options = read("options").split(/\n+/).map(x => x.trim()).filter(Boolean).slice(0, 6);
                if (!question || options.length < 2) { toast("至少填写两个投票选项"); return; }
                state.communityChats[key].push({ id: `poll-${Date.now().toString(36)}`, kind: "poll", sender: "owner", question, options, votes: options.map(() => 0), myVotes: [], note: read("note"), at: Date.now() });
              }
              q('[data-role="publish-sheet"]')?.classList.remove("is-open");
              await persist();
              const [ownerType, ownerId, tier] = String(key).split(":");
              openCommunityChat(ownerType, ownerId, tier);
            };

            const voteCommunityPoll = async (key, cardId, optionIndex) => {
              const card = safeArray(state.communityChats[key]).find(item => String(item.id) === String(cardId));
              const index = Number(optionIndex);
              if (!card || card.kind !== "poll" || !Number.isInteger(index) || index < 0 || index >= safeArray(card.options).length) return;
              card.votes = safeArray(card.votes).length === card.options.length ? card.votes : card.options.map((_, i) => Number(card.votes?.[i] || 0));
              card.myVotes = safeArray(card.myVotes);
              if (card.myVotes.includes(index)) return;
              card.myVotes = [index];
              card.votes[index] = Number(card.votes[index] || 0) + 1;
              await persist();
              const [ownerType, ownerId, tier] = String(key).split(":");
              openCommunityChat(ownerType, ownerId, tier);
            };

            const joinCommunityLottery = async (key, cardId) => {
              const card = safeArray(state.communityChats[key]).find(item => String(item.id) === String(cardId));
              if (!card || card.kind !== "lottery" || card.joined) return;
              card.joined = true;
              card.joinedCount = Number(card.joinedCount || 0) + 1;
              await persist();
              const [ownerType, ownerId, tier] = String(key).split(":");
              openCommunityChat(ownerType, ownerId, tier);
            };

            const resolveCommunityLottery = async (key, cardId) => {
              const [ownerType, ownerId] = String(key).split(":");
              if (!isOwnCommunity(ownerType, ownerId)) return;
              const card = safeArray(state.communityChats[key]).find(item => String(item.id) === String(cardId));
              if (!card || card.kind !== "lottery" || card.status === "ended") return;
              const count = Math.max(1, Number(card.winners || 1));
              const participantNames = [...new Set(safeArray(state.communityChats[key]).filter(item => item?.kind === "text" && item?.sender !== "owner" && item?.sender !== "bot" && item?.name).map(item => String(item.name)))];
              const shuffled = participantNames.sort(() => Math.random() - 0.5);
              card.results = shuffled.slice(0, Math.min(count, shuffled.length));
              card.status = "ended";
              card.endedAt = Date.now();
              const text = `${card.prize || "群内抽奖"}：${card.results.join("、")}`;
              const tier = String(key).split(":")[2] || "free";
              pushCommunityBotEvent(ownerType, ownerId, "lottery_result", { title:"群内抽奖结果", text }, tier);
              await persist();
              openCommunityChat(ownerType, ownerId, tier);
            };

            const clearCommunityChat = async (key, ownerType, ownerId, tier) => {
              const own = isOwnCommunity(ownerType, ownerId);
              const wording = own ? "清空这个社群的全部聊天记录？" : "清空你本地看到的这段社群聊天记录？";
              if (typeof window.confirm === "function" && !window.confirm(wording)) return;
              state.communityChats[key] = [];
              state.communityChatQueues[key] = [];
              await persist();
              openCommunityChat(ownerType, ownerId, tier);
            };

            const openCommunitySettings = (ownerType, ownerId, tier) => {
              if (!canEditCommunitySettings(ownerType, ownerId)) return;
              const entity = getProfileEntity(ownerType, ownerId);
              const cfg = getCommunitySettings(ownerType, ownerId, tier);
              if (!entity) return;
              const avatarURLValue = /^https?:\/\//i.test(String(cfg.avatar || "")) ? String(cfg.avatar || "") : "";
              const botAvatarURLValue = /^https?:\/\//i.test(String(cfg.botAvatar || "")) ? String(cfg.botAvatar || "") : "";
              openScreen("community-settings", `<div class="v-community-modal-backdrop" data-action="close-community-settings-bg"><section class="v-community-modal-card"><header class="v-community-modal-head"><div><strong>编辑社群</strong><small>${ownerType === "channel" ? `角色频道 · ${escapeHTML(profileName(entity, ownerType))}` : `我的账号 · ${escapeHTML(profileName(entity, ownerType))}`}</small></div><button data-action="close-screen" data-screen-name="community-settings" aria-label="关闭">×</button></header><div class="v-community-modal-scroll"><div class="v-community-edit-grid"><div class="v-community-preview"><label class="v-community-avatar-uploader" title="点击上传头像"><div class="v-social-avatar" data-role="community-avatar-preview">${avatarHTML(cfg.avatar || entity.avatar || "", cfg.name)}</div><input type="file" accept="image/*" data-community-avatar-file><span>点击更换头像</span></label><div class="v-meta"><div class="v-name">${escapeHTML(cfg.name)}</div><div class="v-sub">${escapeHTML(cfg.fanName)} · 社群设置</div></div></div><div class="v-card"><div class="v-editform"><div class="v-field"><label>群头像</label><input data-community-avatar-url value="${escapeHTML(avatarURLValue)}" placeholder="或在此处粘贴图片 URL"><input type="hidden" data-community-field="avatar" value="${escapeHTML(cfg.avatar || "")}"></div><div class="v-field"><label>群名</label><input data-community-field="name" maxlength="48" value="${escapeHTML(cfg.name)}"></div><div class="v-field"><label>群公告</label><textarea data-community-field="announcement" maxlength="600">${escapeHTML(cfg.announcement)}</textarea></div><div class="v-field"><label>粉丝名</label><input data-community-field="fanName" maxlength="24" value="${escapeHTML(cfg.fanName)}"></div><div class="v-field"><label>粉丝画像</label><textarea data-community-field="fanProfile" maxlength="800">${escapeHTML(cfg.fanProfile)}</textarea></div><div class="v-field"><label>聊天内容 / 氛围</label><textarea data-community-field="chatStyle" maxlength="800">${escapeHTML(cfg.chatStyle)}</textarea></div></div></div><div class="v-card"><div class="v-name">机器人助手</div><div class="v-community-botchecks"><label class="v-community-check"><span>启用机器人助手</span><input class="v-switch" type="checkbox" data-community-field="botEnabled" ${cfg.botEnabled ? "checked" : ""}></label></div><div class="v-community-bot-options" data-role="community-bot-options" ${cfg.botEnabled ? "" : "hidden"}><div class="v-community-bot-profile"><label class="v-community-bot-avatar v-community-avatar-uploader is-bot"><span data-role="community-bot-avatar-preview">${avatarHTML(cfg.botAvatar || "", cfg.botName)}</span><input type="file" accept="image/*" data-community-bot-avatar-file></label><div class="v-editform"><div class="v-field"><label>机器人名字</label><input data-community-field="botName" maxlength="28" value="${escapeHTML(cfg.botName)}"></div><div class="v-field"><label>机器人头像 URL</label><input data-community-bot-avatar-url value="${escapeHTML(botAvatarURLValue)}" placeholder="或在此处粘贴图片 URL"><input type="hidden" data-community-field="botAvatar" value="${escapeHTML(cfg.botAvatar || "")}"></div></div></div><div class="v-field" style="margin-top:10px"><label>预约直播提前提醒 · 小时</label><input data-community-field="botLeadHours" type="number" min="1" max="24" value="${Number(cfg.botLeadHours || 3)}"></div><div class="v-community-botchecks"><label class="v-community-check"><span>推送预约直播提醒</span><input class="v-switch" type="checkbox" data-community-field="botScheduled" ${cfg.botScheduled ? "checked" : ""}></label><label class="v-community-check"><span>推送普通开播消息</span><input class="v-switch" type="checkbox" data-community-field="botLive" ${cfg.botLive ? "checked" : ""}></label><label class="v-community-check"><span>推送群内抽奖结果</span><input class="v-switch" type="checkbox" data-community-field="botLottery" ${cfg.botLottery ? "checked" : ""}></label></div></div></div></div></div><footer class="v-community-modal-foot"><button class="v-action light" data-action="close-screen" data-screen-name="community-settings">取消</button><button class="v-action" data-action="save-community-settings" data-owner-type="${escapeHTML(ownerType)}" data-owner-id="${escapeHTML(ownerId)}" data-tier="paid">保存</button></footer></section></div>`);
            };

            const saveCommunitySettings = async (ownerType, ownerId, tier) => {
              const screen = q('[data-screen="community-settings"]');
              if (!screen || !canEditCommunitySettings(ownerType, ownerId)) return;
              const key = communityChatKey(ownerType, ownerId, tier);
              const read = name => String(screen.querySelector(`[data-community-field="${name}"]`)?.value || "").trim();
              const checked = name => Boolean(screen.querySelector(`[data-community-field="${name}"]`)?.checked);
              const current = getCommunitySettings(ownerType, ownerId, tier);
              state.communitySettings[key] = {
                ...current,
                avatar: read("avatar"),
                name: read("name") || current.name,
                announcement: read("announcement"),
                fanName: read("fanName") || current.fanName,
                fanProfile: read("fanProfile"),
                chatStyle: read("chatStyle"),
                botEnabled: checked("botEnabled"),
                botName: read("botName") || current.botName || "Vela 小助手",
                botAvatar: read("botAvatar"),
                botLeadHours: Math.max(1, Math.min(24, Number(read("botLeadHours") || current.botLeadHours || 3))),
                botScheduled: checked("botScheduled"),
                botLive: checked("botLive"),
                botLottery: checked("botLottery")
              };
              await persist();
              closeScreen("community-settings");
              openCommunityChat(ownerType, ownerId, tier);
              renderMessages();
              armCommunityBotTimers?.();
            };

            const pushCommunityBotEvent = (ownerType, ownerId, eventType, payload = {}, onlyTier = "") => {
              if (!subscriptionEnabled(ownerType, ownerId)) return;
              ["paid"].forEach(tier => {
                if (onlyTier && tier !== onlyTier) return;
                const key = communityChatKey(ownerType, ownerId, tier);
                const cfg = getCommunitySettings(ownerType, ownerId, tier);
                if (!cfg.botEnabled) return;
                if (eventType === "scheduled" && !cfg.botScheduled) return;
                if (eventType === "live" && !cfg.botLive) return;
                if (eventType === "lottery_result" && !cfg.botLottery) return;
                state.communityChats[key] = safeArray(state.communityChats[key]);
                state.communityChats[key].push({
                  id: `bot-${eventType}-${Date.now().toString(36)}-${tier}`,
                  kind: "bot_card",
                  sender: "bot",
                  role: "bot",
                  botName: cfg.botName || "Vela 小助手",
                  botAvatar: cfg.botAvatar || "",
                  eventType,
                  title: String(payload.title || (eventType === "live" ? "开播啦" : eventType === "lottery_result" ? "抽奖结果" : "今天有预约直播")),
                  text: String(payload.text || payload.dateText || ""),
                  dateText: String(payload.dateText || ""),
                  at: Date.now()
                });
                state.communityChats[key] = state.communityChats[key].slice(-300);
              });
            };

            const continueSubscription = (channelId) => {
              const channel = state.channels.find(ch => String(ch.id) === String(channelId));
              if (!channel) return;
              openProfile("channel", channel.id);
              renderProfileTab("channel", channel.id, "subscriptions");
            };

            const cancelSubscription = async (channelId) => {
              const channel = state.channels.find(ch => String(ch.id) === String(channelId));
              if (!channel || getSubscriptionTierFor(state.viewerIdentityId, channelId) === "none") return;
              if (typeof window.confirm === "function" && !window.confirm(`取消订阅 ${channel.name || channel.handle || "这个主播"}？`)) return;
              setSubscriptionTierFor(state.viewerIdentityId, channelId, "none");
              delete state.wallet.subscriptionPurchases[`${String(state.viewerIdentityId || "")}:${String(channelId)}`];
              await persist();
              renderMessages(); renderChannels(); renderHome();
              renderProfileTab("identity", state.viewerIdentityId, "subscriptions");
              toast("已取消订阅");
            };

            const openSubscriptionConfirm = (channelId, tier) => {
              const channel = state.channels.find(ch => String(ch.id) === String(channelId));
              const identity = state.identities.find(item => String(item.id) === String(state.viewerIdentityId)) || state.identities[0];
              if (!channel || !identity) return;
              const normalizedTier = tier === "paid" ? "paid" : "free";
              if (!subscriptionEnabled("channel", channel.id)) { toast("这个频道还没有开启订阅计划"); return; }
              const cfg = getSubscriptionProfile("channel", channel.id)[normalizedTier];
              const currentTier = getSubscriptionTierFor(identity.id, channel.id);
              if (normalizedTier === "free" && (currentTier === "free" || currentTier === "paid")) { toast("这个账号已经拥有免费订阅权限"); return; }
              if (normalizedTier === "paid" && currentTier === "paid") { toast("这个账号已经是付费会员"); return; }
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              if (!sheet || !host) return;
              const note = normalizedTier === "paid"
                ? `确认后将扣除 🔷${Number(cfg.coinPrice || 0).toLocaleString("zh-CN")} / 月，并自动加入「${cfg.communityName || "会员社群"}」。`
                : "确认后将解锁免费订阅贴文与直播推送，不会加入会员社群。";
              host.innerHTML = `<div class="v-publish-sheet-title"><h3>确认订阅</h3><button data-action="close-publish-sheet">×</button></div><div class="v-card v-sub-confirm"><div class="v-kicker">谨防误触</div><h3>将以「${escapeHTML(identity.displayName || identity.handle || "当前账号")}」订阅</h3><p>${escapeHTML(channel.name || channel.handle || "这个频道")}</p><div class="v-sub-confirm-note">${escapeHTML(note)}</div></div><div class="v-publish-sheet-actions"><button class="v-action light" data-action="close-publish-sheet">取消</button><button class="v-action" data-action="confirm-subscribe-channel" data-channel-id="${escapeHTML(channel.id)}" data-tier="${normalizedTier}" data-identity-id="${escapeHTML(identity.id)}">确定订阅</button></div>`;
              sheet.classList.add("is-open");
            };

            const confirmSubscription = async (channelId, tier, identityId) => {
              const channel = state.channels.find(ch => String(ch.id) === String(channelId));
              const identity = state.identities.find(item => String(item.id) === String(identityId || ""));
              if (!channel || !identity) return;
              const normalizedTier = tier === "paid" ? "paid" : "free";
              if (!subscriptionEnabled("channel", channel.id)) { toast("这个频道还没有开启订阅计划"); return; }
              const currentTier = getSubscriptionTierFor(identity.id, channel.id);
              if (normalizedTier === "paid" && currentTier !== "paid") {
                const cfg = getSubscriptionProfile("channel", channel.id).paid;
                const coinPrice = Math.max(1, Math.round(Number(cfg.coinPrice || 0)));
                if (Number(state.wallet.coinBalance || 0) < coinPrice) { toast(`Vela Coin 不足，需要 🔷${coinPrice.toLocaleString("zh-CN")}`); return; }
                state.wallet.coinBalance -= coinPrice;
                state.wallet.subscriptionPurchases[`${String(identity.id)}:${String(channel.id)}`] = { tier:"paid", coinPriceSnapshot:coinPrice, subscribedAt:Date.now(), identityId:identity.id };
                state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-sub`, type:"coin", title:"付费订阅", note:`${channel.name || channel.handle || "频道"} · ${cfg.name || "会员"}`, coin:-coinPrice, identityId:identity.id, at:Date.now() });
                recordSubscriptionAccrual("channel", channel.id, coinPrice, `订阅者 ${identity.id}`);
                setSubscriptionTierFor(identity.id, channel.id, "paid");
                const communityKey = communityChatKey("channel", channel.id, "paid");
                state.communityChats[communityKey] = safeArray(state.communityChats[communityKey]);
                state.communityChats[communityKey].push({ id:`system-sub-join-${identity.id}-${Date.now().toString(36)}`, kind:"system", eventType:"subscription_joined", text:"您已加入TA的付费订阅社群", identityId:String(identity.id), at:Date.now() });
                state.communityChatQueues[communityKey] = [];
              } else if (normalizedTier === "free" && currentTier === "none") {
                state.wallet.subscriptionPurchases[`${String(identity.id)}:${String(channel.id)}`] = { tier:"free", coinPriceSnapshot:0, subscribedAt:Date.now(), identityId:identity.id };
                setSubscriptionTierFor(identity.id, channel.id, "free");
              }
              q('[data-role="publish-sheet"]')?.classList.remove("is-open");
              await persist();
              renderMessages(); renderChannels(); renderHome(); renderWallet();
              if (q('[data-screen="profile"]')?.classList.contains("is-open")) {
                openProfile("channel", channel.id);
                renderProfileTab("channel", channel.id, "subscriptions");
              }
              toast(normalizedTier === "paid" ? "付费订阅成功 · 已加入会员社群" : "免费订阅成功");
            };

            const openPersonaAccountCreator = (personaId) => {
              const persona = rocheRuntime.personas.find(p => String(p?.id || "") === String(personaId || "")) ||
                (String(rocheRuntime.activePersona?.id || "") === String(personaId || "") ? rocheRuntime.activePersona : null);
              if (!persona || isRocheBotIdentity(persona)) { toast("这个 Roche 身份不能用于创建 Vela 账号"); return; }
              if (String(state.rocheLink.ownerPersonaId || "") !== String(personaId || "")) { toast("请先关联这份 Roche 用户资料"); return; }
              const main = getPersonaMainAccount(personaId);
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              if (!sheet || !host) return;
              const name = displayProfileName(persona);
              const aliases = getPersonaAliasAccounts(personaId);
              const hasLinkedAlias = aliases.some(item => item.linkedToMain);
              const hasHiddenAlias = aliases.some(item => !item.linkedToMain);
              if (!main) {
                host.innerHTML = `<div class="v-publish-sheet-title"><h3>创建主账号</h3><button data-action="close-publish-sheet">×</button></div><div class="v-card"><div class="v-author"><div class="v-mini">${avatarHTML(persona?.avatar || "", name)}</div><div class="v-meta"><div class="v-name">${escapeHTML(name)}</div><div class="v-sub">底层使用这份 Roche 人设</div></div></div><p class="v-hint">每份 Roche 用户人设最多创建 1 个主账号 + 2 个副账号（1 个关联副号 + 1 个隐藏副号）。</p><button class="v-action" style="width:100%" data-action="create-vela-user-account" data-persona-id="${escapeHTML(String(personaId))}" data-account-role="primary">创建主账号</button></div>`;
              } else if (aliases.length >= 2 && hasLinkedAlias && hasHiddenAlias) {
                host.innerHTML = `<div class="v-publish-sheet-title"><h3>账号已创建完整</h3><button data-action="close-publish-sheet">×</button></div><div class="v-card"><div class="v-author"><div class="v-mini">${avatarHTML(main.avatar || "", main.displayName)}</div><div class="v-meta"><div class="v-name">${escapeHTML(main.displayName || name)}</div><div class="v-sub">已有主账号、关联副号和隐藏副号</div></div></div><p class="v-hint">这个 Roche 用户人设已经达到当前上限：1 个主账号 + 2 个副账号。</p></div>`;
              } else {
                host.innerHTML = `<div class="v-publish-sheet-title"><h3>创建副账号</h3><button data-action="close-publish-sheet">×</button></div><div class="v-card"><div class="v-author"><div class="v-mini">${avatarHTML(main.avatar || "", main.displayName)}</div><div class="v-meta"><div class="v-name">${escapeHTML(main.displayName || name)}</div><div class="v-sub">${escapeHTML(main.handle || "")} · 主账号</div></div></div><p class="v-hint">现在允许最多 2 个副账号：一个公开关联主账号，一个不公开关联的隐藏副号。</p><div class="v-settings-actions">${hasLinkedAlias ? `<button class="v-action light" disabled>关联副号已创建</button>` : `<button class="v-action" data-action="create-vela-user-account" data-persona-id="${escapeHTML(String(personaId))}" data-account-role="alias" data-linked-main="yes">创建关联副号</button>`}${hasHiddenAlias ? `<button class="v-action light" disabled>隐藏副号已创建</button>` : `<button class="v-action light" data-action="create-vela-user-account" data-persona-id="${escapeHTML(String(personaId))}" data-account-role="alias" data-linked-main="no">创建隐藏副号</button>`}</div></div>`;
              }
              sheet.classList.add("is-open");
            };

            const createVelaUserAccount = async (personaId, accountRole, linkedToMain = false) => {
              const persona = rocheRuntime.personas.find(p => String(p?.id || "") === String(personaId || "")) ||
                (String(rocheRuntime.activePersona?.id || "") === String(personaId || "") ? rocheRuntime.activePersona : null);
              if (!persona || isRocheBotIdentity(persona)) { toast("没有可用的 Roche 用户资料"); return; }
              const main = getPersonaMainAccount(personaId);
              const existingAliases = getPersonaAliasAccounts(personaId);
              const role = accountRole === "alias" && main ? "alias" : "primary";
              if (role === "primary" && main) { toast("这份 Roche 身份已经有主账号"); return; }
              if (role === "alias" && existingAliases.length >= 2) { toast("这份 Roche 身份的副账号已满"); return; }
              if (role === "alias" && existingAliases.some(item => Boolean(item.linkedToMain) === Boolean(linkedToMain))) { toast(linkedToMain ? "关联副号已经存在" : "隐藏副号已经存在"); return; }
              const name = displayProfileName(persona);
              const cleanId = String(personaId || "").replace(/[^a-zA-Z0-9_-]/g, "").slice(-12) || Math.random().toString(36).slice(2,8);
              const id = role === "primary"
                ? `persona-${cleanId}-main`
                : `persona-${cleanId}-sub-${Date.now().toString(36).slice(-5)}`;
              const baseHandle = normalizeHandle(persona?.handle || "") || `@vela_${cleanId.slice(-6)}`;
              const item = {
                id,
                type: "vela-account",
                sourcePersonaId: String(personaId),
                displayName: role === "primary" ? name : (linkedToMain ? "关联副账号" : "隐藏副账号"),
                handle: nextUniqueHandle(role === "primary" ? baseHandle : `vela_sub_${cleanId.slice(-4)}`, state.identities),
                avatar: role === "primary" ? String(persona?.avatar || initials(name)) : "",
                bio: role === "primary" ? String(persona?.bio || "") : "",
                banner: "",
                followers: 0,
                following: 0,
                accountRole: role,
                mainAccountId: role === "alias" ? String(main?.id || "") : "",
                linkedToMain: role === "alias" ? Boolean(linkedToMain) : false,
                knownByCharacters: {},
                faceMode: role === "alias" && !linkedToMain ? "hidden" : "mixed",
                identityVisibility: role === "alias" && linkedToMain
                  ? String(main?.identityVisibility || "partial")
                  : (role === "alias" ? "hidden" : "partial"),
                contentStyle: "",
                fanProfile: "",
                scheduledLive: null, scheduledLives: [],
                activeLive: null,
                live: false,
                isAnonymous: role === "alias" && !linkedToMain
              };
              state.identities.push(item);
              if (role === "primary") {
                state.channels.filter(channel => channel.accountRole !== "alias" && Boolean(channel.sourceCharacterId)).forEach(channel => {
                  setFollowing("identity", item.id, "channel", channel.id, true);
                  setFollowing("channel", channel.id, "identity", item.id, true);
                  channel.followers = Math.max(0, Number(channel.followers || 0) + 1);
                  channel.following = Math.max(0, Number(channel.following || 0) + 1);
                  item.following = Math.max(0, Number(item.following || 0) + 1);
                  item.followers = Math.max(0, Number(item.followers || 0) + 1);
                });
              }
              if (!state.viewerIdentityId || role === "primary") state.viewerIdentityId = item.id;
              q('[data-role="publish-sheet"]')?.classList.remove("is-open");
              await persist();
              mountMarkup(root, state); rerender(); switchPage(state.activePage || "home"); openIdentities();
              toast(role === "primary" ? "主账号已创建" : (linkedToMain ? "关联副账号已创建" : "隐藏副账号已创建"));
            };

            const openIdentities = () => {
              const ownerId = String(state.rocheLink?.ownerPersonaId || "");
              const owner = rocheRuntime.personas.find(p => String(p?.id || "") === ownerId) ||
                (String(rocheRuntime.activePersona?.id || "") === ownerId ? rocheRuntime.activePersona : null);
              const rocheStatus = rocheRuntime.error
                ? `读取失败：${escapeHTML(rocheRuntime.error)}`
                : rocheRuntime.loaded ? "已读取 Roche 用户资料" : "正在读取 Roche";
              const personaMap = new Map();
              [...safeArray(rocheRuntime.personas), rocheRuntime.activePersona].filter(Boolean).forEach(p => personaMap.set(String(p?.id || displayProfileName(p)), p));
              const usablePersonas = [...personaMap.values()].filter(p => !isRocheBotIdentity(p));

              const personaRows = usablePersonas.map(p => {
                const id = String(p?.id || "");
                const name = displayProfileName(p);
                const selected = ownerId === id;
                const current = String(rocheRuntime.activePersona?.id || "") === id;
                const accounts = getPersonaAccounts(id);
                const aliasAccounts = getPersonaAliasAccounts(id);
                const capReached = Boolean(getPersonaMainAccount(id) && aliasAccounts.some(a => a.linkedToMain) && aliasAccounts.some(a => !a.linkedToMain));
                return `<div class="v-roche-persona"><div class="v-mini">${avatarHTML(p?.avatar || "", name)}</div><div class="v-meta"><div class="v-name">${escapeHTML(name)}</div><div class="v-sub">${current ? "当前 Roche 用户资料" : "Roche 用户资料"}${accounts.length ? ` · 已创建 ${accounts.length} 个 Vela 账号` : ""}</div></div><div class="v-identity-actions" style="margin-top:0">${selected ? `<button class="v-action light" disabled>已关联</button>${capReached ? `<button class="v-action light" disabled>已满 1主 + 2副</button>` : `<button class="v-action" data-action="open-user-account-creator" data-persona-id="${escapeHTML(id)}">使用此身份创建新账号</button>`}` : `<button class="v-action" data-action="bind-owner-persona" data-persona-id="${escapeHTML(id)}">关联</button>`}</div></div>`;
              }).join("");

              const identitiesHTML = state.identities.length ? state.identities.map(item => {
                const active = String(state.viewerIdentityId || "") === String(item.id);
                return `<div class="v-identity-card"><div class="v-author"><div class="v-mini">${avatarHTML(item.avatar || "", item.displayName)}</div><div class="v-meta"><div class="v-name">${escapeHTML(item.displayName || "Vela User")}</div><div class="v-sub">${escapeHTML(item.handle || "@vela")} · ${accountRoleLabel(item)}${item.accountRole === "alias" ? ` · ${subLinkLabel(item)}` : ""}</div></div>${active ? '<span class="v-pill" style="background:#111;color:#fff">当前</span>' : ''}</div><div class="v-identity-actions"><button class="v-action light" data-action="open-profile-by-id" data-owner-type="identity" data-owner-id="${escapeHTML(item.id)}">主页</button>${active ? '' : `<button class="v-action" data-action="switch-identity" data-identity-id="${escapeHTML(item.id)}">切换到此账号</button>`}<button class="v-action v-account-delete" data-action="delete-vela-account" data-owner-type="identity" data-owner-id="${escapeHTML(item.id)}">删除本账号</button></div></div>`;
              }).join("") : `<div class="v-empty" style="padding:28px 8px">还没有 Vela 账号。<br>先在上面关联一份非 Bot 的 Roche 用户资料，再创建主账号。</div>`;

              openScreen("identities", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="identities">‹</button><div class="v-meta"><strong>身份与账号</strong><div class="v-hint">Roche 人设负责底层，Vela 账号负责对外身份</div></div><button class="v-head-action" data-action="refresh-roche">刷新</button></header><div class="v-subbody">
                <div class="v-roche-compact"><div class="v-author"><div class="v-meta"><div class="v-name">Roche 用户资料</div><div class="v-sub">${rocheStatus}${owner ? ` · 已关联：${escapeHTML(displayProfileName(owner))}` : " · 尚未关联"}</div></div></div><p class="v-hint">Bot 类型 Roche 身份不会显示，也不会被 Vela 关联。关联只代表后台人设来源，不会自动公开 Roche 资料。</p>${personaRows || '<div class="v-hint">没有读取到可关联的 Roche 用户资料。</div>'}</div>
                <div class="v-card" style="margin-top:12px"><b>我的 Vela 身份</b><p class="v-hint">主账号是主要使用的账号；现在最多可有 2 个副账号：一个公开关联主账号，一个隐藏小号独立运行。露脸方式与现实身份公开程度都到各自主页的「账号风格」里设置。</p>${identitiesHTML}</div>
              </div>`);
            };

            const openCharacterAccountCreator = (characterId) => {
              const char = rocheRuntime.characters.find(item => String(item?.id || "") === String(characterId || ""));
              if (!char) { toast("没有读取到这个 Roche 角色"); return; }
              const main = getCharacterMainAccount(characterId);
              const sheet = q('[data-role="publish-sheet"]');
              const host = q('[data-role="publish-sheet-content"]');
              if (!sheet || !host) return;
              const aliases = getCharacterAliasAccounts(characterId);
              const hasLinkedAlias = aliases.some(item => item.linkedToMain);
              const hasHiddenAlias = aliases.some(item => !item.linkedToMain);
              if (!main) {
                host.innerHTML = `<div class="v-publish-sheet-title"><h3>创建角色主账号</h3><button data-action="close-publish-sheet">×</button></div><p class="v-hint">每个 Roche 角色最多创建 1 个主账号 + 2 个副账号。</p><button class="v-action" style="width:100%" data-action="link-roche-character" data-character-id="${escapeHTML(String(characterId))}">创建主账号</button>`;
              } else if (aliases.length >= 2 && hasLinkedAlias && hasHiddenAlias) {
                host.innerHTML = `<div class="v-publish-sheet-title"><h3>角色账号已创建完整</h3><button data-action="close-publish-sheet">×</button></div><div class="v-card"><p class="v-hint">这个角色已经有主账号、关联副号和隐藏副号。</p></div>`;
              } else {
                host.innerHTML = `<div class="v-publish-sheet-title"><h3>创建角色副账号</h3><button data-action="close-publish-sheet">×</button></div><div class="v-card"><div class="v-author"><div class="v-mini">${avatarHTML(main.avatar || "", main.name)}</div><div class="v-meta"><div class="v-name">${escapeHTML(main.name || displayProfileName(char))}</div><div class="v-sub">${escapeHTML(main.handle || "")} · 主账号</div></div></div><p class="v-hint">现在允许最多 2 个副账号：一个关联主账号，一个隐藏副号。</p><div class="v-settings-actions">${hasLinkedAlias ? `<button class="v-action light" disabled>关联副号已创建</button>` : `<button class="v-action" data-action="create-character-sub-account" data-character-id="${escapeHTML(String(characterId))}" data-linked-main="yes">创建关联副号</button>`}${hasHiddenAlias ? `<button class="v-action light" disabled>隐藏副号已创建</button>` : `<button class="v-action light" data-action="create-character-sub-account" data-character-id="${escapeHTML(String(characterId))}" data-linked-main="no">创建隐藏副号</button>`}</div></div>`;
              }
              sheet.classList.add("is-open");
            };

            const createCharacterSubAccount = async (characterId, linkedToMain = false) => {
              const id = String(characterId || "");
              const char = rocheRuntime.characters.find(item => String(item?.id || "") === id);
              const main = getCharacterMainAccount(id);
              const existingAliases = getCharacterAliasAccounts(id);
              if (!char || !main) { toast("请先创建角色主账号"); return; }
              if (existingAliases.length >= 2) { toast("这个角色的副账号已满"); return; }
              if (existingAliases.some(item => Boolean(item.linkedToMain) === Boolean(linkedToMain))) { toast(linkedToMain ? "这个角色已经有一个关联副号" : "这个角色已经有一个隐藏副号"); return; }
              const name = displayProfileName(char);
              const baseHandle = normalizeHandle(char?.handle || "") || `@${String(name || "vela").replace(/\s+/g,"_")}`;
              const channel = {
                id: `roche-char-${id}-sub-${Date.now().toString(36).slice(-5)}`,
                kind: "character",
                sourceCharacterId: id,
                name: linkedToMain ? "关联副账号" : "隐藏副账号",
                realName: String(char?.name || ""),
                handle: nextUniqueHandle(`vela_sub_${id.slice(-4)}`, state.channels),
                avatar: "",
                bio: "",
                banner: "",
                followers: 0,
                following: 0,
                accountRole: "alias",
                mainChannelId: String(main.id),
                linkedToMain: Boolean(linkedToMain),
                userKnowsAlias: false,
                characterKnowsUserKnows: false,
                faceMode: linkedToMain ? "mixed" : "hidden",
                identityVisibility: linkedToMain ? String(main.identityVisibility || "partial") : "hidden",
                contentStyle: "",
                fanProfile: "",
                liveDurationMode: String(main.liveDurationMode || "irregular"),
                liveDurationText: String(main.liveDurationText || ""),
                liveSchedule: String(main.liveSchedule || ""),
                liveAutonomy: String(main.liveAutonomy || "flexible"),
                profileCustomized: true,
                scheduledLive: null, scheduledLives: [],
                live: false,
                title: "",
                viewers: 0
              };
              state.channels.push(channel);
              q('[data-role="publish-sheet"]')?.classList.remove("is-open");
              await persist();
              rerender();
              openRoles();
              toast(linkedToMain ? "角色关联副账号已创建" : "角色隐藏副账号已创建");
            };

            const openRoles = () => {
              const linkedIds = new Set(safeArray(state.rocheLink?.linkedCharacterIds).map(String));
              const status = rocheRuntime.error
                ? `<div class="v-linkstatus"><span class="v-dot"></span>读取失败：${escapeHTML(rocheRuntime.error)}</div>`
                : `<div class="v-linkstatus"><span class="v-dot ${rocheRuntime.loaded ? "on" : ""}"></span>${rocheRuntime.loaded ? `已读取 ${rocheRuntime.characters.length} 个 Roche 角色` : "正在读取 Roche 角色"}</div>`;

              const rows = rocheRuntime.characters.length ? rocheRuntime.characters.map(char => {
                const id = String(char?.id || "");
                const name = displayProfileName(char);
                const linked = linkedIds.has(id);
                const accounts = getCharacterAccounts(id);
                const accountRows = accounts.map(channel => `<div class="v-identity-card" style="margin-top:8px"><div class="v-author"><div class="v-mini">${avatarHTML(channel.avatar || "", channel.name || name)}</div><div class="v-meta"><div class="v-name">${escapeHTML(channel.name || name)}</div><div class="v-sub">${escapeHTML(channel.handle || "")} · ${accountRoleLabel(channel)}${channel.accountRole === "alias" ? ` · ${subLinkLabel(channel)}` : ""}</div></div></div><div class="v-identity-actions"><button class="v-action light" data-action="open-profile-by-id" data-owner-type="channel" data-owner-id="${escapeHTML(channel.id)}">主页</button><button class="v-action v-account-delete" data-action="delete-vela-account" data-owner-type="channel" data-owner-id="${escapeHTML(channel.id)}">删除本账号</button></div></div>`).join("");
                const aliasAccounts = getCharacterAliasAccounts(id);
                const capReached = Boolean(getCharacterMainAccount(id) && aliasAccounts.some(a => a.linkedToMain) && aliasAccounts.some(a => !a.linkedToMain));
                return `<div class="v-rolecard"><div class="v-author"><div class="v-mini">${avatarHTML(char?.avatar || "", name)}</div><div class="v-meta"><div class="v-name">${escapeHTML(name)}</div><div class="v-sub">${escapeHTML(char?.name || "")}${char?.handle ? ` · ${escapeHTML(normalizeHandle(char.handle))}` : ""}</div></div>${linked ? `<button class="v-action light" data-action="unlink-roche-character" data-character-id="${escapeHTML(id)}">移除</button>` : `<button class="v-action" data-action="open-character-account-creator" data-character-id="${escapeHTML(id)}">加入 Vela</button>`}</div>${linked ? `${accountRows}${capReached ? `<button class="v-action light" style="width:100%;margin-top:10px" disabled>已满 1主 + 2副</button>` : `<button class="v-action light" style="width:100%;margin-top:10px" data-action="open-character-account-creator" data-character-id="${escapeHTML(id)}">使用此角色创建新账号</button>`}` : ""}</div>`;
              }).join("") : `<div class="v-empty">没有读取到 Roche 角色。<br>可以先点右上角“刷新”。</div>`;

              openScreen("roles", `<header class="v-subhead"><button data-action="close-screen" data-screen-name="roles">‹</button><div class="v-meta"><strong>角色与频道管理</strong><div class="v-hint">这里只管理角色与 Vela 账号的对应关系</div></div><button class="v-head-action" data-action="refresh-roche-roles">刷新</button></header><div class="v-subbody"><div class="v-card"><p class="v-hint" style="margin-top:0">每个 Roche 角色先创建主账号，再按需要创建最多两个副账号（关联副号 + 隐藏副号）。主页编辑、商业合作与接单设置都放到角色主页里。</p>${status}${rows}</div></div>`);
            };

            const deleteVelaAccount = async (ownerType, ownerId) => {
              const id = String(ownerId || "");
              if (!id) return;
              if (ownerType === "identity") {
                const item = state.identities.find(x => String(x.id) === id);
                if (!item) return;
                const children = state.identities.filter(x => x.accountRole === "alias" && String(x.mainAccountId || "") === id);
                if (item.accountRole !== "alias" && children.length) {
                  toast("请先删除这个主账号下面的副账号");
                  return;
                }
                if (typeof window.confirm === "function" && !window.confirm(`删除 Vela 账号 ${item.displayName || item.handle || ""}？不会删除 Roche 用户资料。`)) return;
                state.identities = state.identities.filter(x => String(x.id) !== id);
                if (String(state.viewerIdentityId || "") === id) state.viewerIdentityId = state.identities[0]?.id || "";
                Object.keys(state.subscriptionProfiles || {}).forEach(key => { if (key === `identity:${id}`) delete state.subscriptionProfiles[key]; });
                Object.keys(state.communitySettings || {}).forEach(key => { if (key.startsWith(`identity:${id}:`)) delete state.communitySettings[key]; });
                Object.keys(state.communityChats || {}).forEach(key => { if (key.startsWith(`identity:${id}:`)) delete state.communityChats[key]; });
                Object.keys(state.communityChatQueues || {}).forEach(key => { if (key.startsWith(`identity:${id}:`)) delete state.communityChatQueues[key]; });
              } else if (ownerType === "channel") {
                const item = state.channels.find(x => String(x.id) === id);
                if (!item) return;
                const children = state.channels.filter(x => x.accountRole === "alias" && String(x.mainChannelId || "") === id);
                if (item.accountRole !== "alias" && children.length) {
                  toast("请先删除这个主账号下面的副账号");
                  return;
                }
                if (typeof window.confirm === "function" && !window.confirm(`删除角色的 Vela 账号 ${item.name || item.handle || ""}？不会删除 Roche 角色资料。`)) return;
                state.channels = state.channels.filter(x => String(x.id) !== id);
                Object.keys(state.subscriptionProfiles || {}).forEach(key => { if (key === `channel:${id}`) delete state.subscriptionProfiles[key]; });
                Object.keys(state.communitySettings || {}).forEach(key => { if (key.startsWith(`channel:${id}:`)) delete state.communitySettings[key]; });
                Object.keys(state.communityChats || {}).forEach(key => { if (key.startsWith(`channel:${id}:`)) delete state.communityChats[key]; });
                Object.keys(state.communityChatQueues || {}).forEach(key => { if (key.startsWith(`channel:${id}:`)) delete state.communityChatQueues[key]; });
                state.messages = state.messages.filter(msg => String(msg.channelId || "") !== id);
              } else return;
              await persist();
              mountMarkup(root, state);
              rerender();
              switchPage(state.activePage || "home");
              if (ownerType === "identity") openIdentities();
              else openRoles();
              toast("Vela 账号已删除");
            };

            const clearVelaPluginData = async () => {
              if (typeof window.confirm === "function" && !window.confirm("清空 Vela 插件数据？账号、DM、社群、直播、帖子、钱包和设置都会重置，但不会删除 Roche 用户/角色资料。")) return;
              if (typeof window.confirm === "function" && !window.confirm("再次确认：此操作无法撤销。继续清空 Vela 数据？")) return;
              const fresh = createDefaultState();
              fresh.identities = [];
              fresh.viewerIdentityId = "";
              fresh.channels = [];
              fresh.rocheLink = { ownerPersonaId: "", linkedCharacterIds: [] };
              fresh.messages = [];
              fresh.dmThreads = {};
              fresh.communityChats = {};
              fresh.communityChatQueues = {};
              fresh.communitySettings = {};
              fresh.communityPosts = [];
              fresh.liveReplays = [];
              fresh.postReplies = {};
              fresh.postOverrides = {};
              fresh.liveProgress = {};
              fresh.liveStats = {};
              fresh.liveChat = [];
              fresh.subscriptions = {};
              fresh.subscriptionsByIdentity = {};
              fresh.follows = {};
              fresh.followDefaultsInitialized = false;
              fresh.subscriptionProfiles = {};
              fresh.postInteractionIdentity = {};
              fresh.communityInteractionIdentity = {};
              fresh.dmInteractionIdentity = {};
              fresh.liveInteractionIdentity = {};
              fresh.homeFeedByChannel = {};
              fresh.roleCommerce = {};
              fresh.roleEarnings = {};
              fresh.businessDeals = {};
              fresh.wallet = {
                cashBalanceCNY: 0,
                coinBalance: 0,
                linkedAccount: null,
                settlementCurrency: "",
                fxRates: { CNY: 1, USD: 0.149, JPY: 21.9, KRW: 207, HKD: 1.16, EUR: 0.128 },
                fxUpdatedAt: 0,
                subscriptionAccruals: [],
                subscriptionPurchases: {},
                transactions: []
              };
              fresh.appearance = { topbarHeight: 62, bottombarHeight: 78 };
              fresh.platformSettings = { recommendationCount: 3, allowBusinessDM: true, activityLevel: 50, strangerInteraction: "normal", allowRoleAliasesInRecommendations: false, languagePreference:"overseas_more" };
              state = fresh;
              await roche?.storage?.set?.("vela-shell-state", {});
              await persist();
              mountMarkup(root, state);
              rerender();
              switchPage("home");
              toast("Vela 插件数据已清空");
            };

            const deleteDMConversation = async messageId => {
              const index = state.messages.findIndex(item => String(item.id) === String(messageId));
              if (index < 0) return;
              const msg = state.messages[index];
              if (typeof window.confirm === "function" && !window.confirm(`删除与 ${msg.name || "这个账号"} 的私信？聊天记录也会一起删除。`)) return;
              state.messages.splice(index, 1);
              delete state.dmThreads[String(messageId)];
              await persist();
              if (q('[data-screen="message"]')?.classList.contains("is-open")) closeScreen("message");
              renderMessages();
              toast("私信已删除");
            };

            const deleteChatItem = async (threadType, threadKey, itemId) => {
              const store = threadType === "community" ? state.communityChats : state.dmThreads;
              const rows = safeArray(store?.[String(threadKey)]);
              const index = rows.findIndex(item => String(item.id) === String(itemId));
              if (index < 0) return;
              if (typeof window.confirm === "function" && !window.confirm("删除这条消息？")) return;
              rows.splice(index, 1);
              store[String(threadKey)] = rows;
              await persist();
              if (threadType === "community") {
                const [ownerType, ownerId, tier] = String(threadKey).split(":");
                openCommunityChat(ownerType, ownerId, tier);
              } else {
                const msg = state.messages.find(item => String(item.id) === String(threadKey));
                if (msg) {
                  const last = [...rows].reverse().find(item => item.kind === "text");
                  msg.preview = last?.text || "";
                }
                renderMessages();
                renderDirectThread(threadKey);
              }
            };

            let appointmentTimer = null;
            let communityBotTimer = null;
            let appointmentReminderShown = false;

            const viewerAppointments = () => {
              const viewerId = String(state.viewerIdentityId || "");
              const rows = [];
              state.identities.forEach(entity => {
                scheduledLiveList(entity).forEach(item => {
                  const participants = safeArray(item.participantIds).map(String);
                  if (String(entity.id) === viewerId || participants.includes(viewerId)) rows.push({ ownerType:"identity", ownerId:entity.id, entity, item });
                });
              });
              state.channels.forEach(entity => {
                scheduledLiveList(entity).forEach(item => {
                  if (safeArray(item.participantIds).map(String).includes(viewerId)) rows.push({ ownerType:"channel", ownerId:entity.id, entity, item });
                });
              });
              return rows.sort((a,b)=>Number(a.item.scheduledAt || Number.MAX_SAFE_INTEGER)-Number(b.item.scheduledAt || Number.MAX_SAFE_INTEGER));
            };

            const localDateKey = (date = new Date()) => `${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,"0")}-${String(date.getDate()).padStart(2,"0")}`;
            const openAppointmentReminder = (entry, due = false) => {
              if (!entry?.item || state.appointmentReminderMuteDate === localDateKey()) return;
              const item = entry.item, hostName = profileName(entry.entity, entry.ownerType), biz = item.business || null;
              const label = biz ? `商务合作 · ${biz.brandName || "品牌"}${biz.productName ? ` · ${biz.productName}` : ""}` : (item.mode === "same-room" ? "线下同镜" : "线上联播");
              openScreen("appointment-reminder", `<div class="v-center-modal-backdrop"><div class="v-appointment-card"><div class="v-kicker">${due ? "直播时间到了" : "预约提醒"} · ${escapeHTML(item.dateText || "时间待定")}</div><h3>${escapeHTML(item.title || "预约直播")}</h3><p>主场：${escapeHTML(hostName)}<br>${escapeHTML(label)}${item.content ? `<br>${escapeHTML(item.content)}` : ""}</p><div class="v-appointment-actions"><button class="v-go" data-action="view-appointment-host" data-owner-type="${escapeHTML(entry.ownerType)}" data-owner-id="${escapeHTML(entry.ownerId)}">查看预约</button><button class="v-later" data-action="appointment-reminder-later">稍后</button><button class="v-today" data-action="mute-appointment-reminders-today">本日不再提醒</button></div></div></div>`);
            };

            const checkAppointmentReminder = ({ entry = false } = {}) => {
              if (state.appointmentReminderMuteDate === localDateKey()) return;
              const rows = viewerAppointments(); if (!rows.length) return;
              const now = Date.now();
              const due = rows.find(x => { const start=Number(x.item.scheduledAt||0), end=Number(x.item.endsAt || (start ? start+2*3600000 : 0)); return start && now >= start-60000 && (!end || now <= end); });
              if (due) { openAppointmentReminder(due, true); return; }
              const soon = rows.find(x => { const start=Number(x.item.scheduledAt||0); return start > now && start-now <= 60*60000 && !x.item.leadReminderShown; });
              if (soon) { soon.item.leadReminderShown = true; persist(); openAppointmentReminder(soon, false); armAppointmentTimer(); return; }
              if (entry && !appointmentReminderShown) { appointmentReminderShown = true; openAppointmentReminder(rows[0], false); }
            };

            const armAppointmentTimer = () => {
              if (appointmentTimer) clearTimeout(appointmentTimer);
              const now = Date.now(), targets = [];
              viewerAppointments().forEach(x => { const start=Number(x.item.scheduledAt||0); if (!start || start <= now) return; if (!x.item.leadReminderShown && start-60*60000 > now) targets.push(start-60*60000); else targets.push(start); });
              if (!targets.length) return;
              const target=Math.min(...targets), delay=Math.max(250,Math.min(2147480000,target-now));
              appointmentTimer=setTimeout(() => { checkAppointmentReminder({entry:false}); armAppointmentTimer(); }, delay);
            };

            const allHostAppointments = () => {
              const rows = [];
              state.identities.forEach(entity => scheduledLiveList(entity).forEach(item => rows.push({ ownerType:"identity", ownerId:entity.id, entity, item })));
              state.channels.forEach(entity => scheduledLiveList(entity).forEach(item => rows.push({ ownerType:"channel", ownerId:entity.id, entity, item })));
              return rows;
            };

            const processCommunityBotReminders = async () => {
              if (communityBotTimer) { clearTimeout(communityBotTimer); communityBotTimer = null; }
              const now = Date.now();
              let nextDue = Number.MAX_SAFE_INTEGER;
              let changed = false;
              for (const entry of allHostAppointments()) {
                if (!subscriptionEnabled(entry.ownerType, entry.ownerId)) continue;
                const scheduledAt = Number(entry.item?.scheduledAt || 0);
                if (!scheduledAt || scheduledAt <= now) continue;
                entry.item.botReminderSentKeys = { ...(entry.item.botReminderSentKeys || {}) };
                for (const tier of ["paid"]) {
                  const key = communityChatKey(entry.ownerType, entry.ownerId, tier);
                  const cfg = getCommunitySettings(entry.ownerType, entry.ownerId, tier);
                  if (!cfg.botEnabled || !cfg.botScheduled) continue;
                  const due = scheduledAt - Math.max(1, Number(cfg.botLeadHours || 3)) * 60 * 60 * 1000;
                  if (now >= due && !entry.item.botReminderSentKeys[key]) {
                    pushCommunityBotEvent(entry.ownerType, entry.ownerId, "scheduled", {
                      title: entry.item.title || "今天有预约直播",
                      dateText: entry.item.dateText || "",
                      text: `今天 ${entry.item.dateText || "稍后"} 开播，记得来。`
                    }, tier);
                    entry.item.botReminderSentKeys[key] = true;
                    changed = true;
                  } else if (!entry.item.botReminderSentKeys[key] && due > now) {
                    nextDue = Math.min(nextDue, due);
                  }
                }
              }
              if (changed) await persist();
              if (nextDue < Number.MAX_SAFE_INTEGER) {
                const delay = Math.max(250, Math.min(2147480000, nextDue - Date.now()));
                communityBotTimer = setTimeout(() => processCommunityBotReminders(), delay);
              }
            };

            const armCommunityBotTimers = () => { processCommunityBotReminders(); };

            const startAppointmentHostLive = async (ownerType, ownerId, scheduleId = "") => {
              const entity = getProfileEntity(ownerType, ownerId);
              if (!entity) return;
              const rows = scheduledLiveList(entity);
              const item = rows.find(row => String(row.scheduleId || "") === String(scheduleId || "")) || rows[0];
              if (!item) return;
              const now = Date.now(), start = Number(item.scheduledAt || 0), end = Number(item.endsAt || (start ? start + 2*3600000 : 0));
              if (item.sourceType === "business") {
                if (start && now < start) { toast(`这场商务直播还没到约定时间 · ${item.dateText || ""}`); return; }
                if (end && now > end) { toast("这场商务直播已超过约定时间段"); return; }
              }
              const contract = item.business ? { ...item.business, scheduledAt:start, endsAt:end, active:true, startedAt:now } : null;
              if (contract?.sourceMessageId && contract?.sourceCardId) {
                const card=findDMCard(contract.sourceMessageId,contract.sourceCardId); if (card) { card.liveStartedAt=now; card.status="accepted"; }
              }
              if (ownerType === "channel") {
                const live = { ...entity, id:entity.id, ownerType:"channel", ownerId:entity.id, title:item.title || "预约直播", category:item.content || "预约直播", mode:item.mode || "solo", startedAt:now, live:true, viewers:Math.max(1,Number(entity.viewers||0)), userParticipant:safeArray(item.participantIds).map(String).includes(String(state.viewerIdentityId)), participants:safeArray(item.participants).filter(p => String(p?.id||"") !== String(entity.id)).map(p => { if (String(p?.id||"") === String(state.viewerIdentityId)) { const viewer=state.identities.find(x=>String(x.id)===String(state.viewerIdentityId)); return {id:String(viewer?.id||p.id),name:String(viewer?.displayName||p.name||"user"),handle:String(viewer?.handle||""),avatar:String(viewer?.avatar||"")}; } const ch=state.channels.find(x=>String(x.id)===String(p?.id||"")); return {id:String(p?.id||""),name:String(ch?.name||p?.name||"嘉宾"),handle:String(ch?.handle||""),avatar:String(ch?.avatar||"")}; }), business:contract, ad:contract ? {...contract} : null };
                entity.live=true; entity.title=live.title; removeScheduledLive(entity, item.scheduleId); pushCommunityBotEvent("channel",entity.id,"live",{title:live.title,text:"直播已经开始，点击频道即可进入。"}); await persist(); openLive(live);
              } else {
                const live = { id:`own-scheduled-${entity.id}-${Date.now().toString(36)}`, ownerType:"identity", ownerId:entity.id, isOwn:true, name:entity.displayName||"我的频道", handle:entity.handle||"@me", avatar:entity.avatar||"U", followers:Number(entity.followers||0), title:item.title||"我的直播间", category:item.content||"预约直播", cover:item.cover||"", viewers:0, mode:item.mode||"solo", accessMode:item.accessMode||"public", ageRequirement:item.ageRequirement||"none", participants:safeArray(item.participants).filter(p=>String(p?.id||"")!==String(entity.id)).map(p=>{const ch=state.channels.find(x=>String(x.id)===String(p?.id||""));return{id:String(p?.id||""),name:String(ch?.name||p?.name||"嘉宾"),handle:String(ch?.handle||""),avatar:String(ch?.avatar||"")};}), business:contract, ad:contract ? {...contract} : null, startedAt:now };
                entity.live=true; entity.activeLive={...live}; removeScheduledLive(entity, item.scheduleId); state.liveChat=[]; state.pendingLiveUserMessage=null; pushCommunityBotEvent("identity",entity.id,"live",{title:live.title,text:"直播已经开始。"}); await persist(); openLive(live);
              }
            };

            const openScheduleDeleteConfirm = (identityId, scheduleId = "") => {
              const identity = state.identities.find(item => String(item.id) === String(identityId || ""));
              const item = scheduledLiveList(identity).find(row => String(row.scheduleId || "") === String(scheduleId || "")) || nearestScheduledLive(identity);
              if (!identity || !item) return;
              const business = item.sourceType === "business" || Boolean(item.business?.sourceCardId);
              openScreen("live-summary", `<div class="v-center-modal-backdrop"><div class="v-confirm-card"><div class="v-kicker">删除预约直播</div><h3>确认删除这个预约直播吗？</h3><p>${business ? "删除后不会取消对应商务合作；你仍可以从 DM 里的商务合作卡重新创建预约直播。" : "删除后这条预约将从预约列表移除。"}</p><div class="v-confirm-actions"><button class="is-cancel" data-action="close-schedule-delete-confirm">取消</button><button class="is-danger" data-action="confirm-delete-scheduled-live" data-identity-id="${escapeHTML(identity.id)}" data-schedule-id="${escapeHTML(item.scheduleId || "")}">确认删除</button></div></div></div>`);
            };
            const deleteScheduledLiveConfirmed = async (identityId, scheduleId = "") => {
              const identity = state.identities.find(item => String(item.id) === String(identityId || ""));
              if (!identity) { closeScreen("live-summary"); return; }
              const item = scheduledLiveList(identity).find(row => String(row.scheduleId || "") === String(scheduleId || "")) || nearestScheduledLive(identity);
              if (!item) { closeScreen("live-summary"); return; }
              if (item.sourceMessageId && item.sourceCardId) {
                const card = findDMCard(item.sourceMessageId, item.sourceCardId);
                if (card?.kind === "business" && card.status !== "completed") card.scheduleDeletedAt = Date.now();
              }
              removeScheduledLive(identity, item.scheduleId);
              await persist();
              closeScreen("live-summary");
              openScheduledLiveDetail("identity", identity.id);
              renderProfileTab("identity", identity.id, "activity");
              toast("预约直播已删除");
            };

            const rerender = () => {
              renderChannels();
              renderSchedule();
              renderHome();
              renderRecommended();
              renderMessages();
              renderWallet();
            };

            await refreshRocheData({ announce: false, sync: true });
            await migrateExistingPrimaryMutualFollows();
            ensureMellowBusinessDemo();
            await persist();
            rerender();
            armAppointmentTimer();
            armCommunityBotTimers();
            settleWeeklySubscriptions();
            setTimeout(() => checkAppointmentReminder({ entry: true }), 420);

            let longPressTimer = null;
            let suppressClickUntil = 0;
            const triggerLongPressAction = async target => {
              const kind = String(target?.dataset?.longpress || "");
              if (!kind) return;
              suppressClickUntil = Date.now() + 650;
              if (kind === "delete-dm-conversation") {
                await deleteDMConversation(target.dataset.messageId || "");
              } else if (kind === "delete-chat-item") {
                await deleteChatItem(target.dataset.threadType || "dm", target.dataset.threadKey || "", target.dataset.itemId || "");
              }
            };
            root.addEventListener("pointerdown", event => {
              const target = event.target.closest?.("[data-longpress]");
              if (!target || !root.contains(target)) return;
              if (longPressTimer) clearTimeout(longPressTimer);
              longPressTimer = setTimeout(() => triggerLongPressAction(target), 560);
            });
            ["pointerup","pointercancel","pointerleave"].forEach(type => root.addEventListener(type, () => {
              if (longPressTimer) clearTimeout(longPressTimer);
              longPressTimer = null;
            }));
            root.addEventListener("pointermove", event => {
              if (!longPressTimer) return;
              if (Math.abs(Number(event.movementX || 0)) > 3 || Math.abs(Number(event.movementY || 0)) > 3) {
                clearTimeout(longPressTimer); longPressTimer = null;
              }
            });
            root.addEventListener("contextmenu", event => {
              const target = event.target.closest?.("[data-longpress]");
              if (!target || !root.contains(target)) return;
              event.preventDefault();
              triggerLongPressAction(target);
            });

            let dmSwipe = null;
            root.addEventListener("pointerdown", event => {
              const row = event.target.closest?.("[data-dm-row]");
              if (!row || !root.contains(row)) return;
              dmSwipe = { row, x:Number(event.clientX || 0), y:Number(event.clientY || 0), moved:false, cancelTap:false };
            });
            root.addEventListener("pointermove", event => {
              if (!dmSwipe) return;
              const dx = Number(event.clientX || 0) - dmSwipe.x;
              const dy = Number(event.clientY || 0) - dmSwipe.y;
              // Vertical scrolling must never be mistaken for a DM tap.
              if (Math.abs(dy) > 12 && Math.abs(dy) >= Math.abs(dx)) { dmSwipe.cancelTap = true; return; }
              // Require a deliberate horizontal swipe. Small finger jitter must not eat ordinary DM taps.
              if (Math.abs(dx) < 30 || Math.abs(dx) <= Math.abs(dy) * 1.2) return;
              dmSwipe.moved = true;
              if (dx < -30) dmSwipe.row.classList.add("is-swipe-open");
              if (dx > 30) dmSwipe.row.classList.remove("is-swipe-open");
            });
            root.addEventListener("pointerup", event => {
              if (!dmSwipe) return;
              const swipe = dmSwipe;
              dmSwipe = null;
              if (swipe.moved || swipe.cancelTap) {
                suppressClickUntil = Date.now() + (swipe.moved ? 320 : 180);
                return;
              }
              // Roche mobile shells can occasionally swallow the synthetic click after pointer events.
              // Open the thread directly on a clean tap and suppress the duplicate click that follows.
              const deleteButton = event.target.closest?.('[data-action="delete-dm-conversation"]');
              if (deleteButton) return;
              const messageId = String(swipe.row?.dataset?.messageId || swipe.row?.dataset?.dmRow || "");
              if (messageId) {
                suppressClickUntil = Date.now() + 320;
                event.preventDefault?.();
                event.stopPropagation?.();
                openMessage(messageId);
              }
            });
            root.addEventListener("pointercancel", () => { dmSwipe = null; });

            // Roche mobile shells may occasionally swallow synthetic click on the three top refresh controls.
            // Handle pointerup directly, then suppress the duplicate click when the browser does emit one.
            root.addEventListener("pointerup", event => {
              const refreshButton = event.target.closest?.('[data-action="refresh-home"],[data-action="refresh-recommended"],[data-action="refresh-messages"]');
              if (!refreshButton || !root.contains(refreshButton) || refreshButton.disabled) return;
              const action = String(refreshButton.dataset.action || "");
              suppressClickUntil = Date.now() + 320;
              event.preventDefault?.();
              event.stopPropagation?.();
              if (action === "refresh-home") void refreshHomeFeedContent();
              else if (action === "refresh-recommended") void refreshRecommendedLives([]);
              else if (action === "refresh-messages") void refreshDirectMessages();
            });

            root.addEventListener("click", async (event) => {
              if (Date.now() < suppressClickUntil) { event.preventDefault(); event.stopPropagation(); return; }
              const button = event.target.closest("button,[data-action]");
              if (!button || !root.contains(button)) return;

              if (button.dataset.nav) {
                switchPage(button.dataset.nav);
                return;
              }

              const action = button.dataset.action;
              if (!action) return;

              if (action === "open-drawer") {
                q('[data-role="drawer"]')?.classList.add("is-open");
                q('[data-role="mask"]')?.classList.add("is-open");
              } else if (action === "close-drawer") {
                closeDrawer();
              } else if (action === "exit-vela") {
                closeDrawer();
                try {
                  await roche?.ui?.closeApp?.();
                } catch (err) {
                  console.warn("[Vela] closeApp failed", err);
                  toast("退出失败，请从 Roche 返回");
                }
              } else if (action === "wallet-bind-account" || action === "wallet-edit-account") {
                openWalletBind();
              } else if (action === "save-wallet-account") {
                await saveWalletAccount();
              } else if (action === "wallet-recharge-coin") {
                openWalletCoinRecharge();
              } else if (action === "confirm-wallet-coin-recharge") {
                await confirmWalletCoinRecharge();
              } else if (action === "wallet-cashout-coin") {
                openWalletCoinCashout();
              } else if (action === "confirm-wallet-coin-cashout") {
                await confirmWalletCoinCashout();
              } else if (action === "wallet-withdraw") {
                openWalletWithdraw();
              } else if (action === "confirm-wallet-withdraw") {
                await confirmWalletWithdraw();
              } else if (action === "wallet-refresh-fx") {
                await loadWalletFxRates({ force:true });
                renderWallet();
                toast("结算汇率已更新");
              } else if (action === "open-publish") {
                if (!state.viewerIdentityId || !state.identities.some(item => String(item.id) === String(state.viewerIdentityId))) {
                  openIdentities();
                  toast("先关联 Roche 用户资料并创建 Vela 主账号");
                  return;
                }
                renderPublishMenu();
                q('[data-role="publish-sheet"]')?.classList.add("is-open");
              } else if (action === "close-publish-sheet") {
                q('[data-role="publish-sheet"]')?.classList.remove("is-open");
              } else if (action === "render-publish-menu") {
                renderPublishMenu();
              } else if (action === "clear-live-cover") {
                const hidden = q('[data-role="publish-sheet"] [data-live-publish-field="cover"]');
                const url = q('[data-role="publish-sheet"] [data-live-cover-url]');
                if (hidden) hidden.value = "";
                if (url) url.value = "";
                setPublishImagePreview("live", "");
              } else if (action === "clear-post-image") {
                const hidden = q('[data-role="publish-sheet"] [data-post-field="image"]');
                const url = q('[data-role="publish-sheet"] [data-post-image-url]');
                if (hidden) hidden.value = "";
                if (url) url.value = "";
                setPublishImagePreview("post", "");
              } else if (action === "open-live-publisher") {
                openLivePublisher();
              } else if (action === "choose-live-option") {
                const field = String(button.dataset.liveField || "");
                const value = String(button.dataset.liveValue || "");
                const sheet = q('[data-role="publish-sheet"]');
                if (!sheet || !field || button.disabled) return;
                const hidden = sheet.querySelector(`[data-live-publish-field="${CSS.escape(field)}"]`);
                if (hidden) hidden.value = value;
                sheet.querySelectorAll(`[data-live-choice-group="${CSS.escape(field)}"] button`).forEach(item => item.classList.toggle("is-selected", item === button));
                if (field === "broadcastStyle") {
                  const multi = sheet.querySelector('[data-role="multi-live-fields"]');
                  if (multi) multi.hidden = value !== "multi";
                  if (value !== "multi") sheet.querySelectorAll('[data-live-cohost-id]:checked').forEach(input => { input.checked = false; });
                }
                if (field === "accessMode") {
                  const ageHidden = sheet.querySelector('[data-live-publish-field="ageRequirement"]');
                  const adult = sheet.querySelector('[data-live-choice-group="ageRequirement"] [data-live-value="18+"]');
                  const unrestricted = sheet.querySelector('[data-live-choice-group="ageRequirement"] [data-live-value="none"]');
                  const locked = value === "public";
                  if (adult) { adult.disabled = locked; adult.classList.toggle("is-disabled", locked); }
                  if (locked) { if (ageHidden) ageHidden.value = "none"; unrestricted?.classList.add("is-selected"); adult?.classList.remove("is-selected"); }
                }
              } else if (action === "choose-post-option") {
                const field = String(button.dataset.postField || "");
                const value = String(button.dataset.postValue || "");
                const sheet = q('[data-role="publish-sheet"]');
                if (!sheet || !field || button.disabled) return;
                const hidden = sheet.querySelector(`[data-post-field="${CSS.escape(field)}"]`);
                if (hidden) hidden.value = value;
                sheet.querySelectorAll(`[data-post-choice-group="${CSS.escape(field)}"] button`).forEach(item => item.classList.toggle("is-selected", item === button));
                if (field === "accessMode") {
                  const ageHidden = sheet.querySelector('[data-post-field="ageRequirement"]');
                  const adult = sheet.querySelector('[data-post-choice-group="ageRequirement"] [data-post-value="18+"]');
                  const unrestricted = sheet.querySelector('[data-post-choice-group="ageRequirement"] [data-post-value="none"]');
                  const locked = value === "public";
                  if (adult) { adult.disabled = locked; adult.classList.toggle("is-disabled", locked); }
                  if (locked) { if (ageHidden) ageHidden.value = "none"; unrestricted?.classList.add("is-selected"); adult?.classList.remove("is-selected"); }
                }
              } else if (action === "start-prepared-live") {
                await startPreparedLive();
              } else if (action === "open-publish-post") {
                openCommunityPostEditor("identity", state.viewerIdentityId);
              } else if (action === "open-invite-guest") {
                openInviteGuest();
              } else if (action === "confirm-invite-guest") {
                await confirmInviteGuest();
              } else if (action === "dm-tab") {
                state.dmTab = button.dataset.dmTab === "community" ? "community" : "dm";
                await persist();
                renderMessages();
              } else if (action === "continue-subscription") {
                continueSubscription(button.dataset.channelId || "");
              } else if (action === "cancel-subscription") {
                await cancelSubscription(button.dataset.channelId || "");
              } else if (action === "subscribe-channel") {
                openSubscriptionConfirm(button.dataset.channelId || "", button.dataset.tier || "free");
              } else if (action === "confirm-subscribe-channel") {
                await confirmSubscription(button.dataset.channelId || "", button.dataset.tier || "free", button.dataset.identityId || state.viewerIdentityId);
              } else if (action === "set-host-input-mode") {
                setHostInputMode(button.dataset.hostInputMode || "speech");
              } else if (action === "send-host-stage-input") {
                await sendHostStageInput();
              } else if (action === "summon-host-live") {
                await summonHostLive();
              } else if (action === "home-filter") {
                state.homeFilter = ["all","live","post"].includes(button.dataset.homeFilter) ? button.dataset.homeFilter : "all";
                renderHome();
                await persist();
              } else if (action === "refresh-home") {
                await refreshHomeFeedContent();
              } else if (action === "refresh-recommended") {
                await refreshRecommendedLives([]);
              } else if (action === "open-recommendation-topics") {
                openRecommendationTopics();
              } else if (action === "toggle-recommendation-topic") {
                toggleRecommendationTopic(button);
              } else if (action === "refresh-recommended-topics") {
                const topics = selectedRecommendationTopics();
                q('[data-role="publish-sheet"]')?.classList.remove("is-open");
                await refreshRecommendedLives(topics);
              } else if (action === "refresh-messages") {
                await refreshDirectMessages();
              } else if (action === "open-post-by-id") {
                openPostDetail(button.dataset.postId || "");
              } else if (action === "toggle-post-menu") {
                const menu=q(`[data-post-menu="${CSS.escape(String(button.dataset.postId||""))}"]`); if(menu) menu.classList.toggle("is-open");
              } else if (action === "toggle-inline-comments") {
                const id=String(button.dataset.postId||""); const box=q(`[data-inline-comments="${CSS.escape(id)}"]`); if(box){box.classList.toggle("is-open"); if(box.classList.contains("is-open")) renderPostComments(id); }
              } else if (action === "delete-any-post") {
                await deleteAnyPost(button.dataset.postId||"");
              } else if (action === "toggle-dm-bubble-translation") {
                const target = String(button.dataset.translationTarget || "");
                const el = qa('[data-translation-id]').find(node => String(node.dataset.translationId || "") === target);
                if (el && !String(el.textContent || "").trim()) {
                  await translateDMBubble(button.dataset.messageId || "", button.dataset.itemId || "", target);
                } else if (el) {
                  const isHidden = el.hasAttribute("hidden");
                  if (isHidden) el.removeAttribute("hidden"); else el.setAttribute("hidden", "");
                  button.classList.toggle("is-translation-open", isHidden);
                }
              } else if (action === "toggle-community-bubble-translation") {
                const target = String(button.dataset.translationTarget || "");
                const el = qa('[data-translation-id]').find(node => String(node.dataset.translationId || "") === target);
                if (el) {
                  const isHidden = el.hasAttribute("hidden");
                  if (isHidden) el.removeAttribute("hidden"); else el.setAttribute("hidden", "");
                  button.classList.toggle("is-translation-open", isHidden);
                }
              } else if (action === "toggle-translation") {
                const target = String(button.dataset.translationTarget || "");
                const el = qa('[data-translation-id]').find(node => String(node.dataset.translationId || "") === target);
                if (el) {
                  const isHidden = el.hasAttribute("hidden");
                  if (isHidden) el.removeAttribute("hidden"); else el.setAttribute("hidden", "");
                  button.textContent = isHidden ? "收起翻译" : "翻译";
                }
              } else if (action === "like-post") {
                const liked = button.dataset.liked === "1";
                button.dataset.liked = liked ? "0" : "1";
                button.classList.toggle("is-liked", !liked);
                const count = button.querySelector("span");
                const raw = Number(String(count?.textContent || "0").replace(/\D/g, "")) || 0;
                if (count) count.textContent = String(Math.max(0, raw + (liked ? -1 : 1)));
              } else if (action === "summon-post-replies") {
                await summonPostReplies(button.dataset.postId || "");
              } else if (action === "send-post-comment") {
                await sendPostComment(button.dataset.postId || "");
              } else if (action === "reply-post-comment") {
                replyToPostComment(button.dataset.postId || "", button.dataset.commentIndex);
              } else if (action === "delete-post-comment") {
                await deletePostComment(button.dataset.postId || "", button.dataset.commentIndex);
              } else if (action === "delete-community-post") {
                await deleteCommunityPost(button.dataset.postId || "");
              } else if (action === "edit-post") {
                openEditPostEditor(button.dataset.postId || "");
              } else if (action === "save-post-edit") {
                await saveEditedPost(button.dataset.postId || "");
              } else if (action === "open-community-post-editor") {
                openCommunityPostEditor(button.dataset.ownerType || "identity", button.dataset.ownerId || state.viewerIdentityId);
              } else if (action === "save-community-post") {
                await saveCommunityPost(button.dataset.ownerType || "identity", button.dataset.ownerId || state.viewerIdentityId);
              } else if (action === "close-community-settings-bg" && event.target === button) {
                closeScreen("community-settings");
              } else if (action === "sheet-bg-close" && event.target === button) {
                q('[data-role="publish-sheet"]')?.classList.remove("is-open");
              } else if (action === "open-channel") {
                const channel = state.channels.find(x => x.id === button.dataset.channelId);
                if (channel) openChannel(channel);
              } else if (action === "toggle-home-live-menu") {
                const id = String(button.dataset.channelId || "");
                const menu = q(`[data-home-live-menu="${CSS.escape(id)}"]`);
                if (menu) menu.classList.toggle("is-open");
              } else if (action === "edit-generated-live") {
                openGeneratedLiveEditor(button.dataset.channelId || "");
              } else if (action === "save-generated-live") {
                await saveGeneratedLiveEdit(button.dataset.channelId || "");
              } else if (action === "close-generated-live") {
                await closeGeneratedLive(button.dataset.channelId || "");
              } else if (action === "open-live") {
                const channel = state.channels.find(x => x.id === button.dataset.liveId);
                if (channel) openLive({ ...channel });
              } else if (action === "open-own-profile-live") {
                const identity = state.identities.find(x => String(x.id) === String(button.dataset.identityId || ""));
                if (identity?.activeLive) openLive({ ...identity.activeLive });
              } else if (action === "open-recommended-live") {
                const live = state.recommendedLives.find(x => x.id === button.dataset.recId);
                if (live) openLive(live);
              } else if (action === "remove-recommended-live") {
                const id = String(button.dataset.recId || "");
                state.recommendedLives = safeArray(state.recommendedLives).filter(item => String(item.id) !== id);
                if (state.liveChatsById) delete state.liveChatsById[id];
                await persist();
                renderRecommended();
              } else if (action === "follow-rec") {
                const live = state.recommendedLives.find(x => x.id === button.dataset.recId);
                if (live) {
                  const channelId = String(live.ownerId || live.id);
                  const followed = isFollowing("identity", state.viewerIdentityId, "channel", channelId);
                  const channel = state.channels.find(item => String(item.id) === channelId);
                  const viewer = state.identities.find(item => String(item.id) === String(state.viewerIdentityId));
                  setFollowing("identity", state.viewerIdentityId, "channel", channelId, !followed);
                  if (channel) channel.followers = Math.max(0, Number(channel.followers || 0) + (followed ? -1 : 1));
                  if (viewer) viewer.following = Math.max(0, Number(viewer.following || 0) + (followed ? -1 : 1));
                  renderRecommended(); renderChannels(); renderHome(); await persist();
                  toast(followed ? "已取消关注" : "已关注");
                }
              } else if (action === "open-live-lottery") {
                openLiveLotteryPanel();
              } else if (action === "close-live-economy") {
                closeLiveEconomyPanel();
              } else if (action === "close-live-summary-card") {
                closeLiveEconomyPanel();
                closeScreen("live");
              } else if (action === "add-live-prize-row") {
                const host = q('[data-screen="live"] [data-role="live-prize-rows"]');
                if (host) host.insertAdjacentHTML("beforeend", livePrizeRowHTML({}, host.querySelectorAll('[data-prize-row]').length));
              } else if (action === "remove-live-prize-row") {
                const row = button.closest('[data-prize-row]');
                const host = q('[data-screen="live"] [data-role="live-prize-rows"]');
                if (row && host && host.querySelectorAll('[data-prize-row]').length > 1) row.remove();
                else toast("至少保留一个奖项");
              } else if (action === "save-live-lottery") {
                await saveLiveLottery();
              } else if (action === "resolve-live-lottery") {
                await resolveLiveLottery();
              } else if (action === "new-live-lottery") {
                const live = state.liveSession;
                if (live) { getLiveStats(live).lottery = null; await persist(); openLiveLotteryPanel(); }
              } else if (action === "open-live-ad") {
                openLiveAdPanel();
              } else if (action === "save-live-ad") {
                await saveLiveAd();
              } else if (action === "clear-live-ad") {
                await clearLiveAd();
              } else if (action === "open-live-exit") {
                q('[data-screen="live"] [data-role="live-exit-menu"]')?.classList.add("is-open");
              } else if (action === "close-live-exit") {
                q('[data-screen="live"] [data-role="live-exit-menu"]')?.classList.remove("is-open");
              } else if (action === "leave-live") {
                closeScreen("live");
                state.liveSession = null;
                toast("已退出直播间，主播仍可能继续直播");
              } else if (action === "hang-own-live") {
                closeScreen("live");
                toast("已退出直播间，直播仍在继续");
              } else if (action === "end-current-live" || action === "end-own-live") {
                await endCurrentLive();
              } else if (action === "continue-live") {
                await advanceLive({ fromUser: false });
              } else if (action === "summon-live") {
                await summonLive();
              } else if (action === "delete-live-chat") {
                await deleteLiveChat(button.dataset.chatIndex);
              } else if (action === "toggle-gift-rank") {
                q('[data-screen="live"] [data-role="gift-rank-panel"]')?.classList.toggle("is-open");
                renderLiveHUD();
              } else if (action === "close-gift-rank") {
                q('[data-screen="live"] [data-role="gift-rank-panel"]')?.classList.remove("is-open");
              } else if (action === "open-gift-picker") {
                q('[data-screen="live"] [data-role="gift-picker"]')?.classList.add("is-open");
                renderLiveHUD();
              } else if (action === "close-gift-picker") {
                q('[data-screen="live"] [data-role="gift-picker"]')?.classList.remove("is-open");
              } else if (action === "send-live-gift") {
                const amount = Math.max(0, Number(button.dataset.giftAmount || 0));
                const live = state.liveSession;
                if (!live || !amount) return;
                if (Number(state.wallet?.coinBalance || 0) < amount) { toast("Vela Coin 余额不足"); return; }
                state.wallet.coinBalance = Number(state.wallet.coinBalance || 0) - amount;
                const liveKey = String(live.id || live.ownerId || "live");
                const selectedIdentityId = String(q('[data-screen="live"] [data-role="live-identity-select"]')?.value || state.liveInteractionIdentity?.[liveKey] || state.viewerIdentityId || "");
                const giftIdentity = state.identities.find(x => String(x.id) === selectedIdentityId) || state.identities.find(x => String(x.id) === String(state.viewerIdentityId)) || state.identities[0];
                if (giftIdentity) state.liveInteractionIdentity[liveKey] = String(giftIdentity.id);
                state.wallet.transactions.unshift({ id:`tx-${Date.now().toString(36)}-giftout`, type:"coin", title:"直播送礼", note: live.handle || live.name || "直播间", coin: -amount, identityId:giftIdentity?.id || state.viewerIdentityId, at:Date.now() });
                const stats = getLiveStats(live);
                stats.userGiftTotal = Number(stats.userGiftTotal || 0) + amount;
                stats.giftReceivedCoin = Number(stats.giftReceivedCoin || 0) + amount;
                const ownHandle = giftIdentity?.handle || "@user";
                stats.gifts.push({ user:ownHandle, amount });
                stats.heat = Math.min(100, Number(stats.heat || 50) + Math.min(18, amount / 80));
                advanceViewerCount(live, { heatBoost: Math.min(10, amount / 120) });
                state.liveChat.push({ user: "系统", avatar: "🎁", text: `${giftIdentity?.handle || "@user"} 送出了 🔷${amount} 的礼物`, translation: "", at:Date.now() });
                await persist();
                renderLiveChatLines();
                renderLiveHUD();
                q('[data-screen="live"] [data-role="gift-picker"]')?.classList.remove("is-open");
                toast(`已送出 🔷${amount}`);
              } else if (action === "delete-dm-conversation") {
                await deleteDMConversation(button.dataset.messageId || "");
              } else if (action === "open-message") {
                openMessage(button.dataset.messageId);
              } else if (action === "go-home-from-task") {
                closeScreen("message");
                switchPage("home");
                const main = q(".v-main"); if (main) main.scrollTop = 0;
              } else if (action === "close-screen") {
                closeScreen(button.dataset.screenName);
              } else if (action === "switch-home-identity-profile") {
                const identityId = String(button.dataset.identityId || "");
                if (state.identities.some(item => String(item.id) === identityId)) {
                  state.viewerIdentityId = identityId;
                  state.subscriptions = { ...getIdentitySubscriptions(identityId) };
                  await persist();
                  mountMarkup(root, state); rerender(); switchPage("home");
                  openProfile("identity", identityId);
                }
              } else if (action === "open-user-profile") {
                closeDrawer(); openUserProfile();
              } else if (action === "open-profile-by-id") {
                openProfile(button.dataset.ownerType || "identity", button.dataset.ownerId || "");
              } else if (action === "edit-profile") {
                openProfileEditor(button.dataset.ownerType || "identity", button.dataset.ownerId || "");
              } else if (action === "save-profile") {
                await saveProfileEditor(button.dataset.ownerType || "identity", button.dataset.ownerId || "");
              } else if (action === "profile-tab") {
                renderProfileTab(button.dataset.ownerType || "identity", button.dataset.ownerId || "", button.dataset.profileTab || "activity");
              } else if (action === "open-subscription-settings") {
                openSubscriptionSettings(button.dataset.ownerType || "identity", button.dataset.ownerId || "");
              } else if (action === "toggle-subscription-plan-editor") {
                const fields = q('[data-screen="subscription-settings"] [data-role="subscription-plan-fields"]');
                if (fields) fields.hidden = !Boolean(button.checked);
              } else if (action === "save-subscription-settings") {
                await saveSubscriptionSettings(button.dataset.ownerType || "identity", button.dataset.ownerId || "");
              } else if (action === "toggle-profile-follow") {
                const channelId = String(button.dataset.channelId || "");
                const channel = state.channels.find(item => String(item.id) === channelId);
                const identity = state.identities.find(item => String(item.id) === String(state.viewerIdentityId)) || state.identities[0];
                if (channel && identity) {
                  const followed = isFollowing("identity", identity.id, "channel", channel.id);
                  setFollowing("identity", identity.id, "channel", channel.id, !followed);
                  channel.followers = Math.max(0, Number(channel.followers || 0) + (followed ? -1 : 1));
                  identity.following = Math.max(0, Number(identity.following || 0) + (followed ? -1 : 1));
                  await persist();
                  renderChannels();
                  renderHome();
                  renderRecommended();
                  openProfile("channel", channel.id);
                }
              } else if (action === "message-channel") {
                await messageChannel(button.dataset.channelId || "");
              } else if (action === "open-community-chat") {
                openCommunityChat(button.dataset.ownerType || "channel", button.dataset.ownerId || "", button.dataset.tier || "paid");
              } else if (action === "send-community-message") {
                await sendCommunityMessage(button.dataset.communityKey || "");
              } else if (action === "send-direct-message") {
                await sendDirectMessage(button.dataset.messageId || "");
              } else if (action === "summon-direct-message") {
                await summonDirectMessage(button.dataset.messageId || "");
              } else if (action === "open-dm-plus") {
                openDMPlus(button.dataset.messageId || "");
              } else if (action === "open-dm-tool-form") {
                openDMToolForm(button.dataset.messageId || "", button.dataset.toolType || "live_invite");
              } else if (action === "send-dm-tool-card") {
                await sendDMToolCard(button.dataset.messageId || "", button.dataset.toolType || "live_invite");
              } else if (action === "accept-dm-card") {
                await acceptDMCard(button.dataset.messageId || "", button.dataset.cardId || "");
              } else if (action === "decline-dm-card") {
                await declineDMCard(button.dataset.messageId || "", button.dataset.cardId || "");
              } else if (action === "cancel-dm-card") {
                await cancelDMCard(button.dataset.messageId || "", button.dataset.cardId || "");
              } else if (action === "edit-business-card") {
                openBusinessCardEditor(button.dataset.messageId || "", button.dataset.cardId || "");
              } else if (action === "send-business-revision") {
                await sendBusinessRevision(button.dataset.messageId || "", button.dataset.cardId || "");
              } else if (action === "fill-winner-card") {
                fillWinnerCard(button.dataset.messageId || "", button.dataset.cardId || "");
              } else if (action === "save-winner-reply") {
                await saveWinnerReply(button.dataset.messageId || "", button.dataset.cardId || "");
              } else if (action === "toggle-chat-topmenu") {
                q('[data-screen="message"] [data-role="chat-topmenu"]')?.classList.toggle("is-open");
              } else if (action === "clear-direct-chat") {
                await clearDirectChat(button.dataset.messageId || "");
              } else if (action === "summon-community-chat") {
                await summonCommunityChat(button.dataset.communityKey || "");
              } else if (action === "open-community-plus") {
                openCommunityPlus(button.dataset.communityKey || "");
              } else if (action === "open-community-tool-form") {
                openCommunityToolForm(button.dataset.communityKey || "", button.dataset.toolType || "poll");
              } else if (action === "publish-community-tool") {
                await publishCommunityTool(button.dataset.communityKey || "", button.dataset.toolType || "poll");
              } else if (action === "vote-community-poll") {
                await voteCommunityPoll(button.dataset.communityKey || "", button.dataset.cardId || "", button.dataset.optionIndex || "0");
              } else if (action === "join-community-lottery") {
                await joinCommunityLottery(button.dataset.communityKey || "", button.dataset.cardId || "");
              } else if (action === "resolve-community-lottery") {
                await resolveCommunityLottery(button.dataset.communityKey || "", button.dataset.cardId || "");
              } else if (action === "clear-community-chat") {
                await clearCommunityChat(button.dataset.communityKey || "", button.dataset.ownerType || "channel", button.dataset.ownerId || "", button.dataset.tier || "paid");
              } else if (action === "edit-community") {
                openCommunitySettings(button.dataset.ownerType || "identity", button.dataset.ownerId || "", button.dataset.tier || "paid");
              } else if (action === "save-community-settings") {
                await saveCommunitySettings(button.dataset.ownerType || "identity", button.dataset.ownerId || "", button.dataset.tier || "paid");
              } else if (action === "view-appointment-host") {
                const ownerType = button.dataset.ownerType || "channel", ownerId = button.dataset.ownerId || "";
                closeScreen("appointment-reminder");
                openScheduledLiveDetail(ownerType, ownerId);
              } else if (action === "appointment-reminder-later") {
                closeScreen("appointment-reminder");
              } else if (action === "mute-appointment-reminders-today") {
                state.appointmentReminderMuteDate = localDateKey();
                await persist();
                closeScreen("appointment-reminder");
              } else if (action === "go-appointment-live") {
                closeScreen("appointment-reminder");
                await startAppointmentHostLive(button.dataset.ownerType || "channel", button.dataset.ownerId || "");
              } else if (action === "close-publish-sheet") {
                q('[data-role="publish-sheet"]')?.classList.remove("is-open");
              } else if (action === "open-replay") {
                openReplayDetail(button.dataset.replayId || "");
              } else if (action === "delete-replay") {
                await deleteReplay(button.dataset.replayId || "");
              } else if (action === "create-scheduled-live") {
                openScheduledLiveEditor(button.dataset.identityId || state.viewerIdentityId, button.dataset.scheduleId || "");
              } else if (action === "save-scheduled-live") {
                await saveScheduledLive(button.dataset.identityId || state.viewerIdentityId, button.dataset.scheduleId || "");
              } else if (action === "open-scheduled-live") {
                openScheduledLiveDetail(button.dataset.ownerType || "identity", button.dataset.ownerId || button.dataset.identityId || state.viewerIdentityId);
              } else if (action === "edit-scheduled-live-inline") {
                openScheduledLiveDetail(button.dataset.ownerType || "identity", button.dataset.ownerId || button.dataset.identityId || state.viewerIdentityId, button.dataset.scheduleId || "");
              } else if (action === "cancel-scheduled-live-inline") {
                openScheduledLiveDetail(button.dataset.ownerType || "identity", button.dataset.ownerId || state.viewerIdentityId);
              } else if (action === "save-scheduled-live-inline") {
                await saveScheduledLive(button.dataset.identityId || state.viewerIdentityId, button.dataset.scheduleId || "");
              } else if (action === "delete-scheduled-live-from-list" || action === "cancel-scheduled-live") {
                openScheduleDeleteConfirm(button.dataset.identityId || "", button.dataset.scheduleId || "");
              } else if (action === "close-schedule-delete-confirm") {
                closeScreen("live-summary");
              } else if (action === "confirm-delete-scheduled-live") {
                await deleteScheduledLiveConfirmed(button.dataset.identityId || "", button.dataset.scheduleId || "");
              } else if (action === "recreate-business-schedule") {
                const messageId = String(button.dataset.messageId || ""), cardId = String(button.dataset.cardId || "");
                const card = findDMCard(messageId, cardId);
                if (card?.kind === "business" && card.status === "accepted") {
                  if (card.hostOwnerType === "identity") {
                    const activeIdentityId = String(state.dmInteractionIdentity?.[messageId] || state.viewerIdentityId || state.identities[0]?.id || "");
                    const activeIdentity = state.identities.find(item => String(item.id) === activeIdentityId) || state.identities[0];
                    if (activeIdentity) { card.hostOwnerId = String(activeIdentity.id); card.hostLabel = `${activeIdentity.displayName || activeIdentity.handle || "你的账号"}的直播间`; }
                  }
                  await createScheduleFromCard(messageId, card);
                  await persist();
                  renderDirectThread(messageId);
                  renderHome();
                  toast("已从商务合作方案重新创建预约直播");
                }
              } else if (action === "start-scheduled-live-item" || action === "start-scheduled-live") {
                const ownerType = button.dataset.ownerType || "identity";
                const ownerId = button.dataset.ownerId || button.dataset.identityId || state.viewerIdentityId;
                closeScreen("schedule-detail");
                await startAppointmentHostLive(ownerType, ownerId, button.dataset.scheduleId || "");
              } else if (action === "settings-note") {
                closeDrawer(); openSettingsNote(button.dataset.settingsKind || "platform");
              } else if (action === "save-generation-settings") {
                await saveGenerationSettings();
              } else if (action === "save-appearance-settings") {
                await saveAppearanceSettings();
              } else if (action === "save-platform-settings") {
                await savePlatformSettings();
              } else if (action === "reset-appearance") {
                state.appearance = { topbarHeight: 62, bottombarHeight: 78 };
                root.style.setProperty("--v-top-h", "62px");
                root.style.setProperty("--v-bottom-h", "78px");
                await persist();
                openSettingsNote("appearance");
                toast("界面比例已恢复默认");
              } else if (action === "clear-vela-data") {
                closeDrawer();
                await clearVelaPluginData();
              } else if (action === "delete-vela-account") {
                await deleteVelaAccount(button.dataset.ownerType || "identity", button.dataset.ownerId || "");
              } else if (action === "open-identities") {
                closeDrawer(); openIdentities();
              } else if (action === "open-roles") {
                closeDrawer(); openRoles();
              } else if (action === "refresh-roche") {
                await refreshRocheData({ announce: true, sync: true });
                openIdentities();
                rerender();
              } else if (action === "refresh-roche-roles") {
                await refreshRocheData({ announce: true, sync: true });
                openRoles();
                rerender();
              } else if (action === "bind-owner-persona") {
                const personaId = String(button.dataset.personaId || "");
                const persona = rocheRuntime.personas.find(p => String(p?.id || "") === personaId) ||
                  (String(rocheRuntime.activePersona?.id || "") === personaId ? rocheRuntime.activePersona : null);
                if (!persona || isRocheBotIdentity(persona)) {
                  toast("Bot 类型 Roche 身份不会关联到 Vela");
                  return;
                }
                state.rocheLink.ownerPersonaId = personaId;
                await persist();
                openIdentities();
                toast("已关联 Roche 用户资料");
              } else if (action === "open-user-account-creator") {
                openPersonaAccountCreator(button.dataset.personaId || "");
              } else if (action === "create-vela-user-account") {
                await createVelaUserAccount(
                  button.dataset.personaId || "",
                  button.dataset.accountRole || "primary",
                  button.dataset.linkedMain === "yes"
                );
              } else if (action === "open-character-account-creator") {
                openCharacterAccountCreator(button.dataset.characterId || "");
              } else if (action === "create-character-sub-account") {
                await createCharacterSubAccount(button.dataset.characterId || "", button.dataset.linkedMain === "yes");
              } else if (action === "link-roche-character") {
                await linkCharacter(button.dataset.characterId);
                q('[data-role="publish-sheet"]')?.classList.remove("is-open");
                openRoles();
              } else if (action === "unlink-roche-character") {
                await unlinkCharacter(button.dataset.characterId);
                openRoles();
              } else if (action === "switch-identity") {
                state.viewerIdentityId = button.dataset.identityId;
                state.subscriptions = { ...getIdentitySubscriptions(state.viewerIdentityId) };
                await persist();
                mountMarkup(root, state); rerender(); switchPage(state.activePage || "home"); toast("身份已切换");
              } else if (action === "start-own-live") {
                openLivePublisher();
              }
            });

            root.addEventListener("keydown", async (event) => {
              if (event.key !== "Enter" || event.shiftKey) return;
              const target = event.target;
              if (target?.matches?.('[data-role="inline-post-comment-input"]')) {
                event.preventDefault();
                await sendPostComment(target.dataset.postId || "");
              } else if (target?.matches?.('[data-role="post-comment-input"]')) {
                event.preventDefault();
                await sendPostComment(target.dataset.postId || "");
              } else if (target?.matches?.('[data-role="live-input"]')) {
                event.preventDefault();
                await sendLiveChatOnly();
              } else if (target?.matches?.('[data-role="host-live-input"]')) {
                event.preventDefault();
                await sendHostStageInput();
              } else if (target?.matches?.('[data-role="dm-input"]')) {
                event.preventDefault();
                await sendDirectMessage(target.dataset.messageId || "");
              } else if (target?.matches?.('[data-role="community-message-input"]')) {
                event.preventDefault();
                await sendCommunityMessage(target.dataset.communityKey || "");
              }
            });

            root.addEventListener("change", async (event) => {
              const input = event.target;
              if (input?.matches?.('[data-role="post-identity-select"]')) {
                state.postInteractionIdentity[String(input.dataset.postId || "")] = String(input.value || "");
                updateIdentityPickerAvatar(input);
                await persist();
              } else if (input?.matches?.('[data-role="community-identity-select"]')) {
                state.communityInteractionIdentity[String(input.dataset.communityKey || "")] = String(input.value || "");
                updateIdentityPickerAvatar(input);
                await persist();
              } else if (input?.matches?.('[data-role="dm-identity-select"]')) {
                state.dmInteractionIdentity[String(input.dataset.messageId || "")] = String(input.value || "");
                updateIdentityPickerAvatar(input);
                await persist();
              } else if (input?.matches?.('[data-role="live-identity-select"]')) {
                state.liveInteractionIdentity[String(input.dataset.liveKey || "live")] = String(input.value || "");
                updateIdentityPickerAvatar(input);
                await persist();
              } else if (input?.matches?.('[data-live-cohost-id]')) {
                const checked = qa('[data-role="publish-sheet"] [data-live-cohost-id]:checked');
                if (checked.length > 3) { input.checked = false; toast("最多选择 3 位一起直播"); }
              } else if (input?.matches?.('[data-sub-cover-file]')) {
                try {
                  const value = await readLocalImage(input.files?.[0]);
                  if (!value) return;
                  const tier = String(input.dataset.subCoverFile || "") === "paid" ? "paid" : "free";
                  const field = q(`[data-screen="subscription-settings"] [data-sub-field="${tier}Cover"]`);
                  if (field) field.value = value;
                  toast("卡片封面已载入");
                } catch (err) { toast(String(err?.message || err || "读取封面失败")); }
              } else if (input?.matches?.('[data-live-cover-file]')) {
                try {
                  const value = await readLocalImage(input.files?.[0]);
                  if (!value) return;
                  const hidden = q('[data-role="publish-sheet"] [data-live-publish-field="cover"]');
                  const url = q('[data-role="publish-sheet"] [data-live-cover-url]');
                  if (hidden) hidden.value = value;
                  if (url) url.value = "";
                  setPublishImagePreview("live", value);
                } catch (err) { toast(String(err?.message || err || "读取图片失败")); }
              } else if (input?.matches?.('[data-post-image-file]')) {
                try {
                  const value = await readLocalImage(input.files?.[0]);
                  if (!value) return;
                  const hidden = q('[data-role="publish-sheet"] [data-post-field="image"]');
                  const url = q('[data-role="publish-sheet"] [data-post-image-url]');
                  if (hidden) hidden.value = value;
                  if (url) url.value = "";
                  setPublishImagePreview("post", value);
                } catch (err) { toast(String(err?.message || err || "读取图片失败")); }
              } else if (input?.matches?.('[data-community-avatar-file]')) {
                try {
                  const value = await readLocalImage(input.files?.[0]);
                  if (!value) return;
                  const hidden = q('[data-screen="community-settings"] [data-community-field="avatar"]');
                  if (hidden) hidden.value = value;
                  const preview = q('[data-screen="community-settings"] [data-role="community-avatar-preview"]');
                  if (preview) preview.innerHTML = `<img src="${escapeHTML(value)}" alt="">`;
                } catch (err) { toast(String(err?.message || err || "读取头像失败")); }
              } else if (input?.matches?.('[data-community-avatar-url]')) {
                const value = String(input.value || "").trim();
                const hidden = q('[data-screen="community-settings"] [data-community-field="avatar"]');
                if (hidden) hidden.value = value;
                const preview = q('[data-screen="community-settings"] [data-role="community-avatar-preview"]');
                if (preview) preview.innerHTML = value ? avatarHTML(value, "社") : "社";
              } else if (input?.matches?.('[data-community-bot-avatar-file]')) {
                try {
                  const value = await readLocalImage(input.files?.[0]);
                  if (!value) return;
                  const hidden = q('[data-screen="community-settings"] [data-community-field="botAvatar"]');
                  if (hidden) hidden.value = value;
                  const preview = q('[data-screen="community-settings"] [data-role="community-bot-avatar-preview"]');
                  if (preview) preview.innerHTML = `<img src="${escapeHTML(value)}" alt="">`;
                } catch (err) { toast(String(err?.message || err || "读取机器人头像失败")); }
              } else if (input?.matches?.('[data-community-bot-avatar-url]')) {
                const value = String(input.value || "").trim();
                const hidden = q('[data-screen="community-settings"] [data-community-field="botAvatar"]');
                if (hidden) hidden.value = value;
                const preview = q('[data-screen="community-settings"] [data-role="community-bot-avatar-preview"]');
                if (preview) preview.innerHTML = value ? avatarHTML(value, "B") : "B";
              } else if (input?.matches?.('[data-community-field="botEnabled"]')) {
                const options = q('[data-screen="community-settings"] [data-role="community-bot-options"]');
                if (options) options.hidden = !input.checked;
              }
            });

            root.addEventListener("input", async (event) => {
              const input = event.target;
              if (input?.matches?.('[data-role="commerce-range"]')) {
                const value = Math.max(0, Math.min(100, Number(input.value || 0)));
                const charId = String(input.dataset.characterId || "");
                const cfg = state.roleCommerce[charId] || (state.roleCommerce[charId] = { enabled: true, tendency: value });
                cfg.tendency = value;
                const label = qa('[data-screen="roles"] [data-role="commerce-value"]').find(el => String(el.dataset.characterId || "") === charId);
                if (label) label.textContent = `${value}%`;
                await persist();
              } else if (input?.matches?.('[data-edit-field="commerceTendency"]')) {
                const label = q('[data-screen="profile-edit"] [data-role="profile-commerce-value"]');
                if (label) label.textContent = `${Math.max(0, Math.min(100, Number(input.value || 0)))}%`;
              } else if (input?.matches?.('[data-edit-field="linkedToMain"]')) {
                const visibility = q('[data-screen="profile-edit"] [data-edit-field="identityVisibility"]');
                const knowledge = q('[data-screen="profile-edit"] [data-role="hidden-sub-knowledge"]');
                const linked = String(input.value || "") === "yes";
                if (visibility) visibility.disabled = linked;
                if (knowledge) knowledge.style.display = linked ? "none" : "";
              } else if (input?.matches?.('[data-setting-field="topbarHeight"]')) {
                const value = Math.max(52, Math.min(86, Number(input.value || 62)));
                const label = q('[data-screen="settings"] [data-role="topbar-value"]');
                if (label) label.textContent = `${value}px`;
                root.style.setProperty("--v-top-h", `${value}px`);
              } else if (input?.matches?.('[data-setting-field="bottombarHeight"]')) {
                const value = Math.max(64, Math.min(100, Number(input.value || 78)));
                const label = q('[data-screen="settings"] [data-role="bottombar-value"]');
                if (label) label.textContent = `${value}px`;
                root.style.setProperty("--v-bottom-h", `${value}px`);
              } else if (input?.matches?.('[data-live-cover-url]')) {
                const value = String(input.value || "").trim();
                const hidden = q('[data-role="publish-sheet"] [data-live-publish-field="cover"]');
                if (hidden) hidden.value = value;
                setPublishImagePreview("live", value);
              } else if (input?.matches?.('[data-post-image-url]')) {
                const value = String(input.value || "").trim();
                const hidden = q('[data-role="publish-sheet"] [data-post-field="image"]');
                if (hidden) hidden.value = value;
                setPublishImagePreview("post", value);
              } else if (input?.matches?.('[data-setting-field="activityLevel"]')) {
                const label = q('[data-screen="settings"] [data-role="activity-value"]');
                if (label) label.textContent = `${Number(input.value || 0)}%`;
              } else if (input?.matches?.('[data-setting-field="languagePreference"]')) {
                const index = Math.max(0, Math.min(4, Math.round(Number(input.value || 0))));
                const label = q('[data-screen="settings"] [data-role="language-preference-value"]');
                if (label) label.textContent = LANGUAGE_PREFERENCE_OPTIONS[index]?.label || "海外较多";
              }
            });
          },
          async unmount(container) {
            container.replaceChildren();
          }
        }
      ]
    });
  }

  registerApp();
})();
